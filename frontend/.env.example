# Frontend Environment Configuration
# Copy this file to .env.local for local development

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL (used during development via Vite proxy)
# In production, this is typically set at build time
VITE_API_URL=http://localhost:8000

# =============================================================================
# Authentication - Supabase Auth (ADR-009; Phase 6 P6-06)
# =============================================================================

# Supabase project URL and anon (public) key. Both required: the guardian
# surface (src/auth/supabaseClient.ts) reads these at module load via
# import.meta.env and throws if either is missing. The anon key is safe to
# ship to the browser; it only grants what Supabase Row Level Security
# policies allow.
# Find both in the Supabase dashboard: Project Settings > API.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Show the "Continue with Apple" button on the guardian login page. Hidden by
# default because Apple sign-in needs a paid Apple Developer account plus a
# Services ID and a signed, 6-month-expiring client secret. Only the literal
# "true" enables it; any other value (or unset) hides it. Set it to "true" only
# after the Apple provider is enabled in Supabase (Authentication > Providers).
# Google's button is always shown; Apple is the only flag-gated provider.
# VITE_ENABLE_APPLE_OAUTH=false

# =============================================================================
# Feature Flags (optional)
# =============================================================================

# Enable debug mode (shows additional logging)
# VITE_DEBUG=false

# =============================================================================
# Third-Party Services (optional)
# =============================================================================

# Analytics tracking ID
# VITE_ANALYTICS_ID=

# Sentry DSN for frontend error tracking
# VITE_SENTRY_DSN=
