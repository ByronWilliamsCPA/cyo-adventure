// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { ActivateCharacterApiV1CharactersCharacterIdActivatePostData, ActivateCharacterApiV1CharactersCharacterIdActivatePostErrors, ActivateCharacterApiV1CharactersCharacterIdActivatePostResponses, AddAllowlistEntryApiV1AdminProviderAllowlistPostData, AddAllowlistEntryApiV1AdminProviderAllowlistPostErrors, AddAllowlistEntryApiV1AdminProviderAllowlistPostResponses, ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostData, ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostErrors, ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostResponses, ApproveStorybookApiV1StorybooksStorybookIdApprovePostData, ApproveStorybookApiV1StorybooksStorybookIdApprovePostErrors, ApproveStorybookApiV1StorybooksStorybookIdApprovePostResponses, ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostData, ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostErrors, ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostResponses, ArchiveStorybookApiV1StorybooksStorybookIdArchivePostData, ArchiveStorybookApiV1StorybooksStorybookIdArchivePostErrors, ArchiveStorybookApiV1StorybooksStorybookIdArchivePostResponses, AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostData, AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostErrors, AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostResponses, ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostData, ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostErrors, ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostResponses, CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetData, CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetErrors, CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetResponses, CreateAdminProfileApiV1AdminProfilesPostData, CreateAdminProfileApiV1AdminProfilesPostErrors, CreateAdminProfileApiV1AdminProfilesPostResponses, CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostData, CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostErrors, CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostResponses, CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostData, CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostErrors, CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostResponses, CreateCharacterApiV1CharactersPostData, CreateCharacterApiV1CharactersPostErrors, CreateCharacterApiV1CharactersPostResponses, CreateChildSessionApiV1ChildSessionsPostData, CreateChildSessionApiV1ChildSessionsPostErrors, CreateChildSessionApiV1ChildSessionsPostResponses, CreateConceptApiV1ConceptsPostData, CreateConceptApiV1ConceptsPostErrors, CreateConceptApiV1ConceptsPostResponses, CreateDeviceGrantApiV1DeviceGrantsPostData, CreateDeviceGrantApiV1DeviceGrantsPostErrors, CreateDeviceGrantApiV1DeviceGrantsPostResponses, CreateFamilyApiV1AdminFamiliesPostData, CreateFamilyApiV1AdminFamiliesPostErrors, CreateFamilyApiV1AdminFamiliesPostResponses, CreateFamilyConnectionApiV1AdminFamilyConnectionsPostData, CreateFamilyConnectionApiV1AdminFamilyConnectionsPostErrors, CreateFamilyConnectionApiV1AdminFamilyConnectionsPostResponses, CreateFlagApiV1FlagsPostData, CreateFlagApiV1FlagsPostErrors, CreateFlagApiV1FlagsPostResponses, CreateProfileApiV1ProfilesPostData, CreateProfileApiV1ProfilesPostErrors, CreateProfileApiV1ProfilesPostResponses, CreateStoryRequestApiV1StoryRequestsPostData, CreateStoryRequestApiV1StoryRequestsPostErrors, CreateStoryRequestApiV1StoryRequestsPostResponses, CreateUserApiV1AdminUsersPostData, CreateUserApiV1AdminUsersPostErrors, CreateUserApiV1AdminUsersPostResponses, DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostData, DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostErrors, DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostResponses, DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteData, DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteErrors, DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteResponses, DeleteCharacterApiV1CharactersCharacterIdDeleteData, DeleteCharacterApiV1CharactersCharacterIdDeleteErrors, DeleteCharacterApiV1CharactersCharacterIdDeleteResponses, DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteData, DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteErrors, DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteResponses, DeleteMyFamilyApiV1MeFamilyDeleteData, DeleteMyFamilyApiV1MeFamilyDeleteErrors, DeleteMyFamilyApiV1MeFamilyDeleteResponses, DeleteProfileApiV1ProfilesProfileIdDeleteData, DeleteProfileApiV1ProfilesProfileIdDeleteErrors, DeleteProfileApiV1ProfilesProfileIdDeleteResponses, DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteData, DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteErrors, DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteResponses, EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchData, EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchErrors, EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchResponses, EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostData, EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostErrors, EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostResponses, ExportMyFamilyApiV1MeExportGetData, ExportMyFamilyApiV1MeExportGetErrors, ExportMyFamilyApiV1MeExportGetResponses, FlushReadingTimeApiV1MeReadingTimePostData, FlushReadingTimeApiV1MeReadingTimePostErrors, FlushReadingTimeApiV1MeReadingTimePostResponses, ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostData, ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostErrors, ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostResponses, GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetData, GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetErrors, GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetResponses, GetFamilyBudgetApiV1FamiliesMeBudgetGetData, GetFamilyBudgetApiV1FamiliesMeBudgetGetErrors, GetFamilyBudgetApiV1FamiliesMeBudgetGetResponses, GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetData, GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetErrors, GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetResponses, GetGenerationJobApiV1GenerationJobsJobIdGetData, GetGenerationJobApiV1GenerationJobsJobIdGetErrors, GetGenerationJobApiV1GenerationJobsJobIdGetResponses, GetMyProgressApiV1MeProgressGetData, GetMyProgressApiV1MeProgressGetErrors, GetMyProgressApiV1MeProgressGetResponses, GetNoiseFloorApiV1AdminModerationNoiseFloorGetData, GetNoiseFloorApiV1AdminModerationNoiseFloorGetErrors, GetNoiseFloorApiV1AdminModerationNoiseFloorGetResponses, GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetData, GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetErrors, GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetResponses, GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetData, GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetErrors, GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetResponses, GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetData, GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetErrors, GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetResponses, GetReadingHistoryApiV1ReadingHistoryProfileIdGetData, GetReadingHistoryApiV1ReadingHistoryProfileIdGetErrors, GetReadingHistoryApiV1ReadingHistoryProfileIdGetResponses, GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetData, GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetErrors, GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetResponses, GetRecommendationsApiV1RecommendationsProfileIdGetData, GetRecommendationsApiV1RecommendationsProfileIdGetErrors, GetRecommendationsApiV1RecommendationsProfileIdGetResponses, GetReviewQueueApiV1ReviewQueueGetData, GetReviewQueueApiV1ReviewQueueGetErrors, GetReviewQueueApiV1ReviewQueueGetResponses, GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetData, GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetErrors, GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetResponses, GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetData, GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetErrors, GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetResponses, GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetData, GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetErrors, GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetResponses, GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostData, GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostErrors, GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostResponses, InviteGuardianApiV1MeFamilyInviteGuardianPostData, InviteGuardianApiV1MeFamilyInviteGuardianPostErrors, InviteGuardianApiV1MeFamilyInviteGuardianPostResponses, ListAdminProfilesApiV1AdminProfilesGetData, ListAdminProfilesApiV1AdminProfilesGetErrors, ListAdminProfilesApiV1AdminProfilesGetResponses, ListAdminStorybooksApiV1AdminStorybooksGetData, ListAdminStorybooksApiV1AdminStorybooksGetErrors, ListAdminStorybooksApiV1AdminStorybooksGetResponses, ListAllowlistApiV1AdminProviderAllowlistGetData, ListAllowlistApiV1AdminProviderAllowlistGetErrors, ListAllowlistApiV1AdminProviderAllowlistGetResponses, ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetData, ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetErrors, ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetResponses, ListAuditEventsApiV1AdminAuditGetData, ListAuditEventsApiV1AdminAuditGetErrors, ListAuditEventsApiV1AdminAuditGetResponses, ListCharactersApiV1CharactersGetData, ListCharactersApiV1CharactersGetErrors, ListCharactersApiV1CharactersGetResponses, ListCompletionsApiV1CompletionsProfileIdGetData, ListCompletionsApiV1CompletionsProfileIdGetErrors, ListCompletionsApiV1CompletionsProfileIdGetResponses, ListDeviceDownloadsApiV1DeviceDownloadsGetData, ListDeviceDownloadsApiV1DeviceDownloadsGetErrors, ListDeviceDownloadsApiV1DeviceDownloadsGetResponses, ListDeviceGrantsApiV1DeviceGrantsGetData, ListDeviceGrantsApiV1DeviceGrantsGetErrors, ListDeviceGrantsApiV1DeviceGrantsGetResponses, ListFamiliesApiV1AdminFamiliesGetData, ListFamiliesApiV1AdminFamiliesGetErrors, ListFamiliesApiV1AdminFamiliesGetResponses, ListFamilyConnectionsApiV1AdminFamilyConnectionsGetData, ListFamilyConnectionsApiV1AdminFamilyConnectionsGetErrors, ListFamilyConnectionsApiV1AdminFamilyConnectionsGetResponses, ListGenerationJobsApiV1GenerationJobsGetData, ListGenerationJobsApiV1GenerationJobsGetErrors, ListGenerationJobsApiV1GenerationJobsGetResponses, ListGuardianBooksApiV1GuardianBooksGetData, ListGuardianBooksApiV1GuardianBooksGetErrors, ListGuardianBooksApiV1GuardianBooksGetResponses, ListLibraryApiV1LibraryGetData, ListLibraryApiV1LibraryGetErrors, ListLibraryApiV1LibraryGetResponses, ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetData, ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetErrors, ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetResponses, ListNotificationsApiV1NotificationsGetData, ListNotificationsApiV1NotificationsGetErrors, ListNotificationsApiV1NotificationsGetResponses, ListOpenFlagsApiV1AdminFlagsGetData, ListOpenFlagsApiV1AdminFlagsGetErrors, ListOpenFlagsApiV1AdminFlagsGetResponses, ListProfilesApiV1ProfilesGetData, ListProfilesApiV1ProfilesGetErrors, ListProfilesApiV1ProfilesGetResponses, ListProfileStoryStatusApiV1ProfilesStoryStatusGetData, ListProfileStoryStatusApiV1ProfilesStoryStatusGetErrors, ListProfileStoryStatusApiV1ProfilesStoryStatusGetResponses, ListRatingsApiV1RatingsProfileIdGetData, ListRatingsApiV1RatingsProfileIdGetErrors, ListRatingsApiV1RatingsProfileIdGetResponses, ListStoryRequestsAdminApiV1AdminStoryRequestsGetData, ListStoryRequestsAdminApiV1AdminStoryRequestsGetErrors, ListStoryRequestsAdminApiV1AdminStoryRequestsGetResponses, ListStoryRequestsApiV1StoryRequestsGetData, ListStoryRequestsApiV1StoryRequestsGetErrors, ListStoryRequestsApiV1StoryRequestsGetResponses, ListThresholdsApiV1AdminModerationThresholdsGetData, ListThresholdsApiV1AdminModerationThresholdsGetErrors, ListThresholdsApiV1AdminModerationThresholdsGetResponses, ListUsersApiV1AdminUsersGetData, ListUsersApiV1AdminUsersGetErrors, ListUsersApiV1AdminUsersGetResponses, LivenessApiV1HealthLiveGetData, LivenessApiV1HealthLiveGetResponses, ModerationDashboardApiV1AdminModerationDashboardGetData, ModerationDashboardApiV1AdminModerationDashboardGetErrors, ModerationDashboardApiV1AdminModerationDashboardGetResponses, ModerationSuggestionsApiV1AdminModerationSuggestionsGetData, ModerationSuggestionsApiV1AdminModerationSuggestionsGetErrors, ModerationSuggestionsApiV1AdminModerationSuggestionsGetResponses, OnboardApiV1OnboardingPostData, OnboardApiV1OnboardingPostErrors, OnboardApiV1OnboardingPostResponses, PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutData, PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutErrors, PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutResponses, PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutData, PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutErrors, PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutResponses, PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutData, PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutErrors, PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutResponses, ReadinessApiV1HealthReadyGetData, ReadinessApiV1HealthReadyGetErrors, ReadinessApiV1HealthReadyGetResponses, RecordCompletionApiV1CompletionsPostData, RecordCompletionApiV1CompletionsPostErrors, RecordCompletionApiV1CompletionsPostResponses, RecordRatingApiV1RatingsPostData, RecordRatingApiV1RatingsPostErrors, RecordRatingApiV1RatingsPostResponses, RemoveDeviceDownloadApiV1DeviceDownloadsDeleteData, RemoveDeviceDownloadApiV1DeviceDownloadsDeleteErrors, RemoveDeviceDownloadApiV1DeviceDownloadsDeleteResponses, ReportDeviceDownloadApiV1DeviceDownloadsPutData, ReportDeviceDownloadApiV1DeviceDownloadsPutErrors, ReportDeviceDownloadApiV1DeviceDownloadsPutResponses, RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostData, RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostErrors, RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostResponses, ResolveFlagApiV1AdminFlagsFlagIdResolvePostData, ResolveFlagApiV1AdminFlagsFlagIdResolvePostErrors, ResolveFlagApiV1AdminFlagsFlagIdResolvePostResponses, RetireCharacterApiV1CharactersCharacterIdRetirePostData, RetireCharacterApiV1CharactersCharacterIdRetirePostErrors, RetireCharacterApiV1CharactersCharacterIdRetirePostResponses, RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteData, RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteErrors, RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteResponses, RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteData, RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteErrors, RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteResponses, RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteData, RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteErrors, RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteResponses, SendBackStorybookApiV1StorybooksStorybookIdSendBackPostData, SendBackStorybookApiV1StorybooksStorybookIdSendBackPostErrors, SendBackStorybookApiV1StorybooksStorybookIdSendBackPostResponses, StartKwsVerificationApiV1ConsentKwsStartPostData, StartKwsVerificationApiV1ConsentKwsStartPostErrors, StartKwsVerificationApiV1ConsentKwsStartPostResponses, StartupApiV1HealthStartupGetData, StartupApiV1HealthStartupGetResponses, StreamNotificationsApiV1NotificationsStreamGetData, StreamNotificationsApiV1NotificationsStreamGetErrors, StreamNotificationsApiV1NotificationsStreamGetResponses, SubmitStorybookApiV1StorybooksStorybookIdSubmitPostData, SubmitStorybookApiV1StorybooksStorybookIdSubmitPostErrors, SubmitStorybookApiV1StorybooksStorybookIdSubmitPostResponses, TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostData, TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostErrors, TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostResponses, TriggerRescreenApiV1AdminRescreenPostData, TriggerRescreenApiV1AdminRescreenPostErrors, TriggerRescreenApiV1AdminRescreenPostResponses, UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteData, UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteErrors, UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteResponses, UpdateAdminProfileApiV1AdminProfilesProfileIdPatchData, UpdateAdminProfileApiV1AdminProfilesProfileIdPatchErrors, UpdateAdminProfileApiV1AdminProfilesProfileIdPatchResponses, UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutData, UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutErrors, UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutResponses, UpdateCharacterApiV1CharactersCharacterIdPatchData, UpdateCharacterApiV1CharactersCharacterIdPatchErrors, UpdateCharacterApiV1CharactersCharacterIdPatchResponses, UpdateFamilyApiV1AdminFamiliesFamilyIdPatchData, UpdateFamilyApiV1AdminFamiliesFamilyIdPatchErrors, UpdateFamilyApiV1AdminFamiliesFamilyIdPatchResponses, UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutData, UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutErrors, UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutResponses, UpdateProfileApiV1ProfilesProfileIdPatchData, UpdateProfileApiV1ProfilesProfileIdPatchErrors, UpdateProfileApiV1ProfilesProfileIdPatchResponses, UpdateUserApiV1AdminUsersUserIdPatchData, UpdateUserApiV1AdminUsersUserIdPatchErrors, UpdateUserApiV1AdminUsersUserIdPatchResponses, UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutData, UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutErrors, UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutResponses, ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostData, ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostErrors, ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostResponses, WhoamiApiV1MeGetData, WhoamiApiV1MeGetErrors, WhoamiApiV1MeGetResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Liveness probe
 *
 * Indicates if the application is running. Used by Kubernetes liveness probe.
 */
export const livenessApiV1HealthLiveGet = <ThrowOnError extends boolean = false>(options?: Options<LivenessApiV1HealthLiveGetData, ThrowOnError>): RequestResult<LivenessApiV1HealthLiveGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<LivenessApiV1HealthLiveGetResponses, unknown, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/health/live',
    ...options
});

/**
 * Readiness probe
 *
 * Checks if the application can serve traffic. Used by Kubernetes readiness probe.
 */
export const readinessApiV1HealthReadyGet = <ThrowOnError extends boolean = false>(options?: Options<ReadinessApiV1HealthReadyGetData, ThrowOnError>): RequestResult<ReadinessApiV1HealthReadyGetResponses, ReadinessApiV1HealthReadyGetErrors, ThrowOnError> => (options?.client ?? client).get<ReadinessApiV1HealthReadyGetResponses, ReadinessApiV1HealthReadyGetErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/health/ready',
    ...options
});

/**
 * Startup probe
 *
 * Indicates if the application has completed startup. Used by Kubernetes startup probe.
 */
export const startupApiV1HealthStartupGet = <ThrowOnError extends boolean = false>(options?: Options<StartupApiV1HealthStartupGetData, ThrowOnError>): RequestResult<StartupApiV1HealthStartupGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<StartupApiV1HealthStartupGetResponses, unknown, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/health/startup',
    ...options
});

/**
 * List Library
 *
 * List published stories visible to the given profile.
 *
 * Args:
 * profile_id: The child profile requesting its library.
 * principal: The authenticated principal.
 * session: The request session.
 *
 * Returns:
 * LibraryView: The published stories in the profile's family.
 */
export const listLibraryApiV1LibraryGet = <ThrowOnError extends boolean = false>(options: Options<ListLibraryApiV1LibraryGetData, ThrowOnError>): RequestResult<ListLibraryApiV1LibraryGetResponses, ListLibraryApiV1LibraryGetErrors, ThrowOnError> => (options.client ?? client).get<ListLibraryApiV1LibraryGetResponses, ListLibraryApiV1LibraryGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/library',
    ...options
});

/**
 * Get Storybook Version
 *
 * Return the immutable Storybook JSON for a specific version.
 *
 * Args:
 * storybook_id: The story id.
 * version: The story version.
 * principal: The authenticated principal.
 * session: The request session.
 *
 * Returns:
 * dict[str, object]: The Storybook content blob.
 *
 * Raises:
 * ResourceNotFoundError: If the story or version does not exist.
 */
export const getStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGet = <ThrowOnError extends boolean = false>(options: Options<GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetData, ThrowOnError>): RequestResult<GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetResponses, GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetErrors, ThrowOnError> => (options.client ?? client).get<GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetResponses, GetStorybookVersionApiV1StorybooksStorybookIdVersionsVersionGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/versions/{version}',
    ...options
});

/**
 * Get Reading State
 *
 * Return a child's reading state for a story.
 *
 * Args:
 * profile_id: The child profile.
 * storybook_id: The story.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * ReadingStateView: The stored reading state.
 *
 * Raises:
 * ResourceNotFoundError: If the story or reading state does not exist.
 */
export const getReadingStateApiV1ReadingStateProfileIdStorybookIdGet = <ThrowOnError extends boolean = false>(options: Options<GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetData, ThrowOnError>): RequestResult<GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetResponses, GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetResponses, GetReadingStateApiV1ReadingStateProfileIdStorybookIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reading-state/{profile_id}/{storybook_id}',
    ...options
});

/**
 * Put Reading State
 *
 * Save reading progress with revision-based optimistic concurrency.
 *
 * Args:
 * profile_id: The child profile (authoritative; body has no profile_id).
 * storybook_id: The story.
 * body: The save payload.
 * ctx: The request context.
 *
 * Returns:
 * ReadingStateView | JSONResponse: The saved row on success, or a 409
 * conflict body carrying the current row on a revision/version clash.
 *
 * Raises:
 * ResourceNotFoundError: If the story, or the version body.version cites,
 * does not exist.
 * ValidationError: If a first (create) save does not start at revision 0,
 * or the submitted state fails the structural floor or full replay.
 */
export const putReadingStateApiV1ReadingStateProfileIdStorybookIdPut = <ThrowOnError extends boolean = false>(options: Options<PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutData, ThrowOnError>): RequestResult<PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutResponses, PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutErrors, ThrowOnError> => (options.client ?? client).put<PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutResponses, PutReadingStateApiV1ReadingStateProfileIdStorybookIdPutErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reading-state/{profile_id}/{storybook_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Series Next
 *
 * Resolve the next book in this storybook's series for a profile.
 *
 * Expected absences (not a series book, no next book yet, next book
 * unpublished, next book not readable by this profile) answer 200 with
 * ``next: null``; errors are reserved for the CURRENT book being unknown
 * or unreadable, matching the other kid-scoped reading routes (WS-G spec
 * section 4).
 *
 * Args:
 * profile_id: The child profile asking to continue.
 * storybook_id: The series book the profile just finished.
 * ctx: The request context.
 *
 * Returns:
 * SeriesNextView: The next book's id, published version, title,
 * declared entry node, and state-carry flag, or ``next: null``.
 *
 * Raises:
 * ResourceNotFoundError: If the current storybook does not exist.
 * AuthorizationError: If the profile is not the principal's, or the
 * CURRENT book is not readable by it.
 */
export const getSeriesNextApiV1SeriesNextProfileIdStorybookIdGet = <ThrowOnError extends boolean = false>(options: Options<GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetData, ThrowOnError>): RequestResult<GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetResponses, GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetResponses, GetSeriesNextApiV1SeriesNextProfileIdStorybookIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/series-next/{profile_id}/{storybook_id}',
    ...options
});

/**
 * Record Completion
 *
 * Record that a child reached an ending of a story version.
 *
 * Args:
 * body: The completion request.
 * ctx: The request context.
 *
 * Returns:
 * CompletionRecordedView: The recorded (or pre-existing) completion,
 * plus ``is_new`` (whether this call inserted the row rather than
 * hitting an existing one), ``found`` (this profile's distinct-ending
 * count for the book/version after this call), and ``total`` (the
 * version's declared ending count). See design review 2026-08-01
 * section 3.4 / kid-appeal-implementation-plan.md W0.3.
 *
 * Raises:
 * ResourceNotFoundError: If the story or version does not exist.
 * ValidationError: If the ending id is not part of the cited version.
 */
export const recordCompletionApiV1CompletionsPost = <ThrowOnError extends boolean = false>(options: Options<RecordCompletionApiV1CompletionsPostData, ThrowOnError>): RequestResult<RecordCompletionApiV1CompletionsPostResponses, RecordCompletionApiV1CompletionsPostErrors, ThrowOnError> => (options.client ?? client).post<RecordCompletionApiV1CompletionsPostResponses, RecordCompletionApiV1CompletionsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Completions
 *
 * List every ending a child profile has completed.
 *
 * Phase 3d (COPPA 312.6(a) access / GDPR Article 15): ``completion`` was
 * the one child-linked table with no read path at all before this endpoint;
 * a guardian requesting an access/export report for their child had no way
 * to retrieve it.
 *
 * Args:
 * profile_id: The child profile whose completions are requested.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * CompletionListView: The profile's completions.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID.
 * AuthorizationError: If the profile is not the caller's.
 */
export const listCompletionsApiV1CompletionsProfileIdGet = <ThrowOnError extends boolean = false>(options: Options<ListCompletionsApiV1CompletionsProfileIdGetData, ThrowOnError>): RequestResult<ListCompletionsApiV1CompletionsProfileIdGetResponses, ListCompletionsApiV1CompletionsProfileIdGetErrors, ThrowOnError> => (options.client ?? client).get<ListCompletionsApiV1CompletionsProfileIdGetResponses, ListCompletionsApiV1CompletionsProfileIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/completions/{profile_id}',
    ...options
});

/**
 * Get Reading History
 *
 * Return a profile's per-storybook reading history (K6 endings tracker).
 *
 * Args:
 * profile_id: The child profile whose history is requested.
 * principal: The authenticated principal.
 * session: The request session.
 *
 * Returns:
 * ReadingHistoryView: One row per storybook the profile has any
 * ``Completion`` or ``ReadingState`` row for.
 *
 * Raises:
 * ValidationError: If ``profile_id`` is not a UUID.
 * AuthorizationError: If a non-admin principal does not own the profile.
 */
export const getReadingHistoryApiV1ReadingHistoryProfileIdGet = <ThrowOnError extends boolean = false>(options: Options<GetReadingHistoryApiV1ReadingHistoryProfileIdGetData, ThrowOnError>): RequestResult<GetReadingHistoryApiV1ReadingHistoryProfileIdGetResponses, GetReadingHistoryApiV1ReadingHistoryProfileIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetReadingHistoryApiV1ReadingHistoryProfileIdGetResponses, GetReadingHistoryApiV1ReadingHistoryProfileIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/reading-history/{profile_id}',
    ...options
});

/**
 * Get Family Reading Summary
 *
 * Return per-child engagement signals for the caller's own family (G9).
 *
 * W3.3 (gamification-recommendation-2026-08-01.md section 2.4) extends this
 * with ``minutes_last_7_days`` (a rolling 7-calendar-day breakdown, oldest
 * first, zero-filled for a day with no reading) and
 * ``days_read_this_week`` (count of ISO-week days, Monday start, with any
 * active reading time), both derived from ``reading_activity_day``.
 *
 * Args:
 * principal: The authenticated principal.
 * session: The request session.
 *
 * Returns:
 * FamilyReadingSummaryView: One row per child profile in the caller's
 * family, ordered like ``profiles.py::list_profiles`` (creation order).
 *
 * Raises:
 * AuthorizationError: If the caller is neither a guardian nor an admin.
 */
export const getFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGet = <ThrowOnError extends boolean = false>(options?: Options<GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetData, ThrowOnError>): RequestResult<GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetResponses, GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetErrors, ThrowOnError> => (options?.client ?? client).get<GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetResponses, GetFamilyReadingSummaryApiV1FamiliesMeReadingSummaryGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/families/me/reading-summary',
    ...options
});

/**
 * Create Concept
 *
 * Create a concept brief for story generation.
 *
 * The brief is PII-screened against the family's real child names before
 * being persisted. A brief that embeds a real child's display name is
 * rejected with 422.
 *
 * Args:
 * body: The request body containing the validated ``ConceptBrief``.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * ConceptCreatedResponse: The id of the newly created concept.
 *
 * Raises:
 * AuthorizationError: If the principal is not a guardian (-> 403).
 * ValidationError: If the brief contains a real child name (-> 422).
 */
export const createConceptApiV1ConceptsPost = <ThrowOnError extends boolean = false>(options: Options<CreateConceptApiV1ConceptsPostData, ThrowOnError>): RequestResult<CreateConceptApiV1ConceptsPostResponses, CreateConceptApiV1ConceptsPostErrors, ThrowOnError> => (options.client ?? client).post<CreateConceptApiV1ConceptsPostResponses, CreateConceptApiV1ConceptsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/concepts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enqueue Concept Generation
 *
 * Enqueue a generation job for an existing concept.
 *
 * Creates a ``GenerationJob`` row with status ``queued`` and schedules a
 * best-effort RQ enqueue as a background task. If Redis is unreachable the row
 * is still created and a 202 is returned.
 *
 * Args:
 * concept_id: The UUID string of the concept to generate from.
 * ctx: The request context (principal and session).
 * background_tasks: FastAPI background-task collector; the enqueue runs
 * here so it fires after the request unit-of-work commits.
 *
 * Returns:
 * GenerationEnqueuedResponse: The new job id and initial status.
 *
 * Raises:
 * AuthorizationError: If the principal is not a guardian (-> 403) or if
 * the concept belongs to another family (-> 403).
 * ResourceNotFoundError: If the concept does not exist (-> 404).
 * StateTransitionError: If the family is at its active-job cap (-> 409).
 * ValidationError: If ``concept_id`` is not a valid UUID (-> 422).
 */
export const enqueueConceptGenerationApiV1ConceptsConceptIdGeneratePost = <ThrowOnError extends boolean = false>(options: Options<EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostData, ThrowOnError>): RequestResult<EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostResponses, EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostErrors, ThrowOnError> => (options.client ?? client).post<EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostResponses, EnqueueConceptGenerationApiV1ConceptsConceptIdGeneratePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/concepts/{concept_id}/generate',
    ...options
});

/**
 * List Generation Jobs
 *
 * List the calling guardian's family generation jobs, newest first.
 *
 * Guardian-only. Scoped to the principal's family via the Concept join. The
 * raw ``report`` column is never selected or returned (ADR-007): only status,
 * the linked storybook id/status/version, the short error, and enough brief
 * context to label the row.
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * GenerationJobListView: Up to the 50 most recent jobs for the family.
 *
 * Raises:
 * AuthorizationError: If the principal is not a guardian (-> 403).
 */
export const listGenerationJobsApiV1GenerationJobsGet = <ThrowOnError extends boolean = false>(options?: Options<ListGenerationJobsApiV1GenerationJobsGetData, ThrowOnError>): RequestResult<ListGenerationJobsApiV1GenerationJobsGetResponses, ListGenerationJobsApiV1GenerationJobsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListGenerationJobsApiV1GenerationJobsGetResponses, ListGenerationJobsApiV1GenerationJobsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/generation-jobs',
    ...options
});

/**
 * Get Generation Job
 *
 * Return the status for a generation job, and the report for admins only.
 *
 * Args:
 * job_id: The UUID string of the job to fetch.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * GenerationJobResponse: Status, storybook link, and error for any
 * family-scoped guardian; ``report`` is populated only when the
 * principal also holds the admin capability, and is ``None`` otherwise.
 *
 * Raises:
 * AuthorizationError: If the principal is not a guardian (-> 403) or if
 * the job's concept belongs to another family (-> 403).
 * ResourceNotFoundError: If the job or its concept does not exist (-> 404).
 * ValidationError: If ``job_id`` is not a valid UUID (-> 422).
 */
export const getGenerationJobApiV1GenerationJobsJobIdGet = <ThrowOnError extends boolean = false>(options: Options<GetGenerationJobApiV1GenerationJobsJobIdGetData, ThrowOnError>): RequestResult<GetGenerationJobApiV1GenerationJobsJobIdGetResponses, GetGenerationJobApiV1GenerationJobsJobIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetGenerationJobApiV1GenerationJobsJobIdGetResponses, GetGenerationJobApiV1GenerationJobsJobIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/generation-jobs/{job_id}',
    ...options
});

/**
 * Force Fail Generation Job
 *
 * Force-fail a stuck generation job so its family's cap slot is freed.
 *
 * Operator escape hatch for a job wedged at ``"queued"`` or ``"running"``
 * (for example after a hard worker death that the startup reclaim sweep has
 * not yet reached, or a job the operator wants to abandon). Because the
 * per-family active-job cap counts both statuses, two wedged rows otherwise
 * block that family from generating anything, with no other way to clear them.
 *
 * Args:
 * job_id: The UUID string of the job to force-fail.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * AdminJobActionResponse: The job id, its new status, and the error string.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 * ResourceNotFoundError: If the job does not exist (-> 404).
 * StateTransitionError: If the job is already in a terminal state (-> 409).
 * ValidationError: If ``job_id`` is not a valid UUID (-> 422).
 */
export const forceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPost = <ThrowOnError extends boolean = false>(options: Options<ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostData, ThrowOnError>): RequestResult<ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostResponses, ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostErrors, ThrowOnError> => (options.client ?? client).post<ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostResponses, ForceFailGenerationJobApiV1AdminGenerationJobsJobIdForceFailPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/generation-jobs/{job_id}/force-fail',
    ...options
});

/**
 * Validate Storybook Version
 *
 * Re-run the validation gate on a stored storybook version.
 *
 * Args:
 * storybook_id: The story id.
 * version: The version number to validate.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * ValidateResponse: Whether the version is blocked plus the gate report.
 *
 * Raises:
 * AuthorizationError: If the principal is not a guardian (-> 403) or if
 * the storybook belongs to another family (-> 403).
 * ResourceNotFoundError: If the storybook or version does not exist (-> 404).
 */
export const validateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePost = <ThrowOnError extends boolean = false>(options: Options<ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostData, ThrowOnError>): RequestResult<ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostResponses, ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostErrors, ThrowOnError> => (options.client ?? client).post<ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostResponses, ValidateStorybookVersionApiV1StorybooksStorybookIdVersionsVersionValidatePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/versions/{version}/validate',
    ...options
});

/**
 * List Profiles
 *
 * List the child profiles the calling principal may act on.
 *
 * Args:
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * ProfileListView: All family profiles for a guardian or a DEVICE
 * principal (ADR-014 phase 2: the picker needs the family's
 * profiles to offer without a live guardian bearer); the single
 * assigned profile for a child; empty if the principal has none.
 */
export const listProfilesApiV1ProfilesGet = <ThrowOnError extends boolean = false>(options?: Options<ListProfilesApiV1ProfilesGetData, ThrowOnError>): RequestResult<ListProfilesApiV1ProfilesGetResponses, ListProfilesApiV1ProfilesGetErrors, ThrowOnError> => (options?.client ?? client).get<ListProfilesApiV1ProfilesGetResponses, ListProfilesApiV1ProfilesGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles',
    ...options
});

/**
 * Create Profile
 *
 * Create a child profile in the calling guardian's family.
 *
 * Args:
 * body: The new profile's fields.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * ProfileView: The stored profile.
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian.
 * BusinessLogicError: If the calling guardian has no recorded VPC
 * consent (Phase 2 / ADR-018 D1).
 */
export const createProfileApiV1ProfilesPost = <ThrowOnError extends boolean = false>(options: Options<CreateProfileApiV1ProfilesPostData, ThrowOnError>): RequestResult<CreateProfileApiV1ProfilesPostResponses, CreateProfileApiV1ProfilesPostErrors, ThrowOnError> => (options.client ?? client).post<CreateProfileApiV1ProfilesPostResponses, CreateProfileApiV1ProfilesPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Profile Story Status
 *
 * Bulk "new story ready" pill status for every listable profile (W1.4).
 *
 * Serves the profile picker (design review 4.1): a device-grant or
 * not-yet-handed-off guardian principal, called BEFORE any child session is
 * minted, so a per-profile ``GET /library`` (which a picker-stage principal
 * could not even authorize for a sibling child, per
 * ``ProfilePickerPage.tsx``'s own deferred-work comment) is never an option
 * here. ``has_new_story`` is a plain boolean derived purely from
 * ``storybook_assignment`` timing; see ``_NEW_STORY_WINDOW`` for the "new"
 * definition and why it is a 7-day fallback rather than a last-session
 * comparison.
 *
 * Args:
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * ProfileStoryStatusListView: One boolean-only entry per profile
 * ``_listable_profiles`` returns for this caller -- never more, never a
 * title or count. A profile with no recent assignment reads
 * ``has_new_story=False``, not merely absent.
 */
export const listProfileStoryStatusApiV1ProfilesStoryStatusGet = <ThrowOnError extends boolean = false>(options?: Options<ListProfileStoryStatusApiV1ProfilesStoryStatusGetData, ThrowOnError>): RequestResult<ListProfileStoryStatusApiV1ProfilesStoryStatusGetResponses, ListProfileStoryStatusApiV1ProfilesStoryStatusGetErrors, ThrowOnError> => (options?.client ?? client).get<ListProfileStoryStatusApiV1ProfilesStoryStatusGetResponses, ListProfileStoryStatusApiV1ProfilesStoryStatusGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/story-status',
    ...options
});

/**
 * Delete Profile
 *
 * Permanently erase a child profile and all data linked to it.
 *
 * GDPR Article 17 / COPPA 312.10 (remediation plan Phase 3b). Every table
 * keyed on this profile (reading state, completions, ratings, storybook
 * assignments, kid flags, and its own login row if the child has one)
 * cascades at the database level (Phase 3a); story requests the child
 * submitted are de-linked (``profile_id`` set null) rather than deleted,
 * since they remain family-owned content and may already have produced a
 * published story.
 *
 * ``purge_profile_personalization`` (ADR-028) is called explicitly before
 * the row delete: its two target tables (``child_profile_personalization``
 * and ``character``) both already cascade at the database level, so this
 * call changes nothing about what ends up erased, but it makes
 * ``PURGE_TARGETS``'s claim about where the character_name slot's value
 * lives an assertion this route actually exercises, rather than one that
 * is only true because a foreign key happens to agree with it.
 *
 * Unlike ``update_profile``, this deliberately checks family ownership
 * (``authorize_family``) rather than ``authorize_profile``: a profile a
 * guardian has already deactivated is excluded from
 * ``principal.profile_ids`` (see ``api/deps.py::_resolve_profiles``), but
 * an erasure request must still succeed for a deactivated profile -- that
 * is, if anything, the MORE likely case for a real deletion request.
 *
 * Args:
 * profile_id: The child profile to delete (path).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian, or the profile
 * is not in the caller's family.
 * ResourceNotFoundError: If no profile with this id exists.
 */
export const deleteProfileApiV1ProfilesProfileIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteProfileApiV1ProfilesProfileIdDeleteData, ThrowOnError>): RequestResult<DeleteProfileApiV1ProfilesProfileIdDeleteResponses, DeleteProfileApiV1ProfilesProfileIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteProfileApiV1ProfilesProfileIdDeleteResponses, DeleteProfileApiV1ProfilesProfileIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}',
    ...options
});

/**
 * Update Profile
 *
 * Partially update a child profile in the guardian's own family.
 *
 * Args:
 * profile_id: The profile to update.
 * body: The fields to change; omitted fields are untouched. An explicit
 * ``null`` clears only ``avatar`` and ``pin``; on the other fields
 * it is a no-op (see ProfileUpdateBody).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * ProfileView: The updated profile.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID.
 * AuthorizationError: If the caller is not a guardian, or the profile is
 * not in the caller's family (or does not exist; both are 403 so the
 * endpoint leaks nothing about other families' ids).
 * ResourceNotFoundError: If the row vanished between authorization and
 * load (concurrent delete).
 */
export const updateProfileApiV1ProfilesProfileIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateProfileApiV1ProfilesProfileIdPatchData, ThrowOnError>): RequestResult<UpdateProfileApiV1ProfilesProfileIdPatchResponses, UpdateProfileApiV1ProfilesProfileIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateProfileApiV1ProfilesProfileIdPatchResponses, UpdateProfileApiV1ProfilesProfileIdPatchErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Characters
 *
 * List one profile's characters, active first.
 *
 * Args:
 * profile_id: The child profile whose characters are requested (query).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * CharacterListView: The profile's characters, active before retired,
 * then most-recently-created first within each group.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID.
 * AuthorizationError: If the profile is not the caller's.
 */
export const listCharactersApiV1CharactersGet = <ThrowOnError extends boolean = false>(options: Options<ListCharactersApiV1CharactersGetData, ThrowOnError>): RequestResult<ListCharactersApiV1CharactersGetResponses, ListCharactersApiV1CharactersGetErrors, ThrowOnError> => (options.client ?? client).get<ListCharactersApiV1CharactersGetResponses, ListCharactersApiV1CharactersGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/characters',
    ...options
});

/**
 * Create Character
 *
 * Create a character for a child profile, activating it.
 *
 * Creating a new character while one is already active retires the
 * incumbent in the same transaction: a profile has at most one active
 * character at any time.
 *
 * Args:
 * body: The new character's fields.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * CharacterView: The stored, active character with zeroed stats.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID, or the name fails the
 * structural or band-denylist check (see
 * ``_reject_unsafe_character_name``).
 * AuthorizationError: If the profile is not the caller's.
 * ResourceNotFoundError: If the profile row does not exist.
 * StateTransitionError: If a concurrent request already activated a
 * character for this profile between the retire check above and
 * this insert's flush (HTTP 409).
 */
export const createCharacterApiV1CharactersPost = <ThrowOnError extends boolean = false>(options: Options<CreateCharacterApiV1CharactersPostData, ThrowOnError>): RequestResult<CreateCharacterApiV1CharactersPostResponses, CreateCharacterApiV1CharactersPostErrors, ThrowOnError> => (options.client ?? client).post<CreateCharacterApiV1CharactersPostResponses, CreateCharacterApiV1CharactersPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/characters',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Character
 *
 * Permanently erase a character and its progression.
 *
 * Args:
 * character_id: The character to delete.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian, or the
 * character's profile is not the caller's.
 * ResourceNotFoundError: If no character with this id exists.
 */
export const deleteCharacterApiV1CharactersCharacterIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteCharacterApiV1CharactersCharacterIdDeleteData, ThrowOnError>): RequestResult<DeleteCharacterApiV1CharactersCharacterIdDeleteResponses, DeleteCharacterApiV1CharactersCharacterIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteCharacterApiV1CharactersCharacterIdDeleteResponses, DeleteCharacterApiV1CharactersCharacterIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/characters/{character_id}',
    ...options
});

/**
 * Update Character
 *
 * Rename or re-choose a character's look.
 *
 * Args:
 * character_id: The character to update.
 * body: The fields to change; omitted fields are untouched. Attributes,
 * books_completed, and archetype are absent from the body by design
 * (see ``CharacterUpdateBody``'s docstring) and cannot be set here.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * CharacterView: The updated character.
 *
 * Raises:
 * ValidationError: If character_id is not a UUID, or a submitted name
 * fails the structural or band-denylist check (see
 * ``_reject_unsafe_character_name``).
 * ResourceNotFoundError: If no character with this id exists, or its
 * owning profile row has gone missing.
 * AuthorizationError: If the character's profile is not the caller's.
 */
export const updateCharacterApiV1CharactersCharacterIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateCharacterApiV1CharactersCharacterIdPatchData, ThrowOnError>): RequestResult<UpdateCharacterApiV1CharactersCharacterIdPatchResponses, UpdateCharacterApiV1CharactersCharacterIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateCharacterApiV1CharactersCharacterIdPatchResponses, UpdateCharacterApiV1CharactersCharacterIdPatchErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/characters/{character_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Activate Character
 *
 * Make a retired character active again, retiring whichever is active now.
 *
 * Args:
 * character_id: The character to activate.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * CharacterView: The now-active character.
 *
 * Raises:
 * ValidationError: If character_id is not a UUID.
 * ResourceNotFoundError: If no character with this id exists.
 * AuthorizationError: If the character's profile is not the caller's.
 * StateTransitionError: If a concurrent request already activated a
 * different character for this profile between the retire check
 * below and this activation's flush (HTTP 409).
 */
export const activateCharacterApiV1CharactersCharacterIdActivatePost = <ThrowOnError extends boolean = false>(options: Options<ActivateCharacterApiV1CharactersCharacterIdActivatePostData, ThrowOnError>): RequestResult<ActivateCharacterApiV1CharactersCharacterIdActivatePostResponses, ActivateCharacterApiV1CharactersCharacterIdActivatePostErrors, ThrowOnError> => (options.client ?? client).post<ActivateCharacterApiV1CharactersCharacterIdActivatePostResponses, ActivateCharacterApiV1CharactersCharacterIdActivatePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/characters/{character_id}/activate',
    ...options
});

/**
 * Retire Character
 *
 * Retire a character, leaving the profile with no active character.
 *
 * Idempotent: retiring an already-retired character is a no-op rather than
 * overwriting its existing ``retired_at``.
 *
 * Args:
 * character_id: The character to retire.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * CharacterView: The character, now inactive.
 *
 * Raises:
 * ValidationError: If character_id is not a UUID.
 * ResourceNotFoundError: If no character with this id exists.
 * AuthorizationError: If the character's profile is not the caller's.
 */
export const retireCharacterApiV1CharactersCharacterIdRetirePost = <ThrowOnError extends boolean = false>(options: Options<RetireCharacterApiV1CharactersCharacterIdRetirePostData, ThrowOnError>): RequestResult<RetireCharacterApiV1CharactersCharacterIdRetirePostResponses, RetireCharacterApiV1CharactersCharacterIdRetirePostErrors, ThrowOnError> => (options.client ?? client).post<RetireCharacterApiV1CharactersCharacterIdRetirePostResponses, RetireCharacterApiV1CharactersCharacterIdRetirePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/characters/{character_id}/retire',
    ...options
});

/**
 * List Families
 *
 * List families (name order, capped) for the admin console.
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * FamilyListView: Up to ``_FAMILY_LIST_LIMIT`` families ordered by name.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 */
export const listFamiliesApiV1AdminFamiliesGet = <ThrowOnError extends boolean = false>(options?: Options<ListFamiliesApiV1AdminFamiliesGetData, ThrowOnError>): RequestResult<ListFamiliesApiV1AdminFamiliesGetResponses, ListFamiliesApiV1AdminFamiliesGetErrors, ThrowOnError> => (options?.client ?? client).get<ListFamiliesApiV1AdminFamiliesGetResponses, ListFamiliesApiV1AdminFamiliesGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/families',
    ...options
});

/**
 * Create Family
 *
 * Create a family (admin only; WS-J).
 *
 * Args:
 * body: The new family's name.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * FamilyView: The stored family (zero members).
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 */
export const createFamilyApiV1AdminFamiliesPost = <ThrowOnError extends boolean = false>(options: Options<CreateFamilyApiV1AdminFamiliesPostData, ThrowOnError>): RequestResult<CreateFamilyApiV1AdminFamiliesPostResponses, CreateFamilyApiV1AdminFamiliesPostErrors, ThrowOnError> => (options.client ?? client).post<CreateFamilyApiV1AdminFamiliesPostResponses, CreateFamilyApiV1AdminFamiliesPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/families',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Family
 *
 * Rename and/or change a family's active/deactivated status (WS-J).
 *
 * Deactivating a family cascades: every member ``User`` (guardian or admin)
 * and ``ChildProfile`` in it is deactivated in the same transaction, so the
 * auth boundary (``api/deps.py::require_principal``) only ever needs to
 * check ``User.status`` and never a family join. Reactivating a family does
 * **not** auto-reactivate its members; an admin reactivates people
 * individually (deliberate asymmetry).
 *
 * Args:
 * family_id: The family to update (path).
 * body: The fields to change; omitted fields are untouched.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * FamilyView: The updated family with refreshed member counts.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 * ResourceNotFoundError: If no family with this id exists (-> 404).
 */
export const updateFamilyApiV1AdminFamiliesFamilyIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateFamilyApiV1AdminFamiliesFamilyIdPatchData, ThrowOnError>): RequestResult<UpdateFamilyApiV1AdminFamiliesFamilyIdPatchResponses, UpdateFamilyApiV1AdminFamiliesFamilyIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateFamilyApiV1AdminFamiliesFamilyIdPatchResponses, UpdateFamilyApiV1AdminFamiliesFamilyIdPatchErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/families/{family_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record Rating
 *
 * Set or update the calling child's rating of a storybook.
 *
 * Args:
 * body: The rating request (profile, storybook, value 1-5).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * RatingView: The stored rating.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID.
 * AuthorizationError: If the profile or storybook is not the caller's.
 * ResourceNotFoundError: If the storybook does not exist.
 */
export const recordRatingApiV1RatingsPost = <ThrowOnError extends boolean = false>(options: Options<RecordRatingApiV1RatingsPostData, ThrowOnError>): RequestResult<RecordRatingApiV1RatingsPostResponses, RecordRatingApiV1RatingsPostErrors, ThrowOnError> => (options.client ?? client).post<RecordRatingApiV1RatingsPostResponses, RecordRatingApiV1RatingsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/ratings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Ratings
 *
 * List all ratings recorded by a child profile.
 *
 * Args:
 * profile_id: The child profile whose ratings are requested.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * RatingListView: The profile's ratings.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID.
 * AuthorizationError: If the profile is not the caller's.
 */
export const listRatingsApiV1RatingsProfileIdGet = <ThrowOnError extends boolean = false>(options: Options<ListRatingsApiV1RatingsProfileIdGetData, ThrowOnError>): RequestResult<ListRatingsApiV1RatingsProfileIdGetResponses, ListRatingsApiV1RatingsProfileIdGetErrors, ThrowOnError> => (options.client ?? client).get<ListRatingsApiV1RatingsProfileIdGetResponses, ListRatingsApiV1RatingsProfileIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/ratings/{profile_id}',
    ...options
});

/**
 * Get Content Summary
 *
 * Return the redacted content review summary for a published story.
 *
 * Guardians see this in the assign flow so they know what a book was flagged
 * for before granting it to a child. It carries the gating summary, the total
 * flagged count, story-level findings, and a story-level validator note
 * aggregate (design doc 2.7 option (a): RL-13/PL-19 counts, no node ids);
 * per-node flagged passages are withheld (the admin review surface owns
 * those).
 *
 * Args:
 * storybook_id: The published story to summarize.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * ContentSummaryView: The redacted guardian content summary.
 *
 * Raises:
 * AuthorizationError: Child caller, or a guardian from another family
 * reading a non-catalog (family-visibility) book (403).
 * ResourceNotFoundError: Unknown or unpublished story, or a missing
 * published version row (404).
 * ValidationError: If the stored moderation report is corrupt at rest.
 */
export const getContentSummaryApiV1StorybooksStorybookIdContentSummaryGet = <ThrowOnError extends boolean = false>(options: Options<GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetData, ThrowOnError>): RequestResult<GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetResponses, GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetErrors, ThrowOnError> => (options.client ?? client).get<GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetResponses, GetContentSummaryApiV1StorybooksStorybookIdContentSummaryGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/content-summary',
    ...options
});

/**
 * List Assignments
 *
 * List the child profiles a story is currently assigned to.
 *
 * Args:
 * storybook_id: The story whose assignments are requested.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AssignmentListView: The current assigned profile ids.
 *
 * Raises:
 * AuthorizationError: Non-guardian caller, or a storybook that is
 * neither own-family nor catalog.
 * ResourceNotFoundError: Unknown storybook id.
 */
export const listAssignmentsApiV1StorybooksStorybookIdAssignmentsGet = <ThrowOnError extends boolean = false>(options: Options<ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetData, ThrowOnError>): RequestResult<ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetResponses, ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetErrors, ThrowOnError> => (options.client ?? client).get<ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetResponses, ListAssignmentsApiV1StorybooksStorybookIdAssignmentsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/assignments',
    ...options
});

/**
 * Assign Storybook
 *
 * Assign a published story to one or more of the guardian's child profiles.
 *
 * Args:
 * storybook_id: The story to assign.
 * body: The requested profile ids.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * AssignmentListView: The full current set of assigned profile ids.
 *
 * Raises:
 * AuthorizationError: Non-guardian caller, a storybook that is neither
 * own-family nor catalog, or a profile outside the family.
 * ResourceNotFoundError: Unknown storybook id.
 * BusinessLogicError: The story is not published.
 * ValidationError: A profile id is not a UUID.
 */
export const assignStorybookApiV1StorybooksStorybookIdAssignmentsPost = <ThrowOnError extends boolean = false>(options: Options<AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostData, ThrowOnError>): RequestResult<AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostResponses, AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostErrors, ThrowOnError> => (options.client ?? client).post<AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostResponses, AssignStorybookApiV1StorybooksStorybookIdAssignmentsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/assignments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unassign Storybook
 *
 * Revoke a single child's access to a story (G8 per-child kill switch).
 *
 * Removing the assignment row is the whole operation: the child's library
 * listing and direct version fetch both gate on ``storybook_assignment``
 * (api/library.py), so deleting the row hides the book at the next sync and
 * the offline cache evicts it via ``reconcileOfflineCache``. Reading state,
 * completion, and rating rows are deliberately PRESERVED (invisible behind the
 * read gate) so re-assigning resumes the child's progress. Unlike the assign
 * path there is no published-status gate, so a guardian can still pull an
 * already-archived book off a child's shelf. The response is the book's full
 * remaining assignment set, matching the POST path.
 *
 * Args:
 * storybook_id: The story to revoke.
 * profile_id: The child profile to revoke it from.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * AssignmentListView: The book's remaining assigned profile ids for the
 * caller's family.
 *
 * Raises:
 * AuthorizationError: Non-guardian caller, a storybook that is neither
 * own-family nor catalog, or a profile outside the family.
 * ResourceNotFoundError: Unknown storybook id.
 * ValidationError: The profile id is not a UUID.
 */
export const unassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDelete = <ThrowOnError extends boolean = false>(options: Options<UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteData, ThrowOnError>): RequestResult<UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteResponses, UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteResponses, UnassignStorybookApiV1StorybooksStorybookIdAssignmentsProfileIdDeleteErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/assignments/{profile_id}',
    ...options
});

/**
 * List Guardian Books
 *
 * List the family's published books with content tags and assignments.
 *
 * A guardian browses every published, approved book in their OWN family (not
 * just their own request history), each carrying a redacted content badge
 * (screened flag + flagged count) and the set of child profiles it is
 * currently assigned to, so they can decide what to grant. Books are ordered
 * newest-first by creation time (ties broken by id) so the list has a stable,
 * guardian-friendly order across requests.
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * GuardianBooksView: The family's published books, each with a content
 * badge and its current assignment set.
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian; a child cannot
 * enumerate the family's books and an admin has no assign authority on
 * this family surface (403).
 */
export const listGuardianBooksApiV1GuardianBooksGet = <ThrowOnError extends boolean = false>(options?: Options<ListGuardianBooksApiV1GuardianBooksGetData, ThrowOnError>): RequestResult<ListGuardianBooksApiV1GuardianBooksGetResponses, ListGuardianBooksApiV1GuardianBooksGetErrors, ThrowOnError> => (options?.client ?? client).get<ListGuardianBooksApiV1GuardianBooksGetResponses, ListGuardianBooksApiV1GuardianBooksGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/guardian/books',
    ...options
});

/**
 * Submit Storybook
 *
 * Submit a draft or needs-revision story for review (admin only).
 */
export const submitStorybookApiV1StorybooksStorybookIdSubmitPost = <ThrowOnError extends boolean = false>(options: Options<SubmitStorybookApiV1StorybooksStorybookIdSubmitPostData, ThrowOnError>): RequestResult<SubmitStorybookApiV1StorybooksStorybookIdSubmitPostResponses, SubmitStorybookApiV1StorybooksStorybookIdSubmitPostErrors, ThrowOnError> => (options.client ?? client).post<SubmitStorybookApiV1StorybooksStorybookIdSubmitPostResponses, SubmitStorybookApiV1StorybooksStorybookIdSubmitPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/submit',
    ...options
});

/**
 * Approve Storybook
 *
 * Approve and publish the latest version of an in-review story (admin only).
 */
export const approveStorybookApiV1StorybooksStorybookIdApprovePost = <ThrowOnError extends boolean = false>(options: Options<ApproveStorybookApiV1StorybooksStorybookIdApprovePostData, ThrowOnError>): RequestResult<ApproveStorybookApiV1StorybooksStorybookIdApprovePostResponses, ApproveStorybookApiV1StorybooksStorybookIdApprovePostErrors, ThrowOnError> => (options.client ?? client).post<ApproveStorybookApiV1StorybooksStorybookIdApprovePostResponses, ApproveStorybookApiV1StorybooksStorybookIdApprovePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send Back Storybook
 *
 * Send an in-review story back for revision with a reason (admin only).
 */
export const sendBackStorybookApiV1StorybooksStorybookIdSendBackPost = <ThrowOnError extends boolean = false>(options: Options<SendBackStorybookApiV1StorybooksStorybookIdSendBackPostData, ThrowOnError>): RequestResult<SendBackStorybookApiV1StorybooksStorybookIdSendBackPostResponses, SendBackStorybookApiV1StorybooksStorybookIdSendBackPostErrors, ThrowOnError> => (options.client ?? client).post<SendBackStorybookApiV1StorybooksStorybookIdSendBackPostResponses, SendBackStorybookApiV1StorybooksStorybookIdSendBackPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/send-back',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Archive Storybook
 *
 * Archive a published story, removing it from the library (admin only).
 */
export const archiveStorybookApiV1StorybooksStorybookIdArchivePost = <ThrowOnError extends boolean = false>(options: Options<ArchiveStorybookApiV1StorybooksStorybookIdArchivePostData, ThrowOnError>): RequestResult<ArchiveStorybookApiV1StorybooksStorybookIdArchivePostResponses, ArchiveStorybookApiV1StorybooksStorybookIdArchivePostErrors, ThrowOnError> => (options.client ?? client).post<ArchiveStorybookApiV1StorybooksStorybookIdArchivePostResponses, ArchiveStorybookApiV1StorybooksStorybookIdArchivePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/archive',
    ...options
});

/**
 * Get Review Surface
 *
 * Return the review surface for a story version.
 *
 * Admin (any family), or guardian for their own family's story.
 *
 * Args:
 * storybook_id: The story to review.
 * ctx: The request context (principal and session).
 * version: The version to review; defaults to the latest.
 *
 * Returns:
 * ReviewSurfaceView: Blob plus moderation summary, flagged passages,
 * story-level findings, the ranked/structural/low-advisory merged-
 * finding buckets, and the story's validator (RL-13/PL-19) findings.
 *
 * Raises:
 * AuthorizationError: If the caller is neither admin nor guardian
 * (403), or a guardian requests another family's story (403).
 * ValidationError: If a supplied version is not a positive integer, or the
 * stored moderation report is corrupt at rest.
 * ResourceNotFoundError: If the story or the requested version does not exist.
 */
export const getReviewSurfaceApiV1StorybooksStorybookIdReviewGet = <ThrowOnError extends boolean = false>(options: Options<GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetData, ThrowOnError>): RequestResult<GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetResponses, GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetErrors, ThrowOnError> => (options.client ?? client).get<GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetResponses, GetReviewSurfaceApiV1StorybooksStorybookIdReviewGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/review',
    ...options
});

/**
 * Get Review Queue
 *
 * Return every storybook awaiting an admin publish decision (admin only).
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * ReviewQueueView: One item per ``in_review`` storybook, across all
 * families, carrying the screened flag and flagged count so the
 * console can bucket Flagged versus Ready to review.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 */
export const getReviewQueueApiV1ReviewQueueGet = <ThrowOnError extends boolean = false>(options?: Options<GetReviewQueueApiV1ReviewQueueGetData, ThrowOnError>): RequestResult<GetReviewQueueApiV1ReviewQueueGetResponses, GetReviewQueueApiV1ReviewQueueGetErrors, ThrowOnError> => (options?.client ?? client).get<GetReviewQueueApiV1ReviewQueueGetResponses, GetReviewQueueApiV1ReviewQueueGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/review-queue',
    ...options
});

/**
 * List Admin Storybooks
 *
 * Return every storybook for the admin master library (admin only, P19).
 *
 * Unlike the review queue (in_review only), this lists stories in ANY
 * lifecycle status so an admin can browse and re-open a published, archived,
 * needs_revision, or draft story via the existing review detail view. Newest
 * activity (latest version's creation time) first.
 *
 * Args:
 * ctx: The request context (principal and session).
 * status: Optional lifecycle-status filter (e.g. ``"published"``); when
 * omitted, every status is returned.
 *
 * Returns:
 * StorybookLibraryView: One summary per storybook, newest activity first.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 * ValidationError: If ``status`` is not a recognized lifecycle status.
 */
export const listAdminStorybooksApiV1AdminStorybooksGet = <ThrowOnError extends boolean = false>(options?: Options<ListAdminStorybooksApiV1AdminStorybooksGetData, ThrowOnError>): RequestResult<ListAdminStorybooksApiV1AdminStorybooksGetResponses, ListAdminStorybooksApiV1AdminStorybooksGetErrors, ThrowOnError> => (options?.client ?? client).get<ListAdminStorybooksApiV1AdminStorybooksGetResponses, ListAdminStorybooksApiV1AdminStorybooksGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/storybooks',
    ...options
});

/**
 * Edit Node
 *
 * Apply a prose-only edit to one node, re-running the gate and moderation.
 *
 * Args:
 * storybook_id: The story id from the path.
 * version: The version number from the path; must be the story's
 * latest version.
 * node_id: The node id from the path.
 * body: The edit: new body text and/or new choice label text, prose only.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * ReviewSurfaceView: The refreshed review surface (the same shape
 * ``GET .../review`` returns), reflecting the newly persisted blob
 * and reports.
 *
 * Raises:
 * AuthorizationError: Neither admin nor guardian (-> 403); a guardian
 * outside the story's family (-> 403).
 * ResourceNotFoundError: Unknown story/version/node (-> 404).
 * StateTransitionError: The story is not ``in_review`` or
 * ``needs_revision`` (-> 409), or ``version`` is not the latest
 * version (-> 409).
 * ValidationError: ``choice_labels`` names an id the node does not
 * have (-> 422), or the edited blob fails the deterministic gate
 * (-> 422; the stored blob is left unchanged).
 */
export const editNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatch = <ThrowOnError extends boolean = false>(options: Options<EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchData, ThrowOnError>): RequestResult<EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchResponses, EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchResponses, EditNodeApiV1StorybooksStorybookIdVersionsVersionNodesNodeIdPatchErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/versions/{version}/nodes/{node_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cover Status
 *
 * Return current cover status/URL for polling (admin only).
 */
export const coverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGet = <ThrowOnError extends boolean = false>(options: Options<CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetData, ThrowOnError>): RequestResult<CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetResponses, CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetErrors, ThrowOnError> => (options.client ?? client).get<CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetResponses, CoverStatusApiV1StorybooksStorybookIdVersionsVersionCoverGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/versions/{version}/cover',
    ...options
});

/**
 * Request Cover
 *
 * Enqueue cover generation for a story version (admin only).
 */
export const requestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPost = <ThrowOnError extends boolean = false>(options: Options<RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostData, ThrowOnError>): RequestResult<RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostResponses, RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostErrors, ThrowOnError> => (options.client ?? client).post<RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostResponses, RequestCoverApiV1StorybooksStorybookIdVersionsVersionCoverPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/versions/{version}/cover',
    ...options
});

/**
 * Approve Cover
 *
 * Approve a pending-review cover so it becomes visible to children (admin only).
 *
 * H2 fix (security-hardening-plan-2026-07.md): the human-approval gate that
 * ``generate_cover`` stops short of. A cover generated successfully sits at
 * ``cover_status == "pending_review"``. ``_cover_url`` presigns it for this
 * admin review surface only, and it stays withheld from every child library
 * card (see ``api/library.py``'s ``cover_status == "ready"`` filter) until an
 * admin calls this endpoint. That holds for every API read path; it says
 * nothing about direct access to the stored object, whose key is
 * deterministic and whose exposure is governed by the bucket-privacy
 * invariant in ``covers/storage.py``.
 */
export const approveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePost = <ThrowOnError extends boolean = false>(options: Options<ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostData, ThrowOnError>): RequestResult<ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostResponses, ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostErrors, ThrowOnError> => (options.client ?? client).post<ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostResponses, ApproveCoverApiV1StorybooksStorybookIdVersionsVersionCoverApprovePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/versions/{version}/cover/approve',
    ...options
});

/**
 * List Thresholds
 *
 * List all overrides plus the code default (admin only).
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * ThresholdListView: The code default, known categories, and all
 * stored override rows.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 */
export const listThresholdsApiV1AdminModerationThresholdsGet = <ThrowOnError extends boolean = false>(options?: Options<ListThresholdsApiV1AdminModerationThresholdsGetData, ThrowOnError>): RequestResult<ListThresholdsApiV1AdminModerationThresholdsGetResponses, ListThresholdsApiV1AdminModerationThresholdsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListThresholdsApiV1AdminModerationThresholdsGetResponses, ListThresholdsApiV1AdminModerationThresholdsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation-thresholds',
    ...options
});

/**
 * Delete Threshold
 *
 * Remove one override (reverting to the default); audit it (admin only).
 *
 * ``category`` is a QUERY parameter for the same slash-in-category reason
 * as the upsert route.
 *
 * Args:
 * age_band: The age band half of the natural key (path).
 * category: The category half of the natural key (query).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * ThresholdListView: The full list view after the delete.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If ``age_band`` is not a known band (422).
 * ResourceNotFoundError: If no override exists for the key (404).
 */
export const deleteThresholdApiV1AdminModerationThresholdsAgeBandDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteData, ThrowOnError>): RequestResult<DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteResponses, DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteResponses, DeleteThresholdApiV1AdminModerationThresholdsAgeBandDeleteErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation-thresholds/{age_band}',
    ...options
});

/**
 * Upsert Threshold
 *
 * Create or update one override; write an audit row (admin only).
 *
 * ``category`` is a QUERY parameter (FastAPI treats a str param absent
 * from the path template as query), never a path segment: five known
 * categories contain ``/`` (e.g. ``self-harm/instructions``), and a
 * decoded slash in a path segment breaks route matching and 404s.
 *
 * Args:
 * age_band: The age band half of the natural key (path).
 * category: The category half of the natural key (query).
 * body: The desired min_verdict/min_score.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * ThresholdView: The stored override after the write.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If ``age_band`` is not a known band (422).
 */
export const upsertThresholdApiV1AdminModerationThresholdsAgeBandPut = <ThrowOnError extends boolean = false>(options: Options<UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutData, ThrowOnError>): RequestResult<UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutResponses, UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutErrors, ThrowOnError> => (options.client ?? client).put<UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutResponses, UpsertThresholdApiV1AdminModerationThresholdsAgeBandPutErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation-thresholds/{age_band}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Noise Floor
 *
 * Return the global admin noise floor (admin only).
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * NoiseFloorView: The stored floor, or the code default when no row
 * has been persisted yet.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 */
export const getNoiseFloorApiV1AdminModerationNoiseFloorGet = <ThrowOnError extends boolean = false>(options?: Options<GetNoiseFloorApiV1AdminModerationNoiseFloorGetData, ThrowOnError>): RequestResult<GetNoiseFloorApiV1AdminModerationNoiseFloorGetResponses, GetNoiseFloorApiV1AdminModerationNoiseFloorGetErrors, ThrowOnError> => (options?.client ?? client).get<GetNoiseFloorApiV1AdminModerationNoiseFloorGetResponses, GetNoiseFloorApiV1AdminModerationNoiseFloorGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation/noise-floor',
    ...options
});

/**
 * Update Noise Floor
 *
 * Create or update the global admin noise floor (admin only).
 *
 * Args:
 * body: The desired floor value, already bounded to [0, 1] by the
 * schema (out-of-range values 422 before this runs).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * NoiseFloorView: The stored floor after the write.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 */
export const updateNoiseFloorApiV1AdminModerationNoiseFloorPut = <ThrowOnError extends boolean = false>(options: Options<UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutData, ThrowOnError>): RequestResult<UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutResponses, UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutErrors, ThrowOnError> => (options.client ?? client).put<UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutResponses, UpdateNoiseFloorApiV1AdminModerationNoiseFloorPutErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation/noise-floor',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Moderation Dashboard
 *
 * Aggregated override evidence plus recent threshold changes.
 */
export const moderationDashboardApiV1AdminModerationDashboardGet = <ThrowOnError extends boolean = false>(options?: Options<ModerationDashboardApiV1AdminModerationDashboardGetData, ThrowOnError>): RequestResult<ModerationDashboardApiV1AdminModerationDashboardGetResponses, ModerationDashboardApiV1AdminModerationDashboardGetErrors, ThrowOnError> => (options?.client ?? client).get<ModerationDashboardApiV1AdminModerationDashboardGetResponses, ModerationDashboardApiV1AdminModerationDashboardGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation/dashboard',
    ...options
});

/**
 * Moderation Suggestions
 *
 * Computed threshold proposals awaiting admin ratification.
 *
 * Never applied automatically (umbrella decision 3); the apply control on
 * the dashboard calls the existing audited threshold upsert (F3), and a
 * raised threshold retires its own suggestion (F2).
 */
export const moderationSuggestionsApiV1AdminModerationSuggestionsGet = <ThrowOnError extends boolean = false>(options?: Options<ModerationSuggestionsApiV1AdminModerationSuggestionsGetData, ThrowOnError>): RequestResult<ModerationSuggestionsApiV1AdminModerationSuggestionsGetResponses, ModerationSuggestionsApiV1AdminModerationSuggestionsGetErrors, ThrowOnError> => (options?.client ?? client).get<ModerationSuggestionsApiV1AdminModerationSuggestionsGetResponses, ModerationSuggestionsApiV1AdminModerationSuggestionsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/moderation/suggestions',
    ...options
});

/**
 * List Audit Events
 *
 * List pipeline_event rows, newest first, with optional filters (admin only).
 *
 * Register A13, the view half: a filterable projection over the append-only
 * pipeline event log answering "who did what to child-linked data" (M5 /
 * Phase 5). Every filter is optional and they compose with AND.
 *
 * Args:
 * ctx: The request context (principal + session).
 * filters: The query parameters, bundled into ``AuditFilters`` to stay
 * within the argument-count budget (see that class's docstring):
 * ``kind`` (one of ``EventType``'s values), ``actor_id`` (a UUID
 * string), ``storybook_id`` (see ``_storybook_match`` for the
 * entity_id shapes this matches), ``profile_id`` (a UUID string;
 * see ``_profile_match``), ``since``/``until`` (ISO-8601 bounds,
 * inclusive, on ``occurred_at``), ``limit`` (default 50, clamped
 * to [1, 200]), and ``offset`` (clamped to >= 0).
 *
 * Returns:
 * AuditListView: Up to ``limit`` matching rows, newest first
 * (``occurred_at`` descending, ``id`` descending as a stable
 * tie-break), plus ``has_more`` for the next page.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If ``kind`` is not a known event type, ``actor_id``
 * or ``profile_id`` is not a UUID, or ``since``/``until`` is not
 * ISO-8601 (422).
 */
export const listAuditEventsApiV1AdminAuditGet = <ThrowOnError extends boolean = false>(options?: Options<ListAuditEventsApiV1AdminAuditGetData, ThrowOnError>): RequestResult<ListAuditEventsApiV1AdminAuditGetResponses, ListAuditEventsApiV1AdminAuditGetErrors, ThrowOnError> => (options?.client ?? client).get<ListAuditEventsApiV1AdminAuditGetResponses, ListAuditEventsApiV1AdminAuditGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/audit',
    ...options
});

/**
 * Trigger Rescreen
 *
 * Re-screen published storybooks against the current policy/thresholds (admin only).
 *
 * Runs synchronously (see the module docstring for why no async/enqueue
 * path is offered in this first cut) and returns the full summary in the
 * response; a flagged book is never auto-archived (ADR-005) -- an admin
 * reviews ``results`` and archives through the existing admin path if
 * warranted.
 *
 * Args:
 * body: The optional storybook id scope.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * RescreenSummaryView: Checked/passed/flagged/errored counts plus every
 * book's verdict.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 */
export const triggerRescreenApiV1AdminRescreenPost = <ThrowOnError extends boolean = false>(options: Options<TriggerRescreenApiV1AdminRescreenPostData, ThrowOnError>): RequestResult<TriggerRescreenApiV1AdminRescreenPostResponses, TriggerRescreenApiV1AdminRescreenPostErrors, ThrowOnError> => (options.client ?? client).post<TriggerRescreenApiV1AdminRescreenPostResponses, TriggerRescreenApiV1AdminRescreenPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/rescreen',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Trigger Remoderate
 *
 * Re-run moderation over one storybook version (admin only).
 *
 * Accepts a version whose storybook is ``published`` or ``in_review``, and
 * runs synchronously, returning the fresh report's verdict summary. The
 * book's ``status`` is never changed by this call (ADR-005: a published book
 * stays published, an in_review book stays at the human gate; the
 * guardian/admin remains the only path that can ever move either one).
 *
 * Auto-repair follows the status: a ``published`` book is reported on and
 * never rewritten, while an ``in_review`` book may have a soft-flagged node
 * repaired in place, exactly as on the generation path that produced it,
 * because a human still reviews the result before any child sees it. See the
 * module docstring for the full scope rule and what a hard block does not do.
 *
 * Args:
 * storybook_id: The storybook id from the path.
 * version: The version number from the path.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * RemoderateResultView: The fresh report's verdict summary.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ResourceNotFoundError: If the storybook or version does not exist (404).
 * BusinessLogicError: If the storybook's status is neither ``published``
 * nor ``in_review``, or if a re-moderation is already in flight on
 * this worker (400).
 */
export const triggerRemoderateApiV1AdminRemoderateStorybookIdVersionPost = <ThrowOnError extends boolean = false>(options: Options<TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostData, ThrowOnError>): RequestResult<TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostResponses, TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostErrors, ThrowOnError> => (options.client ?? client).post<TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostResponses, TriggerRemoderateApiV1AdminRemoderateStorybookIdVersionPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/remoderate/{storybook_id}/{version}',
    ...options
});

/**
 * List Allowlist
 *
 * List every allowlist row, ordered by (provider, model_id) (admin only).
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AllowlistListView: Every row, ordered by (provider, model_id).
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 */
export const listAllowlistApiV1AdminProviderAllowlistGet = <ThrowOnError extends boolean = false>(options?: Options<ListAllowlistApiV1AdminProviderAllowlistGetData, ThrowOnError>): RequestResult<ListAllowlistApiV1AdminProviderAllowlistGetResponses, ListAllowlistApiV1AdminProviderAllowlistGetErrors, ThrowOnError> => (options?.client ?? client).get<ListAllowlistApiV1AdminProviderAllowlistGetResponses, ListAllowlistApiV1AdminProviderAllowlistGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/provider-allowlist',
    ...options
});

/**
 * Add Allowlist Entry
 *
 * Add a new (provider, model_id) pair to the allowlist (admin only).
 *
 * Args:
 * body: The provider/model_id/display_name to add.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AllowlistView: The created row.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If the provider is one the family generation lane
 * forbids (422); this endpoint only ever creates enabled rows.
 * StateTransitionError: If the pair already exists (409).
 */
export const addAllowlistEntryApiV1AdminProviderAllowlistPost = <ThrowOnError extends boolean = false>(options: Options<AddAllowlistEntryApiV1AdminProviderAllowlistPostData, ThrowOnError>): RequestResult<AddAllowlistEntryApiV1AdminProviderAllowlistPostResponses, AddAllowlistEntryApiV1AdminProviderAllowlistPostErrors, ThrowOnError> => (options.client ?? client).post<AddAllowlistEntryApiV1AdminProviderAllowlistPostResponses, AddAllowlistEntryApiV1AdminProviderAllowlistPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/provider-allowlist',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Allowlist Entry
 *
 * Remove one row and audit it before deletion (admin only).
 *
 * Args:
 * entry_id: The row's id (path).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AllowlistListView: The full list view after the delete.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ResourceNotFoundError: If no row exists for ``entry_id`` (404).
 */
export const deleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteData, ThrowOnError>): RequestResult<DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteResponses, DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteResponses, DeleteAllowlistEntryApiV1AdminProviderAllowlistEntryIdDeleteErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/provider-allowlist/{entry_id}',
    ...options
});

/**
 * Update Allowlist Entry
 *
 * Toggle enabled and/or update display_name for one row (admin only).
 *
 * Args:
 * entry_id: The row's id (path).
 * body: The desired enabled/display_name state (full replace).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AllowlistView: The row after the update.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ResourceNotFoundError: If no row exists for ``entry_id`` (404).
 * ValidationError: If the request would enable a row whose provider the
 * family generation lane forbids (422).
 */
export const updateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPut = <ThrowOnError extends boolean = false>(options: Options<UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutData, ThrowOnError>): RequestResult<UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutResponses, UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutErrors, ThrowOnError> => (options.client ?? client).put<UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutResponses, UpdateAllowlistEntryApiV1AdminProviderAllowlistEntryIdPutErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/provider-allowlist/{entry_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Whoami
 *
 * Return the authenticated caller's own identity and role.
 *
 * Args:
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * MeResponse: The principal's subject, role, family, profile ids, and
 * ADR-018 D1 verification state.
 */
export const whoamiApiV1MeGet = <ThrowOnError extends boolean = false>(options?: Options<WhoamiApiV1MeGetData, ThrowOnError>): RequestResult<WhoamiApiV1MeGetResponses, WhoamiApiV1MeGetErrors, ThrowOnError> => (options?.client ?? client).get<WhoamiApiV1MeGetResponses, WhoamiApiV1MeGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me',
    ...options
});

/**
 * Export My Family
 *
 * Export every record tied to the caller's family and its child profiles.
 *
 * COPPA 312.6(a) access / GDPR Article 20 portability (remediation plan
 * Phase 3c). Deliberately excludes ``generation_job.report`` (raw
 * multi-stage LLM output): that field is admin-only everywhere else in this
 * API (``api/generation.py::get_generation_job``), and a plain guardian's
 * export must not become a side channel around that restriction.
 *
 * Each profile also carries its ADR-023 P4 personalization data (Task B3):
 * every ``ChildProfilePersonalization`` row (a sibling-slot row includes
 * both the referenced profile's id and its display name), the two
 * real-name consent-ring booleans, and every
 * ``PersonalizationDisclosureConsent`` row, including a tombstoned one
 * whose ``family_connection_id`` has gone ``NULL`` because the underlying
 * connection was deleted -- the tombstone is evidence the guardian's own
 * export must keep, not something to hide.
 *
 * Args:
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * FamilyExportView: The full family export.
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian.
 */
export const exportMyFamilyApiV1MeExportGet = <ThrowOnError extends boolean = false>(options?: Options<ExportMyFamilyApiV1MeExportGetData, ThrowOnError>): RequestResult<ExportMyFamilyApiV1MeExportGetResponses, ExportMyFamilyApiV1MeExportGetErrors, ThrowOnError> => (options?.client ?? client).get<ExportMyFamilyApiV1MeExportGetResponses, ExportMyFamilyApiV1MeExportGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/export',
    ...options
});

/**
 * Delete My Family
 *
 * Permanently erase the caller's entire family account.
 *
 * GDPR Article 17 / COPPA 312.10 (remediation plan Phase 3b). ADR-018's
 * already-decided item 4 frames account deletion as family-scoped ("in-app
 * account deletion erases the family"): this is the single guardian-facing
 * deletion action, and it satisfies Article 17 for the calling guardian as
 * a data subject in their own right (not only as the parent exercising a
 * child's rights), since every ``user`` row in the family -- including the
 * caller's own -- cascades away with it (Phase 3a).
 *
 * Every family-owned table (series, storybooks and their versions, child
 * profiles and everything linked to them, concepts, story requests, device
 * grants, kid flags, and every guardian/admin/child login row) cascades at
 * the database level. One thing cannot cascade cleanly: a ``kid_flag`` row
 * this family's admin(s) resolved may belong to a DIFFERENT family
 * entirely (any admin can resolve any family's flags), and
 * ``ck_kid_flag_resolved_pairing`` requires ``resolved_by``/``resolved_at``
 * to be null together, so a bare cascade would violate that CHECK. Those
 * flags are explicitly reopened (both columns nulled) here, before the
 * family delete, rather than left to a cascade that cannot express it.
 *
 * Args:
 * ctx: The request context (principal + unit-of-work session).
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian.
 * BusinessLogicError: If the caller's family is the sentinel catalog
 * family (should be unreachable: no real guardian ever belongs to
 * it, but guarded explicitly as defense in depth).
 */
export const deleteMyFamilyApiV1MeFamilyDelete = <ThrowOnError extends boolean = false>(options?: Options<DeleteMyFamilyApiV1MeFamilyDeleteData, ThrowOnError>): RequestResult<DeleteMyFamilyApiV1MeFamilyDeleteResponses, DeleteMyFamilyApiV1MeFamilyDeleteErrors, ThrowOnError> => (options?.client ?? client).delete<DeleteMyFamilyApiV1MeFamilyDeleteResponses, DeleteMyFamilyApiV1MeFamilyDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/family',
    ...options
});

/**
 * Invite Guardian
 *
 * Invite a co-parent into the caller's OWN family (guardian self-service; G14).
 *
 * Complements the admin-mediated ``POST /admin/users`` (WS-J), which can
 * invite a second guardian into ANY family: this endpoint is the
 * guardian-initiated counterpart the register flagged as the real gap, and
 * it is deliberately narrower. It reuses
 * ``api/admin_users.py::create_pending_invite``, the exact same pending-row
 * creation and duplicate-email guard the admin path uses, so the two paths
 * can never drift into inconsistent invite semantics.
 *
 * Args:
 * body: The invitee's email; nothing else is caller-supplied.
 * ctx: The request context (principal + unit-of-work session).
 *
 * The invited address is NOT thereby pulled into the caller's family. The
 * row is created with ``status="pending_guardian_invite"``, which
 * ``api/onboarding.py::_bind_pending_invite`` binds to
 * ``"awaiting_approval"`` rather than ``"active"``: an admin must still
 * approve before the invitee can authenticate at all. Only the
 * admin-mediated ``POST /admin/users`` produces an invite that binds
 * straight to ``"active"``.
 *
 * Returns:
 * UserView: The created ``status="pending_guardian_invite"`` row,
 * scoped to the caller's own family.
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian (403).
 * StateTransitionError: If a pending invite of either kind already
 * exists for this email (409).
 */
export const inviteGuardianApiV1MeFamilyInviteGuardianPost = <ThrowOnError extends boolean = false>(options: Options<InviteGuardianApiV1MeFamilyInviteGuardianPostData, ThrowOnError>): RequestResult<InviteGuardianApiV1MeFamilyInviteGuardianPostResponses, InviteGuardianApiV1MeFamilyInviteGuardianPostErrors, ThrowOnError> => (options.client ?? client).post<InviteGuardianApiV1MeFamilyInviteGuardianPostResponses, InviteGuardianApiV1MeFamilyInviteGuardianPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/family/invite-guardian',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Story Requests
 *
 * List the caller's own family's story requests, newest first.
 *
 * This is the guardian/kid surface: it is family-scoped for EVERY caller,
 * including one holding the admin capability (the surface selects the
 * scope, not the caller's maximal privilege); the global queue lives at
 * ``GET /admin/story-requests``. Flag surfacing depth still follows the
 * capability: an admin sees every well-formed flag on their own family's
 * rows, per the invariant that thresholds only filter what guardians see.
 *
 * Args:
 * ctx: The request context (principal and session).
 * status: Optional status filter (pending/approved/declined/blocked).
 * profile_id: Optional profile filter (the kid status view passes this).
 *
 * Returns:
 * StoryRequestListView: The caller's family's requests.
 *
 * Raises:
 * AuthorizationError: If a guardian filters on an inaccessible profile.
 * ValidationError: If ``status`` or ``profile_id`` is malformed (-> 422).
 */
export const listStoryRequestsApiV1StoryRequestsGet = <ThrowOnError extends boolean = false>(options?: Options<ListStoryRequestsApiV1StoryRequestsGetData, ThrowOnError>): RequestResult<ListStoryRequestsApiV1StoryRequestsGetResponses, ListStoryRequestsApiV1StoryRequestsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListStoryRequestsApiV1StoryRequestsGetResponses, ListStoryRequestsApiV1StoryRequestsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/story-requests',
    ...options
});

/**
 * Create Story Request
 *
 * Submit a child's free-text story request (guardian-scoped in R1).
 *
 * ``body.proposed_series_title`` and ``body.anchor_storybook_id`` are
 * mutually exclusive (schema XOR): the former proposes a brand-new,
 * unratified series name (screened alongside the request text and stored
 * for a guardian to ratify or decline at approval); the latter asks for a
 * soft continuation anchored to an existing, published, series-linked
 * storybook in the caller's own family and profile band (WS-B PR 3).
 *
 * ADR-015 G3: a non-blocked request auto-approves through the SAME
 * ``service.approve_story_request`` path a guardian's explicit click uses
 * (see the call below) when the profile has pre-authorization
 * (``request_auto_approve``) AND its own monthly envelope is not yet
 * exhausted AND the family's monthly quota is not yet exhausted (see
 * ``service.can_auto_approve``); otherwise the row rests ``pending`` as
 * before. Auto-approval uses the profile's own band, a ``short`` length,
 * and ``prose`` style -- it never overrides a guardian's stated
 * preference, since there is no guardian input to override.
 *
 * Args:
 * body: The profile id, request text, and optional series proposal or
 * continuation anchor.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * StoryRequestCreatedView: The new request id and its status after
 * screening AND, when applicable, G3 auto-approval (``approved``
 * rather than ``pending``).
 *
 * Raises:
 * AuthorizationError: If the caller may not act on the profile (-> 403).
 * ResourceNotFoundError: If the profile no longer exists, or the anchor
 * storybook is missing or outside the caller's family (-> 404).
 * StateTransitionError: If the profile is at its pending cap (-> 409).
 * ValidationError: If ``profile_id`` is not a valid UUID; if the anchor
 * storybook is not published or not series-linked; or if the
 * profile's age band does not match the anchor's series (-> 422).
 * BusinessLogicError: If the profile is currently restricted (Article
 * 18/21) (-> 400).
 */
export const createStoryRequestApiV1StoryRequestsPost = <ThrowOnError extends boolean = false>(options: Options<CreateStoryRequestApiV1StoryRequestsPostData, ThrowOnError>): RequestResult<CreateStoryRequestApiV1StoryRequestsPostResponses, CreateStoryRequestApiV1StoryRequestsPostErrors, ThrowOnError> => (options.client ?? client).post<CreateStoryRequestApiV1StoryRequestsPostResponses, CreateStoryRequestApiV1StoryRequestsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/story-requests',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Authored Story Request
 *
 * Create a pre-approved request as a guardian or admin (WS-B PR 2).
 *
 * The author sets band, length, and style at creation, so the guardian
 * approval step is skipped: the row is created ``approved`` with its Concept
 * built immediately, ready for the admin authoring-plan step. Screening
 * still runs; a blocked outcome persists a ``blocked`` row with no concept.
 *
 * ``body.series_title`` and ``body.anchor_storybook_id`` are mutually
 * exclusive (schema XOR, WS-B PR 3): the former creates a brand-new series
 * immediately, but only for a non-blocked outcome, so a blocked row never
 * leaves an orphan series; the latter continues an existing, published,
 * series-linked storybook in the target family and body's age band.
 *
 * Args:
 * body: The request text, band/length/style, optional profile, and
 * (admin-only) target family, plus an optional series title or
 * continuation anchor.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * StoryRequestAuthoredCreatedView: Id, post-screening status, concept id.
 *
 * Raises:
 * AuthorizationError: If the caller is a child, or the profile does not
 * belong to the target family (-> 403).
 * ResourceNotFoundError: If the named family, profile, or anchor
 * storybook is missing, or the anchor is outside the target family
 * (-> 404).
 * ValidationError: If a supplied ``family_id`` is malformed, the anchor
 * storybook is not published or not series-linked, the body's age
 * band does not match the anchor's series, or the built brief trips
 * the PII backstop in ``_build_concept`` (-> 422). An admin-only
 * adult omitting ``family_id`` is a catalog-origin request, not an
 * error (#173).
 */
export const createAuthoredStoryRequestApiV1StoryRequestsAuthoredPost = <ThrowOnError extends boolean = false>(options: Options<CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostData, ThrowOnError>): RequestResult<CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostResponses, CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostErrors, ThrowOnError> => (options.client ?? client).post<CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostResponses, CreateAuthoredStoryRequestApiV1StoryRequestsAuthoredPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/story-requests/authored',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Story Requests Admin
 *
 * List story requests across every family (the admin review queue).
 *
 * The global counterpart of ``GET /story-requests``: the admin console's
 * request queue reads this surface, so holding the admin capability never
 * changes what the family-scoped guardian surface returns.
 *
 * Args:
 * ctx: The request context (principal and session).
 * status: Optional status filter (pending/approved/declined/blocked).
 * family_id: Optional family filter for drilling into one family.
 *
 * Returns:
 * StoryRequestListView: The matching requests, newest first, with
 * every well-formed moderation flag surfaced (no threshold filtering).
 *
 * Raises:
 * AuthorizationError: If the caller lacks the admin capability (-> 403).
 * ValidationError: If ``status`` or ``family_id`` is malformed (-> 422).
 */
export const listStoryRequestsAdminApiV1AdminStoryRequestsGet = <ThrowOnError extends boolean = false>(options?: Options<ListStoryRequestsAdminApiV1AdminStoryRequestsGetData, ThrowOnError>): RequestResult<ListStoryRequestsAdminApiV1AdminStoryRequestsGetResponses, ListStoryRequestsAdminApiV1AdminStoryRequestsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListStoryRequestsAdminApiV1AdminStoryRequestsGetResponses, ListStoryRequestsAdminApiV1AdminStoryRequestsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/story-requests',
    ...options
});

/**
 * Get Family Budget
 *
 * Return the caller's own family's monthly story budget (ADR-015 G7/G3).
 *
 * Numbers only, no balance-display styling: the guardian/kid-facing
 * balance UI is a later, separately-scoped piece (see the CLAUDE.md
 * task note). ``spent_this_month`` is derived, not stored (ADR-015 G13,
 * interim): a count of the family's story requests that entered
 * ``approved`` in the current UTC calendar month
 * (``story_requests/service.py::family_monthly_spend``), not a decremented
 * ledger balance.
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * FamilyBudgetView: The family's quota, this month's spend, remaining
 * headroom (floored at 0, never negative even if a quota was lowered
 * below an already-spent month), and each child's own envelope usage.
 *
 * Raises:
 * AuthorizationError: If the caller is neither a guardian nor an admin
 * (a child or device token; this is an adults-only surface, same
 * gate as ``GET /families/me/reading-summary``) (-> 403).
 * ResourceNotFoundError: If the caller's family row is missing
 * (-> 404; not expected in practice, see the inline note).
 */
export const getFamilyBudgetApiV1FamiliesMeBudgetGet = <ThrowOnError extends boolean = false>(options?: Options<GetFamilyBudgetApiV1FamiliesMeBudgetGetData, ThrowOnError>): RequestResult<GetFamilyBudgetApiV1FamiliesMeBudgetGetResponses, GetFamilyBudgetApiV1FamiliesMeBudgetGetErrors, ThrowOnError> => (options?.client ?? client).get<GetFamilyBudgetApiV1FamiliesMeBudgetGetResponses, GetFamilyBudgetApiV1FamiliesMeBudgetGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/families/me/budget',
    ...options
});

/**
 * Approve Story Request Endpoint
 *
 * Approve a pending request, creating its concept (guardian or admin).
 *
 * No GenerationJob is created here; an admin picks the authoring method,
 * mechanism, and model afterward via POST .../authoring-plan, which is what
 * creates the job (see story_requests/authoring_plan.py).
 *
 * ``body.series_title`` ratifies or edits a series for a non-anchored
 * request (WS-B PR 3): supplying it creates a new series row and links the
 * request to it; omitting it declines the kid's proposal (no series is
 * created, and ``proposed_series_title`` remains stored on the request as
 * an audit trail). An anchored (continuation) request is re-validated
 * against the confirmed band and rejects a supplied ``series_title``
 * outright (a continuation cannot also fork a new series).
 *
 * Args:
 * request_id: The request id from the path.
 * body: The guardian's band/length/style confirmation (WS-B); this
 * becomes the request's stored band and length, overriding
 * whatever was stamped at creation. A gamebook style below the
 * teen bands (13-16, 16+) is rejected here with a 422 before the
 * service layer runs. ``series_title``, if present, is screened
 * here before the service layer runs, so a blocked title never
 * reaches the row.
 * ctx: The request context.
 *
 * Returns:
 * StoryRequestApprovedView: The linked concept id.
 *
 * Raises:
 * ResourceNotFoundError: If the request is out of scope (-> 404).
 * StateTransitionError: If the request is not pending (-> 409).
 * AuthorizationError: If a child token reaches this endpoint (-> 403).
 * ValidationError: If ``series_title`` fails content screening; if an
 * anchored request also carries a ``series_title``; or if the
 * confirmed age band does not match the anchor's series band
 * (-> 422).
 */
export const approveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePost = <ThrowOnError extends boolean = false>(options: Options<ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostData, ThrowOnError>): RequestResult<ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostResponses, ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostErrors, ThrowOnError> => (options.client ?? client).post<ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostResponses, ApproveStoryRequestEndpointApiV1StoryRequestsRequestIdApprovePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/story-requests/{request_id}/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Authoring Plan
 *
 * Choose an authoring method/mechanism/model for an approved request.
 *
 * Admin-only: a guardian may approve a request but does not pick its
 * authoring backend or model.
 *
 * Args:
 * request_id: The request id from the path.
 * body: The chosen method, mechanism, and prep model.
 * ctx: The request context.
 * background_tasks: The enqueue (fresh_generation only) runs here so it
 * fires after commit.
 *
 * Returns:
 * AuthoringPlanResponse: The created job id, status, matched or
 * overridden skeleton (if any), every in-cell skeleton_alternatives,
 * and any non-blocking eligibility/override warnings.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (-> 403).
 * ResourceNotFoundError: If the request is out of scope, or its concept
 * is missing (-> 404).
 * StateTransitionError: If the request is not approved, or a job
 * already exists for its concept (-> 409).
 * ValidationError: On an invalid method/mechanism combination, an
 * unrecognized skill-mechanism model, no matching skeleton, or an
 * unknown skeleton_slug override (-> 422).
 */
export const createAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPost = <ThrowOnError extends boolean = false>(options: Options<CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostData, ThrowOnError>): RequestResult<CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostResponses, CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostErrors, ThrowOnError> => (options.client ?? client).post<CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostResponses, CreateAuthoringPlanApiV1StoryRequestsRequestIdAuthoringPlanPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/story-requests/{request_id}/authoring-plan',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Decline Story Request Endpoint
 *
 * Decline a pending request (guardian own-family or admin global).
 *
 * Args:
 * request_id: The request id from the path.
 * ctx: The request context.
 *
 * Returns:
 * StoryRequestDeclinedView: The declined request id and status.
 *
 * Raises:
 * ResourceNotFoundError: If the request is out of scope (-> 404).
 * StateTransitionError: If the request is not pending (-> 409).
 * AuthorizationError: If a child token reaches this endpoint (-> 403).
 */
export const declineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePost = <ThrowOnError extends boolean = false>(options: Options<DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostData, ThrowOnError>): RequestResult<DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostResponses, DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostErrors, ThrowOnError> => (options.client ?? client).post<DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostResponses, DeclineStoryRequestEndpointApiV1StoryRequestsRequestIdDeclinePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/story-requests/{request_id}/decline',
    ...options
});

/**
 * Create Child Session
 *
 * Mint a child session token for one profile.
 *
 * Callable by a guardian (own family), an admin (any family), or a
 * family-scoped ``DEVICE`` principal (own family only; ADR-014 phase 2):
 * a device grant lets an authorized device mint a reading session for one
 * of its own family's profiles without a live guardian bearer.
 *
 * Args:
 * body: The target child profile id, plus the profile's picker PIN when
 * one is set (P6-07); ignored for a PIN-less profile.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * ChildSessionView: The signed token, its expiry, and the profile id.
 *
 * Raises:
 * AuthorizationError: If a child token reaches this endpoint, a
 * guardian or device grant names a profile outside its own family
 * (for a device grant, a nonexistent profile also maps here with the
 * identical body, so it cannot probe cross-family existence, #249),
 * or the profile has a picker PIN and the body's ``pin`` is missing
 * or wrong (all -> 403; the PIN case carries code ``PIN_MISMATCH``).
 * ResourceNotFoundError: If the profile does not exist on the admin path,
 * or a resolved own-family profile is deactivated (-> 404).
 * ValidationError: If ``profile_id`` is not a valid UUID (-> 422).
 */
export const createChildSessionApiV1ChildSessionsPost = <ThrowOnError extends boolean = false>(options: Options<CreateChildSessionApiV1ChildSessionsPostData, ThrowOnError>): RequestResult<CreateChildSessionApiV1ChildSessionsPostResponses, CreateChildSessionApiV1ChildSessionsPostErrors, ThrowOnError> => (options.client ?? client).post<CreateChildSessionApiV1ChildSessionsPostResponses, CreateChildSessionApiV1ChildSessionsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/child-sessions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Device Grants
 *
 * List the caller's family's currently-active device grants.
 *
 * Never returns the token (it is returned only once, at mint time). Scoped
 * to the caller's own family for both a guardian and an admin; unlike the
 * mint endpoint, an admin does not get a cross-family override here (no
 * ``family_id`` filter is accepted), since a device-list view is a
 * per-family, guardian-facing surface, not a global admin one.
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * list[DeviceGrantListItem]: The family's non-revoked device grants.
 *
 * Raises:
 * AuthorizationError: If a device or child principal reaches this
 * endpoint (-> 403).
 */
export const listDeviceGrantsApiV1DeviceGrantsGet = <ThrowOnError extends boolean = false>(options?: Options<ListDeviceGrantsApiV1DeviceGrantsGetData, ThrowOnError>): RequestResult<ListDeviceGrantsApiV1DeviceGrantsGetResponses, ListDeviceGrantsApiV1DeviceGrantsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListDeviceGrantsApiV1DeviceGrantsGetResponses, ListDeviceGrantsApiV1DeviceGrantsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/device-grants',
    ...options
});

/**
 * Create Device Grant
 *
 * Mint a device grant for a family (guardian or admin only).
 *
 * Args:
 * ctx: The request context (principal and session).
 * body: The optional target family id (admin only; a guardian must
 * omit it) and an optional guardian-facing device label. Mirrors
 * ``onboarding.py::onboard``'s ``body: OnboardingBody | None = None``
 * pattern: every field is itself optional, so an entirely absent
 * request body (no ``Content-Type``, not just ``{}``) is also
 * valid, not just a 422.
 *
 * Returns:
 * DeviceGrantView: The signed token, its expiry, and the grant record.
 * The token is returned ONLY here; ``GET /device-grants`` never
 * includes it.
 *
 * Raises:
 * AuthorizationError: If a device or child principal reaches this
 * endpoint, or a guardian names a family other than its own
 * (-> 403).
 * ResourceNotFoundError: If an admin-named family does not exist
 * (-> 404).
 * ValidationError: If an admin-only caller omits ``family_id``, or the
 * supplied id is malformed (-> 422).
 */
export const createDeviceGrantApiV1DeviceGrantsPost = <ThrowOnError extends boolean = false>(options?: Options<CreateDeviceGrantApiV1DeviceGrantsPostData, ThrowOnError>): RequestResult<CreateDeviceGrantApiV1DeviceGrantsPostResponses, CreateDeviceGrantApiV1DeviceGrantsPostErrors, ThrowOnError> => (options?.client ?? client).post<CreateDeviceGrantApiV1DeviceGrantsPostResponses, CreateDeviceGrantApiV1DeviceGrantsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/device-grants',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Revoke Device Grant
 *
 * Revoke a device grant belonging to the caller's family.
 *
 * Sets ``revoked_at``; the row is kept (not deleted) so the jti stays a
 * stable revocation record. Revocation is enforced only on the online path
 * (``deps.py::_device_principal`` rejects a grant whose ``revoked_at`` is
 * set before building the principal); an already-offline device is not
 * affected until it reconnects (ADR-014, "Negative / risks").
 *
 * Args:
 * grant_id: The device grant's id (path).
 * ctx: The request context (principal and session).
 *
 * Raises:
 * AuthorizationError: If a device or child principal reaches this
 * endpoint (-> 403).
 * ResourceNotFoundError: If no grant with ``grant_id`` exists in the
 * caller's own family (-> 404). Deliberately the same 404 whether
 * the id does not exist at all or belongs to another family, so
 * this is not a cross-family existence oracle.
 */
export const revokeDeviceGrantApiV1DeviceGrantsGrantIdDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteData, ThrowOnError>): RequestResult<RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteResponses, RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteResponses, RevokeDeviceGrantApiV1DeviceGrantsGrantIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/device-grants/{grant_id}',
    ...options
});

/**
 * Onboard
 *
 * Provision, or return, the caller's family and guardian user (P6-03).
 *
 * On first login (no ``User`` for the verified subject) this creates a
 * ``Family`` and a guardian ``User`` atomically and returns 201. On any
 * later call, or for an already-provisioned guardian/admin, it returns the
 * existing row with 200 and creates nothing. A consent payload, if present,
 * is recorded onto whichever ``User`` row this call resolves to (Phase 2 /
 * ADR-018 D1), after that row is known -- never before, since there is
 * nothing to attach a consent record to until then.
 *
 * Args:
 * identity: The verified onboarding identity (subject, optional email,
 * and observed client address for the consent record).
 * session: The request unit-of-work session.
 * response: The response, whose status code is set to 201 (created) or
 * 200 (idempotent) here.
 * body: The optional request body carrying the Phase 2 consent payload.
 *
 * Returns:
 * OnboardingView: The resolved or created family/guardian identity.
 *
 * Raises:
 * ValidationError: If a consent payload has ``accepted=True`` but is
 * missing ``policy_version`` or ``signer_name`` (422).
 */
export const onboardApiV1OnboardingPost = <ThrowOnError extends boolean = false>(options?: Options<OnboardApiV1OnboardingPostData, ThrowOnError>): RequestResult<OnboardApiV1OnboardingPostResponses, OnboardApiV1OnboardingPostErrors, ThrowOnError> => (options?.client ?? client).post<OnboardApiV1OnboardingPostResponses, OnboardApiV1OnboardingPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/onboarding',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Start Kws Verification
 *
 * Ask KWS to email this adult a parent-verification link.
 *
 * 202 rather than 201: what the request achieves is an attempt in flight,
 * not a verified parent. The attempt is resolved later, and only ever by the
 * webhook.
 *
 * Args:
 * body: The parent's location and language. Deliberately carries no
 * email address; see the module docstring.
 * identity: The verified token identity. Does not require an active
 * account, because verification precedes admin approval.
 * session: The request unit of work. Used for the caller lookup and the
 * two limit reads only; the attempt row itself is written on a
 * separate committed session inside ``start_parent_verification``.
 *
 * Returns:
 * KwsVerificationStartView: The attempt id and when it was started.
 *
 * Raises:
 * ConfigurationError: If KWS is not configured on this tier, or if KWS
 * rejects our credentials or blocks the request (400). Both are the
 * operator's to fix, and the single status keeps the two
 * indistinguishable to the caller.
 * BusinessLogicError: If the caller has no ``User`` row yet, or no email
 * address to send to (400).
 * AuthorizationError: If the caller is a child account, or the account
 * is in a state that may not send (403).
 * StateTransitionError: If an unresolved attempt is still recent (409).
 * RateLimitedError: If the caller's hourly attempt cap is spent (429).
 * ExternalServiceError: If the outbound send fails in a way that may
 * clear on its own, a KWS 5xx, a timeout, or a dropped connection
 * (502). Unlike the refusals above, this one is worth retrying, and
 * the closed-out `send_failed` row lets an immediate retry through
 * (UW-A55).
 */
export const startKwsVerificationApiV1ConsentKwsStartPost = <ThrowOnError extends boolean = false>(options: Options<StartKwsVerificationApiV1ConsentKwsStartPostData, ThrowOnError>): RequestResult<StartKwsVerificationApiV1ConsentKwsStartPostResponses, StartKwsVerificationApiV1ConsentKwsStartPostErrors, ThrowOnError> => (options.client ?? client).post<StartKwsVerificationApiV1ConsentKwsStartPostResponses, StartKwsVerificationApiV1ConsentKwsStartPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/consent/kws/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Flag
 *
 * Submit a child's structured flag for a storybook passage.
 *
 * Args:
 * body: The profile, storybook, version, reason, and optional node id.
 * ctx: The request context (principal and session).
 *
 * Returns:
 * KidFlagCreatedView: The new flag's id and reason.
 *
 * Raises:
 * AuthorizationError: If the caller may not act on the profile, or the
 * storybook is not assigned to it (-> 403).
 * ResourceNotFoundError: If the profile does not exist (-> 404).
 * StateTransitionError: If the profile is at its open-flag cap (-> 409).
 * ValidationError: If ``profile_id`` is not a valid UUID (-> 422).
 */
export const createFlagApiV1FlagsPost = <ThrowOnError extends boolean = false>(options: Options<CreateFlagApiV1FlagsPostData, ThrowOnError>): RequestResult<CreateFlagApiV1FlagsPostResponses, CreateFlagApiV1FlagsPostErrors, ThrowOnError> => (options.client ?? client).post<CreateFlagApiV1FlagsPostResponses, CreateFlagApiV1FlagsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/flags',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Open Flags
 *
 * List every open (unresolved) flag across families, newest first.
 *
 * The admin moderation queue surface (A1); no guardian equivalent exists
 * yet (the guardian alert feed is G10, a separate ``pipeline_event``
 * projection built by a sibling workstream).
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * KidFlagListView: Every unresolved flag, newest first.
 *
 * Raises:
 * AuthorizationError: If the caller lacks the admin capability (-> 403).
 */
export const listOpenFlagsApiV1AdminFlagsGet = <ThrowOnError extends boolean = false>(options?: Options<ListOpenFlagsApiV1AdminFlagsGetData, ThrowOnError>): RequestResult<ListOpenFlagsApiV1AdminFlagsGetResponses, ListOpenFlagsApiV1AdminFlagsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListOpenFlagsApiV1AdminFlagsGetResponses, ListOpenFlagsApiV1AdminFlagsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/flags',
    ...options
});

/**
 * Resolve Flag
 *
 * Resolve one open flag (admin only).
 *
 * Args:
 * flag_id: The flag id from the path.
 * body: The admin's resolution decision.
 * ctx: The request context.
 *
 * Returns:
 * KidFlagView: The resolved flag.
 *
 * Raises:
 * AuthorizationError: If the caller lacks the admin capability (-> 403).
 * ResourceNotFoundError: If the flag does not exist (-> 404).
 * StateTransitionError: If the flag is already resolved (-> 409).
 * ValidationError: If ``flag_id`` is not a valid UUID (-> 422).
 */
export const resolveFlagApiV1AdminFlagsFlagIdResolvePost = <ThrowOnError extends boolean = false>(options: Options<ResolveFlagApiV1AdminFlagsFlagIdResolvePostData, ThrowOnError>): RequestResult<ResolveFlagApiV1AdminFlagsFlagIdResolvePostResponses, ResolveFlagApiV1AdminFlagsFlagIdResolvePostErrors, ThrowOnError> => (options.client ?? client).post<ResolveFlagApiV1AdminFlagsFlagIdResolvePostResponses, ResolveFlagApiV1AdminFlagsFlagIdResolvePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/flags/{flag_id}/resolve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Notifications
 *
 * List the calling guardian's family-scoped notification feed.
 *
 * A read-only projection over ``pipeline_event`` (S9 delivery
 * infrastructure; see ``notifications/service.py`` for the family-scoping
 * mechanism). Unread state is client-side for this first slice: a caller
 * tracks the newest ``occurred_at`` it has already shown and passes it back
 * as ``since`` on the next poll.
 *
 * Args:
 * ctx: The request context (principal and session).
 * since: Optional ISO-8601 lower bound (exclusive) on ``occurred_at``.
 * limit: Maximum items to return (default 30, clamped to [1, 100]).
 *
 * Returns:
 * NotificationListView: Up to ``limit`` items, newest first.
 *
 * Raises:
 * AuthorizationError: If the caller does not hold the guardian base
 * role (-> 403). This is a guardian-only surface: it composes
 * safety-sensitive text (a blocked or flagged story, naming the
 * child involved) that a child must never see, and it is scoped to
 * ``Principal.family_id``, which has no guardian meaning for an
 * admin-only adult (mirrors the guardian-only gate in
 * api/assignments.py and api/profiles.py).
 * ValidationError: If ``since`` is present but not ISO-8601 (-> 422).
 */
export const listNotificationsApiV1NotificationsGet = <ThrowOnError extends boolean = false>(options?: Options<ListNotificationsApiV1NotificationsGetData, ThrowOnError>): RequestResult<ListNotificationsApiV1NotificationsGetResponses, ListNotificationsApiV1NotificationsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListNotificationsApiV1NotificationsGetResponses, ListNotificationsApiV1NotificationsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications',
    ...options
});

/**
 * Stream Notifications
 *
 * Authenticated SSE push transport for the guardian notification feed.
 *
 * Additive alongside ``GET /notifications``: reuses the same family-scoped
 * projection and never replaces the poll path (see the module docstring).
 * The caller resolves and role-checks the principal itself, with its own
 * short-lived database session closed before the stream begins, rather
 * than depending on ``Context``/``CurrentPrincipal`` -- a FastAPI ``yield``
 * dependency's session stays open for the entire response, which for a
 * ``StreamingResponse`` means the whole connection's lifetime; resolving
 * auth on a session that closes immediately, then having the generator
 * open its own short-lived sessions per poll tick, is what keeps this
 * endpoint from holding one connection per open guardian tab. Both of
 * those session sets come from the INJECTED ``SessionFactory``, so
 * declining ``DbSession`` costs this route no test seam: an override
 * installed on ``get_session_factory`` reaches the auth check and every
 * poll tick alike.
 *
 * Args:
 * request: The ASGI request; only ``is_disconnected()`` is used, to
 * let the streaming generator notice a client-side close.
 * session_factory: Injected opener for the short-lived sessions this
 * route and its generator use (see ``deps.get_session_factory``).
 * authorization: The ``Authorization`` header (same bearer contract as
 * every other authenticated route; see ``api/deps.py``).
 * since: Optional ISO-8601 lower bound (exclusive) on ``occurred_at``,
 * same contract as ``GET /notifications``'s query param, so a
 * client can pass its last-known timestamp and the newly (re)opened
 * stream still pushes anything that arrived while it was closed.
 *
 * Returns:
 * StreamingResponse: A ``text/event-stream`` response.
 *
 * Raises:
 * AuthenticationError: If the token is missing or fails verification
 * (-> 401), raised by ``require_principal``.
 * AuthorizationError: If the caller does not hold the guardian base
 * role (-> 403); same guardian-only rationale as
 * ``GET /notifications`` (see its docstring).
 * ValidationError: If ``since`` is present but not ISO-8601 (-> 422).
 */
export const streamNotificationsApiV1NotificationsStreamGet = <ThrowOnError extends boolean = false>(options?: Options<StreamNotificationsApiV1NotificationsStreamGetData, ThrowOnError>): RequestResult<StreamNotificationsApiV1NotificationsStreamGetResponses, StreamNotificationsApiV1NotificationsStreamGetErrors, ThrowOnError> => (options?.client ?? client).get<StreamNotificationsApiV1NotificationsStreamGetResponses, StreamNotificationsApiV1NotificationsStreamGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/notifications/stream',
    ...options
});

/**
 * Remove Device Download
 *
 * Report that a device no longer has a book cached offline.
 *
 * Takes no ``profile_id``, because the client's own eviction paths
 * (``downloadBudget.ts``'s space-pressure eviction,
 * ``revocation.ts``'s server-directed removal) operate on
 * ``deleteStorybooksById``, which removes every cached version of a book
 * ID for every profile on the device at once, not one profile at a time.
 * Removes every matching row the caller may act on, which is more than one
 * whenever two profiles the caller controls both had it downloaded on that
 * device.
 *
 * Scope is the principal's own profile set, not the whole family. A
 * guardian's set covers the family, so an adult eviction still clears every
 * profile's row as the client paths expect; a child's set covers only
 * itself, so a sibling's row survives its eviction and is left to go stale
 * like any other unreported removal (see ``DeviceDownload``'s
 * best-effort-snapshot contract).
 *
 * Profile-agnostic does not mean unauthenticated: a bare device-grant
 * principal (paired but with no child profile selected yet, ``Role.DEVICE``)
 * is rejected outright rather than silently matching no rows. Only a child,
 * guardian, or admin already inside the family may remove anything here, and
 * then only within the profile set described above.
 *
 * Args:
 * device_id: The reporting device's persistent id (query param).
 * storybook_id: The no-longer-cached book (query param).
 * ctx: The request context (principal and session).
 *
 * Raises:
 * AuthorizationError: If a bare device principal calls this (-> 403).
 */
export const removeDeviceDownloadApiV1DeviceDownloadsDelete = <ThrowOnError extends boolean = false>(options: Options<RemoveDeviceDownloadApiV1DeviceDownloadsDeleteData, ThrowOnError>): RequestResult<RemoveDeviceDownloadApiV1DeviceDownloadsDeleteResponses, RemoveDeviceDownloadApiV1DeviceDownloadsDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RemoveDeviceDownloadApiV1DeviceDownloadsDeleteResponses, RemoveDeviceDownloadApiV1DeviceDownloadsDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/device-downloads',
    ...options
});

/**
 * List Device Downloads
 *
 * List the caller's family's offline-download inventory.
 *
 * Guardian/admin only, family-scoped (mirrors
 * ``device_grants.py::list_device_grants``); a device or child principal
 * has no guardian-console use for this view.
 *
 * Args:
 * ctx: The request context (principal and session).
 *
 * Returns:
 * list[DeviceDownloadView]: Every download row for the family, newest
 * first. The frontend groups by ``device_id`` for display; this
 * endpoint does not group, since a single flat list is simpler to
 * project and test, and grouping is presentation, not data.
 *
 * Raises:
 * AuthorizationError: If a device or child principal reaches this
 * endpoint (-> 403).
 */
export const listDeviceDownloadsApiV1DeviceDownloadsGet = <ThrowOnError extends boolean = false>(options?: Options<ListDeviceDownloadsApiV1DeviceDownloadsGetData, ThrowOnError>): RequestResult<ListDeviceDownloadsApiV1DeviceDownloadsGetResponses, ListDeviceDownloadsApiV1DeviceDownloadsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListDeviceDownloadsApiV1DeviceDownloadsGetResponses, ListDeviceDownloadsApiV1DeviceDownloadsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/device-downloads',
    ...options
});

/**
 * Report Device Download
 *
 * Report that a device has (or still has) a book cached offline.
 *
 * Upserts on ``(device_id, profile_id, storybook_id)``: a first report
 * creates the row (``created_at`` = now); a repeat report (a later re-open
 * of the same cached book, or a periodic re-confirm) only advances
 * ``updated_at``, the guardian-visible "last confirmed" signal.
 *
 * Unlike ``flags.py``'s assignment gate, this endpoint does NOT require
 * the book still be assigned: a book unassigned after it was downloaded
 * should still show up as "still cached" until the client's own eviction
 * (``revocation.ts``) reports it gone, since that removal path is the
 * thing this table exists to make visible, not something this endpoint
 * should race ahead of.
 *
 * Args:
 * body: The reporting device, profile, and book.
 * ctx: The request context (principal and session).
 *
 * Raises:
 * AuthorizationError: If the caller may not act on the profile (-> 403).
 * ResourceNotFoundError: If the profile or the book does not exist
 * (-> 404).
 */
export const reportDeviceDownloadApiV1DeviceDownloadsPut = <ThrowOnError extends boolean = false>(options: Options<ReportDeviceDownloadApiV1DeviceDownloadsPutData, ThrowOnError>): RequestResult<ReportDeviceDownloadApiV1DeviceDownloadsPutResponses, ReportDeviceDownloadApiV1DeviceDownloadsPutErrors, ThrowOnError> => (options.client ?? client).put<ReportDeviceDownloadApiV1DeviceDownloadsPutResponses, ReportDeviceDownloadApiV1DeviceDownloadsPutErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/device-downloads',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Users
 *
 * List guardian/admin accounts, optionally filtered (admin only).
 *
 * Args:
 * ctx: The request context (principal + session).
 * family_id: Optional family filter.
 * role: Optional role filter (guardian or admin).
 * status: Optional status filter.
 *
 * Returns:
 * UserListView: Up to ``_USER_LIST_LIMIT`` matching rows, created_at
 * order; role='child' rows are always excluded.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If ``family_id`` is not a valid UUID (422).
 */
export const listUsersApiV1AdminUsersGet = <ThrowOnError extends boolean = false>(options?: Options<ListUsersApiV1AdminUsersGetData, ThrowOnError>): RequestResult<ListUsersApiV1AdminUsersGetResponses, ListUsersApiV1AdminUsersGetErrors, ThrowOnError> => (options?.client ?? client).get<ListUsersApiV1AdminUsersGetResponses, ListUsersApiV1AdminUsersGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users',
    ...options
});

/**
 * Create User
 *
 * Invite a guardian or admin into a family (admin only; WS-J).
 *
 * Args:
 * body: The invitee's email, target family, role, and dual-role flag.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * UserView: The created ``status="pending"`` row.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If ``family_id`` is not a valid UUID (422).
 * ResourceNotFoundError: If the target family does not exist (404).
 * StateTransitionError: If a pending invite already exists for this
 * email (409) -- onboarding's email-match bind
 * (``select(...).scalar()``) requires at most one pending row per
 * email, so a second is rejected rather than left ambiguous.
 */
export const createUserApiV1AdminUsersPost = <ThrowOnError extends boolean = false>(options: Options<CreateUserApiV1AdminUsersPostData, ThrowOnError>): RequestResult<CreateUserApiV1AdminUsersPostResponses, CreateUserApiV1AdminUsersPostErrors, ThrowOnError> => (options.client ?? client).post<CreateUserApiV1AdminUsersPostResponses, CreateUserApiV1AdminUsersPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update User
 *
 * Reassign, re-role, or activate/deactivate a guardian/admin (WS-J).
 *
 * An admin may not edit their own row through this endpoint (self-lockout
 * guard): every field here (family, role, capability, status) could strand
 * the caller without admin access, and this console has no "who else is an
 * admin" recovery path, so self-editing is refused outright rather than
 * reasoned about field-by-field.
 *
 * Args:
 * user_id: The account to update (path).
 * body: The fields to change; omitted fields are untouched.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * UserView: The updated account.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin, or targets their
 * own account (403).
 * ResourceNotFoundError: If no guardian/admin row with this id exists
 * (404; a role='child' row 404s here too, see the module docstring).
 * ValidationError: If a ``status`` transition through/from either
 * invite status ('pending', 'pending_guardian_invite') is
 * requested, or into 'awaiting_approval' directly, or
 * ``family_id`` is not a valid UUID (422).
 */
export const updateUserApiV1AdminUsersUserIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateUserApiV1AdminUsersUserIdPatchData, ThrowOnError>): RequestResult<UpdateUserApiV1AdminUsersUserIdPatchResponses, UpdateUserApiV1AdminUsersUserIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateUserApiV1AdminUsersUserIdPatchResponses, UpdateUserApiV1AdminUsersUserIdPatchErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/users/{user_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Admin Profiles
 *
 * List child profiles across families, optionally filtered (admin only).
 *
 * Args:
 * ctx: The request context (principal + session).
 * family_id: Optional family filter.
 *
 * Returns:
 * AdminProfileListView: Up to ``_PROFILE_LIST_LIMIT`` matching profiles,
 * including deactivated ones (the console needs to show and
 * reactivate them).
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If ``family_id`` is not a valid UUID (422).
 */
export const listAdminProfilesApiV1AdminProfilesGet = <ThrowOnError extends boolean = false>(options?: Options<ListAdminProfilesApiV1AdminProfilesGetData, ThrowOnError>): RequestResult<ListAdminProfilesApiV1AdminProfilesGetResponses, ListAdminProfilesApiV1AdminProfilesGetErrors, ThrowOnError> => (options?.client ?? client).get<ListAdminProfilesApiV1AdminProfilesGetResponses, ListAdminProfilesApiV1AdminProfilesGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/profiles',
    ...options
});

/**
 * Create Admin Profile
 *
 * Create a child profile in any family (admin only; WS-J).
 *
 * Args:
 * body: The target family and the new profile's fields.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AdminProfileView: The stored profile.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ResourceNotFoundError: If the target family does not exist (404).
 * BusinessLogicError: If the target family has no recorded VPC consent
 * (400); see ``_require_family_consent``.
 */
export const createAdminProfileApiV1AdminProfilesPost = <ThrowOnError extends boolean = false>(options: Options<CreateAdminProfileApiV1AdminProfilesPostData, ThrowOnError>): RequestResult<CreateAdminProfileApiV1AdminProfilesPostResponses, CreateAdminProfileApiV1AdminProfilesPostErrors, ThrowOnError> => (options.client ?? client).post<CreateAdminProfileApiV1AdminProfilesPostResponses, CreateAdminProfileApiV1AdminProfilesPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/profiles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Admin Profile
 *
 * Partially update a child profile in any family (admin only; WS-J).
 *
 * Args:
 * profile_id: The profile to update (path).
 * body: The fields to change; omitted fields are untouched. An explicit
 * ``null`` clears only ``avatar`` and ``pin``; on the other fields
 * it is a no-op (mirrors ``api/profiles.py::update_profile``).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * AdminProfileView: The updated profile.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ResourceNotFoundError: If no profile with this id exists (404).
 */
export const updateAdminProfileApiV1AdminProfilesProfileIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateAdminProfileApiV1AdminProfilesProfileIdPatchData, ThrowOnError>): RequestResult<UpdateAdminProfileApiV1AdminProfilesProfileIdPatchResponses, UpdateAdminProfileApiV1AdminProfilesProfileIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateAdminProfileApiV1AdminProfilesProfileIdPatchResponses, UpdateAdminProfileApiV1AdminProfilesProfileIdPatchErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/profiles/{profile_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Family Connections
 *
 * List every directional family connection, with both family names.
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * FamilyConnectionListView: Every connection row, ordered for stable
 * display.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 */
export const listFamilyConnectionsApiV1AdminFamilyConnectionsGet = <ThrowOnError extends boolean = false>(options?: Options<ListFamilyConnectionsApiV1AdminFamilyConnectionsGetData, ThrowOnError>): RequestResult<ListFamilyConnectionsApiV1AdminFamilyConnectionsGetResponses, ListFamilyConnectionsApiV1AdminFamilyConnectionsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListFamilyConnectionsApiV1AdminFamilyConnectionsGetResponses, ListFamilyConnectionsApiV1AdminFamilyConnectionsGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/family-connections',
    ...options
});

/**
 * Create Family Connection
 *
 * Opt one family in to another's recommendations (admin only; WS-J).
 *
 * Args:
 * body: The viewer family and the recommendation-source family.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * FamilyConnectionView: The created connection.
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ValidationError: If either id is not a UUID, or they are equal (422).
 * ResourceNotFoundError: If either family does not exist (404).
 * StateTransitionError: If this exact directional pair already exists
 * (409).
 */
export const createFamilyConnectionApiV1AdminFamilyConnectionsPost = <ThrowOnError extends boolean = false>(options: Options<CreateFamilyConnectionApiV1AdminFamilyConnectionsPostData, ThrowOnError>): RequestResult<CreateFamilyConnectionApiV1AdminFamilyConnectionsPostResponses, CreateFamilyConnectionApiV1AdminFamilyConnectionsPostErrors, ThrowOnError> => (options.client ?? client).post<CreateFamilyConnectionApiV1AdminFamilyConnectionsPostResponses, CreateFamilyConnectionApiV1AdminFamilyConnectionsPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/family-connections',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Family Connection
 *
 * Hard-delete a family connection (admin only; WS-J).
 *
 * Unlike a guardian/admin/kid/family removal, this is a real delete: a
 * connection is a permission edge, not identity data with history worth
 * preserving (mirrors ``provider_allowlist``'s DELETE route).
 *
 * Any ring-2 disclosure consent granted on this connection is NOT deleted
 * with it: it is evidence, and it tombstones instead (see
 * ``_tombstone_attached_consents`` for why the revocation stamp has to
 * happen here rather than in the database).
 *
 * Args:
 * connection_id: The connection to delete (path).
 * ctx: The request context (principal + session).
 *
 * Raises:
 * AuthorizationError: If the caller is not an admin (403).
 * ResourceNotFoundError: If no connection with this id exists (404).
 */
export const deleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteData, ThrowOnError>): RequestResult<DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteResponses, DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteResponses, DeleteFamilyConnectionApiV1AdminFamilyConnectionsConnectionIdDeleteErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/admin/family-connections/{connection_id}',
    ...options
});

/**
 * List My Family Connections
 *
 * List every connection touching the caller's family, from their side.
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * FamilyConnectionMineListView: One item per connection where the
 * caller's family is the viewer or the sharer.
 *
 * Raises:
 * AuthorizationError: If the caller does not hold the guardian role
 * (403).
 */
export const listMyFamilyConnectionsApiV1FamilyConnectionsMineGet = <ThrowOnError extends boolean = false>(options?: Options<ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetData, ThrowOnError>): RequestResult<ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetResponses, ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetErrors, ThrowOnError> => (options?.client ?? client).get<ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetResponses, ListMyFamilyConnectionsApiV1FamilyConnectionsMineGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/family-connections/mine',
    ...options
});

/**
 * Revoke Family Connection Consent
 *
 * Revoke the caller's guardian consent for their side of a connection.
 *
 * ADR-016: revoking either side deactivates the connection immediately.
 * ``api/recommendations.py`` (K17) re-derives ``active`` from these same
 * two columns on every read (never a cached flag), so the deactivation is
 * visible on the very next recommendation fetch, not after some delay.
 *
 * Args:
 * connection_id: The connection to revoke consent on (path).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * FamilyConnectionMineItem: The connection's updated state from the
 * caller's side.
 *
 * Raises:
 * AuthorizationError: If the caller does not hold the guardian role, or
 * their family is on neither side of the connection (403).
 * ResourceNotFoundError: If no connection with this id exists (404).
 */
export const revokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteData, ThrowOnError>): RequestResult<RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteResponses, RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteResponses, RevokeFamilyConnectionConsentApiV1FamilyConnectionsConnectionIdConsentDeleteErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/family-connections/{connection_id}/consent',
    ...options
});

/**
 * Consent Family Connection
 *
 * Record the caller's guardian consent for their side of a connection.
 *
 * Args:
 * connection_id: The connection to consent to (path).
 * ctx: The request context (principal + session).
 *
 * Returns:
 * FamilyConnectionMineItem: The connection's updated state from the
 * caller's side.
 *
 * Raises:
 * AuthorizationError: If the caller does not hold the guardian role, or
 * their family is on neither side of the connection (403).
 * ResourceNotFoundError: If no connection with this id exists (404).
 */
export const consentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPost = <ThrowOnError extends boolean = false>(options: Options<ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostData, ThrowOnError>): RequestResult<ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostResponses, ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostErrors, ThrowOnError> => (options.client ?? client).post<ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostResponses, ConsentFamilyConnectionApiV1FamilyConnectionsConnectionIdConsentPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/family-connections/{connection_id}/consent',
    ...options
});

/**
 * Get Recommendations
 *
 * Return a profile's book recommendations (ADR-016 rings 1-2).
 *
 * Args:
 * profile_id: The child profile requesting recommendations.
 * principal: The authenticated principal.
 * session: The request session.
 *
 * Returns:
 * RecommendationsView: Every 4-5 star rating from another profile, in
 * the caller's own family (ring 1) or an actively dual-consented
 * connected family where the caller's family is the viewer (ring 2), on
 * a book this profile can already see. Sorted highest rating first,
 * then title, for a stable feed order.
 *
 * Raises:
 * ValidationError: If ``profile_id`` is not a UUID.
 * AuthorizationError: If a non-admin principal does not own the profile
 * (mirrors ``reading_history.py::get_reading_history``: child own
 * profile, guardian family, admin any).
 * ResourceNotFoundError: If no profile with this id exists (404).
 */
export const getRecommendationsApiV1RecommendationsProfileIdGet = <ThrowOnError extends boolean = false>(options: Options<GetRecommendationsApiV1RecommendationsProfileIdGetData, ThrowOnError>): RequestResult<GetRecommendationsApiV1RecommendationsProfileIdGetResponses, GetRecommendationsApiV1RecommendationsProfileIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetRecommendationsApiV1RecommendationsProfileIdGetResponses, GetRecommendationsApiV1RecommendationsProfileIdGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/recommendations/{profile_id}',
    ...options
});

/**
 * Get Personalization
 *
 * Read a profile's personalization state for the guardian settings UI.
 *
 * Args:
 * profile_id: The child profile (path).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * PersonalizationView: The real-name ring flags plus every stored slot,
 * each with a read-only ``ring2_eligible`` ceiling.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID.
 * AuthorizationError: If the caller is not a guardian, or the profile
 * is not in the caller's family (403; no existence oracle).
 * ResourceNotFoundError: If the row vanished between authorization and
 * load.
 */
export const getPersonalizationApiV1ProfilesProfileIdPersonalizationGet = <ThrowOnError extends boolean = false>(options: Options<GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetData, ThrowOnError>): RequestResult<GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetResponses, GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetErrors, ThrowOnError> => (options.client ?? client).get<GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetResponses, GetPersonalizationApiV1ProfilesProfileIdPersonalizationGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}/personalization',
    ...options
});

/**
 * Put Personalization
 *
 * Replace a profile's whole personalization state (plan section 6.1).
 *
 * A full replace, never a patch: any slot_type stored today but absent
 * from ``body.slots`` is cleared. Every incoming value clears the write-time
 * validation gate (structural injection guard, band-mandatory denylist,
 * closed-enum membership, sibling-in-family) before anything is persisted.
 *
 * Args:
 * profile_id: The child profile (path).
 * body: The whole new personalization state.
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * PersonalizationView: The stored state after the replace.
 *
 * Raises:
 * ValidationError: If profile_id is not a UUID, a slot_type requests a
 * ring-2 ceiling it may not hold, or any slot value fails the
 * write-time validation gate (422).
 * AuthorizationError: If the caller is not a guardian, or the profile
 * is not in the caller's family (403).
 * ResourceNotFoundError: If the row vanished between authorization and
 * load.
 */
export const putPersonalizationApiV1ProfilesProfileIdPersonalizationPut = <ThrowOnError extends boolean = false>(options: Options<PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutData, ThrowOnError>): RequestResult<PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutResponses, PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutErrors, ThrowOnError> => (options.client ?? client).put<PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutResponses, PutPersonalizationApiV1ProfilesProfileIdPersonalizationPutErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}/personalization',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Grant Ring2 Consent
 *
 * Grant, or supersede, a ring-2 disclosure consent (sharer-side only).
 *
 * Re-consent supersedes the existing row in place with a new
 * ``consent_accepted_at``/``consent_policy_version`` (plan section 5.3,
 * ADR-023 OD-5(c)); narrowing ``covered_slot_types`` does not require
 * re-signing.
 *
 * Args:
 * profile_id: The subject child profile, in the SHARER family (path).
 * body: The consent grant.
 * ctx: The request context (principal + unit-of-work session).
 * request: The inbound request, read only for its client address
 * (stamped server-side, never accepted from the client).
 *
 * Returns:
 * Ring2ConsentView: The stored consent row.
 *
 * Raises:
 * ValidationError: If profile_id or family_connection_id is not a
 * UUID, covered_slot_types names an ineligible slot, or the
 * sibling slot is covered without ``sibling_authority_attested``
 * (422).
 * AuthorizationError: If the caller is not a guardian, the profile is
 * not in the caller's family, or the caller's family is not the
 * connection's sharer side (403).
 * ResourceNotFoundError: If the profile or connection does not exist.
 */
export const grantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPost = <ThrowOnError extends boolean = false>(options: Options<GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostData, ThrowOnError>): RequestResult<GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostResponses, GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostErrors, ThrowOnError> => (options.client ?? client).post<GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostResponses, GrantRing2ConsentApiV1ProfilesProfileIdRing2ConsentPostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}/ring2-consent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke Ring2 Consent
 *
 * Revoke a ring-2 disclosure consent (sharer-side only).
 *
 * Args:
 * profile_id: The subject child profile, in the SHARER family (path).
 * connection_id: The connection the consent was granted on (path).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * Ring2ConsentView: The revoked (but not deleted) consent row.
 *
 * Raises:
 * ValidationError: If either id is not a UUID.
 * AuthorizationError: If the caller is not a guardian, the profile is
 * not in the caller's family, or the caller's family is not the
 * connection's sharer side (403).
 * ResourceNotFoundError: If the profile, connection, or consent row
 * does not exist.
 */
export const revokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteData, ThrowOnError>): RequestResult<RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteResponses, RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteResponses, RevokeRing2ConsentApiV1ProfilesProfileIdRing2ConsentConnectionIdDeleteErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/profiles/{profile_id}/ring2-consent/{connection_id}',
    ...options
});

/**
 * Get Personalization Receive
 *
 * Read the caller's own family's viewer-side receive switch.
 *
 * Args:
 * ctx: The request context (principal + session).
 *
 * Returns:
 * PersonalizationReceiveView: Whether this family accepts personalized
 * content across its connections.
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian (403).
 * ResourceNotFoundError: If the caller's family row is missing (404).
 */
export const getPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGet = <ThrowOnError extends boolean = false>(options?: Options<GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetData, ThrowOnError>): RequestResult<GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetResponses, GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetErrors, ThrowOnError> => (options?.client ?? client).get<GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetResponses, GetPersonalizationReceiveApiV1FamiliesMePersonalizationReceiveGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/families/me/personalization-receive',
    ...options
});

/**
 * Put Personalization Receive
 *
 * Set the caller's own family's viewer-side receive switch.
 *
 * The write half of design plan 8.6's opt-OUT, and the only one: the
 * switch is a stored preference, not an evidentiary consent, so it takes
 * no signature, policy version, or IP.
 *
 * Args:
 * body: The desired switch state.
 * ctx: The request context (principal + session).
 *
 * Returns:
 * PersonalizationReceiveView: The state after the write.
 *
 * Raises:
 * AuthorizationError: If the caller is not a guardian (403).
 * ResourceNotFoundError: If the caller's family row is missing (404).
 */
export const putPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePut = <ThrowOnError extends boolean = false>(options: Options<PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutData, ThrowOnError>): RequestResult<PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutResponses, PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutErrors, ThrowOnError> => (options.client ?? client).put<PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutResponses, PutPersonalizationReceiveApiV1FamiliesMePersonalizationReceivePutErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/families/me/personalization-receive',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Personalization Values
 *
 * Resolve the values payload for one book, at whichever ring applies.
 *
 * No requested-slot-type parameter exists (plan section 6.1): the server
 * returns every slot the subject has enabled and consented for at the
 * applicable ring, and the client discards what it does not need. This is
 * a genuinely new authorization shape (plan section 8.5): the route does
 * NOT authorize on the subject profile via ``authorize_profile``; it
 * authorizes on the connection (a family-level fact) plus the caller's own
 * family membership, so a child session in the viewer family may read a
 * payload about a profile it could never otherwise act on.
 *
 * Deliberately NOT guardian-gated, unlike the four configuration routes
 * above it: this is the reader-facing render path, so a child or device
 * session must be able to call it or a personalized book cannot render on
 * a kid's tablet at all. What that costs is made up for by the two checks
 * below, which the configuration routes get from ``_require_guardian``.
 *
 * #CRITICAL: security: EVERY empty-payload branch returns the identical
 * shape, never a 404, and always with ``slot_bindings={}``. Raising 404
 * here made the route an existence oracle over the whole storybook table:
 * any authenticated caller could enumerate ids and learn which exist
 * globally, before any family check had run. The bindings half is the same
 * oracle in a subtler coat: bindings are a property of the book's contract,
 * so an empty payload that carried populated bindings for a book with
 * personalizable slots would distinguish "not addressable by you" (empty)
 * from "addressable but no values for you" (populated). Bindings are
 * therefore computed ONLY on the happy path that returns actual values
 * (`_resolve_ring1_view`/`_resolve_ring2_view`); every predicate failure
 * renders `_empty_values_view()`, which hard-codes the empty map. Uniform
 * disclosure is the only way this route can honor its own "never reveal
 * anything about another family" contract, since it has no 403 branch to
 * hide behind either. A side benefit: the contract's synchronous disk read
 * no longer runs for unauthorized or empty-outcome callers at all.
 * #VERIFY: tests/integration/test_personalization_api.py::
 * test_values_missing_storybook_returns_the_empty_payload,
 * ::test_values_cross_family_private_book_returns_the_empty_payload, and
 * ::test_empty_values_payload_carries_no_slot_bindings; the empty view's
 * fixed shape is pinned by tests/unit/test_personalization_empty_view.py.
 *
 * Args:
 * storybook_id: The book (path).
 * ctx: The request context (principal + unit-of-work session).
 *
 * Returns:
 * PersonalizationValuesView: The resolved payload, or the universal
 * empty payload (never a 403, never a 404) on any predicate failure.
 */
export const getPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGet = <ThrowOnError extends boolean = false>(options: Options<GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetData, ThrowOnError>): RequestResult<GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetResponses, GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetErrors, ThrowOnError> => (options.client ?? client).get<GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetResponses, GetPersonalizationValuesApiV1StorybooksStorybookIdPersonalizationValuesGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/storybooks/{storybook_id}/personalization-values',
    ...options
});

/**
 * Get My Progress
 *
 * Return the caller's own badges, collection state, and lifetime totals.
 *
 * Args:
 * principal: The authenticated principal (must be a child token).
 * session: The request session.
 *
 * Returns:
 * ProgressView: Earned badges, per-book collection state, and lifetime
 * totals for the caller's own profile.
 *
 * Raises:
 * AuthorizationError: If the caller is not a child.
 */
export const getMyProgressApiV1MeProgressGet = <ThrowOnError extends boolean = false>(options?: Options<GetMyProgressApiV1MeProgressGetData, ThrowOnError>): RequestResult<GetMyProgressApiV1MeProgressGetResponses, GetMyProgressApiV1MeProgressGetErrors, ThrowOnError> => (options?.client ?? client).get<GetMyProgressApiV1MeProgressGetResponses, GetMyProgressApiV1MeProgressGetErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/progress',
    ...options
});

/**
 * Flush Reading Time
 *
 * Idempotently add a clamped active-reading-time delta to a day bucket.
 *
 * Args:
 * body: The flush payload.
 * principal: The authenticated principal (must be a child token).
 * session: The request session.
 *
 * Returns:
 * ReadingActivityDayView: The bucket's state after this flush (or its
 * unchanged current state, on a deduped replay).
 *
 * Raises:
 * AuthorizationError: If the caller is not a child.
 * ValidationError: If ``date`` is implausibly far in the future.
 */
export const flushReadingTimeApiV1MeReadingTimePost = <ThrowOnError extends boolean = false>(options: Options<FlushReadingTimeApiV1MeReadingTimePostData, ThrowOnError>): RequestResult<FlushReadingTimeApiV1MeReadingTimePostResponses, FlushReadingTimeApiV1MeReadingTimePostErrors, ThrowOnError> => (options.client ?? client).post<FlushReadingTimeApiV1MeReadingTimePostResponses, FlushReadingTimeApiV1MeReadingTimePostErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/v1/me/reading-time',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
