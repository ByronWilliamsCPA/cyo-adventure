# PR Summaries: #715-#718 (ByronWilliamsCPA/cyo-adventure)

Checked against the 15-item analysis of the generation pipeline (fill delivery, skeleton bar,
walk floor, safety, approval, cost, job lifecycle, providers, testing, instruments, model
selection, reading level, register integrity, scope gaps).

---

## #715 - docs(research): story-quality technique review, pre-registered experiment rigs, and session handoff

**State**: merged (2026-08-15T19:55:20Z)

**What it did**: Pure documentation/research PR (22 files, +6742/-0, no `src/` changes). Adds
`docs/planning/story-quality-technique-review-2026-08-15.md` (a technique-gap sweep), three
pre-registered experiment rigs under `docs/planning/evidence/` (D-7c binding-notes ablation,
W16 fill-guide-content ablation, a recognition-protocol pilot), a new `score_fills.py` scorer
(shared four-grams, Flesch-Kincaid, dialogue share, "you"-density, words-on-target), and lessons
log entries `AL-353`-`AL-357` / register rows `UW-C248`-`UW-C252`. All three rigs are staged with
**zero results**: two usage-window exhaustions killed the author waves before any fill ran.

**Impact on the analysis**: `TOUCHES`.
- Builds new instrumentation adjacent to items **2** (fill delivery/word-count scoring) and **11**
  (instruments: the recognition protocol, four-gram similarity). But by the PR's own text the
  experiments produced no results ("zero results by design of circumstance"), and no `generation/`,
  `validator/`, or `moderation/` source file changed. Nothing in items 2 or 11 is updated: the
  recognition protocol still "has never returned different" (it hasn't run yet), and
  `check_fill_integrity`/`_WORD_COUNT_TOLERANCE` are untouched.
- **Reading level (item 13)**: `score_fills.py` computes FK as one of several research metrics,
  but this is a standalone evidence-directory script, not a change to the production
  `validator/band_profile` FK gate. **Does not bear on item 13.** (Contrast with #719, which
  rebuilt the production syllable counter and is out of scope for this batch.)
- **Register integrity (item 14)**: only adds new lessons/register rows; does not touch or rewrite
  any previously closed row, so it does not bear on the `bf7cad1` finding.

---

## #716 - chore(release): v0.79.1

**State**: merged (2026-08-15T17:40:18Z)

**What it did**: Automated python-semantic-release version bump. Touches only `CHANGELOG.md`,
`pyproject.toml`, `uv.lock` (+16/-2, 3 files). Closes issues #183/#157/#158 (release-workflow
plumbing, not product code).

**Impact on the analysis**: `NONE`. No source, validator, generation, or docs-planning content
changed; purely release metadata.

---

## #717 - feat(landing): redesign the homepage as the sales funnel

**State**: closed, **unmerged** (closed 2026-08-15T19:43:04Z). Per the closing comment (from the
PR author, `williaby`): **closed in favor of #720**, which "supersedes this PR." #717's CI was
red (`format:check` failure on `demoStory.ts`, plus a coverage-matrix drift-guard gap for two new
test files never registered in `docs/testing/coverage-matrix.md`), and it carried an unfinished
review-cycle backlog. #720 carries the same four commits, fixes the CI failures, and completes a
third adversarial review cycle (catching two additional WCAG blockers) before merging.

**What it did** (as authored, never shipped): full rebuild of `frontend/src/landing/`
(LandingPage, landing.css, DemoAdventure/demoStory, pricing.ts, headline.ts) into a marketing
funnel with an interactive demo story, pricing/FAQ sections, and CTAs into self-signup. Included
copy claiming "You approve, then they read" / "you always have the final word" about guardian
approval authority.

**Impact on the analysis**: `NONE` (as this PR itself). It never merged, so nothing it contains
reached `main` under PR #717's number. Flagged for context only: the misleading approval-authority
copy it introduced *did* ship, via superseding PR #720, and was subsequently corrected by #718
(see below) - so the provenance of that defect traces through #717/#720, not #715/#716/#718
directly.

---

## #718 - fix(landing): correct the approval claims, and plan the onboarding overhaul

**State**: merged (2026-08-16T02:25:53Z)

**What it did**: Frontend copy-only fix (11 files, +760/-13; "no runtime, schema, or API contract
surface is touched") across `LandingPage.tsx`, `GuardianAwaitingApprovalPage.tsx`, and
`ConsolePage.tsx`, correcting five strings that told guardians *they* approve stories. Cites
`api/approval.py` (admin-role-only) and `api/node_edit.py` as the authorization ground truth.
Rewords to "A reviewer approves, you choose who reads it," names guardians' real controls
(passage editing, per-child assignment), and adds a support link on the awaiting-approval page.
Also adds two planning docs (`docs/qa/onboarding-flow-walkthrough-2026-08-14.md`,
`docs/planning/onboarding-overhaul-plan-2026-08-15.md`) and 14 new register rows
(`UW-J23`-`UW-J36`).

**Impact on the analysis**: `TOUCHES` item **6** (Approval) - explicitly called out per your
instruction:
- This PR bears directly on **what the product claims about human approval**: it removes the
  landing/console copy asserting guardians hold approval authority and replaces it with copy
  naming a platform reviewer as the approver, matching the actual admin-only RBAC in
  `api/approval.py`.
- It does **not** touch the specific defect in item 6 (`publishing/service.py` guarding only
  `moderation_report is None`, never `has_hard_block`). The PR states plainly no runtime code
  changed, and a local check of `src/cyo_adventure/publishing/service.py` on this branch confirms
  the hard-block guard is still absent (only `moderation_report is None` is checked, lines
  148/412). So item 6's core finding - a hard-blocked book can still publish in two clicks -
  **is unchanged and still valid**; #718 only makes the product's *messaging* about who approves
  more honest, it does not close the bypass.

  > **Correction, 2026-08-30.** The observation above was accurate for #718 and for `main` as it
  > stood on 2026-08-22; it is no longer true of `main`. The bypass was closed later, by the
  > **ADR-005 amendment of 2026-08-25** (#764, #769, #776, #778):
  > `publishing/service.py::_assert_report_permits_approval` now stacks four refusals, the fourth
  > being `approve_requires_override_reason` (`:474`), which refuses an approval over a block or
  > high-severity finding unless a non-whitespace `override_reason` is given and then stamps
  > `overridden_block_count` / `overridden_high_count` onto the RELEASED audit payload. So
  > "a hard-blocked book can still publish in two clicks" is retired. Two things this sweep noted
  > do still hold on `origin/main`: `submit()` (`service.py:161`) still guards only
  > `moderation_report is None`, and the `has_hard_block` symbol still appears nowhere under
  > `publishing/` (the guard is expressed through `severe_finding_counts()` instead), so the
  > symbol-level check this entry performed would still report "absent" today and would still be
  > the wrong oracle.
- **Reading level (item 13)**: no bearing. #718 is landing/onboarding copy and planning docs only;
  it does not touch `validator/`, FK scoring, or the syllable counter (that's #719, out of scope
  here).

---

## Summary table

| PR | State | Impact tag(s) |
|----|-------|----------------|
| #715 | merged | TOUCHES (items 2, 11; explicitly NONE on 13, 14) |
| #716 | merged | NONE |
| #717 | closed, unmerged (superseded by #720) | NONE (context only, via #720) |
| #718 | merged | TOUCHES item 6 (messaging, not the code bug); NONE on 13 |
