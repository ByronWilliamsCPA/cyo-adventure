---
title: "Authoring Lessons Log"
schema_type: planning
status: accepted
owner: core-maintainer
purpose: "Append-only log of lessons learned from each story development run, so recurring problems become tooling changes instead of re-learned folklore."
tags:
  - planning
  - authoring
  - quality
  - process
component: Development-Tools
source: "story-quality-lessons-2026-07.md"
---

# Authoring Lessons Log

> **Status**: Accepted | **Updated**: 2026-07-25
> **Serves**: [A11](./capability-register.md) (structural quality tools across the corpus)
> **Validated by**: `uv run python scripts/check_lessons_log.py`

<!-- -->

> **Note on this first landing.** The lessons below were learned building a
> three-book 16+ gamebook series (the Wyrmreach chain), and several `Ref` cells
> cite that series' spec, skeleton, and filled-story artifacts. Those artifacts
> are **not** part of this change: they land in a separate follow-up push so the
> process and tooling work can be reviewed on its own, alongside the other
> in-flight revisions to story development. Every lesson and every tooling change
> here stands without them; the story-dependent tests skip until they arrive
> (6 skips in the suite today), and the two Wyrmreach design and report documents
> are included because they are the evidence base each lesson cites.

Every story development run appends here. The point is that a lesson learned once becomes a
**change to the tooling** rather than folklore an author has to rediscover, so each row carries a
proposed change and a status that tracks whether it landed.

## How to use this log

**When to append.** At the end of any authoring run: a new story or series book, a skeleton
promotion, a fill pass, or a validator/gate change made in service of authoring. If the run
produced no lesson, append nothing; an empty run is a real outcome.

**What counts as a lesson.** Something a future author or a future tooling change should act on.
Three tests, any one of which qualifies it:

1. It cost real iteration to discover (a rule interaction, a calibration, an ordering constraint).
2. The tooling let a defect through, or reported it in a way that did not point at the cause.
3. It would be re-learned from scratch by the next person, because nothing in the repo records it.

A one-off typo is not a lesson. "The gate reports band membership, so the floor reads as a pass"
is.

**Fields.**

| Field | Rule |
| --- | --- |
| `ID` | `AL-NNN`, sequential, never reused or renumbered. |
| `Date` | ISO date the lesson was recorded. |
| `Source` | The run that produced it (story slug, series book, or workstream). |
| `Category` | One of `validator`, `tooling`, `authoring-craft`, `scale`, `metadata`, `process`, `docs`, `product`. |
| `Lesson` | What is true, stated so it is actionable without reading the source run. |
| `Proposed change` | The change that would stop this recurring, or `none (craft note)` when the lesson is guidance rather than a code change. |
| `Status` | `open`, `accepted`, `applied`, `rejected`, or `superseded`. |
| `Ref` | Commit, PR, file, or doc. **Required** when status is `applied`, `rejected`, or `superseded`. |

**Review cadence.** Sweep the `open` rows whenever a phase is planned. An `open` row that has
survived two phase boundaries is either genuinely low value (mark it `rejected`, with the reason in
`Ref`) or it is real debt being avoided; either way, decide rather than let it sit.

**Status discipline.** `accepted` means the change is agreed but unbuilt, and it must appear in a
plan somewhere. `applied` means the change is merged and the `Ref` proves it. Nothing moves to
`applied` on the strength of intent.

---

## Log

| ID | Date | Source | Category | Lesson | Proposed change | Status | Ref |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AL-001 | 2026-07-25 | wyrmreach b1-b3 | tooling | Compiling the FILL skeleton and the filled story from one spec makes drift between them structurally impossible; two hand-maintained artifacts drift silently. | Keep one-spec compilation as the authoring pattern for any multi-artifact story. | applied | `scripts/build_series_book.py` |
| AL-002 | 2026-07-25 | wyrmreach b2 | authoring-craft | In a `carries_state: true` continuation a carried variable initialises `true`, so every `== false` condition on it is unsatisfiable and a hard L2-11. Carried state must be read-only; acquisition moves to a new book-local variable. | none (craft note); recorded in the series design doc's carried-variable rule. | applied | `wyrmreach-series-design.md` section 4 |
| AL-003 | 2026-07-25 | wyrmreach b2, b3 | validator | A gate on a *new* variable placed earlier in the graph than any node granting it is also an L2-11 dead branch. This is the same class as AL-002 but not obvious from the rule text. | Add a cause hint to the L2-11 message for both patterns; it also improves the Stage C repair prompt, which gets findings verbatim. | applied | `validator/layer2.py::_dead_branch_cause` names carried-variable polarity or grant-order in the L2-11 message; both verified against the real book plus 3 tests |
| AL-004 | 2026-07-25 | wyrmreach b1-b3 | authoring-craft | Flesch-Kincaid is driven by sentence count, not passage length: at a fixed ~60 words the same content scores FK 5 in four or five sentences, 9-11 in three, 13+ in two. The practical 16+ rule is three sentences per passage. | none (craft note); recorded in the series design doc's reading-level section. | applied | `wyrmreach-series-design.md` section 6 |
| AL-005 | 2026-07-25 | wyrmreach b3 | authoring-craft | An RL-13 outlier can be retuned without rewriting a word by joining or splitting sentence boundaries; 185 of 212 cleared this way. Two guards are required: only downcase a folded-in leading word if it is seen lowercase in the corpus and never capitalised mid-sentence, and only split where both sides are 8+ words and the right side opens with a subject. | none (craft note); the guards are the reusable part. | applied | `wyrmreach-series-report.md` N8 |
| AL-006 | 2026-07-25 | wyrmreach b3 | validator | RL-13 scores `<<FILL>>` directive text, so every skeleton emits one warning per node (746 for a ceiling-scale book), training reviewers to ignore the rule. PL-19 already skips FILL bodies and is the pattern to copy. | Skip bodies containing the FILL marker in `check_reading_level`; hoist the marker constant so both modules import it. | applied | `validator/reading_level.py` skips FILL bodies; the ceiling-scale skeleton now emits 0 RL-13 warnings instead of 746, with a regression test |
| AL-007 | 2026-07-25 | wyrmreach b1 | authoring-craft | The Layer-2 configuration key includes the visited set of nodes carrying `once` effects, so each `once` effect doubles the config space. In an acyclic graph `once` is redundant, because no node is entered twice on a path. | none (craft note): never use `once` in a DAG-shaped story. | applied | `wyrmreach-series-design.md` section 6 |
| AL-008 | 2026-07-25 | wyrmreach b3 | authoring-craft | Declare a carried integer variable's range as what the continuation can actually reach, not what the variable could theoretically hold. Narrowing `renown` to 3-5 and dropping an unreachable carried flag is the difference between 36,781 configurations and risking the 100k L2-12 cap. **Superseded**: narrowing is right for the configuration budget but must never narrow below the range the SENDING book can end in, or the client clamps carried outcomes away. | Corrected rule in AL-038: narrow toward what the continuation can reach, but the receiving range must contain the sending range. | superseded | AL-038; `renown` widened back to 0-5 in `data/series/wyrmreach/book3.spec.json` |
| AL-009 | 2026-07-25 | wyrmreach b3 | scale | A ceiling-scale gamebook needs breadth, not length: rooms hung as parallel chains off an act hub and reconverging on an act gate fit 746 nodes inside 74 of 93 permitted hops, and keep a reader's route at 52 nodes. | none (craft note); the shape is recorded as the six room shapes. | applied | `wyrmreach-series-design.md` section 5 |
| AL-010 | 2026-07-25 | wyrmreach b3 | process | Generate wiring, never voice. Structure was generated from six formal room shapes and every beat, label and ending title was then authored over it through a patch keyed by node id, with an assertion that label count equals choice count. That assertion caught eleven real mismatches that would have shipped as generated text. | Keep the two-stage split for any story past the ~460-node hand-authoring ceiling; the label-count assertion is the load-bearing part. | applied | `wyrmreach-series-report.md` N7 |
| AL-011 | 2026-07-25 | wyrmreach b3 | scale | L2-13 firing past 460 nodes is the expected and correct output, not a defect: it states that the completed Layer-2 walk, not human review, is the sole correctness guarantee. At that scale the walk has to be treated as the acceptance test rather than a lint pass. Every structural problem in book 3 was found by L2-11 or the budget report, none by reading the spec. | none; behaviour is correct as designed. Document it so the warning is not "fixed" by someone later. | accepted | `story-quality-lessons-2026-07.md` finding N6 |
| AL-012 | 2026-07-25 | wyrmreach b1-b3 | metadata | `metadata.estimated_minutes` is ADR-011's fastest-finish clock, and nothing validates it, so on a hand-authored or imported book it is whatever the author typed. All three books were wrong by 3-6x (40/40/90 declared against 8/8/14 canonical). | Fixed in the books. Still open: a PL-series advisory comparing the declared value against `mutation/identity.py::recompute_estimated_minutes`, which already exists and is tested. | applied | commit `86dafda` (books); gate advisory tracked as AL-021 |
| AL-013 | 2026-07-25 | wyrmreach b1-b3 | validator | PL-19 reports band membership only, so the 55 words/node advisory floor reads as a pass when the cell target is 80. Authoring to the floor put books 1 and 2 below their cell's intended whole-world range (77 and 82 minutes against ~110-175) and dragged the fastest-finish clock under its cell target. | Report distance from target, not just band membership. Consider a tighter two-sided band for `production_eligible` stories, keeping the wide band for MVP/test tiers. | open | `story-quality-lessons-2026-07.md` finding D |
| AL-014 | 2026-07-25 | wyrmreach b1-b3 | tooling | No hand-authored skeleton can pass the `skeleton-promotion` CI gate: `check_promotion_bundle.py` requires a lineage sidecar unconditionally, and no sidecar exists anywhere in `skeletons/`. The thirteen existing hand-authored skeletons are green only because CI proves changed files and none has been touched since the workflow landed. | Add an authored-bundle proof path (origin sidecar, `check_skeleton`, theme contract when present, WS-5 anti-clone floor), failing closed when neither sidecar is present. Not an exemption. | open | `story-quality-lessons-2026-07.md` finding A |
| AL-015 | 2026-07-25 | wyrmreach b3 | validator | Nothing checks the within-story ending mix. Book 3 is 118 death endings of 232 (51%) and gates clean; a 95%-death book would too. The normalised kind and valence histograms already exist in `diversity/structure.py`, used only for between-story anti-clone distance. | A PL-series advisory over the existing histograms: warn when one ending kind exceeds a per-band share, or positive-valence endings fall below a floor. | applied | PL-24 in `validator/policy.py`: a 60% single-kind ceiling plus a style-aware winnability floor (prose share, gamebook absolute count). Fires on 6 pre-existing books, silent on the wyrmreach three |
| AL-016 | 2026-07-25 | wyrmreach b2 | process | A comparison check is only as good as the independence of its two inputs. A builder bug wrote the prose story to the skeleton path, so `check_fill_integrity.py` compared a file with itself and **passed**, silently making an earlier verification vacuous. A check that cannot fail manufactures confidence. | Give `check_fill_integrity.py` a self-test: assert the two inputs are not the same file, that the skeleton contains FILL markers, and that the filled story contains none. Apply the same reasoning to any proof comparing two generated files. | applied | `scripts/check_fill_integrity.py` rejects same-file inputs and a marker-less skeleton; both negative cases verified |
| AL-017 | 2026-07-25 | wyrmreach b3 | docs | The rule catalog has drifted from the code it documents, despite stating that rule changes require a revision to it: L2-13 is absent entirely, and RL-13's entry says the grade is "computed by textstat" when the implementation is deliberately vendored and textstat is not a dependency. `_MIN_WORDS_FOR_FK` (20) is also undocumented and changes which nodes get scored. | Add the L2-13 row, correct RL-13, bump the catalog version, and add a test asserting every rule id the validator can emit appears in the catalog. | applied | `validator-rules.md` v1.3 adds L2-13, PL-23, PL-24 and a whole missing SR-1..SR-7 section, and corrects RL-13's textstat claim; `tests/unit/test_rule_catalog_lockstep.py` makes drift a build failure |
| AL-018 | 2026-07-25 | wyrmreach b1-b3 | tooling | The budget-headroom report is what made ceiling-scale authoring tractable (nodes against the cell, endings and decisions against floors, word mean, depth against the cap, fastest finish against the arc floor, ending mix). It lives in a series-only script; `check_skeleton.py` prints pass or fail with no sense of proximity to any edge. | Move the headroom report into `check_skeleton.py`, computed from the typed Storybook rather than a spec format, so the generation pipeline and the guardian editor get it too. | applied | `scripts/check_skeleton.py --headroom` reports nodes/depth/endings/decisions/words/arc-floor proximity from the typed document; 2 tests |
| AL-019 | 2026-07-25 | wyrmreach b1-b3 | product | No reader outcome ever reaches an author. `Completion`, `ReadingState.path` and `Rating` all exist and `StorybookVersion.skeleton_slug` links a version to its skeleton, but `Completion` is read in five places and every one is a per-child read. Nobody can say which of book 3's 232 endings any reader has reached, or where readers stop. | Designed: reader-path retention plus a de-identified engagement rollup, under S12's minimum-population constraint. | accepted | `reader-path-engagement-design.md` |
| AL-020 | 2026-07-25 | wyrmreach b1-b3 | product | The offline client already transmits the full accumulated `path` on every save (`frontend/src/offline/sync.ts::toPutPayload`), and the server overwrites it (`api/reading.py`, `row.path = list(body.path)`). The route data we want for engagement analysis already arrives and is discarded, so retaining it needs no client change and no request-contract change. | Append an immutable trail row on each reading-state write; see the design doc. | accepted | `reader-path-engagement-design.md` section 3 |
| AL-021 | 2026-07-25 | wyrmreach b1-b3 | validator | Split from AL-012 so the gate change is tracked separately from the book fix: the declared/derived read-time mismatch is undetectable today. | Add the PL-series `estimated_minutes` advisory comparing declared against `recompute_estimated_minutes`, tolerance ~25%, advisory not blocking. | applied | PL-23 in `validator/policy.py` compares declared `estimated_minutes` against the derived fastest-finish clock; found read-time drift in 13 pre-existing books |
| AL-022 | 2026-07-25 | wyrmreach b1-b3 | product | `estimated_minutes` is the fastest-finish clock, so a 746-node, 42,085-word gamebook whose whole-world clock is 3.2 hours advertises "14 min" to a child choosing a book. Technically correct, practically a broken promise. ADR-011 defines the whole-world clock; the schema surfaces one field. | Derive both clocks, add `estimated_minutes_whole_world`, and show "about 15 minutes to finish, hours to explore" on the kid and guardian surfaces. Owner decision, not an implementation detail. | open | `story-quality-lessons-2026-07.md` finding B |
| AL-023 | 2026-07-25 | wyrmreach b1-b3 | tooling | The shipped client never sends `choice_path`, so the server-side engine replay that exists to stop a forged `current_node`/`var_state`/`path` is dormant (`api/reading.py` carries the `#ASSUME` admitting it). Any analytics derived from the client-supplied `path` is therefore unverified reader-reported data. | Enable `choice_path` in `toPutPayload` and make the replay authoritative before trail data is used for anything beyond directional signal. Security and data-integrity relevance, not only analytics. | open | `reader-path-engagement-design.md` section 6 |
| AL-024 | 2026-07-25 | adversarial review | tooling | Committing a 746-node skeleton falsified the stated derivation of a resource cap without breaking anything. `api/schemas.py:38-52` derives `VISIT_SET_MAX_LENGTH` from "the largest currently-authored skeleton is the-ashfall-expedition.json with 505 nodes ... so 505 is the exact real ceiling", and tells the reader not to invent numbers. The largest is now 746. Not a live bug: `visit_set` resets per read-through (`player/engine.ts::start`) and shrinks on undo (`back()` replays from the start), and book 3's depth is 74, so a real read never approaches 505. Nothing tests the constant against the catalog, so the false basis is invisible. | Update the derivation and the number to the real largest skeleton, and add a lockstep test asserting the constant matches the largest `production_eligible` skeleton in `skeletons/`, so the next oversized book fails a test instead of quietly invalidating a comment. Raising an audit-derived (F8) cap wants the audit owner's sign-off, so recommend rather than change unilaterally. | open | `api/schemas.py:38-52`, `tests/unit/test_schemas.py` |
| AL-025 | 2026-07-25 | adversarial review | process | An adversarial review is only as good as its ability to finish. Four review agents died on a session limit having delivered no findings, and the only value recovered came from two half-formed leads left in their reasoning traces. The reasoning trace, not the final report, was the durable artifact. | For any delegated review, ask for findings incrementally (highest-value first) rather than one report at the end, so a truncated run still yields something. Recover partial work from `subagents/agent-*.jsonl` rather than assuming a failed agent produced nothing. | applied | this review; leads chased manually and closed |
| AL-026 | 2026-07-25 | adversarial review | authoring-craft | **Breadth built from terminating leaves halves the typical read.** Reproduced with `player/engine.StoryEngine` over 3,000 condition-aware random reads: book 1 (305 nodes) has a median read of 11 pages / 650 words; book 3 (746 nodes) has a median of **5 pages / 302 words** of a 42,085-word book. Termination density is identical (22.3% vs 22.5% of choices land straight on an ending), so the extra 441 nodes went into *width made of failure leaves*, not into reader-visible journey. 7 endings sit within two taps of the start, 3 decision nodes have every choice end the book, and 3,000 random reads reached only 94 of 232 endings. This qualifies AL-009: hanging rooms off an act hub does control depth, but paying for breadth in terminating leaves makes the book *feel smaller* the bigger it gets. | Author breadth as reconvergent detours, not terminal leaves: a room should more often cost `vigor` and rejoin than end the run. PL-17's endings floor actively pushes toward terminal leaves at gamebook breadth, so the floor and this lesson are in tension and the floor's shape should be revisited. | applied | 45 shallowest failure leaves converted to vigor-costing pass-throughs; median read 5 -> 20 pages, endings reached 39% -> 56%; gate clean, chain clean, endings exactly on the 187 floor |
| AL-027 | 2026-07-25 | adversarial review | validator | Nothing constrains the **typical** path length. PL-20 floors only the *fastest satisfying* path (37 nodes for this cell), which book 3 satisfies at 52, while the median actual read is 5 pages. A book can pass every scale rule and still hand most readers a 90-second encounter. | Add a scale advisory on expected read length over the walk machinery `validator/walk.py` already has: warn when the median uniform-walk playthrough falls below a per-cell page floor, or when more than ~20% of a node's choices terminate. Roughly 20 lines on existing infrastructure, and it would have caught AL-026 before the fill pass. | open | `validator/walk.py`, `validator/policy.py` |
| AL-028 | 2026-07-25 | adversarial review | product | K6's "found N of M endings" replay motivator inverts at large M. At M=232 a finished read reports "1 of 232" (0.4%), and `EndingsBadge` silently drops its dot row above `MAX_DOTS = 10`, removing the only kid-legible part of the mechanic precisely for the books that most need replay motivation. Part of the denominator is also effectively unreachable: 3,000 random reads found 94 of 232. | Stop showing a raw denominator above a threshold: milestone framing ("3 new endings found, lots more to go") with a tier ladder, or scope the denominator to a curated signature-endings subset. `EndingsProgress` and `EndingsBadge` must share the threshold so the ending screen and the shelf never disagree. | open | `frontend/src/reader/EndingsProgress.tsx:60-65`, `frontend/src/library/EndingsBadge.tsx:19-40` |
| AL-029 | 2026-07-25 | adversarial review | product | The reader progress bar is `visit_set.length / story.nodes.length`, so it measures corpus coverage, not route progress. A median book-3 read shows 1% and then snaps to 100% at the ending screen; the bigger the book the more broken the signal. The numeric label is already suppressed as untrustworthy, but the `aria-label` still announces "5 of 746 pages explored". | Make in-read progress route-relative (path length against distance-to-nearest-ending from the current node, precomputable from the walk the validator already does), or show "Page 5" for large books. Corpus coverage belongs on the shelf card, not in the reader chrome. | open | `frontend/src/reader/readerProgress.ts:7-11`, `ReaderChrome.tsx:88` |
| AL-030 | 2026-07-25 | adversarial review | product | Every page turn synchronously replays the entire read from page 1: `canGoBack` calls `back()`, which DFS-replays the recorded path, and discards everything but a boolean. Compounding it, `nodeIndex` rebuilds a 746-entry Map on every engine entry point, so a depth-71 replay performs ~285 rebuilds. Measured O(depth x nodes): 26 ms at depth 70 on a container CPU, and a BACK press pays it three times, so a phone plausibly freezes 300ms+ on the one control built for children who mis-tap. Cost grows with the depth the book is designed to reward. | Memoize the node index and `intBounds` per Storybook in a `WeakMap` (story identity is stable for the reader's lifetime, satisfying the existing `#ASSUME`); stop `canGoBack` recomputing an answer it throws away; short-circuit the common "no" case off the render path. | open | `frontend/src/player/engine.ts:17-19,325-347`, `reader/Reader.tsx:216`, `player/machine.ts:99-111` |
| AL-031 | 2026-07-25 | adversarial review | product | No response compression is configured anywhere in the stack (no `GZipMiddleware` in `app.py`, nothing in either compose file), so a story download ships **405 KB instead of 107 KB** and the 3-book series costs 750 KB instead of 202 KB. Separately, `list_library` and `/v1/reading-history` `select(StorybookVersion)` whole-row including the `blob` JSONB for every book, decoding 768 KB of prose per shelf render to produce three small cards; the reading-history call fires on every ending-screen mount, which at a 5-page median read is constantly. | Add `GZipMiddleware(minimum_size=1024)`, a one-line ~4x win on the one step where a child waits with nothing to do. Separately, denormalise `title`, `node_count`, `ending_count` and `ending_node_ids` onto `StorybookVersion` at publish time so the listing paths never load prose. | applied | `GZipMiddleware(minimum_size=1024)` in `app.py`; the listing-path denormalisation half stays open under this id |
| AL-032 | 2026-07-25 | adversarial review | product | Reconnecting mid-read can teleport a child backwards to an earlier page and remount the reader, from **this device's own queued past**. `persist()` updates `revisionRef` only on `saved`, never on `queued`, so after offline reading the live save carries a stale base revision; the replay-on-reconnect advances the server revision, the next tap 409s, and the 409 handler adopts the server row unconditionally and bumps `readerKey` to remount. The existing `#ASSUME` deliberately accepts losing position to *another device*, but explicitly reasons about "another device advanced this story's row" and does not contemplate this case. A 74-choice offline read queues 74 sequential writes, so the 409 window is proportional to how much the child read offline. Traced end to end, not executed. | Collapse the offline queue to the newest state per (profile, storybook) before replay so 74 round-trips become 1; advance `revisionRef` on `queued`; and guard the adopt branch so a `current_row` that is a strict prefix of the local path is recognised as this device's own stale past and re-saved rather than adopted. | open | `frontend/src/reader/ReaderPage.tsx:296,304-305,317-337`, `offline/sync.ts:277-325`, `api/reading.py:363-371` |
| AL-033 | 2026-07-25 | adversarial review | validator | **Safety hole.** `_screen_all_nodes` calls the bright-line classifiers once per node in a sequential loop guarded by `if openai_key and openai_reason is None`, so the FIRST `ClassifierUnavailable` disables that classifier **for every remaining node**, with no retry and no per-node coverage record. At 746 nodes Stage 0 makes 1,492 sequential third-party calls, which is exactly the shape that trips a Perspective ~1 QPS quota or an OpenAI per-minute limit; a 429 at node 50 leaves 93% of the book unscreened for `sexual/minors`, `self-harm/instructions` and `illicit/violent`. The resulting report is indistinguishable from a fully-screened clean one apart from one whole-story ADVISORY, and `summary.hard_block`/`soft_flag` are unaffected, so `submit` and `approve` both pass. Verified by reading the loop; the docstring states the no-retry behaviour outright. | Track per-node classifier coverage and emit a **FLAG**, not an advisory, naming the unscreened node count and ids, so the story cannot reach `in_review` clean. Add bounded retry with backoff plus a concurrency limit so a rate limit is absorbed rather than abandoning the book. Make `submit`/`approve` refuse a report declaring incomplete Stage-0 coverage. | applied | `moderation/classifiers.py`: per-node coverage, bounded retry, circuit breaker, and a gating `classifier_coverage_incomplete` FLAG; 6 new tests |
| AL-034 | 2026-07-25 | adversarial review | scale | One import of this book is ~2,986 strictly sequential provider round trips (2 classifier calls plus 2 review calls per node, plus 2 whole-story stages), with no `gather`, semaphore, batching or call budget, all inside **one** Postgres transaction that also holds `SELECT FOR UPDATE` on the Storybook row. That is a 40 to 100 minute `idle in transaction` session against a pooler configured for `transaction` mode, and any single failure discards the entire run with no checkpoint or resume. | Bound the per-node stages with a concurrency semaphore and a per-story call/time budget that records an explicit "moderation incomplete" finding when exceeded. Split the import: commit the draft first, then moderate in short transactions, persisting findings incrementally so a retry re-screens only unscreened nodes. `approve` already refuses a version with no moderation report, so a committed unmoderated draft is safe to leave behind. | open | `moderation/stages.py:196-268`, `pipeline.py:98`, `generation/import_cli.py:92-119` |
| AL-035 | 2026-07-25 | adversarial review | scale | `run_gate` is a measured 3.72 second synchronous CPU burn on this book and is called inline from three `async def` handlers with no `to_thread`, so it stalls the whole event loop: every passage edit, an admin rescreen sweep, and `validate_version`, which is guardian-reachable and therefore a cheap self-DoS on the entire API. | Wrap all three in `anyio.to_thread.run_sync`, cache the gate report per `(storybook_id, version)` so `validate_version` returns the stored report, and rate-limit it. | applied | `anyio.to_thread.run_sync` around `run_gate` in `api/node_edit.py`, `api/generation.py`, `moderation/rescreen.py` |
| AL-036 | 2026-07-25 | adversarial review | product | ADR-005 makes a human's recorded approval the only path to a child, and at 746 nodes that surface cannot deliver it: the review view ships the whole blob (measured 444 KB), renders all 746 passages, 232 ending badges and 1,030 choice rows unvirtualized in one DOM, and offers no pagination, no per-node reviewed state, no resume, and one Approve button at the bottom. A reviewer cannot read 42,085 words in one page load, so the recorded approval attests to far less than it claims. The queue and the guardian browse listing separately load every book's full blob to compute a title and a count. | Add node-range pagination and stop echoing the whole blob; virtualize the read-through; persist per-node review progress keyed by `(storybook_id, version, node_id, reviewer)` so the approval record can state what fraction was actually read. Project listings in SQL or denormalise `title`/`ending_count`/`flagged_count` at publish time. | open | `api/review_surface.py:100-117`, `api/approval.py:222-285`, `frontend/src/admin/ReviewDetailPage.tsx:534-565` |
| AL-037 | 2026-07-25 | adversarial review | tooling | Import silently rewrites the authored series block. `embed_series_block` replaces the declared `series_id`, `book_index`, `is_final` and `series_entry_node` with DB-derived values, and nothing anywhere compares the declared `book_index` to the assigned one, so **import order becomes chain order**. `carries_state` is likewise taken from the `Series` row, whose value is derived from that row's own age band, so a mismatched row silently rewrites all three 16+ books to `carries_state: false`; SR-6 and SR-7 both still pass, and the campaign degrades to three episodic books with no error at import, approval, or read. `--job` (the only route that stamps `skeleton_slug`, runs the Stage 1 fidelity gate, and enables the leaf-diversity guard) and `--series-id` (the only route that links a series) are mutually exclusive, so no combination produces a correct Tier-2 series. | Compare declared against assigned in `embed_series_block` and raise on mismatch rather than overwrite; preserve declared `is_final`; check `Series.age_band` and `Series.family_id`. Move series linkage into `import_filled_story` so both routes carry it, and make `--job --series-id` a hard error instead of a silent ignore. | open | `generation/series_link.py:131-208`, `generation/import_cli.py:52-119` |
| AL-038 | 2026-07-25 | adversarial review | authoring-craft | **Narrowing a carried range erases reader outcomes, which qualifies AL-008.** Verified from the specs: book 2 declares `renown` `[0,5]`, book 3 declares `[3,5]`, and `startContinuation` clamps carried ints into the receiving book's bounds, so every reader who finishes book 2 with renown 0, 1 or 2 arrives at book 3 indistinguishable from a maximum-reputation reader. My AL-008 justification ("a company that shut two doors cannot become unknown") was wrong on book 2's own terms. Two more in the same mechanism: book 2's `deep_charts` and `oath_sworn` are dropped without trace because book 3 does not declare them, and book 3 declares `second_iron` initial `true` while book 2 earns it from `false`, so a reader who finishes book 2 without it carries `false` and reads 746 nodes under a premise the prose contradicts. All of it happens in the client with no server-side cross-check, and no SR rule compares variable names, types or bounds across a chain. | Author side DONE (`renown` widened to `[0,5]`); the rest is not author-fixable. Validator side: add an SR rule that every variable declared in book N appears in book N+1 with the same type and a containing range, error on narrowing, warn on dropped. Make `startContinuation` record a per-variable carry audit. And gate the continuation offer on a satisfying ending: `get_series_next` offers book 3 after ANY book-2 ending, which is what lets a reader carry `second_iron: false` into a book whose premise is two irons. | applied | SR-8 in `validator/series.py` (ERROR on a narrowed range or changed type, WARNING on a dropped variable), catalogued and tested; it flags this chain's 2 dropped variables. Still open: the read-time carry audit and gating the continuation offer on a satisfying ending |
| AL-039 | 2026-07-25 | adversarial review | scale | Two moderation stages are structurally impossible at this size and fail open. The soft-gate auto-repair must return the entire revised story but is capped at 32k output tokens against a ~101k-token blob, so it always truncates, always fails to parse, and always returns `None` after burning a ~101k-token call; on the Ollama leg it can trip a byte ceiling and raise, discarding the whole import. The Stage 1 fidelity gate builds a single ~120k-token prompt (746 beats plus 1,030 label pairs) and asks for a 512-token verdict, with every parse failure returning "pass"; it also embeds untrusted generated prose raw, without the `<untrusted_passage>` wrapper and instruction-hierarchy suffix that `moderation/stages.py` documents as `#CRITICAL: security`. | Make both per-node or per-batch with a required verdict per batch, treating a missing verdict as a violation rather than a pass. Skip repair with a recorded finding when the blob exceeds the output budget. Wrap fidelity prose in `_sanitize_delimited` and append the untrusted suffix, matching stages.py. | open | `moderation/repair.py:93-108`, `moderation/fidelity_review.py:226-261` |
| AL-040 | 2026-07-25 | adversarial review | scale | `POST /admin/rescreen` runs its whole sweep synchronously in one HTTP request, defaulting to every published book: with these three books that is ~2,712 sequential classifier calls plus ~11 seconds of event-loop-blocking gate. The admin gets a gateway timeout while the sweep continues against a request-scoped session whose unit of work may then roll back, discarding the verdicts after the full provider spend. It also inherits AL-033's short-circuit and does not even pass `require_classifiers`. | Enqueue on RQ and return a job id, or require an explicit `storybook_ids` scope with a node cap. Pass `require_classifiers` outside local, and move `run_gate` off the event loop. | open | `api/rescreen.py:121-155`, `moderation/rescreen.py:161-165` |
| AL-041 | 2026-07-25 | lessons-log tooling | tooling | The lessons-log validator passed while silently ignoring eight rows. A blank line ends a markdown table, so an appended block separated by one splits the log in two and `_collect_rows` stops at the gap; the reported count (32 of 40) was plausible enough not to notice. This is AL-016 recurring inside the tool built to record AL-016. | Added an orphan check: scan the whole file for `AL-NNN` rows and fail when any sits outside the checked table. Negative-tested by reintroducing the split. The general rule stands: a validator must assert its own input coverage, not just the validity of what it happened to parse. | applied | `scripts/check_lessons_log.py`, orphan check plus its negative test |
| AL-042 | 2026-07-25 | adversarial review | tooling | **Adding a skeleton left CI red on main.** `skeletons/**` feeds a committed calibration baseline, and `docs/planning/ws5_floor_baseline.json` was never regenerated, so `test_skeleton_mutation_floors.py::test_calibration_is_deterministic_and_committed_baseline_is_current` failed on a clean checkout. The arithmetic is exact: same-cell pairs 67 -> 77 and Tier-2 pairs C(13,2)=78 -> C(16,2)=120 from adding three books, moving TAU_STRUCT 0.332507 -> 0.329972. A red required check on main means every later PR inherits a failing gate, which is how a quality gate stops being one. | Regenerated, and the `skeleton-promotion` workflow now runs `calibrate_mutation_floors.py --check`, so a skeleton cannot be added without recalibrating. | applied | `docs/planning/ws5_floor_baseline.json` regenerated; the failing test now passes |
| AL-043 | 2026-07-25 | adversarial review | docs | Two derived artifacts went stale in the same change, which says the skeleton-addition workflow has no regenerate-derived-artifacts step. `render_skeleton_diagrams.py --check` failed for three `.puml` files plus `docs/architecture/story-skeletons.md`, the human-readable catalog index an author consults to ask what is already in a cell. Unlike the floor baseline, this guard is wired into nothing: no pre-commit hook and no workflow runs it, and its tests only exercise `tmp_path` fixtures, so the suite stayed green while the live docs were wrong. Regenerating also rewrites all 64 SVGs nondeterministically, which the checker does not verify, so that churn should be reverted rather than committed. | Regenerated the three diagrams and the catalog index, and both `--check` invocations are now a step in the `skeleton-promotion` workflow. | applied | `docs/architecture/story-skeletons.md` plus three new `.puml`/`.svg`; guard now exits 0 |
| AL-044 | 2026-07-25 | adversarial review | authoring-craft | **Books 1 and 2 are near-clones by the project's own metric.** Measured `structural_distance` = 0.013939, which is 3.6x below TAU_CELL (0.05), the floor a mutant must clear against every in-cell tree, and the closest pair in cell (16+, medium, gamebook). Deriving book 2 from book 1's topology was deliberate for series continuity, but it means the catalog now holds a pair the mutation gate would reject as a clone, and `select_skeleton_for_cell` de-weights by slug recency only, so a reader handed both gets the same tree twice. Nothing could have caught it: the anti-clone floor is architecturally unreachable for a hand-authored skeleton, since `check_promotion_bundle` returns early on the missing lineage sidecar and `_floor_reason` returns None when there is no mutation parent. | Split the in-cell duplication check out of the lineage-gated block so it runs on any changed `skeletons/**` shell and fails below TAU_CELL. Then either reshape book 2's graph or record the pair as an explicit series-continuity allowlist entry so the check stays meaningful. The pre-existing 0.000947 harrowstone/sunken-temple pair is the same defect. | open | `scripts/check_promotion_bundle.py:196-202`; `reviews/ceiling-scale-review-2026-07-25.md` |
| AL-045 | 2026-07-25 | adversarial review | product | Continuation books are selectable as standalone stories. `skeleton_matches_cell` and `candidates_for_cell` filter on band, length, style and eligibility and never look at `metadata.series`, so roughly 40% of (16+, medium, gamebook) requests draw a Wyrmreach book and about 20% draw book 2 specifically. Book 2 opens pre-seeded with `iron_key: true`, `knows_compact: true` and `renown: 2`, and its start beats name Kar Duhn and the ninth iron, so a guardian asking for any theme gets a mid-trilogy book whose protagonist already owns artifacts from a story the reader has never seen, and the fidelity gate ends up fighting the skeleton. | Treat a skeleton with `series.book_index > 1` as ineligible for a request that is not an explicit continuation of that series, keeping book 1 selectable as the entry point, and pin it with a test. | applied | `skeleton_match.is_continuation_skeleton` excludes book_index > 1; books 2 and 3 left their cells, book 1 stayed |
| AL-046 | 2026-07-25 | adversarial review | scale | The automated fill pipeline cannot emit the books the matcher can select. `fill_skeleton` is one-shot: the whole skeleton in, the whole filled document out, capped at `_MAX_TOKENS_PROSE = 32000`, with no chunking anywhere in `generation/`. The 746-node book needs ~101k output tokens, 3.2x the cap, and 13 of the 26 committed fills already exceed it. Selection has no node-count or token-budget predicate, so a doomed job burns roughly four rounds of ~100k input tokens through the repair loop and fails deterministically forever. Pre-existing, but this run added the worst case and confirmed nothing on the promotion path checks fill feasibility. | Add a fill-feasibility predicate to selection (expected output size is computable from the FILL word targets) and a test asserting every `production_eligible` skeleton is fill-feasible, which would fail for 13 books today. The real fix is an act-scoped fill loop; hand authoring already fills these books act by act. | open | `generation/orchestrator.py:95,826`; `generation/skeleton_match.py:176-194` |
| AL-047 | 2026-07-25 | adversarial review | tooling | The skeletons are protected by glob-discovered suites; the **filled stories are not tested at all**. No test or workflow references `out/*.filled.json` or `data/series/wyrmreach/`, and no CI job runs the gate over `out/`. A skeleton passing the gate does not prove its fill passes: they are separate documents and a fill regresses independently. The irony is pointed for the 746-node book, whose own L2-13 finding says the configuration walk is its sole correctness guarantee, and that walk never runs on the filled book in CI. Separately, `test_skeleton_contracts.py` globs `*.contract.json`, so the three contract-less new books are invisible to the WS-2 drift guard, and its guard-the-guard test only asserts the list is non-empty. | Add a glob-discovered fill suite over `out/*.filled.json` asserting `run_gate` does not block and no FILL directive survives; measured cost is 1.7-3.8s per book, so all 26 fit the sub-30s target. Tighten the contract-coverage test to a real assertion or an explicit exempt-slug allowlist. | applied | `tests/unit/test_filled_story_corpus.py` gates every `out/*.filled.json`; found 3 pre-schema-v2 fills (AL-050) |
| AL-048 | 2026-07-25 | adversarial review | tooling | SR-1..SR-7 are proven only against a synthetic 2-node book (`tests/unit/test_series.py:19-65`), a shape no author would write. The Wyrmreach trilogy is the repo's first real state-carrying chain and it validates in 0.02s, so the feared CI cost does not exist: a series fixture needs the meta-validator, not the configuration walk. | Add a case loading the three filled books and asserting `validate_series(books).ok`, plus negative variants mutated in memory (drop book 2 for SR-2, blank book 3's entry node for SR-3, flip book 1's `carries_state` for SR-7). Turns a hand-verified artifact into a permanent regression asset at no meaningful runtime cost. | applied | `tests/unit/test_series.py` validates the real 3-book chain plus SR-2/SR-3/SR-7 negative variants |
| AL-049 | 2026-07-25 | adversarial review | scale | A book authored at the cell node ceiling is a formally valid mutation parent the flywheel cannot use. `the-ninth-hand` has 4 nodes of headroom to the 750 maximum, so only the two smallest additive operators could ever be admitted and M3 graft is dead for any donor region above 4 nodes. Worse, the operators do not terminate on it: M4 insert-linear and insert-decision both exceeded 150s, and the cost tracks configuration-space size rather than node count (the three new books hold the catalog's three largest state spaces, ~100x a same-size book). Operator slowness is pre-existing; landing all three new books on the wrong side of it is new. | Have flywheel parent-selection skip a parent whose ceiling headroom is below the smallest additive operator's delta, and give the operator candidate search a configuration-count budget so a high-state parent fails fast with a reason instead of hanging a run. | open | `mutation/operators.py:3073-3096`; `validator/band_profile.py:177` |
| AL-050 | 2026-07-25 | fill-corpus suite | tooling | The new filled-story suite immediately found three committed fills (`the-lost-mitten`, `the-sunken-signal`, `the-clocktower-cipher`) that cannot pass the gate at all: they carry the retired `ending.type` field instead of `ending.kind` plus `ending.valence`, and omit the now-required `metadata.topology`. Pre-existing legacy artifacts, invisible because nothing tested `out/`. | Quarantined under **strict** xfail so migrating one makes its test XPASS and forces the list to be pruned, which stops the quarantine becoming a dumping ground. Migrate all three to schema v2 and delete `_LEGACY_PRE_V2`. | open | `tests/unit/test_filled_story_corpus.py` |
| AL-051 | 2026-07-25 | PL-24 calibration | validator | Calibrating a new advisory against the whole corpus changed its design. A single positive-valence share floor of 5% fired on **every** gamebook (all nine sit at 2.1-4.8%) and on no prose story (15.2-70.0%), with no overlap. That is not nine defects, it is ADR-011 section 5's declared few-wins-many-fails shape, whose denominator is inflated by failure leaves on purpose. A rule that fires on 100% of one style is a misunderstanding of the style, not a signal. | Made the floor style-aware: a share for prose, an absolute count of winnable endings for gamebooks. General rule: calibrate a new threshold against the existing corpus before shipping it, and if it flags an entire class, suspect the threshold. | applied | `validator/policy.py` PL-24; calibration recorded in the constant's comment |
| AL-052 | 2026-07-25 | PL-23/PL-24 rollout | product | The two new advisories immediately found real defects in pre-existing content that nothing had ever measured: 13 of 23 committed fills declare an `estimated_minutes` more than 25% from their derived clock (`the-vanishing-orchard` says 18 minutes for a 33-minute finish), and 6 books have one ending kind above 60%, including `the-ashfall-expedition` at 98% setback and `the-sunspire-ascent` with only 2 winnable endings in 74. | Triage the 13 read-time drifts and the 6 mix outliers as content work. Both are advisory so nothing is blocked, but the numbers are now visible on every gate run. | open | `validator/policy.py`; corpus scan in this commit's message |
| AL-053 | 2026-07-27 | PR #416 rebase onto main | tooling | The in-cell clone audit (#415) and the continuation filter (#416, AL-045) shared one predicate, `skeleton_matches_cell`, so #416's selection-only change to it silently changed the catalog audit. Rebasing #416 onto a `main` that now carries #415 made `iter_incell_pairs` inherit the `book_index > 1` exclusion: the brass-lantern book1/book2 pair (the sole allowlisted breach) stopped being measured, no pair breached, and the #415 allowlist entry read as stale, turning two green #415 tests red with no conflict marker and no content change. A clean textual rebase hid a semantic collision that only the full suite surfaced. | Decouple the audit from the selection filter: a keyword-only `include_continuations` on `skeleton_matches_cell`/`candidates_for_cell` (default False, so generation selection stays byte-identical) that the audit alone sets True, so a series book 2 which re-skins book 1 is still measured. Owner chose this "keep the gate honest" path over exempting continuations, consistent with #415's on-record rejection of metric-gaming. General rule: when a predicate is shared by a selection policy and a quality audit, a change for one silently rebinds the other; assert the audit's scope independently of the selector. | applied | `generation.skeleton_match.skeleton_matches_cell` include_continuations flag; `diversity/incell.py` iter_incell_pairs; `tests/unit/test_incell_clone_audit.py` (unchanged, green) |
| AL-054 | 2026-07-28 | PR #435 skeleton-promotion repair | tooling | The skeleton-promotion gate had never once gone green, and the cause was a policy gap rather than a bug: `check_skeleton` rejects any shell declaring `metadata.production_eligible: false` unless passed `--allow-mvp`, while the CI job feeds it the raw changed-file list of any PR touching `skeletons/**`. Seeds are legitimate merged catalog members (3 of 61), so every PR that touched one failed unconditionally. A gate that fails on a legitimate input is indistinguishable from a gate nobody wired up, and it stayed unnoticed because a never-green scheduled workflow produces no signal anyone reads. | Choose the envelope from the claim the shell itself makes: pass `--allow-mvp` exactly when the shell declares itself a seed, and print a `note:` line naming every shell proved as a seed so a green run still shows which checks were relaxed. General rule: when a gate is red on 100% of runs, suspect the gate's input contract before its logic, and never let a downgrade happen without a line in the job log. | applied | `scripts/check_promotion_bundle.py` `declares_production_eligible`; `tests/unit/test_ws8_promotion.py` asserts both directions |
| AL-055 | 2026-07-28 | PR #435 review | tooling | Two silent-vacuity patterns in the same PR, both of which report green while measuring nothing. An empty `@pytest.mark.parametrize` list does not fail: pytest emits one `SKIPPED [NOTSET]` placeholder, so a seed-discovery helper that stops finding seeds would silently retire both promotion regressions. The same shape appeared in `safety-eval.yml`, where an all-skipped `llm_eval` run exits 0, so a missing credential read as a passing safety evaluation. A suite's exit code answers "did anything fail", never "did anything run". | Assert non-emptiness at the discovery site (raise at collection when the catalog holds no seed) and assert executed-count at the workflow level (parse the JUnit XML and fail when `executed < 1`). General rule: any gate whose input set is discovered rather than enumerated needs an explicit floor on that set, checked where it is discovered. | applied | `tests/unit/test_ws8_promotion.py` `_catalog_seed_shells` raises on empty; `.github/workflows/safety-eval.yml` "Assert the evaluation actually ran" step |
| AL-056 | 2026-07-28 | ADR-023 Stage A sentinel-survival measurement | tooling | Running `scripts/measure_sentinel_survival.py` from a fresh worktree failed through three sequential environment layers before producing data: (1) `Settings` in `core/config.py` declares no `env_file`, so credentials in `.env` are invisible unless the process environment carries them (VS Code injects them in the main tree; a worktree shell does not); (2) `OLLAMA_CA_BUNDLE=certs/homelab-ca.pem` is cwd-relative, so the same env fails in any directory without a `certs/` symlink; (3) the Anthropic leg then died on an account-billing 400 that only a one-trial probe surfaced cheaply. Each layer cost a full script start to discover because nothing validates the environment before the paid loop begins. | Give the measurement script (and any future paid-provider instrument) a `--preflight` mode that checks, in order: required keys present in the process env, every file-path env var resolves from the current cwd, and one minimal real call per requested provider, then exits before any paid volume. Document the worktree recipe (source `.env` with `set -a`, symlink `certs/`) in the script docstring. General rule: an instrument that spends money must verify its environment with one cheap probe per dependency layer before entering the paid loop; per-layer failure discovery multiplies startup cost by the number of layers. | open | Execution plan Task A1 notes updated (this branch); script change not yet made |
| AL-057 | 2026-07-28 | ADR-023 Stage A sentinel-survival measurement | tooling | Measured first-attempt sentinel survival through the production fill route (`openrouter:anthropic/claude-haiku-4.5`, 30 stories, 4 slots each, `fill_bound.md` "Verbatim tokens" instruction present): **1/30 clean (3.3%)**. Taxonomy: dropped 1,738, forged 207, relocated 111, in-choice-label 3, malformed 7. Dropped dominates by 8x: the model writes prose without the token despite an explicit copy-verbatim instruction, so this is not a delimiter or wording defect to iterate; the model treats an inline token as guidance, not payload. A one-retry policy at this rate adds ~97% to fill spend. | Do not build features that depend on an LLM preserving inline markers through a creative rewrite; the design plan's G1 gate correctly fired STOP, and the deterministic post-fill re-insertion fallback (match the generic default word in filled prose, re-wrap it server-side) becomes the primary design for ADR-023 Stage B+. General rule: any invariant that must hold byte-exactly through an LLM call needs a deterministic re-establishment step after the call, not a prompt instruction before it. | applied | Design plan section 3.4 "MEASURED 2026-07-28" block (`docs/planning/story-personalization-implementation-plan.md`), which restates the clean-pass rate and full failure taxonomy inline |
| AL-058 | 2026-07-28 | PR #449 review | tooling | `SENTINEL_RE` matches only well-formed `{~SLOT:Word~}` tokens, so it fails OPEN on a bisected one: truncating before stripping leaves `{~HERO:Expl`, which no later strip can ever match. The invariant is therefore strip-BEFORE-truncate ordering, not strip-presence, and ordering is invisible to any test whose fixture is shorter than the cut. PR #449 fixed this ordering in `covers/prompt.py` but left the identical shape unfixed in three sibling call sites (`story_requests/anchoring.py`, `moderation/fidelity_review.py`, `flywheel/reguide_draft.py`), two of which feed LLM prompts, so a fragment would have taught the next book's model that `{~HERO:Expl` is ordinary story text. | Two changes. (1) When fixing an ordering bug, grep the repo for every other `[:N]` slice applied to blob-derived text rather than fixing the reported site alone. (2) Every regression test for this class must place the sentinel so the cut bisects it, AND assert that the pre-fix ordering leaked on the same input (`assert "{~" in strip_sentinels(body[:N])`), so the test fails loudly if a constant change later moves the cut and stops exercising the bug. | applied | `tests/unit/test_anchoring.py::test_sentinel_straddling_the_title_cut_is_stripped_before_truncation`, `tests/unit/test_flywheel_reguide_draft.py::test_story_context_sentinel_is_stripped_before_the_600_char_cut`, `tests/unit/test_fidelity_review.py::test_sentinel_bearing_beats_and_body_never_reach_the_review_prompt` |
| AL-059 | 2026-07-29 | ADR-023 re-insertion prototype measurement | tooling | Deterministic strip-all-then-reinsert scored only 10% story-clean against the strict every-expected-node multiset, but the per-slot decomposition showed the metric was wrong, not the transform: named third-party slots (OPERATOR, LISTENER, HUB, FOUNDER) re-insert at 94-99%, while HERO sits at 42% node coverage with zero prose occurrences in 11/30 stories, because the corpus is second-person and the protagonist is the addressee, named only in vocative dialogue. The skeleton's per-node expectation model prescribes a name where the narrative voice structurally cannot put one; no model quality or prompt wording can satisfy it. A further 154 of 1,026 misses were sentence-initial capitalization or inflected forms of lowercase descriptive values (the pup, the grown-up). | Derive the at-rest sentinel expectation from what deterministic re-insertion actually finds in the prose (round-trip-verified), instead of prescribing per-node presence from beats; keep name coverage as a soft quality floor with the P8 dedication overlay guaranteeing one personalized surface. Make the matcher sentence-start case-aware and inflection-aware for lowercase multi-word values. General rule: when a measured failure clusters on one slot or structural position, suspect the expectation model before the generator. | applied | Commits 3fbf0807 and a63c06e9 (derive-not-prescribe re-insertion promoted into storybook/ and wired into the fill path; manifest derived from what re-insertion found, round-trip-verified) and 8916c2db (sentence-start case-aware matching); the inflection-aware half was deliberately not built after AL-060 proved the plural bucket was a classification artifact (0 real occurrences); dedication overlay made mandatory (Task C5) per the Stage R re-plan |
| AL-060 | 2026-07-29 | ADR-023 Stage R matcher hardening (Task R1) | tooling | The "72 inflected-form misses" bucket in the AL-059 analysis was an artifact of the crude prefix heuristic used to classify misses: exact re-measurement with a dedicated plural counter found 0 bare-plural occurrences across the whole 30-story corpus, because the heuristic had been counting possessives, which the word-boundary matcher already wraps correctly (`Explorer's` becomes `{~HERO:Explorer~}'s`). Only the sentence-start capitalization bucket was real (76 of 1,027 misses recovered; 120 widened occurrences wrapped). Building the speculative plural matcher first would have been dead code justified by a phantom number. | Before implementing a matcher or repair policy sized from a bucketed failure analysis, re-derive the bucket with an exact counter for the specific phenomenon; a classification heuristic that merely prefixes or fuzzy-matches will conflate adjacent phenomena and inflate the case for the wrong fix. Ship the counter as a permanent report field so the decision stays checkable on future corpora. | applied | Commit 8916c2d (`plural_occurrences` report field, sentence-start widening, possessive test); design plan section 3.4 "MATCHER HARDENING RE-MEASURED 2026-07-29" block |
| AL-061 | 2026-07-29 | ADR-023 Stage R confirmatory run (Task R4) | tooling | Per-slot re-insertion coverage swung materially between two 30-story runs of the same pipeline (HERO 42.4% then 26.8%, FOUNDER 94.6% then 78.6%, CHAPERONE 63.9% then 30.6%), while the transform-correctness metric (verify_manifest) held at exactly 100% on both. Coverage is a property of each LLM fill, not of the code, so a single-run coverage number quoted as a stable property will mislead the next decision that cites it; only invariants of the deterministic transform are stable enough to gate on. | When a measurement mixes model-behavior metrics with code-correctness metrics, report the model-behavior ones as cross-run ranges and gate only on the code-correctness ones; exit records and plans must quote coverage as a range with run count, never a point estimate. | applied | Execution plan Task R4 "CONFIRMED 2026-07-29" block; design plan section 3.4 "G1-R CONFIRMED 2026-07-29" bullet (both state ranges, not points) |
| AL-062 | 2026-07-29 | ADR-023 vocative nudge experiment (Stage R optional item) | tooling | A one-sentence fill-prompt nudge meant to raise HERO name coverage instead cut it 5x (26.8-42.4% baseline to 4.9%), because the sentence ended with an explanatory rationale clause ("second-person narration alone never surfaces the name") that the model executed as an instruction: it suppressed narration mentions and confined the name to dialogue it then rarely wrote. The A/B measurement caught the regression before the edit was ever committed. | Fill-prompt edits must be imperative-only and additive (one behavior per sentence, no trailing rationale clauses, phrased "in addition to" existing behavior, never "instead of" framing or negative examples), and every prompt-template change that targets a measured metric must be A/B-measured with the saved-fills harness before it is committed. | applied | Design plan section 3.4 "VOCATIVE NUDGE MEASURED AND REJECTED 2026-07-29" block; execution plan Stage R optional bullet marked rejected; template edit reverted uncommitted |
| AL-063 | 2026-07-29 | ADR-023 Stage R+B PR review (PR #466) | tooling | A drift gate that declares one file authoritative but checks only a downstream copy of it leaves the authoritative file unguarded, and it drifts. `scripts/check_er_diagram.py` called `er-diagram.puml` "the authoritative source" in its own header while comparing only the two Mermaid copies against the ORM; the .puml had silently accumulated three missing columns (`child_profile.reduce_motion`, `device_grant.expires_at`, `story_request.interpretation`) that predate this branch and that a green gate reported as no drift. The gate was not weak, it was pointed at the wrong file. | Every drift gate must check the artifact it names as authoritative, not only the copies generated from it; when the authoritative file carries richer prose annotations than the copies, check the subset that can silently disagree with the source of truth (here, column names) and say in the docstring why the rest is excluded. Verify a new gate by breaking the artifact deliberately and confirming the failure message names the break. | applied | Commit `cde11a75`, `scripts/check_er_diagram.py` `puml_problems` / `parse_puml` and the property-3 docstring paragraph |
| AL-064 | 2026-07-29 | ADR-023 Stage R+B PR review (PR #466) | process | Restating a measurement in prose is a claim about evidence, not evidence. Every Stage R number in the design and execution plans cited a run directory under `results/sentinel-survival/`, which `.gitignore` excludes, so a reader could see the figures but had no artifact to check them against and no way to tell a transcription slip from a real result. The gap was invisible while the run directories still existed on the authoring machine. | When a planning document cites a measurement as the basis for a gate decision, commit the aggregate report (not the raw per-story fills) under `docs/planning/evidence/<harness>/` and cite that path; keep `results/` paths only in reproduce commands, where they are instructions rather than claims. Where two harnesses report similar-looking rates, the committed README must name which instrument produced each number. | applied | Commit `8c28f15f`, `docs/planning/evidence/sentinel-reinsertion/` (4 runs plus README); citations repointed in the design and execution plans |
| AL-065 | 2026-07-29 | ADR-023 Stage R+B PR review (PR #466) | tooling | A test that fails when you break the thing it guards is not yet proven useful; it also has to fail for the RIGHT reason. The new `_prefetch_personalizable_slots` empty-sweep test used a bare `AsyncMock` session, so deleting the guard killed the helper on `TypeError: 'coroutine' object is not iterable` several lines before the assertion ran. The break-it check went red, which looks like proof, but the red said nothing about the guard: any later refactor that changed the mock plumbing would have flipped it green while the defect stayed open. Separately, a scoped `-k` filter used to measure coverage excluded the very tests that covered the module, producing a confident "80%, 18 uncovered new lines" reading for a file that is actually at 100%. | Extend the non-vacuity check from "does it go red" to "does the failure message name the break". When it does not, wire the double well enough that the code under test runs to completion and fails on the real assertion. For coverage measurement, never conclude a gap from a `-k`-filtered run: filters silently exclude covering tests, so scope by test FILE (or intersect missed lines against the diff's changed lines) before believing a number. | applied | Commit `fe4b53c5`, `tests/unit/test_rescreen_unit.py::test_prefetch_personalizable_slots_never_queries_for_an_empty_sweep` (docstring records the failure-mode reasoning); measurement correction recorded in the same commit message |
| AL-066 | 2026-07-29 | ADR-023 Stage C client resolution (Task C0) | tooling | The values payload was keyed by slot type (`protagonist_first_name`) while prose sentinels carry the slot id (`{~HERO:...~}`), and nothing in any response joined the two, so the client resolver specified in two plan documents was unimplementable as designed. The gap sat unnoticed through the server-side stages because no server-side consumer needed the join; it surfaced only when the client resolver was specified in detail. | A payload the client must interpret needs a client-resolvable key: when a contract-derived join is required (here the slot-id to slot-type map from the book's theme contract), ship the join in the payload itself and assert its presence in the route's own test, rather than assuming the client can rederive server-side knowledge. | applied | Commit `ca50cbc5`, `slot_bindings` on `PersonalizationValuesView` plus route tests asserting it; consumed in `frontend/src/player/personalization.ts` |
| AL-067 | 2026-07-29 | ADR-023 Stage C client resolution (Task C0) | tooling | A payload field named in two plan documents (`sentinel_pattern`) was never implemented, and no test noticed, because every existing test asserted only the fields its own scenario read; nothing enumerated the response's key set. The field was load-bearing for the client (the resolver compiles it), so the drift would have surfaced as a runtime fallback, not a test failure. | For a payload that crosses a language boundary, one test should assert the full field set of the response, not just the fields a given scenario reads; the empty-payload shape is the natural place, since every predicate failure must return the same universal shape anyway. | applied | Commit `ca50cbc5` (field and route tests); commit `97c2fead` strengthened `test_values_unconnected_family_empty` to assert the full empty shape |
| AL-068 | 2026-07-29 | ADR-023 Stage C client resolution (Task C0e) | tooling | `dedication` was absent from `CLOSED_VOCABULARIES`, so a kid-facing surface accepted free text while two plan sections said it was a closed enum. The registry and the slot taxonomy it claims to cover had no drift guard between them, so a slot type added to one and not the other fails open (free text accepted) rather than closed. | A closed-vocabulary registry needs a drift guard against the slot taxonomy it claims to cover, the same way the DB CHECK lists are pinned to `PERSONALIZATION_FIELDS`: a test that fails when a slot type exists in the taxonomy with neither a vocabulary entry nor an explicit free-text exemption. | applied | Immediate instance fixed in commit `f03c7fdc` (dedication now fails closed); the drift guard itself (UW-C20) shipped in PR #507, `tests/unit/test_personalization_vocab_drift.py` (every `PERSONALIZATION_FIELDS` member has a vocabulary or an exemption, `CLOSED_VOCABULARIES` keys are a subset of `PERSONALIZATION_FIELDS`, every vocabulary is non-empty, exempt fields carry no entry, and the consent scope's admissible vocabulary matches the same ceiling) |
| AL-069 | 2026-07-30 | Moderation QA corpus fixture fixes (PR #497 follow-up) | tooling | Six hand-authored storybook fixtures shipped with schema/shape tests but were never run through `validator/gate.py::run_gate`, and five of the six were gate-blocked: one declared `intense` scariness/peril at a band whose ceiling is `moderate`, two declared a topology their own graph does not admit, and four sat under their band's PL-17 ending/decision floors. None of that is visible to a parse-and-shape test, and none of it stops moderation from running, so it looked fine. What it silently removed was repair coverage: `moderation/pipeline.py` re-runs `run_gate` over a repaired blob before adopting it, so a fixture that cannot clear the gate can have a repair generated but never adopted, and the seed script's docstring claim that the corpus exercises "repair" end to end was false for five of six books. A second defect rode along: the self-harm fixture's prose stated intent and a search for means while its own manifest entry claimed "no method detail", so the label and the labelled artifact disagreed in a public repo. | Any hand-authored storybook fixture set needs a gate assertion, not just a schema assertion, with an explicit allowlist for fixtures whose gate violation IS the test. Adding to that allowlist must be a documented trade (it costs that fixture its repair-adoption coverage), never a way to quiet a red test. Where a manifest asserts a property of fixture prose, the prose is the thing to fix, not the assertion. | applied | `tests/unit/test_moderation_qa_corpus.py::test_gate_blocked_books_are_exactly_the_declared_exceptions`, plus the "what this corpus does and does not exercise" section of `scripts/seed_moderation_qa.py` |
| AL-070 | 2026-07-31 | ADR-023 Task D6 PR review (PR #507) | tooling | A test that reads an artifact's own text can only prove the artifact is self-consistent, never that it is correct, and it silently pins whatever typo it was written alongside. The D6 consent sweep shipped against `"public"."personalization_consent"`; the real table is `personalization_disclosure_consent`. Its guard asserted `"personalization_consent" in ddl` against the migration source, so the test and the typo agreed and every unit test went green. The defect surfaced only in the real-backend CI leg as `relation "public.personalization_consent" does not exist (SQLSTATE 42P01)`, after review, commit, and push. | A guard that reads a migration's text must resolve every identifier it checks against a second, independent source rather than against the same file: here, the set of tables the migration directory as a whole creates. Where the independent source needs a live database (columns, types), say so in the docstring and name the CI job that does check it, so the cheap guard's limits are on the record instead of implied. | applied | PR #507, `tests/unit/test_personalization_vocab_drift.py::test_split_migration_only_names_tables_that_a_migration_creates`; non-vacuity confirmed by reintroducing the typo and checking the failure message names it |
| AL-071 | 2026-07-31 | ADR-023 Task D4 pilot contract (branch `feat/personalization-d4-pilot-contract`) | tooling | `StorybookVersion.personalization_eligible`'s own docstring named `generation/worker.py` and `generation/import_story.py` as the two paths that compute and stamp it, but neither path actually did so; the column existed and D8 (`api/library.py`) already built a reader trusting it, so a value one stage claimed to produce and a later stage claimed to consume sat unwritten in between. Nothing failed: the column defaults to `False`, so every existing row read as "not eligible", which happened to be true of the whole catalog anyway, and the gap was found only when D4 planning went looking for the producer code and it was not there. | When a column's docstring names its producer(s) by file, require a test asserting each named producer actually writes it (a `False` default passing silently is not proof); a comment naming a producer is a claim, and claims about behaviour need a test the same way claims about data need a `Ref`. | applied | commit `d76a1bc7` (this branch): `test_fill_stamps_personalization_eligible_when_contract_declares_slots` and `test_import_stamps_personalization_eligible_when_contract_declares_slots` (plus their false-case pairs) close the gap for both named producers |
| AL-072 | 2026-07-31 | ADR-023 Task D6 migration (PR #507), gap recorded during D4 Task 6 documentation | tooling | D6's migration first shipped as `supabase/migrations/20260730000000_split_favorite_personalization_slot.sql`; PR #494, a concurrent branch, had already taken the same `20260730000000_` prefix for `add_cover_object_salt.sql`. Supabase keys `schema_migrations` on that prefix, so both files were individually valid and both branches passed CI alone; the collision (`duplicate key value violates unique constraint "schema_migrations_pkey"`) surfaced only once the two branches met at merge time, and was fixed by renaming to `20260730010000_`. Nothing on either branch, reviewed alone, could have caught it. | Add a pre-push or CI check that a branch's new migration file's timestamp prefix is strictly greater than the newest prefix already present on `origin/main` at push time, so a same-day collision between two concurrent branches fails locally (or in that branch's own CI run) instead of surfacing only after both merge. | open | `supabase/migrations/20260730010000_split_favorite_personalization_slot.sql`; collided prefix was `supabase/migrations/20260730000000_add_cover_object_salt.sql` (PR #494); tracked by `UW-C21` in `unscheduled-work-register.md` |
| AL-073 | 2026-07-31 | ADR-023 Task D4 Task 5 owner-run live pilot fill (branch `feat/personalization-d4-pilot-contract`) | tooling | The owner's Task 5 run (`20260731T173743Z`) again hit the cwd-relative `OLLAMA_CA_BUNDLE=certs/homelab-ca.pem` setting AL-056 already documents: pydantic-settings resolves the path against the process's current working directory, so a script invoked from this worktree cannot find the CA bundle unless a `certs/` symlink exists there too, and cost two owner round trips (fix, re-run, discover the second cwd-dependent path) before the run above could start. This is not a new defect, it is AL-056 recurring a second time on a second worktree since it was logged still open on 2026-07-28 with no `--preflight` mode or repo-root-relative resolution built. | Same proposed change as AL-056, now reinforced by a second occurrence: resolve `OLLAMA_CA_BUNDLE` and any sibling file-path settings repo-root-relative in `core/config.py` (or add the `--preflight` validate-with-hint mode AL-056 already proposed) rather than leaving them cwd-relative. A lesson that recurs across independent runs after being logged open is real debt, not a one-off; treat this recurrence as the signal to schedule the fix rather than re-log it a third time. | open | recurrence of `AL-056`; run record committed at `d4-pilot-run-20260731.md`; tracked by `UW-C22` in `unscheduled-work-register.md` |
| AL-074 | 2026-07-31 | UW-F22 dead-symbol sweep and PR #520 review | tooling | A TypedDict that is never used as an annotation is prose, not a contract, and it drifts silently from the dict it claims to describe. `SkeletonAuthoringMetadata` was referenced only from a docstring cross-reference in `import_story.py`, so nothing compared it to its producer; the moment it was applied as a real annotation, BasedPyright showed the producer had been writing four A6/A7 differentiation keys the shape never declared, and that four fields typed `str` are in fact written `str \| None`. Vulture found it only as a by-product of hunting unreferenced symbols, which means the detection was luck rather than design. The second half of the lesson is that the type checker is now the ONLY guard: a future producer writing a key absent from the shape is caught by BasedPyright and by no test, so narrowing type-check scope would silently reopen the same hole. | Treat "declared but never annotated" as a defect class in its own right: any TypedDict describing an authoring artifact must be applied at its producer and consumer, not cross-referenced from a docstring. Where the shape crosses a JSONB boundary, add one runtime assertion so the contract survives a type-check gap. | applied | PR #520, commit `83b4c58a`; shape widened and the divergence recorded in the class docstring, with the register outcome in `unscheduled-work-register.md` UW-F22 (d) |
| AL-075 | 2026-08-01 | W2.1 choice-grammar validator (`validator/choice_grammar.py`, kid-appeal implementation plan) | tooling | BasedPyright strict mode can narrow a `tuple[str, ...]`-typed dataclass field to the literal empty-tuple type `tuple[()]` across an `and`-joined boolean guard (`if len(run.node_ids) < 2 and run.terminal_id is None: continue`), then reports a later `run.node_ids[0]` as `Index 0 is out of range for type tuple[()]`. The message names the symptom, not the cause: nothing about it points at the guard clause that produced the narrowing, and the field's real runtime type is never actually the empty tuple (a `_Run` is always constructed with at least one node id). Diagnosed by bisecting the guard out of the loop; the fix cost no logic change, only reordering. | Where a strict-mode narrowing error's line is not the line that caused it, hoist the field read (`len(...)`, indexing) into a local variable *before* the narrowing guard, rather than trusting the reported line number to name the fix site; add this as a named pattern so the next strict-mode surprise on a variadic-tuple field is recognised in one pass instead of re-bisected. | applied | `validator/choice_grammar.py::check_words_per_stop` hoists `run_length`/`head_id` above the guard; `uv run basedpyright src/cyo_adventure/validator/choice_grammar.py` clean (0 errors) |
| AL-076 | 2026-08-01 | PR #532 review (I8), `validator/choice_grammar.py` | tooling | `_FLOWED_RUN_CAP = 6` carried a comment saying the ADR-011 section 10 table "fixes at 6". The table's flowed rows read `1, prefer 0` and `0-1`, and they count STOPS while the code counts NODES; the number 6 appears nowhere in the ADR, and section 10's own closing paragraph scopes graph-level run caps to the discrete-page bands. The citation was invented after the fact to justify a derived backstop, which is worse than no citation: it reads as ratified, so nobody re-checks it, and the real stop-level rule stays unimplemented behind a rule that looks like it. | Where a constant is DERIVED rather than read off a ratified table, say so in the same breath and name the unit it is in; reserve an ADR citation for a number the ADR actually prints. Where a proxy stands in for an unimplemented constraint, tag it `#ASSUME` and register the real rule, so a green check is not mistaken for the constraint being enforced. | applied | `validator/choice_grammar.py` `_FLOWED_RUN_CAP` comment rewritten with the derivation and an `#ASSUME`; stop-level rule registered as `UW-C23` |
| AL-077 | 2026-08-01 | PR #532 review (I12), `frontend/src/kid/badgeCatalog.ts` | tooling | A `#VERIFY` claimed its test pinned "every id/name/description pair" against the Python catalog. The test did `expect(source).toContain('"<id>"')`: ids only, one direction, whole-file substring, so a match inside a comment satisfied it and every kid-facing name and description could drift silently. The marker was written from the intent, not from the assertion. | When writing a `#VERIFY` that names a test, state what the test ASSERTS rather than what it is for, and prefer parsing the mirrored source to substring-matching it so the claim and the check cannot diverge. | applied | `frontend/src/kid/badgeCatalog.test.ts` now parses `BADGE_CATALOG` out of `progress/badges.py` and compares sorted (id, name, description) triples in both directions, with a length guard against a silently non-matching regex; proven by renaming one badge (test fails) and reverting |
| AL-078 | 2026-08-01 | PR #532 review (I15), `scripts/audit_ending_valence.py` | tooling | The ending-valence triage audit read `valence = str(ending.get("valence", ""))`, and its suspect heuristic returns False for anything that is not exactly `negative`. A MISSING valence therefore collapsed into the benign branch: the one ending guaranteed to need a human was the one guaranteed to be invisible. Two more silent skips sat beside it (a node marked `is_ending` with no `ending` object was `continue`d, and a non-list `nodes` returned zero rows), and all three still exited 0, so a partial scan was indistinguishable from a clean one. Separately, `path.relative_to(_REPO_ROOT)` crashed on any `--out-root` outside the repo, which is why the script had no tests. | An audit must never let a missing field take the same branch as a well-formed benign value: validate against the closed set of legal values and report anything outside it, rather than defaulting into the safe branch. Anything the audit could not audit gets its own row, its own count, and its own exit code, kept distinct from the informational triage findings; and display filters (`--band`, `--suspect-only`) must be applied after the exit code is computed, so choosing a filter cannot turn a defective catalog into a clean-looking run. | applied | `scripts/audit_ending_valence.py` emits PROBLEM rows and exit code 2; `tests/unit/test_audit_ending_valence.py` (7 cases) proves each, verified by reverting the valence check |
| AL-079 | 2026-08-02 | Research-base rebuild, `docs/planning/research/` (story-structure diversity analysis) | process | ADR-011 cited its empirical basis as "JHM 2019" plus `docs/planning/research/`, but the acronym was never expanded, no DOI or URL was recorded, and the research directory was never committed. The mechanism of the loss was then found during the rebuild: `.gitignore` listed `docs/planning/research/` under "Local research scratch docs (not source-controlled)", so the ADR cited a directory the repo was configured to never track, and no reviewer could notice the notes missing from a PR that could not contain them. Recovering the source took a dedicated external research pass and succeeded only because the acronym happened to match a real journal (Adams, Beckelhymer and Marr, Journal of Humanistic Mathematics 9(2), 2019, DOI 10.5642/jhummath.201902.05); until then every structural constant was untraceable folklore, and AL-076 had already shown one constant carried an invented citation. Verification found one quoted constant (decisions per playthrough) is derived rather than stated by the paper, and two (words/node, total words) are unverifiable from any indexed source. | ADR citations must carry full bibliographic data with URL or DOI, and the research notes an ADR cites must be committed in the same PR as the ADR change that cites them (now a standing rule in `docs/planning/research/README.md`). Fold the corrected citation and the derived/prior labels into the UW-G17 ADR-011 amendment. | open | |
| AL-080 | 2026-08-05 | Three blind deep-research reports reconciled against JHM 2019; validator gap analysis | tooling | The policy gate measured story shape on one axis only. PL-17 counts decision and ending nodes anywhere in the graph (breadth); nothing measured what a reader meets walking a path (depth). PL-20 came closest but counts NODES on the shortest satisfying path, not decisions along it, so a story could satisfy every floor while the reader walked a corridor. The sweep proved the gap was live, not theoretical: `the-tide-pool-rescue` opens with five consecutive single-choice nodes (385 words) before a 5-8 reader gets any agency, and eight prose skeletons offer a choice every 1.1-1.9 nodes against a measured corpus mean of 3.28. Both shapes passed every rule the gate had. | Add the depth axis: PL-25 (nodes to the first decision, band-keyed) and PL-26 (nodes per decision on the fastest finish, style-keyed), plus an advisory ceiling companion to PL-20's floor. State the breadth/depth split in the policy module docstring so the next reader does not re-derive which rule sees which axis. | applied | `validator/policy.py` PL-25/PL-26 + PL-20 ceiling; `validator/band_profile.py` `_FIRST_DECISION_DEPTH`, `_NODES_PER_DECISION`, `ARC_CEILING_MULTIPLE`; 27 tests in `tests/unit/test_policy.py` and `tests/unit/test_band_profile.py` |
| AL-081 | 2026-08-05 | PL-25 calibration against JHM 2019 Table 4 | process | The first draft of the PL-25 band table set the 3-5 ceiling at 3 and the 5-8 ceiling at 4, against a source corpus whose MEDIAN pages-to-first-decision is 4 and whose range runs to 8.25. Those thresholds would have made median-paced storytelling a blocking ERROR, and they blocked three skeletons on first run. The values were invented for the bands the research does not cover, then applied more strictly than the bands it does cover, which is backwards: the least-evidenced thresholds were the tightest. A second instance survived to the test-writing step, where the 8-11 ceiling of 8 still sat under the corpus maximum of 8.25. | When a threshold is derived from a measured corpus, assert the calibration invariants as tests rather than trusting the table: no threshold may block the corpus median, and any band the corpus covers must admit its full measured range. Where a band is outside the corpus, say so in the table comment and set the threshold looser than the measured bands, not tighter. | applied | `test_band_profile.py::test_first_decision_ceiling_never_below_the_corpus_median` and `::test_first_decision_measured_bands_span_the_corpus_range`, both of which failed against the first draft |
| AL-082 | 2026-08-05 | Validator gap analysis, `validator/band_profile.py` | tooling | `BandProfile.reconvergence_ceiling` is declared on the frozen dataclass, defaults to `None`, is set on no band, and is read only by `mutation/operators.py` (the offline catalog tool). No validator rule consults it, so the request path has no reconvergence constraint at all while the field's presence on the profile implies one. This is the same shape as an inert rule: a reader auditing `BandProfile` sees a reconvergence policy that does not exist. | Either wire a reconvergence rule into `validate_policy` and populate the per-band values, or move the field out of `BandProfile` into the mutation layer that actually reads it. Deciding between them needs an owner ruling on whether reconvergence gets a floor as well as a ceiling; Fridkin and Hurry (2025, DOI 10.1007/s12144-025-07685-3) found perceived choice raised comprehension at d=0.52 with the same underlying text, which argues reconvergent structures are defensible on evidence rather than only on budget. | open | |
| AL-083 | 2026-08-05 | Skeleton compliance sweep, `skeletons/5-8/the-tide-pool-rescue.json` | authoring-craft | The skeleton is a strict `loop_and_grow` whose four rescue quests ran in a hardcoded order (`st` -> `fi` -> `cr` -> `pw` via `t_st_fi`, `t_fi_cr`, `t_cr_pw`), so the reader never chose which stranded creature to help; `n_meet` introduced all four in prose and then offered one choice. That produced two findings at once, and only one was about ordering. PL-25 fired because the first real decision sat 6 nodes in. PL-20 fired because both rest endings are `neutral`/`setback`, so every SATISFYING ending sat past `n_alldone`, reachable only after all four rescues: a 30-node shortest satisfying completion against a ceiling of 22. Reordering alone would not have moved PL-20 by a single node. | Rewritten as the hub the prose already described: `n_meet` offers one choice per creature, and every `*_thanks` ends on the same decision (go back out, or let the tide arrive), so the reader chooses both the order and the length and a satisfying ending is reachable after the first rescue. The transitions survive as the outbound travel legs and finally carry the `Q*_SIGN` cue slots. Both findings and the residual PL-23 clock drift cleared. Generalised check: read what a rule MEASURES, not what its name suggests, before assuming one fix clears two findings; 'shortest satisfying completion' and 'quest order' are independent quantities that happened to share a cause. | applied | `skeletons/5-8/the-tide-pool-rescue.json` |
| AL-084 | 2026-08-05 | PL-26 calibration review during the first full-library sweep | validator | PL-26 shipped as a symmetric window and every one of its 8 library hits was on the LOW side, the opposite of the corridor shape the rule was built to catch. The cause is a selection effect: density was measured along a SHORTEST path, which is biased toward decision nodes (out-degree >= 2 makes a node likelier to sit on a fast route), while the JHM 3.28 anchor it was compared against was measured corpus-wide. Comparing them on the low side compares two different quantities, structurally the same category error as judging a gamebook by a prose window, which the same rule had already been fixed for once. A uniform sign across every hit is the tell: real defects scatter, artifacts do not. | Low bound removed; PL-26 is a ceiling only and now fires on nothing in the library, which is correct for a guard aimed at generated stories rather than hand-authored ones. If a 'choice gauntlet' guard is wanted, it must measure WHOLE-GRAPH density, not path density. Generalised check: before trusting a new threshold's hits, look at whether they share a sign, and confirm the measurement scope matches the anchor's scope. | applied | `UW-C28`, `src/cyo_adventure/validator/band_profile.py` `_NODES_PER_DECISION_CEILING` |
| AL-085 | 2026-08-05 | PL-25 severity review after the first full-library sweep | validator | PL-25's ceiling shipped as a blocking ERROR and immediately unpublished a committed, production-eligible skeleton on a ONE-NODE margin (depth 6 against a ceiling of 5), breaking 6 tests across 4 files. The gate's ERROR tier means unpublishable, and the house precedent was already there to read: PL-20 blocks on its floor (too short to be a story) and only warns on its ceiling (a long arc). A new craft rule applied retroactively to an existing library should not block on a margin narrower than its own calibration confidence. | PL-25 regraded to two tiers: WARNING past the band ceiling, ERROR only past `ARC_CEILING_MULTIPLE` x ceiling, reusing the measured constant rather than inventing a margin. Generalised check: when adding a rule to a gate that has a blocking tier, find the nearest existing rule on the same axis and match its severity split before choosing one; and treat 'my new rule breaks tests across several files' as evidence about the rule, not only about the data. | applied | `src/cyo_adventure/validator/policy.py` `_check_first_decision_depth` |
| AL-086 | 2026-08-05 | First full-library PL-25 sweep over all 61 skeletons | authoring-craft | 20 of 61 committed skeletons opened ON their first decision: `start_node` itself offered two or more choices, so the reader picked before any situation had been established. It spanned five of six bands (5-8 x2, 8-11 x3, 10-13 x4, 13-16 x6, 16+ x5) and both narrative styles, which made it a house convention rather than 20 independent slips. It also sat outside the entire JHM 2019 corpus, whose measured pages-to-first-decision range starts at 2, so no book in the reference corpus opened this way. | Ruled drift, not design (owner, 2026-08-05). Each skeleton was SPLIT rather than padded: the establishing beats moved to a new `n_open` node that became `start_node`, the original node kept its choices, and the word budget was divided rather than added so the fastest-finish clock barely moved. PL-25's floor was then escalated from WARNING to ERROR against a clean library. Generalised check: when a new rule flags a third of the library at once, the finding is about the library's conventions or the rule's calibration, never about 20 separate authors erring the same way; resolve which before touching either. | applied | `src/cyo_adventure/validator/policy.py` `_check_first_decision_depth` |
| AL-087 | 2026-08-06 | Cold-open fix sweep; two skeletons newly blocked by L1-7 | validator | Adding the establishing node PL-25 requires pushed `the-clocktower-cipher` (8 -> 9) and `the-sunken-signal` (12 -> 13) past their L1-7 `branch_depth` cap, so the two rules contradicted each other: obeying the opening rule made the story unpublishable. Only these two were hit, and the reason is where their budget comes from. Both are the only skeletons with `length: None`, so they fall back to the band-level `max_depth` (a pre-ADR-011 legacy number, 4/6/6/8/10/12) instead of a production cell. A cell's depth cap is ~2.5x its `min_complete` floor, and those floors are JHM PAGE counts for a whole playthrough, which already include the pages before the first decision; the band numbers derive from nothing measured. Three other skeletons sat at exactly zero headroom and absorbed the same +1 without complaint. | `ESTABLISHING_STOP_DEPTH = 1` added to `band_profile`, granted on the MVP fallback path only, with the provenance argument recorded at the constant. Generalised check: a new structural rule spends budget that older budgets never allocated; before shipping it, ask which budgets were derived from data that already included the thing the rule now mandates, and which were not. Only the latter need widening. | applied | `src/cyo_adventure/validator/band_profile.py` `mvp_node_budget` |
| AL-088 | 2026-08-06 | Depth-headroom measurement across all 61 skeletons while fixing AL-087 | validator | The MVP fallback depth cap is not calibrated to the MVP node envelope it governs. `mvp_node_budget` pairs a band-independent 8..45 node range with the band's legacy `max_depth`, so a 45-node MVP story in the 3-5 band is capped at depth 4 while ADR-011's own 3-5 cells allow 15 (10-23 nodes) and 18 (23-45 nodes) for the same sizes. A 45-node story at depth 4 is close to geometrically impossible unless it is a pure fan. The two skeletons on this path are both within one node of their cap, which is the symptom rather than a coincidence. | Recalibrate the MVP depth cap against the MVP node envelope rather than inheriting a band number tuned for a different node range; the two ADR-011 cells whose node ranges bracket 8..45 both say 15-18, which is a citable anchor. Not done under AL-087, which deliberately granted only the one node PL-25 mandates rather than relitigating ADR-011 calibration inside a cold-open fix. | open | `UW-C30` |
| AL-089 | 2026-08-06 | Cold-open sweep fallout in the mutation compose and bundle tests | tooling | Three tests grafted ADR-020's worked-example subtree (`n_lockup`, 36 nodes) onto a 64-node host, landing the mutant at EXACTLY the 100-node cell maximum. The establishing node AL-086 added took the host to 65 and the graft became unconstructible, failing at chain step 0 rather than at the behaviour under test. The substitute is squeezed from both sides and the window is narrow: the cell envelope caps the graft at 35 nodes, while the anti-clone floor rejects small ones (a 13-node subtree lands 0.0368 from an in-cell tree against `TAU_CELL` 0.05 and is discarded as a near-duplicate), so only a 34-node subtree satisfies both, with one node of headroom. | Switched the three tests to the 34-node `n_plan` subtree and recorded at the fixture why both bounds force it; ADR-020 keeps its original figures as dated evidence of a D8 run rather than a live fixture. Generalised check: a fixture sitting at exactly a documented maximum is a latent break, not a passing test, so treat zero headroom as a defect to fix while it is still green. Where two floors squeeze from opposite directions, record the arithmetic at the fixture, because the next person will otherwise pick the obvious smaller value and get a discard whose message names diversity, not size. | applied | `tests/unit/test_skeleton_mutation_compose.py` `_GRAFT`; same constant in `test_flywheel_reguide_draft.py` and `test_skeleton_mutation_bundle.py` |
| AL-090 | 2026-08-06 | Flywheel mutants blocked by PL-25 after the floor escalation | validator | A real defect, not fixture drift: M3 prune-graft could manufacture the exact shape PL-25 forbids. A graft hangs one new choice off its host, so grafting onto a single-choice node CONVERTS that node into a decision. Nothing in the operator noticed, because an establishing stop is non-ending and lands inside ADR-011's 2-3 choices window after the add, so every other precondition passed it. Worse, the new rule made the case common rather than rare: the establishing stop is now the first single-choice node in every skeleton and the planner enumerates hosts in document order, so it became the FIRST host tried. | `_pl25_opening_reason` added as a graft precondition, checked in the operator rather than the planner because `scripts/mutate_skeleton.py` passes a hand-chosen host and would bypass a planner-side filter. Generalised check: a new structural rule over documents creates a new way for every generator and mutation operator to violate it. Sweeping the committed corpus proves only that today's documents comply; also enumerate the operators that can manufacture the flagged shape, and ask whether the rule's own fix has made a previously rare host the first one tried. | applied | `src/cyo_adventure/mutation/operators.py` `_pl25_opening_reason`, wired into `_graft_preconditions` |
| AL-091 | 2026-08-06 | Test-fixture fallout across the pipeline suites | tooling | Adding one node to the mock story broke tests that had nothing to do with pacing, and every break was a literal standing in for a derived value: `assert len(book.nodes) == 7` (measuring the fixture, not the round-trip invariant it names), seven `blob['nodes'][0]` lookups that silently retargeted to a different node, and a `review_batch_size=3` chosen so 7 nodes would split 3+3+1 and exercise the single-node path. That last is the sharpest: the remainder became 2, the single-node path stopped being exercised entirely, and only an unrelated count assertion made it visible. A rule change is a fixture change, and a fixture coupled by literals fails far from its cause. | Each literal replaced by the derived value: compare against `len(_CANNED_STORY['nodes'])`, look nodes up by id through a `_node()` helper, and set the batch size to `_NODE_COUNT - 1` so the remainder is one BY CONSTRUCTION. Generalised check: when a test depends on an arithmetic relationship with a fixture (divides evenly, leaves a remainder of one, has N nodes), derive the constant from the fixture instead of writing the number, or the relationship silently stops holding while the test stays green. | applied | `tests/unit/test_moderation_pipeline.py` `_PARTIAL_CHUNK_BATCH_SIZE`; `tests/unit/test_node_edit.py` `_node`; `tests/unit/test_worker.py` node-count assertion |
| AL-092 | 2026-08-06 | Derived-artifact sweep after the 20-skeleton cold-open split | process | Changing 20 skeletons put 26 committed documents derived from them under the escalated PL-25 floor, and they surfaced one test failure at a time over several runs because only SOME derived artifacts are gated. Sweeping the whole derived tree through `run_gate` directly found the rest at once, and re-rendering it through the real producer found something the gate could not: of 45 WS2 bound theme-swaps, only 14 still reproduce from their own skeleton plus binding. 19 had drifted in nodes unrelated to any of this work, and 2 can no longer be rendered AT ALL because their skeleton gained slots (`COMPANION_NAME`, `ROUTE_C_KEEPER`/`ROUTE_D_KEEPER`) the committed binding cannot fill. That tree had been silently desynchronized from the catalog long before this change. Two fills were also missed on the first pass for a second reason: they sit in a strict-xfail quarantine, so a sweep keyed on a clean gate result skips them. | Fixed only what is live: the gate-tested pilot parameterized skeleton, the two pilot fills (registered `CATALOG_ENTRIES`, and catalog import gates every entry), and the diversity panel corpus, re-baselined via `--update-baseline` because R5 is a deliberate fixture-tamper detector; every diversity delta landed in the third or fourth decimal, so the detector kept its calibration. `out/ws2/` was left frozen: re-rendering it would re-baseline a completed workstream's evidence against a catalog that has moved on, and 2 of them cannot be re-rendered without authoring new binding entries. Generalised check: reproducing a derived artifact through its real producer is a STRONGER test than gating it, because it catches drift in everything the gate does not measure; run it before deciding whether an evidence tree is stale or frozen. | open | `UW-C31` |
| AL-093 | 2026-08-06 | Regenerating skeleton diagrams rewrites 5 SVGs whose source did not change | tooling | `scripts/render_skeleton_diagrams.py` regenerates every diagram, not only the stale ones, and its SVG step re-measures text with whatever fonts the running machine has. 21 `.puml` files changed because their skeletons gained an establishing node; 26 `.svg` files changed, and the extra 5 differed only in geometry (`textLength` 69.6035 -> 61.0547, canvas 1507x1058 -> 1360x1062) with no structural or label delta. `--check` compares committed `.puml` content only and inspects SVGs solely for a PlantUML error card, so byte drift in an SVG is invisible to the freshness gate in both directions: it never fails on it, and it never warns that a content commit is carrying it. Left alone, 5 files of pure environment noise would have shipped inside a semantic change, and the next author would have had to re-derive that they meant nothing. | Revert the SVGs whose sibling `.puml` is unchanged before staging: `comm -23` the changed-SVG list against the changed-PUML list, then `git checkout HEAD --` the difference. Proposed change: teach the renderer a `--only-stale` mode that skips rendering when the `.puml` it would render is byte-identical to the committed one, so the noise is never produced rather than produced and reverted. Generalised check: when a generator emits two artifacts and the gate covers only one, assume the ungated one carries environment state, and diff it for semantic content before deciding it belongs in the commit. | open | `UW-C31` |
| AL-094 | 2026-08-06 | PR #635 review, the PL-26 corridor guard | validator | One computed shortest path was shared by two rules, and the sharing was documented as the safety property: "PL-20 and PL-26 both read this one result, so they can never disagree about which walk is the fastest finish". That reasoning WAS the defect. PL-20 reads only the path LENGTH, identical across every minimum-length path, so any tie-break is sound for it. PL-26 reads the DECISION COUNT along the path, which is not invariant, so `_shortest_path_to`'s tie-break (`sorted(graph.successors(...))`, i.e. node-id alphabetical order) silently became part of the rule's semantics: renaming two routes flipped the verdict from 7.0 nodes per decision (WARNING) to 3.5 and SILENT, with zero structural change. Determinism was mistaken for correctness; it guarantees the same arbitrary answer every run, not the right one. Blast radius measured by sweeping the catalog: 19 of 58 PL-26-eligible skeletons had their density mismeasured, a third of the corpus, and 2 were under-reported across the ceiling. | `_fewest_decision_shortest_path`, a layered dynamic program over the shortest-path DAG (BFS levels, then one forward sweep where edge u->v is in the DAG exactly when level[v] == level[u]+1), returning the minimum-decision path among the minimum-length ones. Worst case rather than canonical case, because PL-26 is a CEILING and must fire when ANY equally fast walk is a corridor. O(V+E), deliberately not enumeration: shortest-path count is exponential (a chain of parallel two-way detours doubles it per link) and this runs in the request path. `_decision_node_ids` extracted so PL-25, PL-26 and the search cannot drift on what a decision is. Generalised check: when two rules share one derived artifact, ask which PROPERTIES of it each reads. Sharing is safe only while every reader depends on an INVARIANT of the artifact; the moment one reader depends on a property that varies among equally valid artifacts, the tie-break has become that rule's semantics and must be chosen by the rule, not inherited from the search. State the invariant in the docstring instead of citing the sharing. | applied | `validator/policy.py` `_fewest_decision_shortest_path` / `_decision_node_ids` / `_breadth_first_levels`; verified against a brute-force `nx.all_simple_paths` oracle over 2,614 random digraphs, 0 mismatches on walk validity, endpoints, minimum length, and minimum decisions |
| AL-095 | 2026-08-06 | PR #635 review, the band_profile PL-25 comment | validator | The block comment that declares itself the single source of truth for the PL-25 window documented BOTH tiers backwards: "Below the floor is a WARNING, not an ERROR" and "Above the ceiling is an ERROR", while the code does the exact opposite and `validator-rules.md` had it right. The comment sits directly above the table it describes, so it is what an author actually reads, and it carried an `#ASSUME: data-integrity` marker asserting its own authority. Nothing compares a comment's severity claim against the code that consumes the table, so the two-place split (comment near the data, catalog far from it) diverged with every gate green. | Corrected both bullets with the real rationale per direction (a story opening on its first choice asks the reader to pick before a situation exists, a correctness failure, hence a blocking one-tier ERROR; a buried first choice is a craft defect, hence WARNING, escalating to ERROR only past `ARC_CEILING_MULTIPLE` times the ceiling) and named `policy._check_first_decision_depth` as the authority. Also added the missing `#ASSUME`/`#VERIFY` pairs to `ARC_CEILING_MULTIPLE` and `_NODES_PER_DECISION_CEILING`, whose documented sibling had them. Generalised check: a comment asserting a rule's SEVERITY is a testable claim living in an untested medium. When a rule grades in tiers, name the consuming function rather than restating its verdicts, so there is one statement of the tiering instead of two that can disagree. | applied | `validator/band_profile.py` `_FIRST_DECISION_DEPTH` comment; every `#VERIFY` test name it now cites confirmed to exist |
| AL-096 | 2026-08-06 | PR #635 review, the dev and catalog seed builders | process | `scripts/seed_dev_data.py` and `scripts/seed_series_catalog.py` insert stories at `status="published"`, the state a child can read, and neither file referenced `run_gate` or `validate_policy` at all. So the new PL-25 floor made the seeded corpus gate-failing while every gate stayed green, because the seeders are a write path that bypasses the gate entirely. The stories were ALREADY failing PL-17 (1 ending against a floor of 3, 1 decision against a floor of 3), PL-18 (a declared `branch_and_bottleneck` with no real reconvergence) and L2-11 before this PR; the new rule only added to a pile nothing was checking. | Grew both blobs to be gate-clean and wired `validator.gate.run_gate` (the two-layer entry point the generation pipeline itself calls, not `validate_policy` alone) into all THREE `status="published"` insert sites, failing loudly on any ERROR with the story id and every failing rule id in the message. Generalised check: when adding a blocking rule, enumerate every writer that can put the gated artifact into a published state, not just the pipeline the rule was written for. Grep for the published-state literal, NOT for the gate: the defect is precisely the gate's absence, so grepping for the gate finds nothing and reads as clean. | applied | `scripts/seed_dev_data.py` / `scripts/seed_series_catalog.py` `_assert_gate_clean`, wired at all 3 published inserts; gate output shows 0 ERROR findings for every seeded story |
| AL-097 | 2026-08-06 | PR #635 review, `_pl25_opening_reason` coverage | tooling | A new guard placed at the FRONT of a precondition ladder forces every existing fixture to be updated past it, and that same update is what leaves the guard itself uncovered. `_pl25_opening_reason` (AL-090's own fix) shipped with zero direct coverage while `_tier1_story()` and one inline host fixture both gained an establishing stop in the same change. The review's first reading, that the fixtures had been routed AROUND the new branch, is RETRACTED: the restructure was necessary and the PR documented why inline, since without the stop a graft onto the old start node trips PL-25 first and every test aiming at a LATER rung would stop short and pass for the wrong reason. The two facts share one cause rather than one being evidence of the other. Coverage thresholds hid the result because `mutation/` is offline-only tooling and the global 80% line gate absorbs one uncovered branch. | 8 tests covering the floor boundary in both directions, both live `post_choices` values, and a band whose floor is 1 rather than 2; crucially one test verified to FAIL if the call site in `_evaluate_graft` is removed, since a unit test of the helper alone passes against dead code. Generalised check: when a check goes in at the front of a precondition ladder, expect to update every fixture past it AND to write the check's own tests in the same commit, because those fixture updates guarantee nothing else reaches it. Conversely, do not read a fixture restructure as evasion without first checking whether a later rung of the same ladder is what the test was aiming at. | applied | `tests/unit/test_skeleton_mutation_operators_coverage.py` lines 1370-1471, incl. `test_evaluate_graft_honours_the_pl25_opening_reason` |
| AL-098 | 2026-08-06 | PR #635 review, the repo-wide ruff format gate | tooling | `ruff format --check .` fails on `origin/main`, and has for some time, on a Python fence inside `docs/planning/story-personalization-stage-c-implementation-plan.md`: ruff formats Python inside markdown code fences, so a planning document is a formattable artifact. Nothing reports it, because no gate runs the formatter over the WHOLE tree. The pre-commit `ruff-format` hook sees only STAGED files, so a doc-only commit never presents the fence to it, and Python CI is delegated to the org-level reusable workflow, which scopes its format check to the code directories. The frontend was assumed to have closed the identical gap already; it has not, and its gap is larger (`AL-100`). The cost is asymmetric: the command CONTRIBUTING tells a contributor to run (`uv run ruff format .`) is broader than any gate, so the first person to run it picks up an unrelated file in their diff and has to work out whether it is theirs. | Added a `format-tree` job running `uv tool run --no-build ruff@0.16.0 format --check .`, wired into `ci-gate` with NO skip allowance. The first draft of this row claimed the frontend had already closed the identical hole; checking that claim falsified it and produced `AL-100`, so this job is the FIRST tree-wide format gate in the repo, not a mirror of an existing one. The offending fence was NOT reformatted: it is a fragment shown at its real indentation inside a function body, and ruff's formatting of it dedents to column 0 and collapses the `await` call, turning in-function code into what reads as top-level code (verified by running the formatter on the fence in isolation). Retagged `python` to `text` instead, with prose stating the indentation is deliberate, so the fence is honestly declared an excerpt rather than silently mangled. Placement was moved out of the `contract` job a first pass had put it in, for two reasons: a format failure surfacing under the name "OpenAPI Contract Drift" makes a required check report something other than what it checked, and `contract` skips on merge_group and on release PRs, which would have narrowed the gate to a subset of runs. Generalised check: when a repo holds two language toolchains, a gap closed for one is a LIVE gap for the other until proven otherwise, and the thing to compare is SCOPE: if the gate runs over staged files or over code directories while the documented command runs over the tree, the difference is exactly the set of files that can drift with every gate green. Second check: before accepting a formatter's verdict on embedded code, ask whether the fence is a compilable unit or an excerpt, because the formatter cannot tell and will silently relocate an excerpt to column 0. | applied | `.github/workflows/ci.yml` `format-tree` job + `check_job` entry; `tests/unit/test_ci_gate_contract.py` `test_the_tree_wide_format_check_runs_in_every_context` / `test_a_skipped_format_check_fails_the_gate`, both verified to fail under a workflow mutation; tree now reports `859 files already formatted` |
| AL-099 | 2026-08-06 | PR #635 review, reshaping seed data renamed an e2e test id | tooling | Making the seed stories gate-clean (AL-096) restructured their graphs, and in doing so renamed the courage-gated choice `c_n_e2_carried` to `c_n_e2_courage`. That id is the load-bearing test id of the `carries_state` proof in `frontend/e2e-real/series-continue-real.spec.ts`, whose negative half asserts `toHaveCount(0)`. A count-0 assertion on a test id is satisfied both when the choice is correctly hidden AND when the id does not exist at all, so the rename would have turned the negative proof into a vacuous pass while the positive half failed loudly, i.e. green for the wrong reason on exactly the assertion that proves the feature. PL-25's floor also forbids the entry node from carrying choices, so the gated choice moved from the landing passage to a hub three clicks in, and book 2's own `brave` choice sets the same variable locally: walking to that hub via the brave fork would satisfy the gate with no carry at all, an AL-002 repeat. | Restored the `_carried` id with an inline comment recording that it is part of the test contract and not free to rename, and reworked both nightly specs to reach the hub via the PLAIN fork so local acquisition cannot masquerade as a carry, strengthening the negative assertion to also prove which node it is standing on. Generalised check: a `toHaveCount(0)` or `not.toBe(...)` assertion keyed on an identifier is only a proof while that identifier is guaranteed to exist; pair every absence assertion with a positive assertion that the observer is in the right place, or renaming the subject silently converts the test into a tautology. Corollary: reshaping a fixture that a gate forces you to reshape is not a free refactor, so treat every id in it as an interface until proven otherwise. | applied | `scripts/seed_dev_data.py` / `scripts/seed_series_catalog.py` `_carried` id restore; `frontend/e2e-real/series-continue-real.spec.ts` walk rework |
| AL-100 | 2026-08-06 | PR #635 review, closing the Python format gate exposed the same hole in the frontend | tooling | Writing AL-098's fix required a claim about the frontend, and checking it falsified it. The frontend's `format:check` is `prettier --check "src/**/*.{ts,tsx,css,json}"`, so it is NOT tree-wide: `e2e/`, `e2e-real/`, `eslint.config.js`, `index.html`, and `README.md` are all outside it, and `npx prettier --check .` under `frontend/` reports 130 unformatted files, 3 of them `e2e-real` specs this PR touches the directory of. So the repo has the SAME scope gap on both toolchains, and the frontend one is larger and has two loci: the pre-commit hook `frontend-prettier` is scoped to `^frontend/src/` as well, so staging an `e2e-real` spec reports "no files to check" and passes, which is how this commit's own frontend edits reached a commit unchecked. It stayed invisible for the same reason the Python one did: the gate's glob is narrower than the formatter's reach, and nobody runs the formatter over the tree because the documented command is the gate's command. | Deliberately NOT fixed here: widening the glob is a 130-file mechanical reformat, and bundling it into a validator rule change would bury the review (this PR is already large enough that CodeRabbit skipped it at the 100-file limit). Scheduled as `UW-C33` instead, with the `format-tree` job's comment stating explicitly that it does not cover the frontend, so the new Python gate cannot be misread as having closed both. Generalised check: when writing the justification for a fix, treat every comparative claim in it ("the other side already does this") as a claim to VERIFY, not as context. Here the claim was the reason the fix looked like a mirror of existing practice, and it was false; verifying it found a defect four times the size of the one being fixed. | open | `UW-C33` |
| AL-101 | 2026-08-06 | ADR-025 implementation plan Task 3 fix review (branch `feat/persistent-characters`) | tooling | `import_catalog._needs_legacy_normalization` carried its own independent copy of the schema-version acceptance rule instead of delegating to `Storybook._check_schema_version`/`is_supported_schema_version`, so the two enforcement points could silently disagree: a same-major document at an unsupported minor (or already past the accepted range) was misclassified as legacy, rewritten by `_normalize_legacy_fill`, and stamped back to `"2.0"` rather than rejected. Under ADR-025's accepted-range rule, a future minor such as `2.1`, or a `3.0` document, would have been silently normalized and admitted instead of parsed and rejected. This is a gate letting a defect through by rewriting the evidence it should have refused, and the contradiction survived a first plan self-review. The pre-fix check itself was a literal `!= "2.0"` comparison, not a same-major-only comparison, so it was equally broken for a higher major (for example `3.0`) as for a future minor; describing the defect as "same-major unsupported minor" understated its own scope, and that same understatement is what scoped the Task 3 fix too narrowly, leaving the higher-major and topology-absent branches unaddressed until a later final-review pass (C1) caught them. | Gave `_needs_legacy_normalization` its own explicitly narrower predicate (`parse_schema_version` directly, not `is_supported_schema_version`), so it answers only "is this pre-`SCHEMA_MAJOR`"; a same-major, out-of-range document now falls through to `run_gate` to be rejected loudly instead of silently normalized. General rule: when two enforcement points check the same version or format contract, either share one predicate or make each predicate's narrower scope explicit and tested, so a duplicate copy cannot silently disagree with the source of truth; and when describing a defect's scope, state the actual failing comparison (here, a literal string inequality with no major-awareness at all) rather than a narrower category that happens to cover the case in hand, since the narrower description can itself under-scope the fix. | applied | Fix in PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636) (`src/cyo_adventure/generation/import_catalog.py::_needs_legacy_normalization`), branch `feat/persistent-characters`: originally commits `42439204`/`0b32ddd6`, widened and completed by the C1 final-review fix in `db6de7db`, which covered the higher-major and topology-absent quadrants the original fix left open. Those SHAs are cited for archaeology only; the PR squash-merges, so none of them will exist on `main`. Squash-merged to `main` 2026-08-07 as `4fc65e5b` (PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636)); tracked by `UW-C34` in `unscheduled-work-register.md` |
| AL-102 | 2026-08-06 | ADR-025/character-vocabulary-and-CH-rules plan Task 3 (`accepts_character` on schema minor 1, branch `feat/persistent-characters`) | tooling | Adding `Storybook.accepts_character: dict[str, CharacterRange] \| None = None` (an additive, opt-in field with a `None` default) failed `tests/unit/test_diversity_panel.py::test_main_check_returns_zero_on_committed_tree`, an R5 finding on all 8 committed fill fixtures, even though none of them declares the new field and its structural shape is unchanged. Cause: `diversity/structure.py::structure_fingerprint` hashes `Storybook.model_dump(mode="json")`, and Pydantic v2 always serializes every field, including one left at its `None` default, so a brand-new key with the same value on every fixture still changes every hash uniformly. The task-3 brief's own Step 7 (run the full unit suite, not just the storybook/schema/validator-named suites) is what surfaced this; nothing in the brief or in ADR-025 named this coupling, and a diff of `--check`'s output confirmed all 8 findings were fingerprint-only (no R1-R4/R6 findings), so the graph shape genuinely had not changed. | Any additive `Storybook` field with a non-excluded default will change every committed diversity-panel fingerprint the same way, the day it ships, regardless of whether real stories use it yet. Run `uv run python scripts/run_diversity_eval.py --check` (or the full `tests/unit` suite) after any `Storybook` schema-minor bump and regenerate `tests/data/diversity_panel/baseline.json` via `--update-baseline` in the same commit when the only findings are R5 fingerprint drift with zero R1-R4/R6 findings; treat any non-fingerprint finding in that same run as a real regression, not as more of the same. | applied | Followed in PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636) (`src/cyo_adventure/storybook/models.py`, `tests/data/diversity_panel/baseline.json`), branch `feat/persistent-characters`, commit `d623c15e` (cited for archaeology only; the PR squash-merges, so this SHA will not survive onto `main`): `baseline.json` regenerated via `scripts/run_diversity_eval.py --update-baseline` in that commit, confirmed by `--check` returning `findings=0` and by the diff touching only the 8 `fingerprint` values. It was unmerged when this row was first written, which is why the status was `open` at that point; tracked by `UW-C35` in `unscheduled-work-register.md`. The status is now `applied`, on the strength of the squash SHA that follows: Squash-merged to `main` 2026-08-07 as `4fc65e5b` (PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636)) |
| AL-103 | 2026-08-06 | ADR-025/character-vocabulary-and-CH-rules plan Task 3, split from AL-102's third clause (branch `feat/persistent-characters`) | tooling | AL-102 shows that any additive `Storybook` field left at a non-excluded default moves every committed diversity-panel `structure_fingerprint` uniformly the moment it ships, since `Storybook.model_dump(mode="json")` always serializes every field regardless of whether it was set. `structure_fingerprint` has no notion of "still at schema default", so it cannot distinguish a fill that actually used a new field from one that merely carries it at its default. | Longer-term, `diversity/structure.py::structure_fingerprint` could exclude fields still at their schema default from the hashed representation, so an unused new field cannot move the hash at all. This is a diversity-module design change, out of ADR-025 Task 3's scope; it needs its own design pass to confirm it cannot also mask a genuine structural change that happens to equal a default value. | open | |
| AL-104 | 2026-08-06 | L1-8 field-minor floor review fix wave (branch `feat/persistent-characters`), established by mutating `gate.py` and re-running the L1-8 tests | validator | `validator/gate.py::run_gate`'s `blocked` prefix tuple `("L1", "L2", "PL")` is unreachable for Layer 1: removing `"L1"` from the tuple and re-running the L1-8 suite left every test passing, because `run_gate` early-returns `blocked=True` whenever `l1_report.ok` is `False` (any Layer-1 ERROR of any rule id), long before execution reaches the prefix-tuple computation. So for every Layer-1 rule, that tuple entry is dead code, and a test asserting `result.blocked is True` on a Layer-1 ERROR proves the outcome but not the mechanism. The contrast is the useful half: there is no equivalent short-circuit after the parse, so a rule running after Layer 2 or Policy reaches the final `blocked = any(...)` computation and depends entirely on its prefix being in that tuple. | Add a test that pins the mechanism, not just the outcome, for Layer 1: assert directly that a Layer-1 ERROR short-circuits before the prefix tuple is ever consulted (for example, by asserting on the early-return path itself), or restructure so Layer-1 blocking is computed through the same prefix-tuple path as Layer 2/PL, removing the dead special case. This is a finding from a fix-wave review, not a shipped change; do not change `gate.py` to close it without its own scoped review. | open | `src/cyo_adventure/validator/gate.py:113-120` (early return) and `:160-164` (prefix-tuple computation); no PR yet, tracked by `UW-C37` in `unscheduled-work-register.md` |
| AL-105 | 2026-08-06 | Task-5 CH-rule review fix wave (branch `feat/persistent-characters`), Finding 0 from a controller-supplied review (not in the review file itself) | validator | The task-5 design spec (`docs/superpowers/specs/2026-08-05-persistent-reader-characters-design.md` section 5.1) and the shipped `CH-6` both stated only the opt-out half: "a book that does not declare `accepts_character` may not declare a variable whose name is in the canonical vocabulary". Neither the spec nor the code quantified over the opt-in side: a book that opts in and covers only some canonical names in its envelope could still declare a second canonical-named variable outside that envelope, pass `CH-1` cleanly (`CH-1` only ever walks envelope -> variable, never the converse), and be seeded by G3's name-match carry over states this book's own Layer 2 walk never proved for that variable. This is a spec gap, not an implementation gap: the shipped code faithfully implemented the spec's narrow statement, and the spec itself needed the wider one. | Broadened `CH-6`'s statement in both the spec and `docs/planning/validator-rules.md` to the full bidirectional form: a canonical name may be declared only by a book that opted in AND covered it in the envelope. Implemented as a second half (`_check_ch6_uncovered_canonical_names`) under the same rule id, with its own distinct message, called only on the opt-in branch; the opt-out half is unchanged. Catalog scan (`grep` across `skeletons/` and `tests/fixtures/`) confirms zero existing stories declare a canonical name outside an opted-in envelope, so the broadened check introduces no regression against the committed catalog. General rule: when a bidirectional correspondence rule (CH-1 walks direction A) needs a converse check (direction B), write both halves before calling the correspondence "closed", even when only one half was requested; a rule stated in only one direction reads as complete while leaving the other direction's callers unprotected. | open | `src/cyo_adventure/validator/character.py::_check_ch6_uncovered_canonical_names`, `tests/unit/test_character_rules.py::test_ch6_rejects_an_uncovered_canonical_name_in_an_opted_in_book`; no PR yet (unmerged, branch `feat/persistent-characters`), tracked by `UW-C38` in `unscheduled-work-register.md` |
| AL-106 | 2026-08-06 | Task 6 implementation (CH-3a/CH-3b/CH-4 walk-based envelope rules, branch `feat/persistent-characters`) | validator | The task-6 brief's literal example fixtures used a stale schema shape carried over from an earlier draft of the format: `Choice.text` instead of `Choice.label`, a JSONLogic-style `Effect` (`{"var": ..., "value": ...}` without the required `op`) instead of the enforced `{"op": "set", "var": ..., "value": ...}`, `ending.type` instead of `ending.kind`, and a `"resolution"` ending kind and a `"satisfying"` valence, neither of which exists in `storybook/models.py::EndingKind`/`Valence` (the real values are `success/setback/death/capture/completion/discovery` and `positive/neutral/negative`). Every one of these would have failed `Storybook.model_validate` immediately under `extra="forbid"` and the model's own field validators, so writing the brief's fixtures verbatim would have cost a full test-writing pass before the first failure surfaced. Caught before any fixture was written by reading `storybook/models.py` directly and cross-checking against a real passing fixture (`tests/fixtures/storybook/valid/03_tier2_lantern.json`) rather than trusting the brief's inline JSON. | Any authoring brief or spec that inlines example Storybook JSON should be treated as illustrative, not literal; before writing a fixture from a brief, diff its field names and enum values against `storybook/models.py`'s current field definitions and a real passing fixture, since a brief can predate a schema change (this project's `ending.type`-to-`ending.kind` rename was already fixed in `.claude/skills/cyo-author/reference/skeleton-format.md` well before this task; root `CLAUDE.md` wrongly asserted that file was still stale, a false claim since corrected and removed per `AL-128`/`UW-C63`, and the task-6 brief's stale fixtures were an independent inheritance from an older draft of the format rather than evidence of any real staleness in that file). Consider a standing lint or generator that keeps brief/spec example JSON in sync with the live schema, so this class of drift is caught once centrally instead of once per brief. | open | `tests/unit/test_character_rules.py` (the six new CH-3a/CH-3b/CH-4 fixtures, all schema-valid against the live model); tracked by `UW-C39` in `unscheduled-work-register.md` |
| AL-107 | 2026-08-06 | Task 6 implementation (CH-3b per-state regression signature, branch `feat/persistent-characters`) | validator | The task-6 brief specified CH-3b's baseline-diff signature as the literal tuple `rule_id \| node_id \| choice_id`. None of L2-9 (dead end), L2-10 (trapped), or L2-14 (all-fatal decision) ever populate `choice_id` on their findings, so that signature collapses every finding for a given rule on a given node to one bucket regardless of which branch or variable state produced it. A falsification test (two structurally distinct dead-branch defects on the same node, reachable only in different envelope states) proved the brief's literal signature could not distinguish them: weakening the shipped signature back to the brief's exact form made `test_ch3b_distinguishes_two_dead_branches_on_one_node` fail immediately, then restoring it made the suite pass again. The fix extends the signature with the finding's `message` (which embeds the differing `var_state` for these three rule ids), deliberately diverging from `series.py::_l2_error_signatures`'s existing signature helper, whose own docstring explains the opposite choice is correct for its own caller (SR-9 continuation-entry diffing, where message text is expected to vary harmlessly run to run). A task-6-review.md correction on the residual this widened signature leaves behind: an earlier draft of this record described the risk as a coincidental message collision suppressing an unrelated finding, but widening a signature can only narrow which findings match, so it can only suppress less than the brief's literal three-part key, never more; a coincidental collision was never the direction this change could fail in. The real residual is the opposite kind of noise: because the signature embeds the raising state's own values, a baseline defect is suppressed only for the state that exactly reproduces it, so a structurally identical defect at a different entry state re-reports rather than staying suppressed (harmless, since any such ERROR already blocks the gate through the "L2" prefix, but worth naming correctly). | When a brief specifies a de-duplication or diff signature as a literal field tuple, verify it against the actual fields the compared findings populate before implementing it verbatim, and prove the choice by falsification (deliberately weaken the signature, confirm a test that should fail does fail, then restore it) rather than by inspection alone. Where two signature helpers in the same codebase make opposite choices about including free-text `message` in a signature, document why in both docstrings so neither reads as an oversight of the other. | open | `src/cyo_adventure/validator/character.py::_signature` (contrasted explicitly against `src/cyo_adventure/validator/series.py::_l2_error_signatures` in its own docstring); falsification recorded in this session's verification log; commit in this branch's Task 6 series, `feat/persistent-characters`. Unmerged as of 2026-08-06, so the status stays `open` rather than `applied` until the squash SHA lands; tracked by `UW-C40` in `unscheduled-work-register.md` |
| AL-108 | 2026-08-06 | Task 7 implementation (CH-8 build-node cost pre-flight, branch `feat/persistent-characters`) | validator | `validator/character.py`'s module docstring described a "future" CH rule the module does not implement, and named `docs/planning/validator-rules.md`'s RESERVED row as the source of truth, explicitly to keep the id out of this file's own prose so the catalog lockstep guard (`tests/unit/test_validator_rules_catalog.py`) would not mistake a mention for a shipped rule. That guard only ever scans for rule-id tokens; it has no notion of whether prose elsewhere in the same module still describes an id as reserved or unimplemented after the id ships. Implementing CH-8 and replacing the catalog's RESERVED row left the module's own docstring stale (still saying a future rule is held in reserve) with nothing failing: `test_every_enforced_rule_id_is_documented` and `test_a_catalogued_rule_with_no_code_declares_why` both passed throughout, because neither reads module-docstring prose, only the catalog table and which ids a module emits. Caught only by re-reading the docstring before committing, not by any gate. | No test or lint currently checks that a module's own prose about which rules are "reserved" or "not yet implemented" stays in sync with which rules it actually emits; that drift is invisible to `test_validator_rules_catalog.py` by design (it deliberately avoids parsing prose, to stay robust against wording changes). When a RESERVED catalog row is replaced with a real rule, always check the implementing module's own docstring and any function docstring that references the reservation, not only the catalog table; consider extending the lockstep guard to flag literal phrases like "not implemented" or "RESERVED" appearing adjacent to a rule id the module's own code does emit, so this class of drift fails loudly instead of depending on a reviewer's re-read. | open | `src/cyo_adventure/validator/character.py`, `docs/planning/validator-rules.md`; tracked by `UW-C41` in `unscheduled-work-register.md` |
| AL-109 | 2026-08-06 | Task 7 review fix (CH-8 arity Critical, branch `feat/persistent-characters`) | validator | CH-8's original implementation computed `arity = span.max - span.min` from the `accepts_character` envelope's own declared bounds, which are schema-valid, attacker-influenced values from a generation-pipeline artifact: nothing upstream ties either bound to the canonical archetype count (CH-1 checks only the variable's type against the vocabulary; CH-2 checks only that the envelope equals the declared variable, never that the declared variable equals the canonical `0..6`). A book could declare both its `archetype` variable and its envelope span as `1..6`, schema-valid and passing CH-1/CH-2 cleanly, while an in-story build node still set any of the same six real archetype values a full `0..6` declaration would allow; reading the narrower span computed arity 5 instead of 6, moving the rule's own threshold from 16,666 to 20,000 configurations and letting a real 19,236-config book (`the-flooded-quarter`) through with zero findings from any rule, with `run_gate` returning `blocked=False`. The general lesson: a threshold derived from a document-controlled value is not a guard, even when that value is schema-valid and passes every check written so far; validating a value's *shape* (min <= max, correct type) is not the same as validating that it *means* what the rule's arithmetic assumes it means. | Derive `arity` from the canonical vocabulary (`len(ARCHETYPE_ROSTER)`, a module-level `Final` tuple the caller cannot influence) rather than from the declared span, and add a regression test that reproduces the exact bypass (a `1..6` declared span on `the-flooded-quarter`) so the formula's soundness is pinned, not just its arithmetic. When any future rule derives a threshold or multiplier from a declared, attacker-influenced field, check first whether a canonical, code-owned constant already carries the true value; prefer that constant over the declared field even when the two are expected to agree, because "expected to agree" is exactly the gap CH-2's range-equality check does not close (it enforces internal consistency between two declared values, never consistency with the canonical count). | open | `src/cyo_adventure/validator/character.py::_check_ch8_build_node_cost`, `src/cyo_adventure/storybook/character_vocabulary.py` (`ARCHETYPE_VARIABLE_NAME`), `tests/unit/test_character_rules.py::test_ch8_rejects_a_narrow_declared_span_that_still_means_six_archetypes`; no PR yet (unmerged, branch `feat/persistent-characters`), tracked by `UW-C42` in `unscheduled-work-register.md` |
| AL-110 | 2026-08-06 | Task 8 record (rule-catalog regex family list, branch `feat/persistent-characters`) | tooling | `_RULE_ID_RE` in `tests/unit/test_validator_rules_catalog.py` is a hand-maintained family list; this branch had to add `CH` and an optional `[ab]?` suffix to it for `CH-3a` to be recognized at all (`\d+\b` cannot match `CH-3a`, since there is no word boundary between `3` and `a`), landed in commit `a3839695`. The module's own comment history records this as the third occurrence of the identical drift: `SR` and `CG` both went undocumented for the same reason before joining the list. Fixing the immediate symptom does not fix the mechanism: the list is still hand-maintained, so a fourth rule family will recreate the same drift and again render its ids invisible to both lockstep tests (`test_every_enforced_rule_id_is_documented`, `test_a_catalogued_rule_with_no_code_declares_why`) until someone notices independently. | Make the regex's family list derive from the validator package's rule ids rather than being hand-maintained, for example by scanning `validator/*.py` module names or a registered rule-id prefix set at import time, so adding a family cannot be forgotten. | open | `tests/unit/test_validator_rules_catalog.py::_RULE_ID_RE` (widened for `CH` in commit `a3839695`, the widening itself still hand-maintained); tracked by `UW-C43` in `unscheduled-work-register.md` |
| AL-111 | 2026-08-06 | Task 8 record (`SR-8` duplicate catalog rows, branch `feat/persistent-characters`) | docs | `docs/planning/validator-rules.md` carries two contradictory `SR-8` rows: one (near line 216) documents `SR-8` as a real, implemented carried-variable-integrity rule added 2026-07-25 with its own failure-message template, and another (near line 242, inside a duplicated "Series: Cross-Book Chain Rules" section) still marks it "RESERVED, not implemented here... Claimed by the open PR for the authoring-lessons workstream (#416)." PR #416 merged 2026-07-28 (`gh pr view 416 --json state,mergedAt` returns `MERGED`/`2026-07-28T02:26:53Z`), over a week before this record, so the reservation row is stale on its own terms. Neither lockstep test can see the contradiction: `test_every_enforced_rule_id_is_documented` only checks that `SR-8` appears somewhere in the catalog text, and `test_a_catalogued_rule_with_no_code_declares_why` only inspects `_ids_in(catalog_text) - _code_rule_ids()`; because `SR-8` is emitted in code (`series.py`), it is a member of `_code_rule_ids()` and is excluded from that difference entirely, so neither test ever reads its catalog text for internal consistency. | Add a duplicate-id assertion to `test_validator_rules_catalog.py` that fails when the same rule id opens more than one catalog row (the `\| <id> \|` prefix), then reconcile the two `SR-8` rows: keep the real, implemented definition and remove the stale RESERVED row citing the now-merged PR #416. | open | `docs/planning/validator-rules.md` (rows near line 216 and line 242); `tests/unit/test_validator_rules_catalog.py::test_every_enforced_rule_id_is_documented`, `::test_a_catalogued_rule_with_no_code_declares_why`; `gh pr view 416` (merged 2026-07-28); tracked by `UW-C44` in `unscheduled-work-register.md` |
| AL-112 | 2026-08-06 | Task 8 record (task-6 review findings on CH-3a/CH-3b/CH-4 walk enumeration, branch `feat/persistent-characters`) | validator | PR #636's task-6 review found three real behavioral defects the full green test suite had passed over, all in `character.py`'s walk-enumeration path and all fixed in commit `4000d28d`: a Critical (unbounded Cartesian-product enumeration via `envelope_states`, `character.py:88-89`, where an attacker-influenced envelope could drive a `MemoryError` rather than a controlled rejection), and two Important findings sharing one root cause (`L2-12` excluded from `_PER_STATE_RULES`, `character.py:465`), so a capped walk silently read clean for CH-3b (fail-open on the exact signal CH-3b exists to catch) while CH-3a's own union check read the same capped, non-exhaustive walk as ground truth (fail-open the same direction, for the opposite reason). All three were invisible to the pre-fix test suite because no fixture exercised an envelope large enough to hit the cap or the allocation ceiling; they surfaced only through a dedicated review pass, not through coverage percentage or CI. | When a walk-based rule enumerates envelope states, treat "does the enumeration ever hit its own declared cap" as a first-class test case for every rule that reads the walk, not only the rule whose job is capping it; a rule reading someone else's capped result inherits that result's fail-open or fail-closed direction and needs its own explicit test naming which direction it takes. | open | PR #636 (task-6 review C1/I2/I3, and its re-review verifying the fix, APPROVED); `src/cyo_adventure/validator/character.py` (fix commit `4000d28d`, unmerged branch `feat/persistent-characters`); `tests/unit/test_character_rules.py::test_ch_walk_rules_skip_an_oversized_envelope_instead_of_enumerating_it`, `::test_ch3a_stays_silent_when_a_walk_caps`, `::test_ch3b_reports_a_capped_per_state_walk_instead_of_passing_it_clean`; tracked by `UW-C45` in `unscheduled-work-register.md` |
| AL-113 | 2026-08-06 | Task 8 record (mutation testing boundary gaps across task 6 and task 7, branch `feat/persistent-characters`) | process | Mutation testing on this branch repeatedly caught assertions that read stronger than they were, in a recurring small set of shapes, across two independent reviews: PR #636's task-6 review I4 (a `>= 1` count assertion defeated by an `if False:` mutation that dropped the true finding count from 4 to 2 without failing) and PR #636's task-7 review I1 (`build_node_headroom`'s `<=` decision boundary survives being mutated to `<`, because none of the three catalog books sit at, one below, or one above the 16,666-config threshold), I2 (the capped-walk fail-closed branch's `return False` survives being mutated to `return True`, because no catalog book ever caps), and I3 (the implementer's own reported mutation moved `_WALK_CAP` 10,000x past every catalog measurement, proving only that the constant is read, not that the comparison built on it is correct). All four were fixed with boundary-adjacent or direction-naming pinning tests, independently re-mutated and re-verified in PR #636's task-7 re-review. The common shape: a mutation chosen far from the real decision boundary, or a fixture set that never reaches an edge case, produces a passing mutation-kill report that certifies nothing about the boundary or the untested branch. | When writing a mutation-testing pass for a threshold or a boolean-branch guard, deliberately choose the mutated value or branch closest to the real boundary (an off-by-one on a comparison, the flipped return on an unexercised branch) rather than an arbitrary large change, and require at least one fixture that lands exactly on the boundary so the pinning test fails for the right reason instead of an unrelated one. | open | PR #636 (task-6 review I4; task-7 review I1/I2/I3; task-7 re-review independently re-mutating I1/I2, both confirmed); `src/cyo_adventure/validator/character.py::build_node_headroom`; `tests/unit/test_character_rules.py::test_ch3b_distinguishes_two_dead_branches_on_one_node`, `::test_build_node_headroom_boundary_pins_the_inclusive_comparison`, `::test_build_node_headroom_is_fail_closed_on_a_capped_walk`; tracked by `UW-C46` in `unscheduled-work-register.md` |
| AL-114 | 2026-08-06 | Task 8 record (upstream merge id collision, branch `feat/persistent-characters`) | tooling | Merging upstream main (`463f87f6`, bringing in `a12bc470`/v0.67.0) into this branch collided on every append-only sequential id this branch had independently allocated: `AL-080` through `AL-086` and `UW-C26` through `UW-C32` were already claimed by upstream's own new rows for the same id numbers, because both branches allocated from the same "next free id" starting point without either knowing about the other's in-flight work. The merge commit's own message documents the full renumbering resolution: this branch's rows were renumbered above upstream's true maximum rather than resolved by keeping one side. A `--ours` or `--theirs` conflict resolution would have silently picked one branch's rows and discarded the other's, deleting real lessons or work-items and leaving any citation of the discarded ids (a `Ref` or `UW-C*` cross-reference) pointing at a row that no longer exists, with every existing checker (`check_lessons_log.py`, `check_work_linkage.py`) still passing on the surviving, internally-consistent half. | For append-only sequential-id registers edited by more than one long-lived branch, either allocate ids from a source both branches read at merge time (a shared counter, or ids scoped by branch or date prefix) instead of a purely sequential hand-incremented scheme, or, failing that, always resolve such a conflict by renumbering the incoming branch's rows above the target's true current maximum (never `--ours`/`--theirs`), and add a check that a resolved merge introduces no two rows sharing one id. | open | Merge commit `463f87f6` ("merge: bring main (v0.67.0, a12bc470) into feat/persistent-characters"), full renumbering resolution recorded in its own commit message; branch `feat/persistent-characters`, squash-merged to `main` 2026-08-07 as `4fc65e5b` (PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636)), so that merge commit exists only in branch history and is cited for archaeology. Stays `open` because the proposed change (a check that a resolved merge introduces no two rows sharing one id) has not shipped; tracked by `UW-C47` in `unscheduled-work-register.md` |
| AL-115 | 2026-08-06 | PR #636 review (C1), `docs/superpowers/specs/` | tooling | A design spec added under `docs/` used 13 relative markdown links into the source tree (`../../../src/cyo_adventure/...`). MkDocs `validation.links.not_found` defaults to `warn`, and `mkdocs.yml` sets `strict: true`, which promotes every one of those warnings to an abort: `Aborted with 13 warnings in strict mode`. The failure is invisible locally because nothing in `.pre-commit-config.yaml` builds the docs, and `docs.yml` runs only on `paths: docs/**`, so the author sees a clean commit and a red CI job. The near-miss that makes this worth logging: the obvious fix, rewriting them as absolute `blob/main` URLs, would have passed mkdocs while handing lychee 13 new live targets that rot as line numbers move. Two link checkers with opposite preferences, and satisfying one is how you break the other. | Reference source files from docs prose as plain code spans, never as relative links out of the docs tree, matching the dominant `docs/planning/` convention. Add a cheap pre-commit check that fails on a markdown link from `docs/**` resolving outside `docs/`, so the abort is caught at commit time rather than in CI, and record the absolute-URL alternative as rejected so it is not re-proposed. | open | |
| AL-116 | 2026-08-06 | PR #636 review (I12), `skeletons/` catalog measurements | process | Two documents in the same PR cited different totals for the same directory, and both were internally defensible: the spec's §2.1 said "Skeletons total \| 124" and ADR-025 said "all 61 committed `skeletons/*/*.json` files". `skeletons/*/*.json` really does match 124 files, but 47 are `.contract.json` and 16 are `.lineage.json` sidecars on their own versioning schemes; only 61 are skeleton documents. The glob was cited as if it were the artifact count. The consequence was not cosmetic: every other row of that measurement table is per-skeleton, so the wrong denominator halved the apparent backfill population from 23% to 11%. It also cost real time during review, because a finding was raised, retracted against the raw glob count, then reinstated once the sidecars were classified. | When a doc quotes a catalog measurement, quote the artifact count and name the filter that produced it, never a bare glob match. Add a small `scripts/` helper that reports skeleton counts (total, by band, with/without `variables`) so both ADRs and specs cite one computed source instead of each re-globbing; sidecar suffixes are the filter it encodes. | open | |
| AL-117 | 2026-08-06 | PR #636 review (I7), `validator/gate.py::_parse_storybook` | tooling | The handler for "L1-1 passed but Pydantic still rejected the document" carried an `#EDGE` marker asserting the path "should not occur in practice because L1-1 validates against the schema exported from the same Pydantic models", and a `#VERIFY` instructing the reader to keep `schema_export.build_schema()` in sync with `models.py`. Both were false, and the same PR falsified them: `Storybook`'s checks are `model_validator(mode="after")`, which JSON Schema cannot express at all, so `schema/storybook.schema.json` types `schema_version` as a bare `{"type": "string"}` and every unsupported version clears L1-1 and lands here. The prescribed remedy could never work, because no amount of syncing makes JSON Schema carry an after-validator. The finding message then blamed "(schema drift?)", so the report misdirected the reader too. The marker was written when the claim was true and rotted when the validator layer grew. | A RAD marker asserting a path is unreachable must name the mechanism that makes it unreachable, and the `#VERIFY` must be an action that could actually detect the claim going false. When adding a `model_validator(mode="after")`, check for existing markers that assume schema-export equivalence, since each new after-validator falsifies them a little more. Cover the reachable path with an unmocked test: the two existing tests patched `model_validate` to fake the failure, which can prove the handler runs but never when it runs. | open | |
| AL-118 | 2026-08-06 | PR #636 review fixes; register merge against `main` after PR #635 landed | tooling | Two branches forked from a `main` whose maxima were `AL-079` and `UW-C25`, and both appended rows starting at `AL-080`/`UW-C26` for entirely unrelated content. The one that merged first took the ids, so the second branch's 8 rows all collided. The trap is that this presents as an ordinary text conflict, and the two obvious resolutions are both silently destructive: `--ours` or `--theirs` each DELETE four legitimate rows, and afterwards the merge is clean, `check_lessons_log.py` exits 0 and `check_work_linkage.py` exits 0, because a row that no longer exists breaks no linkage. Nothing in the toolchain can tell a resolved collision from a resolved deletion. The blast radius also extends past the two register files: four external citations (the design spec twice, ADR-025, and a source comment naming the defect class) pointed at ids whose meaning had changed under them. | Treat an append-only register id as claimed at MERGE time, not at write time. Add a `--check-duplicates` pass asserting every `AL-*` and `UW-*` id appears exactly once and that ids are contiguous, so a `--ours` resolution fails a gate instead of passing one; today both checkers validate linkage between rows that exist and are blind to a row that stopped existing. When renumbering, sweep the whole repo for the old ids rather than only the two register files. | open | |
| AL-119 | 2026-08-06 | PR #636 review fixes, the character design spec front matter | tooling | The spec's front matter said `status: draft` while the banner one line below said "Approved design". Fixing it to `accepted` failed the `validate-front-matter` hook, because that validator scopes its allowed `status` vocabulary by `schema_type`: a `common` document admits only `draft`, `in-review`, `published`, while `accepted` belongs to the ADR schema. Surveying the repo (`grep '^status:' docs/`) actively misleads here: it returns 26 uses of `accepted`, every one of them an ADR, which reads as strong convention for a value that is illegal in the document being edited. A repo-wide count answered a schema-scoped question. | When a gate's vocabulary is keyed on a discriminator field, read the schema for THAT discriminator rather than counting values across the corpus. Proposed change: have `check_docs_frontmatter.py` name the `schema_type` it validated against in its error message (it currently prints `common/status: Input should be ...`, which is close, but the connection to the document's own `schema_type` line is left to the reader). | open | |
| AL-120 | 2026-08-06 | PR #636 final whole-branch review, fix I-1 (branch `feat/persistent-characters`) | tooling | `AL-102` predicted that an additive `Storybook` field moves every stored `structure_fingerprint`, but that prediction had only ever been applied to the 8 diversity-panel fixtures. The same rotation hit the 45 committed `out/ws2/*/fingerprint-manifest.json` bundles, and classifying them empirically (recomputing each digest at the merge base `a12bc470` with that commit's own code, not by simulating the old shape) split them three ways: 19 matched the pre-branch value and were genuinely rotated by this branch, 26 matched neither the pre-branch nor the current value and were therefore already stale before the branch existed, and 0 were current. Nothing runs `scripts/check_theme_contract.py --fingerprint-manifest` in CI, so those 26 drifted with no signal at all. The trap is the obvious remedy: regenerating all 45 would have gone green, laundered pre-existing drift into this PR, and destroyed the only evidence that the checker was never being run. | Shipped here: a literal-digest pin (`tests/unit/test_diversity_structure.py::test_structure_fingerprint_is_pinned_to_a_literal_digest`, paired with a test proving the pin actually moves when a field is added) and a `Note:` on `structure_fingerprint` naming every artifact that stores a digest, so the next additive field arrives with the blast radius written down. Still open (`UW-C55`): run the manifest comparison in CI, and triage the 26 stale bundles separately. General rule: before regenerating a committed derived artifact, classify each one against its pre-change value; a wholesale regenerate cannot tell "my change moved this" from "this was already broken", and green looks identical either way. | applied | Squash-merged to `main` 2026-08-07 as `4fc65e5b` (PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636)); `tests/unit/test_diversity_structure.py::test_structure_fingerprint_is_pinned_to_a_literal_digest`; residual tracked by `UW-C55` |
| AL-121 | 2026-08-06 | PR #636 final whole-branch review, fix I-2 (branch `feat/persistent-characters`) | validator | `validator/character.py` skipped the expensive CH-3a/CH-3b/CH-4 walk block on `envelope_size(envelope) <= MAX_ENTRY_STATES`, a condition written to mirror CH-5. That names the rule rather than the outcome, so any other CH rule that had already rejected the book still paid for a per-entry-state Layer 2 walk whose findings could not change the verdict. CH-8 is the worst case, because it fires precisely on the books that are expensive to walk: `longwinter-station` with a 7-state archetype envelope took 12.22s before and 0.98s after. The two conditions are not redundant and both were kept: the size check is the memory guard and must survive independently of CH-5's severity, while the error check is the time guard and covers the rules that decide rejection without saying anything about envelope size. | Shipped here as `and not report.errors` in the dispatcher's walk-skip condition, pinned by a test that monkeypatches `_WALK_CAP` to force CH-8 and asserts CH-3a, CH-3b and CH-4 are all absent (it fails without the new clause). General rule: key an early-exit guard on the decision already made ("this artifact is rejected") rather than on the particular rule that made it, or the guard silently narrows to that one rule while every sibling pays full cost. Note the coupling the fix creates: the skip is sound only because `"CH"` is in `gate.py`'s blocked-prefix tuple, which ADR-028's integrity table now records as condition 6. Sweep tracked by `UW-C56`. | applied | Squash-merged to `main` 2026-08-07 as `4fc65e5b` (PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636)); `src/cyo_adventure/validator/character.py::validate_character`; residual sweep tracked by `UW-C56` |
| AL-122 | 2026-08-06 | PR #636, writing ADR-028's integrity posture (branch `feat/persistent-characters`) | validator | CH-2 required the `accepts_character` span and the declared variable bounds to be **equal**, which reads like a bound on the envelope but is not one: both operands come from the same document, so `might: 0-9` declared consistently on both sides passes with zero findings and the canonical 0-2 vocabulary is silently redefined per book. The canonical constants were the only code-owned operand available and were read by no production code at all: every use of `CANONICAL_CHARACTER_VARIABLES` took its names or its `.type`, never its `.min`/`.max`. A widened range does not break the runtime clamp (G3 clamps to declared bounds, which CH-2 forces equal to the walked set), so the defect surfaces elsewhere: CH-8's cost pre-flight computes `arity` from the roster, and a book that widened its own range would be costed against the wrong arity. | Add a containment leg to CH-2 (`canonical.min <= span.min and span.max <= canonical.max`, narrowing allowed, widening rejected) and pin the canonical bounds as production-read with a boundary test at exactly the canonical max, not a far-away constant mutation that only proves readback. More generally: when writing an integrity rule, check whether every operand it compares comes from the artifact being validated; if so, the rule constrains internal consistency and not the artifact. | applied | `src/cyo_adventure/validator/character.py` `_check_ch2_range_equality`; tests `tests/unit/test_character_rules.py::test_ch2_rejects_a_range_wider_than_the_canonical_vocabulary`; ADR-028 conditions table; Squash-merged to `main` 2026-08-07 as `4fc65e5b` (PR [#636](https://github.com/ByronWilliamsCPA/cyo-adventure/pull/636)) |
| AL-123 | 2026-08-07 | Task 3 of the persistent-characters runtime plan (branch `feat/persistent-characters-runtime`), adding the `character_name` personalization slot | tooling | Two hand-maintained test-side traps surfaced together while wiring a twelfth personalization slot through code that already carried drift guards for the first eleven. First, `tests/unit/test_personalization_vocab_drift.py`'s `_newest_slot_type_check_migration()` helper resolves "the newest migration touching the `slot_type` CHECK" by filename, and two other tests (`test_split_migration_sweeps_the_consent_scope_too`, `test_split_migration_only_names_tables_that_a_migration_creates`) were built on top of it to assert facts specific to one historical migration (Task D6's flat-`favorite` split). Those tests passed for months only because D6 happened to also be the newest migration touching that CHECK; adding this task's migration, itself newer and touching the same CHECK for an unrelated additive reason, silently mis-targeted both tests at the wrong file, and the failure read as a plausible but misleading D6-specific assertion failure rather than naming the real cause (a resolve-by-recency helper reused for a resolve-by-history-event query). Second, `tests/unit/test_theme_contract.py::test_personalization_fields_vocabulary_is_closed` carried its own hardcoded 11/2-entry frozenset copy of `PERSONALIZATION_FIELDS`/`REAL_PERSON_PERSONALIZATION_FIELDS`, a fourth hand-maintained mirror of the same vocabulary (after `theme_contract.py` itself, `db/models.py`'s two `_PERSONALIZATION_*_SLOT_TYPE_VALUES` strings, and `api/schemas.py`'s Literal), not found by the earlier session's audit of "known" mirrors and not caught by any existing drift test, since that drift test only compares the DB-side copies against the source of truth, not this test file's own copy. | Give `_newest_slot_type_check_migration()`-style "resolve by recency" helpers a doc-comment warning against reuse for a historical-event-specific assertion, and prefer a content-keyed resolver (matched on a literal string unique to the event, as `_split_favorite_migration()` now does) whenever a test's claim is about a specific past migration rather than about the vocabulary's current state. Extend `test_personalization_vocab_drift.py`'s scope (or add a sibling test) to also diff `test_theme_contract.py`'s hardcoded set against the source of truth, so a fifth hand-maintained mirror cannot recur invisibly the way this fourth one did. | open | `tests/unit/test_personalization_vocab_drift.py::_split_favorite_migration`, `::test_split_migration_sweeps_the_consent_scope_too`, `::test_split_migration_only_names_tables_that_a_migration_creates`; `tests/unit/test_theme_contract.py::test_personalization_fields_vocabulary_is_closed`; branch `feat/persistent-characters-runtime`, unmerged as of 2026-08-07; tracked by `UW-C58` in `unscheduled-work-register.md` |
| AL-124 | 2026-08-08 | The D-2 RAD-citation checker's delivery and Task 9's re-review, persistent-characters runtime plan (branch `feat/persistent-characters-runtime`) | tooling | Checking that a `#VERIFY` citation names a test that exists is the automatable half of the problem, and it is not the half that matters. This workstream shipped nine non-discriminating or fabricated `#VERIFY` citations before `scripts/check_rad_citations.py` existed; the new checker resolves and grandfathers all nine into `rad-citation-baseline.toml` without complaint, because every one of them names a test that is real, passes, and proves nothing about the claim it is attached to. The ninth is the clearest case: `Reader.tsx`'s flowed-and-seeded branch cited `stops.test.ts`, which proves `stops.ts` forwards a parameter it is handed, never that `Reader.tsx` hands it one; deleting `seed` from that branch left all scoped tests green. A green `check_rad_citations.py` run must never be read as "the citations are sound", only as "the citations are not fabricated"; whether a cited test would actually fail if the tagged assumption broke is a review-time judgment call this or any mechanical checker cannot make. | Add a line to the RAD section of root `CLAUDE.md` and to `docs/response-aware-development.md` stating plainly that citation-existence checking is necessary but not sufficient, and that a reviewer must ask "would this test fail if the assumption were false?" for every `#VERIFY` it reads, not just "does this test exist?". | open | `UW-C59` |
| AL-125 | 2026-08-07 | Tasks 6, 7, 10, and 13 of the persistent-characters runtime plan (branch `feat/persistent-characters-runtime`) | process | Code and paths supplied inside a task brief need the same skepticism as code from any other source, because a brief is not a verified artifact, it is a plan author's best guess written before the implementer opened the files. Four briefs in this plan carried defects an implementer had to catch by reading the real code rather than trusting the brief: Task 6's brief described the character-binding site as the `GET /reading-state/...` path "around line 320", but that endpoint never creates a row in this codebase, the real (and only) creation site is inside `put_reading_state`'s `row is None` branch; Task 7's brief cited a writeback call site by a stale line number that now resolves to `_create_reading_state`, an unrelated function, not `record_completion`; Task 10's brief included sample code, `tg.start_soon(anyio.to_thread.run_sync, _work, limiter=limiter)`, that raises `TypeError` because `anyio.TaskGroup.start_soon()` accepts no keyword arguments; and Task 13's brief named `docs/planning/drafting-guide.md` as the doc to correct, but that guide is loaded verbatim into the Stage A/B LLM generation prompts and has no character-related field at all, the document actually executed step by step when a skeleton is filled is `.claude/skills/cyo-author/SKILL.md`. All four were caught before they shipped, by reading the cited code or file rather than trusting the citation. | Add a line to the SDD/task-brief authoring guidance: a brief's code samples, line numbers, and file targets are a claim, not a fact, and an implementer must verify each one against the real file before acting on it, exactly as this workstream's RAD citations must be verified rather than trusted. | open | `UW-C60` |
| AL-126 | 2026-08-07 | Wave-1 parallel agent dispatch, persistent-characters runtime plan (branch `feat/persistent-characters-runtime`), Task 10's commit | process | Running `pre-commit` while other agents hold uncommitted, unstaged edits in the same shared worktree is unsafe, and not for the reason a "no concurrent git writes" rule protects against. `pre-commit` stashes unstaged changes before running its hooks and restores them afterward; a concurrent agent's write landing inside that stash/restore window can be silently reverted by the restore, with no error anywhere in the output, only a bland "Restored changes from ...patch..." line that reads exactly like success. The other agents' git-write prohibition protects the index; it does nothing for this hazard, since the stash is triggered by any `pre-commit` invocation, including one run by an agent that itself made no other git-write violation. | Document in the parallel-dispatch coordination pattern (wherever concurrent-worktree agent rules are recorded) that `pre-commit` must not run while any other agent in the same worktree may still be mid-write; batch commits after every in-flight agent has reported, against a quiescent tree, one group at a time. | open | `UW-C61` |
| AL-127 | 2026-08-07 | Task 11 (promoting `the-storm-chasers-club` to Tier 2), persistent-characters runtime plan (branch `feat/persistent-characters-runtime`) | validator | Introducing a dedicated build node to set a character's `archetype`, with each of the six archetype choices converging on it, silently disqualifies the `sorting_hat` topology. `validator/topology.py`'s `sorting_hat` admissibility check disqualifies any node with in-degree >= 2, and a build node fed by six archetype-setting choices has in-degree 6 by construction. This was caught by reading `topology.py` before editing the skeleton, not by a failing gate run, so it would not have appeared anywhere in gate output had the author not looked first. The working alternative, used in Task 11: attach the archetype-setting `effects` directly to choices that already exist on the tree's own branch points, adding no new node and no new converging edge. **The promotion that occasioned this was withdrawn on 2026-08-08 (`AL-131`); the topology finding stands on its own and the proposed doc change below is unaffected, since the trap is a property of `sorting_hat` admissibility, not of that book.** | Add a line to `.claude/skills/cyo-author/reference/skeleton-format.md`'s "The archetype build node" section, which is where the gate-node/build-node idiom is actually documented (`docs/architecture/story-skeletons.md` has no build-node section; an earlier draft of this row named one that does not exist), stating explicitly that a `sorting_hat`-topology book must not introduce a build node as a new converging node, and that the safe pattern is attaching the archetype-setting effect to existing choices on existing branch points instead. | open | `UW-C62` |
| AL-128 | 2026-08-08 | Task 13 (authoring docs) and Task 14 (bookkeeping), persistent-characters runtime plan (branch `feat/persistent-characters-runtime`) | docs | An instruction file that is itself stale is worse than no instruction, because it is trusted by default and re-read at the start of every session. Root `CLAUDE.md`:505 asserted that `.claude/skills/cyo-author/reference/skeleton-format.md` "still uses a stale field name (`ending.type`)"; that claim was itself stale, the file was fixed on `main` in `21deea08` (PR #300) and has documented `ending.kind`/`ending.valence`, with an explicit note that there is no `ending.type`, ever since. Task 13's own brief inherited the false claim from `CLAUDE.md` and, had Task 13 not independently verified the file's actual content, would have sent an agent to "fix" an already-fixed file. A second, matching pointer in `docs/architecture/story-skeletons.md` ("see template feedback for the doc-correction follow-up") compounded the problem: it referenced the same non-issue and pointed at `docs/template_feedback.md`, a gitignored, local-only file that resolves for no reader of a fresh clone. | Correct `CLAUDE.md`'s stale claim about `skeleton-format.md` and repoint `story-skeletons.md`'s dead pointer (done on this branch, committed in `7393a8d1`, along with `stage-prompts/prose.md`; `ws1-leaf-diversity-sprint-design.md` followed in `ccbeaada`). Generalized proposal: an instruction file that documents a defect in another file should name the fixing commit or PR once the defect is closed, or be removed outright, rather than persisting as an assertion nothing ever re-checks. | open | `UW-C63`. The corrections are committed on branch `feat/persistent-characters-runtime`, not uncommitted as an earlier revision of this row said, and there are four files rather than two: `CLAUDE.md`, `docs/architecture/story-skeletons.md` and `docs/planning/stage-prompts/prose.md` in `7393a8d1`, plus `docs/planning/ws1-leaf-diversity-sprint-design.md` in `ccbeaada`. The branch is still unmerged and unpushed, so the status stays `open`: committed is not merged, and only a squash SHA on `main` will make this `applied` |
| AL-129 | 2026-08-08 | Task 12 (13-16 stat-envelope gamebook pilot; the pilot was **withdrawn from the branch on 2026-08-08** and is preserved out of tree, so no skeleton path is cited here), persistent-characters runtime plan (branch `feat/persistent-characters-runtime`) | validator | A stat-envelope book (`accepts_character` declaring `might`/`wits`/`nerve`, each immutable per the design's "seeded, not built in-story" cost model) cannot gate a choice condition directly on a stat's threshold value without tripping a blocking `L2-11` dead-branch error: `validator/layer2.py::_check_dead_branches` walks only the book's single declared-initial baseline configuration with zero `accepts_character` awareness, so a `stat == 0` branch is unconditionally unreachable in baseline whenever the book's own declared initial is `stat: 1`, regardless of what the envelope proves reachable. CH-3a's union-quantified walk does not suppress this, because `L2-11` is raised independently on the baseline walk itself. The design spec's section 4.3 "archetype = 0" build-node idiom is the analogous fix for the archetype variable, but does not transfer to stat books, which deliberately have no in-story mutating node (that absence is exactly what keeps the 27-state walk cheap per the design's section 4.5). Working fix (authoring-only, no validator or design-doc change): pair every stat-gated choice with an auxiliary ordinary (non-canonical) boolean `resolve`, reset by an unconditioned binary player choice immediately before the gate, and XOR-combine it with the stat threshold so the pair collapses to depend only on `resolve` at the fixed baseline value (both branches reachable via ordinary play, satisfying `L2-11`) while genuinely differentiating on the stat at other envelope states (for `CH-3a`/`CH-3b`/`CH-4`'s per-state walks). Verified empirically: baseline walk 530 configs; all 27 envelope states also 530 configs each, zero per-walk size cost. **The workaround is what killed the pilot, and that is the larger half of the lesson.** Because the XOR pairing requires an unconditioned player choice able to flip the outcome immediately before every gate, the reader cannot perceive their stat deciding anything: the stat becomes a hidden modifier behind a coin flip the reader just made. Applied across a whole book it made all 14 stations mechanically identical, and the pilot read as scaffolding rather than a gamebook, so it was withdrawn from the branch on 2026-08-08 and preserved out of tree rather than shipped. A cost model that measures only configuration count therefore missed the cost that mattered: the workaround is free in walk time and expensive in reader-perceptible agency, and nothing in the gate could have reported the second. | Two parts, and the second is the real decision. (1) **Done on this branch, uncommitted**: the `L2-11`-vs-immutable-stat interaction and the resolve/XOR pattern are now documented in the two docs an author actually reads, `.claude/skills/cyo-author/reference/skeleton-format.md` ("The stat-gate wall") and `docs/architecture/story-skeletons.md`, each stating plainly that the workaround leaves the reader unable to perceive the choice. The design spec is not the right home; authors do not read it. (2) **Open and undecided**: whether `validator/layer2.py::_check_dead_branches` should become envelope-aware, quantifying reachability over the entry states `accepts_character` declares instead of the baseline configuration alone. That would remove the need for the workaround and make a genuine stat gamebook authorable, but it is a validator change to a rule every skeleton in the catalog is already gated by, so its blast radius is catalog-wide and it needs a measured re-gate of the whole catalog before anyone commits to it. Not proposed as decided; proposed as the question to answer before another stat-envelope pilot is attempted. | open | `UW-C64` |
| AL-130 | 2026-08-08 | D-2 gate delivery (`scripts/check_rad_citations.py`), persistent-characters runtime plan (branch `feat/persistent-characters-runtime`) | tooling | A debt baseline generated from the working tree grandfathers the generating workstream's own new debt, and no amount of shrinking the baseline can surface it. `rad-citation-baseline.toml` was written by running `--write-baseline` over the tree as it then stood, which already contained `src/cyo_adventure/validator/character.py`, a file this workstream created. Its `CH-2` `#VERIFY` cited `test_canonical_bounds_are_read_by_the_validator`, a test that never existed. The row was absorbed as pre-existing debt indistinguishable from the 122 genuine ones, the gate went green on its first live run, and the defect would have shipped inside the very commit that introduced the gate meant to catch it. Found only by intersecting the baseline's file list with the branch's changed-file list, which is not a check anything runs. The general shape: a grandfathering baseline defines "pre-existing" as "present when I looked", but the author is looking at a tree that already contains their own work, so the baseline's own generation is the moment the distinction is lost. | Make `--write-baseline` refuse to add a row whose file is added or modified relative to `origin/main`, printing those citations as errors to fix rather than debt to inherit, so a baseline can only ever grandfather work that predates the branch writing it. Pair it with a CI assertion that the baseline never grows between `origin/main` and a PR head, which turns shrink-only from a comment in the file's header into something enforced. Until that lands, the manual check is the file-list intersection described above, and it belongs in the D-2 handoff as a step the receiving team repeats each time they regenerate. | open | `UW-C65` |
| AL-131 | 2026-08-08 | Final whole-branch review, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | validator | The CH-* rules prove an envelope is *declared* and *survivable*, never that it is *observable*. `the-storm-chasers-club`'s Tier 2 promotion passed all eight CH rules and the full gate while being behaviourally inert in two independent ways. Its single archetype-gated choice (`r_a_fix/c_rafix_explain`, condition `{"==": [{"var": "archetype"}, 4]}`) sits behind an only-inbound edge (`c_rapick_window`) whose effect sets `archetype = 4`, so the condition is unconditionally true for every one of the seven declared entry states; and its six archetype-setting choices are ungated with no bypass, which the same branch's `skeleton-format.md` explicitly forbids. CH-3b and CH-4 walk every entry state and correctly found all 121 nodes and all 25 endings reachable, which is precisely why they were silent: a tautology is maximally reachable, so a reachability rule reads a dead gate as a perfectly healthy one. The 13-16 stat pilot failed the same observability test through an unrelated mechanism (`AL-129`'s XOR workaround), so two pilots built to the same spec were both imperceptible while both passing the gate. The promotion was withdrawn. | Add a CH rule that fails a condition on a canonical character variable when every inbound path to the gate sets that variable to a value satisfying the condition, i.e. a gate whose truth is invariant across the declared envelope; and a companion rule that a book declaring `accepts_character` must have at least one condition whose truth *varies* across the declared entry states. Both are computable from the per-state walk `validator/character.py` already performs, so neither needs new traversal machinery. Until one of them exists, no gate run can distinguish a character-aware book from a book that merely says it is one, and the CH family's passing verdict should not be read as evidence of an observable envelope. | open | `UW-C73` |
| AL-132 | 2026-08-08 | Final whole-branch review fix wave, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | tooling | A hand-maintained happy-path fixture turns adding a gated CI job into a multi-test breakage whose error names the shell instead of the cause. `tests/unit/test_ci_gate_contract.py` executes the extracted CI Gate script against a hardcoded `ALL_SUCCESS` dict holding one `*_RESULT` key per gated job. Adding the `rad-citations` job broke seven tests at once with `bash: RAD_CITATIONS_RESULT: unbound variable` under the script's `set -u`, so every assertion pointed at bash and none pointed at the missing entry. The tempting workaround, defaulting the variable inside the workflow with a `:-success` fallback, is worse than the breakage: it makes an absent job result read as a pass, which is precisely the issue #594 defect this gate exists to prevent. The fixture was data that had to track the job list, not a test to be relaxed, and the distinction is what makes the difference between updating it and weakening it. | Derive the fixture from the workflow instead of hand-listing it. Applied: `ALL_SUCCESS` now spreads `dict.fromkeys(RESULT_VAR_TO_JOB, "success")`, reusing the mapping the module already parses out of the gate's own `env:` block, so any newly gated job joins the happy path automatically. `RELEASE_PR` stays hand-written on purpose, since it reads `.outputs.release_pr` rather than `.result` and so is deliberately outside that mapping. | open | `UW-C74`. The fix is written on the unmerged, unpushed branch `feat/persistent-characters-runtime` (`tests/unit/test_ci_gate_contract.py`, `ALL_SUCCESS` derived from `RESULT_VAR_TO_JOB`), so it is not `applied`: this log's own status discipline says `applied` means the change is merged and the `Ref` proves it, and nothing moves to `applied` on the strength of intent. Flip it on the squash SHA. Run context for the numbers, which were bare in an earlier revision of this row: the command is `uv run pytest tests/unit/test_ci_gate_contract.py`, the module holds 32 test functions as of commit `d9a6a93e`, and the comparison is before-fix versus after-fix on that same module: 25 passed and 7 failed before, 32 passed after. The 7 failures were the cases the hand-listed `ALL_SUCCESS` fixture starved of the newly gated `rad-citations` job. The count is pinned to `d9a6a93e` on purpose, since the module grows whenever a job is added to the gate |
| AL-133 | 2026-08-08 | Final whole-branch review fix wave, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | tooling | An assertion that reads ordering out of source text is fooled by comments, because a comment that explains an ordering constraint has to name the later step first. A new test asserted that the CI step resolving a git ref runs `git rev-parse` before `git cat-file -e`, by finding each command's first occurrence in the extracted step body. It failed against the correct script: the step's own `#CRITICAL` comment names `git cat-file -e` while explaining why `rev-parse` must come first, so the explanation of the rule appeared earlier in the text than the code obeying it. The failure is the dangerous direction only by luck. Had the comment been phrased the other way round, the same assertion would have passed on a script with the calls in the wrong order, and a test asserting an ordering it cannot actually read is worse than no test. The general shape: source text is not the program, and any assertion that treats position-in-file as position-in-execution must first remove everything that is not executed. | Strip comment lines before reading ordering or presence out of any extracted script body, and prefer asserting on the parsed step rather than on raw text wherever the harness already parses it. Applied on this branch in `tests/unit/test_ci_gate_contract.py`, which now filters comment lines out of the step body before locating either command. Worth generalising: the same module makes several presence assertions against raw workflow text, and every one of them can be satisfied by a mention in a comment rather than by real configuration. | open | `UW-C75` |
| AL-134 | 2026-08-08 | Final whole-branch review fix wave, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | tooling | A FastAPI route docstring is published API surface, so documenting behaviour in one breaks the contract gate while changing nothing on the wire. FastAPI copies a route handler's docstring into the OpenAPI operation's `description`, and `@hey-api/openapi-ts` copies that verbatim into `frontend/src/client/sdk.gen.ts`, which is committed and drift-checked by the `contract` CI job. A fix wave documented the new character cap in `create_character`'s docstring as an ordinary code comment would be, which would have failed `contract` on a change with no behavioural content, and the failure would have surfaced in CI as generated-client drift rather than as anything resembling its cause. This is the same channel as the already-known Pydantic model docstring path into `types.gen.ts`, reached from the route side instead of the model side, which is why knowing the first case did not prevent the second. | Treat a route docstring as a contract edit, not a comment: either leave it byte-identical and put implementation notes in a call-site comment or a private helper's docstring, which is what this branch did, or change it deliberately and regenerate the client in the same commit. The verification that settles it in either direction is cheap and worth writing down: dump `app.openapi()` in-process and diff every operation `description` against the committed `sdk.gen.ts`, which reports drift locally instead of waiting for the `contract` job. Worth promoting into the contract-gate documentation so both channels, model docstrings and route docstrings, are named in one place. | open | `UW-C76` |
| AL-135 | 2026-08-08 | PR #649 CI, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | tooling | A schema invariant recorded only as a RAD prose comment is enforced by nothing, so the branch that falsifies it finds out from a downstream job rather than from a test. `scripts/reset_e2e_real_state.py` issued a bare `TRUNCATE TABLE reading_state` under an `#ASSUME` stating that nothing declares a foreign key onto that table. This branch added `character_book_completion` with exactly such a foreign key, and the only thing that noticed was the real-backend Playwright job, which failed in its setup fixture with `FeatureNotSupportedError: cannot truncate a table referenced in a foreign key constraint`. Two compounding causes: the invariant lived in prose, which cannot fail; and the `#VERIFY` it cited, `test_reset_e2e_real_state_truncates_reading_state`, did not exist and was grandfathered in `rad-citation-baseline.toml`, so the new RAD citation gate correctly stayed silent. Note also that `ondelete="CASCADE"` does not rescue a `TRUNCATE` the way it rescues a `DELETE`, so a correctly declared foreign key still breaks the statement. | Convert the prose invariant into a schema-derived test: list the truncated tables in one constant and assert, from live SQLAlchemy metadata, that every table with a foreign key onto a member of that set is itself a member. Done here as `test_truncated_tables_cover_every_dependent_foreign_key`, break-tested against the pre-fix single-table set (fails naming `character_book_completion`) and the fixed set (passes). Prefer naming each table explicitly over `TRUNCATE ... CASCADE`, which converts this loud Postgres error into silent data loss for the next foreign key someone adds. Generalises past this one script: any RAD comment that asserts a whole-schema property is a candidate for a metadata-derived test, and any grandfathered baseline citation is a claim currently proved by nothing. | open | `UW-C77` |
| AL-136 | 2026-08-08 | PR #649 CI, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | process | A gate placed where the data was convenient rather than where the need is silently becomes a product-wide behaviour change, and a green unit suite cannot see it. `KidShell` already fetched the active character for the kid library route, so the first-run character gate was written there: with `status === 'none'` it rendered `CharacterCreator` instead of the library `Outlet`. That inverted the gate's scope from per-book to per-profile. Every existing child was hard-gated into creating a character before reaching their library, with no skip affordance, for a feature no catalog book can use, which also contradicted the branch's own stated posture that the chain merges enforced and unreached. Six unit tests covered the gate and all six passed, because each asserted the gate did what it was built to do; none could ask whether the scope was right. The only thing that caught it was the real-backend Playwright smoke, and it surfaced as a 30s timeout on `getByRole('link', {name: 'The Tide Pool Mystery'})`, a locator that says nothing about characters. | Two changes. First, when a plan says "show X when Y has none", make it state the SCOPE explicitly, per book, per profile, or per route, because the implementer will otherwise put the check wherever the data already is, and that location silently sets the scope. Second, treat "changes what every existing user sees on next visit" as a distinct review question the diff cannot answer: a per-task reviewer reading only the gate sees a correct gate. Resolved here by exposing `accepts_character` on `LibraryItem` and deciding at the book, at click time, failing open on every non-'none' status since the backend already treats an unseeded read as normal. | open | `UW-C78` |
| AL-137 | 2026-08-08 | PR #649 CI, persistent-characters runtime (branch `feat/persistent-characters-runtime`) | tooling | A pull request that conflicts with its base runs ZERO GitHub Actions workflows, and the third-party app checks that remain make it read as green. `pull_request` workflows are evaluated against `refs/pull/N/merge`, a ref GitHub cannot create while the branch conflicts, so after `origin/main` moved under this branch the second push produced no runs at all. What `gh pr checks` then showed was four checks, all passing: CodeRabbit, GitGuardian, Socket Security, and qlty. Those post against the head SHA through the Checks API and are unaffected by the merge ref, so they keep reporting on a branch whose real CI never started. Nothing in that output says "conflicted" or "no runs"; the only tells are the context COUNT collapsing from ~30 to 4, and `gh run list --branch <b>` showing its newest head SHA behind the PR's. This is the same family as the existing roll-up traps (a skipped job renders green, a job with no runner renders red with `steps: []`), but strictly worse, because here the roll-up check does not exist at all rather than reporting the wrong colour. | Never read `gh pr checks` alone as evidence that CI ran. Check `gh pr view <n> --json mergeable,mergeStateStatus` in the same call and treat `CONFLICTING`/`DIRTY` as "CI is not running", not as "merge later"; and cross-check the newest head SHA in `gh run list --branch <b>` against the PR head SHA, since a matching SHA is the only positive evidence the push was picked up. Worth folding into whatever check-verification helper the PR workflow settles on, as a precondition asserted before any check state is interpreted. | open | `UW-C79` |
| AL-138 | 2026-08-09 | Pre-rebuild review, strict-mode implementation (branch `claude/skeleton-story-review-3zy6tq`) | tooling | `load_skeleton` ran the full gate and then threw the `ValidationReport` away unless it blocked, so `check_skeleton.py`, the tool every authoring wave gates shells with, printed a clean ok for skeletons the gate had warned about. A warning-visible sweep found 40 of 61 catalog skeletons carrying hidden advisories: 37 PL-23 clock drifts up to 125%, 25 PL-24 ending-mix breaches including 7 below the winnability floor, 4 L2-13 and 2 PL-26. None of these was visible in any authoring log because the loader dropped the report on the floor; the script even re-implemented one warning (below-cell-min) by hand, which made the omission of the other nine look deliberate. The general shape: a loader that validates and returns only the payload converts every advisory the validator produces into silence, and the tooling downstream inherits that silence as a clean bill of health. | Applied: `load_skeleton` takes an optional `report_sink` callback receiving the full `GateResult` before the block decision, plus an `enforce_grammar` passthrough; `check_skeleton.py` prints every finding at every severity and gains `--strict`, the newly-drafted-skeleton bar that escalates the PL-19/23/24/25/26 and L1-7 advisories, enforces CG-1..CG-3, and requires a band-scaled random-walk satisfying-outcome floor. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-139 | 2026-08-09 | Pre-rebuild review, strict-mode implementation (branch `claude/skeleton-story-review-3zy6tq`) | validator | Arming the CG choice-grammar family on a skeleton shell revealed that CG-4 can never pass one: it checks content-word overlap between a choice label and the opening sentence of the target node's body, and on a shell every body is a `<<FILL ...>>` directive, so the overlap is empty by construction and CG-4 fires on every choice of every shell. The family was designed against filled stories and nothing documented which CG rules are shell-safe, so the first strict run failed the demo fixture on ten findings of which six were structurally meaningless. CG-1 (run cap), CG-2 (option bounds) and CG-3 (words per stop, which reads declared FILL word targets) are all shell-meaningful; only CG-4 requires prose. This is the same shape RL-13 hit before AL-006 taught it to skip FILL bodies. | Applied: `check_skeleton.py --strict` escalates CG-1..CG-3 and deliberately excludes CG-4 with an in-code rationale; CG-4 belongs to the fill gate. When the grammar is armed for filled stories (UW-C24 flip), consider teaching CG-4 to skip FILL bodies outright, the AL-006 precedent, so a partially filled draft cannot fire it spuriously either. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-140 | 2026-08-09 | Reconvergence hard-gate ruling implementation (branch `claude/skeleton-story-review-3zy6tq`) | validator | A reconvergence cap designed from the aggregate (corpus mean max in-degree 7.79 vs the research anchor 1.5) would have banned two whole topology families, because the aggregate mixes two opposite mechanisms. Measured per topology before implementing: `open_map` hubs sit at median max in-degree 9 with one legitimate hub at 126 (`the-blackwood-sanatorium`, every room re-enters the hub by design) and `loop_and_grow` at 5, while the pathological corridor-collapse funnels live in the tree-like topologies (`branch_and_bottleneck` clone pair at 31, `the-cinder-bazaar` at 21). A blanket per-band cap tight enough to catch the funnels excludes every open map; one loose enough to admit open maps catches nothing. The gate had to be topology-aware to mean anything: cap `branch_and_bottleneck`/`gauntlet`/`sorting_hat`/`time_cave` at 4/4/6/6/8/8 by band, exempt the hub topologies whose re-entry is the design, and leave those to the SQ-15 per-path metrics. The general shape: a structural quality gate calibrated on a corpus-wide distribution silently encodes the assumption that one mechanism produced the distribution. | Applied: topology-aware max in-degree hard cap in `check_skeleton.py --strict` with the exemption rationale in-code; when the strict checks are promoted into `validator/` after the grandfathered catalog is removed, carry the topology awareness into the catalogued rule rather than flattening it to one number. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-141 | 2026-08-09 | Strict-bar conformance census after the 2026-08-09 rulings (branch `claude/skeleton-story-review-3zy6tq`) | process | An advisory rule family left inert since landing accumulates near-universal non-conformance, and the flip cost grows monotonically while nobody watches. Arming CG-1..CG-3 inside the strict bar and censusing the catalog showed 57 of 61 skeletons breach CG-2 and 48 breach CG-3: the entire catalog predates the grammar's own bar because `enforce_grammar` defaulted False from the day the family landed (UW-C24) and no production caller ever opted in, so every authoring wave since added non-conforming trees at zero visible cost. The full strict census passes 2 of 61, meaning the ruled bar implies a near-total rebuild; that number was invisible until the rules were armed against the corpus even once. The general shape: an inert gate is not a deferred decision, it is a debt meter running with the display off, and the census that reads the meter costs one afternoon against a rebuild-sized principal. | Applied for this flip: the census ran before any removal and its numbers plus a remove-on-replacement sequencing recommendation are recorded in the 2026-08-09 review (Part 4 census section), so the removal wave is sized by data. Standing practice: before arming any long-inert rule, census the live corpus with the rule in report mode and record the count, so the flip cost is a number in a doc rather than a surprise in CI. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-142 | 2026-08-09 | Strict-bar drafting pilot (branch `claude/skeleton-story-review-3zy6tq`) | tooling | PL-23 only reveals the derived reading clock inside a failure message, so a declared `estimated_minutes` that is wrong but within the 25% tolerance never surfaces the true number, and every out-of-tolerance miss costs a full draft-gate iteration. Both large-cell pilot drafters independently reimplemented the fewest-words Dijkstra inside their generators purely to see the number the checker already computes. A subtlety the 3-5 draft exposed: PL-23 silently no-ops below 200 fastest-path words, so two structurally similar small skeletons get different effective rule sets and the author is never told which regime they are in. | Applied: `check_skeleton.py --headroom`/`--strict` now always prints declared vs derived `estimated_minutes` via the canonical `recompute_estimated_minutes`. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-143 | 2026-08-09 | Strict-bar drafting pilot, three cells with independent adversarial review | process | Strict-clean does not mean approvable, and the gap is systematic rather than incidental: three drafts in three different cells each passed `check_skeleton --strict` with zero findings at any severity, and each was independently REJECTED on adversarial review, on defects no deterministic layer measures (label laundering, positional fate bias, consequence-free choices, merge contradictions, quota-shaped design). The gate verifies topology, budgets, and quota arithmetic; the rejections were all about outcomes, consequences, and continuity. Second half of the lesson: both large-cell drafters concluded a hand-author cannot hold the coupled global constraints at 180+ nodes, and each built an ad-hoc structural generator replicating the strict rule set; the only one-shot passes came from that method. | Make adversarial critique a mandatory stage of the rebuild drafting workflow (draft, strict gate, in-cell audits, adversarial review, then promotion PR), and ship the structural generator harness as a first-class authoring aid instead of expecting every wave to rebuild it ad hoc. | open | `UW-C80` |
| AL-144 | 2026-08-09 | Strict-bar drafting pilot, adversarial critiques of all three drafts | validator | The outcome gates read kind/valence tags, not outcomes, so a draft can satisfy PL-24, the walk floor, and the outcome-spread audit purely by tag distribution over scenes the tags misdescribe. All three pilot drafts did it, two of them unconsciously: the 3-5 draft narrates one identical terminal scene under four kind/valence combinations, one tag contradicting its own beats; the 10-13 draft tags a full mission success neutral and ten failures as discovery; the 13-16 draft has sixteen everyone-survives rescues tagged negative setback and twelve off-page abandonments tagged neutral, plus `role=` derived mechanically from valence so twelve endings instruct the fill to write a discovery where nothing is discovered. Every headline economy number that passed the gates was a tag artifact. | Build the tag-honesty audit: extend `scripts/audit_ending_valence.py` with an outcome-keyed rubric (goal delivered, goal lost, goal deferred) checked against ending beat text, flag valence/kind contradictions and same-terminal-event ending sets (high pairwise beat similarity across endings), decouple `role=` from valence, and wire the audit beside the outcome-spread check in the drafting loop. | open | `UW-C81` |
| AL-145 | 2026-08-09 | Strict-bar drafting pilot, adversarial critiques of the two large drafts | validator | Choice-level fairness and consequence are invisible to every current metric, and generated drafts fail both by construction. Position bias: in the 10-13 draft the third option leads to an ending 77% of the time and the first option never does; in the 13-16 draft always-pick-option-1 walks to the single best ending with zero risk, because the generator ordered options safe-first in every fan. Consequence: 25 decision nodes in the 10-13 draft have sibling choices with byte-identical reachable-ending sets, and staged merges collapse 98 ancestors into one node while never exceeding direct in-degree 5, laundering exactly the funnel the in-degree cap was ruled to stop. | Add computable consequence checks to the strict bar: per-position ending-kind distribution with a skew ceiling plus an argmax-position walk that must not reach a positive ending unscathed; flag sibling choices with identical reachable-ending sets; extend the reconvergence gate with a k-hop ancestor-funnel metric. Generators must seeded-shuffle option order. | open | `UW-C82` |
| AL-146 | 2026-08-09 | Strict-bar drafting pilot, adversarial critiques of the two large drafts | validator | Merge-node beat contradictions, the known `the-hollow-lighthouse` defect class, are reproduced systematically by generation rather than occasionally by hand: eight hard upstream-vs-beat contradictions in each large draft, including endings narrating scenes from choices the reader explicitly declined, characters referenced at merges who are introduced only on other routes, gear appearing from nowhere, and one terminal beat hedging 'whichever stranger you trusted' because the generator knew its parents disagreed. Tier-1 has no state, so every fact a merge-node beat presupposes must hold on every inbound path, and nothing checks it. | Give in-degree >= 2 nodes a machine-checkable contract: lint each merge node's presupposed facts (objects, characters, terrain, time of day) against the union of beats on every inbound simple path, flag hedging disjunctions in terminal beats, and until that exists require human beat-vs-parents review of every shared ending in the drafting workflow. | open | `UW-C83` |
| AL-147 | 2026-08-09 | Strict-bar drafting pilot, 3-5 adversarial critique | tooling | The drafting loop's gate and the catalog's admission audits are disjoint, so a strict-passing draft can be inadmissible three separate ways and nobody sees it until commit: the 3-5 pilot draft was structural distance 0.065 from `puddle-jumping-day` (second-closest pair in the whole catalog), its outcome economy survived the spread audit only by one dubious tag, and it was a slotless, contract-less shell with `production_eligible: true`, which exits the strict gate cleanly and would silently take the legacy free-text fill path in production matching (`load_contract_for` returns None). | Wire `check_incell_clones.py` and `check_outcome_spread.py` into the drafting workflow with the candidate temporarily placed in its target cell, and make `--strict` fail a `production_eligible` shell that has no slot surfaces and contract sidecar. | open | `UW-C84` |
| AL-148 | 2026-08-09 | Strict-bar drafting pilot, both large-cell drafters' friction logs | validator | Several strict constraints are individually reasonable but couple destructively, and the couplings, not the values, dictated the drafts' shape: CG-3's stop ceiling equals roughly two to three nodes at the band words mean (a lone corridor node at the 10-13 mean is already illegal before a normal-weight successor; at 13-16 every checkpoint merge lives within ten words of the wall), so both drafts near-eliminated corridors and one emitted stops at 148-150 of the 150 cap, leaving zero fill headroom; CG-2's exact (3,3) at 8-13 bans the binary dilemma and forces a third strategy at every one of 100+ decisions, which is where template-sameness pressure originates; the 25% gamebook endings fraction makes terminal leaves the only legal padding, pushing toward the shallow-ending sprawl other rules exist to prevent; PL-26's gamebook ceiling of 4.0 is unmotivated by its own comment yet strict-blocking; PL-24's 60% kind ceiling quantizes harshly at four or fewer endings, silently raising the real endings floor for small kid books. | Calibration review before rebuild wave 1, deciding each coupling explicitly: exempt the terminal node from CG-3 composition or raise the prose stop ceiling; widen CG-2 to (2,3) at 8-13; key the endings floor to distinct outcome identities or lower the gamebook fraction toward 0.20; anchor or drop PL-26's gamebook 4.0; make PL-24's ceiling count-aware below five endings. Also adopt the headroom discipline: generators target at most 90% of any hard cap, and strict warns at 98%. | open | `UW-C85` |
| AL-149 | 2026-08-09 | Strict-bar drafting pilot, all three drafters' friction logs | docs | The authoritative spec for newly drafted skeletons lives in a CLI script's docstring and constants, and the authoring-facing docs either omit or contradict it: `skeleton-format.md` never mentions CG-3 or its stop-composition rule (the single rule every grandfathered 10-13 medium skeleton fails), the strict topology constraints, or the FILL grammar's actual quoting behavior (apostrophes inside `beats='...'` are folklore learned from an exemplar); the word 'satisfying' names two different sets (success/completion kinds in PL-20, positive-or-neutral valence in the walk floor) with no doc distinguishing them; ending `role=` vocabulary is unvalidated folklore; and both pilot briefs drifted from the code because they were hand-copied (one mis-stated PL-26's gamebook ceiling as 6.0 against the code's 4.0). | Write the strict-bar authoring reference: a new-skeleton section in the cyo-author skill generated from `band_profile.py`/`choice_grammar.py` values rather than hand-copied, covering the five strict policies, CG-3 composition, the two 'satisfying' senses, FILL quoting, and the role vocabulary decision; generate per-cell drafting briefs from code the same way. Applied in `177a4c6`: `scripts/generate_drafting_brief.py` emits the per-cell constraint set from the enforced sources, and `skeleton-format.md` carries the corrected word table, the strict-bar section (incl. both 'satisfying' senses), the slot/contract grammar with the quoting note, and an honest role= description; the residual (wave-1 briefs actually produced by the generator) rides `UW-C86`. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-150 | 2026-08-09 | Strict-bar drafting pilot, all three drafting subagents independently | process | The authoring-lessons mandate collides with subagent authoring runs: CLAUDE.md requires appending lessons at the end of any authoring run, but a scratchpad-scoped subagent cannot discharge it (repo-mutating MUST, feature-branch and signing rules, UW register cross-reference for open lessons), so all three pilot drafters and all three critics correctly declined to write the log and returned candidate lessons to the orchestrator instead. The mandate as written does not say who lands lessons from delegated runs, and six agents each spent tokens re-deriving the same conclusion. | Amend the authoring-lessons section of CLAUDE.md: a delegated authoring run returns candidate lessons to its orchestrator, and the orchestrator (the session that can branch, sign, and validate) is responsible for landing them; a subagent must not edit the log. | open | `UW-C87` |
| AL-151 | 2026-08-09 | Same-skeleton diversity experiment: three isolated fills of `the-lost-mitten` under maximally different theme bindings (review Part 6) | validator | A same-skeleton story pair has a perceived-similarity floor of 0.50 before any prose exists (PS weights 0.5 leaf + 0.3 structural + 0.2 theme; structural is 1.0 for a same-tree pair by construction and theme was 1.0 because binding never re-themes `metadata.themes`), and the empirical best case, three independent authors filling three maximally different bindings with near-minimal leaf similarity (0.18-0.20, body trigram Jaccard 0.026-0.029), landed at PS 0.59-0.60 against the 0.70 repeat threshold. Meanwhile the qualitative verdict on the same triple was 'one story, three costumes': identical search-strategy menus, clue mechanics, gag, and punchline, with two of three ending titles string-identical. So a beat-identical triple sails under every lexical gate while fill quality has almost no room to move the metric: slot granularity, not prose, is the diversity bottleneck, which is direct experimental evidence for the SQ-11 beat-variant ADR (gate OG3). | Add a structural signal to the family-scoped anti-repeat guard: skeleton id plus beat-variant fingerprint, never serving the same combination twice to one family (cheapest intervention, pure policy); adopt variant beat pools on high-salience nodes (the clue mechanics, the twist ending, the set-piece gag) per SQ-11, which this experiment sizes at roughly 2-3x beat authoring on 4 of 11 nodes with zero topology or validator changes; frame same-skeleton siblings honestly as a series in the product. | open | `UW-C88` |
| AL-152 | 2026-08-09 | Same-skeleton diversity experiment (review Part 6) | tooling | Theme binding rewrites the three slotted surfaces but leaves `metadata.themes` untouched, so a beach book and a meadow book both shipped tagged 'winter'. The stale tags are kid- and guardian-facing library metadata, they feed skeleton matching, and they feed the perceived-similarity theme axis, where they pin 0.20 of PS at maximum for every same-skeleton pair regardless of how different the bound themes actually are. | `render_bound_skeleton` (or the binding pipeline around it) must re-derive or strip `metadata.themes` at binding time, with the contract carrying per-binding theme tags; add a binding acceptance check that no legacy-theme tag survives into a bound skeleton's metadata. | open | `UW-C89` |
| AL-153 | 2026-08-09 | Same-skeleton diversity experiment (review Part 6) | process | Beats encode world facts the slot system does not govern, and the fill contract's reskin rule is ambiguous enough that three independent authors resolved the same conflicts three different ways: the skeleton's un-slotted snow was imported into an autumn park by one author and reskinned to dew by another; 'one bare hand is cold' was inverted to a hot bare head because the bound item was a sunhat; and one binding produced 'fireflies chirped' because the beat verb belonged to the legacy birds, a world-consistency defect no gate catches. Divergent resolutions mean fidelity review cannot tell a faithful fill from a drifted one on exactly these beats. | Slot or neutralize world-fact surfaces in beats during parameterization (weather, anatomy tied to the item, helper-species verbs and time of day), add a binding-consistency lint that checks bound helper species and setting against beat verbs, and sharpen the fill contract's reskin-versus-uphold rule with the three observed cases as worked examples. | open | `UW-C90` |
| AL-154 | 2026-08-09 | Opus pipeline analysis for the narrative redesign (review of every FILL-directive consumer) | tooling | Every consumer of the FILL directive fails open on a parse miss: `binding.py`, `fidelity.py`, `fidelity_review.py`, and `slotted_surfaces.py` all skip the node when the anchored regex does not match, and three more private regex copies live in `mutation/bundle.py`, `mutation/operators.py`, and `parameterize_skeleton.py`. A future format change the regex does not match would therefore silently disable slot substitution, word-count fidelity, semantic fidelity, theme-leak scanning, and slot-set equality simultaneously, with zero findings anywhere. The consumers were each individually reasonable; the cascade is the defect, and it is invisible until the first format evolution, which the narrative redesign now schedules. | Make the parse total before any format work: a body starting with the FILL marker that does not parse becomes a blocking L1 finding; add a lockstep test enumerating every directive consumer and asserting each reports rather than skips on an unparseable directive; collapse the three private regex copies onto `storybook/slotted_surfaces.py`. Prerequisite 1 of the narrative redesign proposal. | open | `UW-C91` |
| AL-155 | 2026-08-09 | Opus format-design analysis, re-expressing `the-lost-mitten` node by node | validator | A node with an in-edge from its own descendant is re-rendered verbatim on every loop, and nothing tells the prose author: `the-lost-mitten`'s three abandon-the-lead choices all target `n_start`, whose beat stages the loss as fresh news, so all three experiment fills wrote a discovery beat and a child who loops back reads the oh-no moment again on every return. The defect is prose tense inside a beat, so no graph, budget, or fidelity check can see it, and it generalizes to every skeleton with hub re-entry (`loop_and_grow` and `open_map` are built on it). | Derive a `reentrant` flag from the graph (any node reachable from its own successors), have the validator REQUIRE the flag rather than trust the author, and carry a reentry contract into the fill prompt (write established fact, not fresh news). Cheap, graph-derived, and worth shipping ahead of the wider redesign; folded into the proposal's ship-regardless stage. | open | `UW-C92` |
| AL-156 | 2026-08-09 | Narrative-redesign pilot, scene-inventory and recognition ratings over nine fills of one skeleton (proposal section 10) | validator | Pairwise distinctness margins can be passed while every story shares an attractor vocabulary, because free LLM invention diversifies pairwise and converges collectively: the three fully-free arm C authors, working in isolation, independently produced the same false-clue retcon in 3 of 3 fills, the same nudge-and-catch retrieval in 3 of 3, the same cozy-container napping reveal in 3 of 3, a near-verbatim 'boing boing' eyewitness in 2 of 3, and regressed the choice-label surface to the control template including three verbatim label collisions with a different arm. The dominant residual recognition leaks after devices vary are ritual phrases in labels and dialogue ('Team time', 'One, two, three, reach it together'). Neither class is visible to pairwise device margins, lexical similarity gates, or the perceived-similarity metric. | Add two sibling-scoped checks to the drafting loop: a convergence check flagging any device that appears in 2 or more sibling fills of one skeleton, and an n-gram overlap check over choice labels and quoted dialogue against sibling fills; both complement rather than replace the pairwise margins. | open | `UW-C93` |
| AL-157 | 2026-08-09 | Narrative-redesign pilot, arm B twist-margin failure (proposal section 10) | validator | Locking an ending's outcome propositions locked its MECHANISM too, and mechanism-sameness is what a reader recognizes: all three arm B twists shared the carried-all-along reveal because `locked_outcome` pinned it, differing only by concealment prop, and the rater scored the twist margin 0 of 3 under the noun-swap rule while the one mechanism-changed twist in the pilot (arm C's third-party recovery) was the only twist no reader predicted. The contract's own `premise.resolution_space` already enumerated three mechanisms; nothing selected among them. | Make the ending mechanism a per-request selection drawn from the contract's `resolution_space` (a selection-vector entry with the same seeded, family-steered discipline as device selection), with `locked_outcome` locking kind, valence, and affect envelope only. | open | `UW-C94` |
| AL-158 | 2026-08-09 | Narrative-redesign pilot, arm B clue-margin failure (proposal section 10) | tooling | Bible authoring converges exactly the way fill authoring does, and a converged bible caps the format's diversity from upstream: bibles a and c were written with near-identical device kinds (flattened grass vs parted clover; yarn strand vs wool wisp), so the a-c pair collapsed to noun-swap on 2 of 3 clue nodes and failed the pre-registered margin even though the obligation format delivered everything else. Device distinctness must be enforced where devices are chosen, not hoped for at fill time. | Bible generation enforces cross-binding device-kind diversity: compute Mechanic Divergence against the family's (and the cell's recent) prior bibles at bind time and re-draw below threshold, backed by a wider per-category device-kind taxonomy so re-draws have somewhere to go. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-159 | 2026-08-09 | Higher-difficulty B-plus pilot on `the-clocktower-cipher` (proposal section 11) | validator | The obligation contract is itself a shared prompt across every fill of its skeleton, and anything concrete in it becomes the new frozen armature: scene devices written into constraints (the seize-then-lubricate remedy, the noise-trap safeguard, the preserved-loft adoption) produced a single scene-device inventory under rotated props in all three isolated fills (clue-analog margin 1 of 3 for every pair), and contract vocabulary seeded convergent prose ('stepped out for tea thirty years ago' verbatim in two books, 'Knowing when not to is a skill' in all three, ten-plus cross-book phrase templates). The sibling-gram check surfaced the leaks but its budget of 8 was calibrated on 500-word fills and reads 95 at 3.1k words, so it cannot yet gate. | Contract hygiene: obligation strings must be device-abstract, every recurring set-piece becomes an invention slot over a bible category with real kind freedom, and the sibling-gram check gains a length-normalized budget (per thousand leaf words) and joins the fill loop as a gate. | open | `UW-C96` |
| AL-160 | 2026-08-09 | Higher-difficulty B-plus pilot, theme-authoring agent measurement (proposal section 11) | tooling | Contract-forced device kinds consume the Mechanic Divergence budget invisibly: `kind_must_be` specs froze most category multisets at pairwise Jaccard 1.0, leaving roughly 0.07 of headroom over the tau, so the MD gate passed while the frozen-kind categories stayed frozen and the room hint-carriers scored 0-2 of 4 distinct per pair downstream. The theme agent could only discover this by reading the checker's source and doing the arithmetic by hand. | `check_bible_diversity` reports per-category Jaccard with forced-by-contract categories marked, so authors and generators see real headroom; world recipes minimize `kind_must_be` to what safety genuinely requires, and a contract whose forced kinds leave less than a floor of free MD headroom fails NC review. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-161 | 2026-08-09 | Higher-difficulty B-plus pilot, recognition rating at 10-13 (proposal section 11) | validator | The reader surfaces the contract cannot vary are the strongest recognition channels, and their strength grows with band: same-order same-function choice menus landed the same-book verdict at node 2 of 26 for a pattern-sharp 10-13 reader, five of eight ending titles were unslotted and therefore byte-identical across books, and one slotted title named a prop the selection had assigned to a room off that title's reaching path. The overall score fell to 2.0 against the 3-5 pilot's 2.5 despite better device work, because recognition capability rises with age while the diversity margins stayed constant. | Generate all ending titles under the title contract with none unslotted, add a selection-time check that a title's named props lie on the reaching path, add a label-surface diversity directive with a menu-surface margin to the pilot protocol, and scale distinctness margins and gram budgets with band. | open | `UW-C98` |
| AL-162 | 2026-08-09 | Third pilot, sibling-gram trace and scene-rater convergence list (proposal section 12) | authoring-craft | Same-model idiom convergence is a distinct channel from spec leakage: three isolated authors with device-abstract inputs produced identical staging idioms at the same beats ("a door stood ajar on a room that smelled of", "stepped out for tea", "the climb was shorter than it looked") that appear in no input file, and after a token-level revision round all five watched idiom families reappeared in paraphrases below the 4-gram floor ("corkscrewed" stairs, the "come back by daylight, and knock" closing beat in all three books). The n-gram check catches token identity, not idiom identity, so a revision pass defeats the metric more thoroughly than the phenomenon. | Add an idiom-family layer to the sibling check: a curated watch list of staging tropes matched by stem or embedding rather than exact tokens, or a rater-in-the-loop sample on the convergence list; budget it per band. | open | `UW-C99` |
| AL-163 | 2026-08-09 | Third pilot, first-pass gram failure root cause (proposal section 12) | authoring-craft | Fact description strings are shared prompt surface exactly like constraints and hints: the hygiene pass abstracted constraints and beat hints but left `rooms_mapped` naming the spiral stair and pendulum ledge, `cipher_decoded` saying "substitution cipher" against a freed cipher-form slot, and `vault_contents_known` enumerating slot-supplied contents, and all three seeded cross-book prose or contradicted selections. | Extend the contract-hygiene rule to every prose-bearing string in the contract (facts, merge notes, reentry contracts), and have NC review scan fact strings for device-category kind tokens and slot-supplied nouns. | open | `UW-C100` |
| AL-164 | 2026-08-09 | Third pilot, menu-frame check and both raters (proposal section 12) | validator | Per-binding label styles close the menu-surface channel cheaply: with a `label_style` drawn per selection and validated by NC-7, shared menu frames went from the pilot-2 recognition driver to 0 deterministic overlaps, and the scene rater scored the label voices 3.0 ("non-interchangeable, not template rewording"). Styling does not vary meaning, so the semantic fingerprint remains (AL-166), but the surface channel is closed. | None further; adopt `label_style` as a standard selection field for every contract. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-165 | 2026-08-09 | Third pilot, revision round (proposal section 12) | tooling | The gate-revise-regate fill loop works agentically in one round: feeding the deterministic shared-gram list back to each isolated author (revise only your own colliding sentences, in your binding's voice) moved 20.4 shared grams per 1000 to 1.2 against a 4.0 budget with integrity and the full gate still green, no human repair. This is the production shape for every deterministic diversity check. | Wire `check_sibling_fills` (and the title and menu checks) into the generation fill loop as a blocking gate with an automated revision pass, capped at N rounds before flagging for review. | open | `UW-C101` |
| AL-166 | 2026-08-09 | Third pilot, recognition rating (proposal section 12) | authoring-craft | With devices, prose, menus, and titles all varied, recognition still lands at node 4 (2.5 against a 2.0 baseline, margin failed) and the residual channels are the skeleton's identity itself: choice-menu SEMANTICS (same options, same meanings, same order at every fork, visible through any label styling) and scene-function order, plus beat-identical emotional payoffs at same-mechanism-family endings. Sibling fills of one skeleton are structurally a "same adventure, new world" series, and downstream hygiene cannot change that. | Owner decision: frame same-skeleton bindings as an explicit series (assignment policy: at most one binding of a skeleton per reader, or market the format honestly), and/or fund per-fork semantics pools rotated at selection time as the mechanism treatment applied to decisions; catalog-level diversity comes from more skeletons, not more hygiene. | open | `UW-C102` |
| AL-167 | 2026-08-09 | Model-tier study, Haiku fill of the clocktower contract (proposal section 13) | validator | Declared node obligations are proved coherent but never verified against the prose that fills them: a Haiku fill decoded the cipher at `n_open`, whose contract declares `forbids: [inside_access, cipher_decoded, keeper_met]`, voiding the entire "solve the cipher" branch because `n_note` then re-presented the same decode as a discovery. It passed fill integrity, the full Layer 1 and Layer 2 gate, and every NC check, because the NC checks run BEFORE prose exists and no later check reads a node's body against its `establishes` and `forbids` sets. A stronger author was papering over the hole, so it becomes load-bearing exactly when a cheaper model is introduced. | Add a fill-vs-contract fact audit as a review-panel stage (per node: does the prose establish anything in `forbids`, and does it establish everything in `establishes`), since it needs a semantic judgment rather than a token metric; wire it into the automated adversarial panel of proposal section 9 rather than the deterministic gate. | open | `UW-C103` |
| AL-168 | 2026-08-09 | Model-tier study, blind three-tier prose comparison (proposal section 13) | product | Model tier moves prose quality but not sameness: across nine books from one skeleton, blind craft scores ran 4.9 (frontier), 4.0 (Sonnet), 2.2 (Haiku) with ship verdicts of as-is, light-edit, and do-not-publish, while the recognition score was 2.5 for BOTH the frontier and Haiku tiers with the same drivers named independently (keeper-plus-maker pair, four-exit hub menu). Diversity is a property of the skeleton and contract; prose is a property of the author; they are close to orthogonal. Separately, convergence was not monotonic in capability, with Sonnet the most convergent tier (29.7 shared grams per 1000) and Haiku the least (8.5), each converging its own way: Haiku near-quotes contract strings, Sonnet reaches for stock genre idioms. | Treat the catalog-diversity work and the fill-model cost decision as independent choices; set the fill model on prose grounds (Sonnet floor, with a reading-level directive for its systematic upward drift at mean FK 9.17) and use Haiku for mechanical roles it passed cleanly: selection derivation, deterministic-finding revision rounds, first-pass triage. | open | `UW-C104` |
| AL-169 | 2026-08-09 | Model-tier study, comparative vs isolated rating of the same books (proposal section 13) | process | Absolute quality judgments are systematically more forgiving than comparative ones: the same three Haiku books scored 2.2 with a rewrite verdict from a rater holding all nine books side by side, and 3.0 with a one-editing-pass verdict from a rater holding only those three. This is the project's single-reviewer problem measured directly, and it means a reviewer approving one story in isolation will pass work that a side-by-side comparison rejects. | Show a sibling fill alongside the story under review in the approval surface, and give the automated review panel a comparative rather than absolute rubric; treat isolated-rating scores as an upper bound. | open | `UW-C105` |
| AL-170 | 2026-08-09 | Model-tier study, deterministic battery across three tiers (proposal section 13) | validator | Gate-clean does not mean publishable: all nine books across three model tiers cleared fill integrity, the full validator gate, safety, topology, word bands, and title contracts on the first pass, and three of them were judged unpublishable by a blind rater for tense instability including breaks inside a single paragraph, a central puzzle that is asserted rather than performed ("the digits spell out language"), and narrator moral tags closing nearly every ending. None of those three defect classes is visible to any existing check. | Add deterministic prose-craft detectors for tense stability, narrator moral tags, and told-emotion density, calibrated so they separate the observed tiers; and state plainly in the authoring docs that the gate certifies validity, not quality. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-171 | 2026-08-09 | Prose-craft detector calibration (proposal section 13, commit `4b93b97`) | validator | The prose-craft detectors separate publishable from unpublishable books perfectly, but the separation is IN-SAMPLE by construction: every threshold (`--max-unstable-nodes 0`, `--min-tense-cues 6`, `--max-node-mix 0.34`, `--max-moral-tags 0`, `--max-told-per-1000 0.5`) was tuned against the same nine books it is reported to separate, so the result measures fit rather than predictive power. Two thresholds are already known to be fragile: the told-emotion rate ranks one unpublishable book (0.31 per 1000) BELOW one shippable book (0.35), so that detector cannot rank those two and the book is caught only by the other two detectors; and the moral-tag budget of zero is unfalsifiable on a corpus where every shippable book scored zero. | Validate out of sample before wiring the detectors into any blocking gate: score a held-out set of books that had no part in calibration, ideally from a different skeleton and band, and re-tune on the union; until then run the detectors as advisory-only and report their findings to the author rather than blocking on them. | open | `UW-C107` |
| AL-172 | 2026-08-09 | Per-child sibling-exposure analysis (`scripts/analyze_sibling_exposure.py`, skeleton-free alternatives proposal section 2b) | product | The unit of story-sameness exposure is the CHILD, not the family, and the system has no per-child skeleton history at all: `Storybook` carries only `family_id` and `personalization_subject_profile_id`, and both reads of prior `skeleton_slug` (`skeleton_match.py:587`, `diversity/history.py`) filter on family with a 20-row window. Family scoping alone triples the second-request repeat rate (0.09 to 0.29 from one child to three) while a child-scoped counterfactual reproduces the single-reader curve, and the shared window hides 81% of a child's own band reading in a three-child household. `StoryRequest.profile_id` and `StorybookAssignment.child_profile_id` already carry the needed evidence. | Scope the anti-repeat history and weighting to the requesting child rather than the family, widen or drop the 20-row window, and close the `visibility='catalog'` leak where a cross-family read is invisible to recency. Recovers 25 to 30% of the required catalog for no new content. | open | `UW-C108` |
| AL-173 | 2026-08-09 | Per-child sibling-exposure analysis (proposal section 2b) | product | A skeleton is consumable within a child but reusable across children, and nothing in the code contradicts that (no global or cross-family anti-repeat exists; `visibility='catalog'` actively amortizes one fill across families). Required catalog therefore scales with per-child lifetime consumption inside a band, roughly `tenure x rate` spread over that band's cells, and is INDEPENDENT of user count, which makes catalog growth a bounded one-time capital cost rather than a cost that grows with the business. Measured need is about 130 skeletons at 0.5 stories per child per month, 408 at 1, and 816 at 2, against 57 production-eligible today. | Treat catalog build as bounded capital and size it against measured demand; instrument per-child stories per month per band, which is the single number that sets the target and is not measured in this deployment. | open | `UW-C109` |
| AL-174 | 2026-08-09 | Per-child sibling-exposure analysis (proposal section 2b) | scale | Every populated production cell holds 3 or 4 skeletons across all six bands, so a child exhausts a cell by their 4th request in it and every later story in that cell is with certainty a tree they have already read; a repeat is more likely than not by request 3 or 4, and rotating across a band's length cells buys about two extra requests rather than an order of magnitude. This, not the skeleton format, is the measured driver of the sameness the three diversity pilots characterized, and it retires the case for replacing the skeleton architecture. | Fund catalog depth and per-child scoping ahead of any pipeline replacement; keep the skeleton-free alternatives as a designed option set to revisit only if those two fixes fail to clear the problem. | open | `UW-C110` |
| AL-175 | 2026-08-09 | Adversarial review of the skeleton-free alternatives proposal | validator | SAFE-14 (`validator/safety.py:41`) is a Phase-2 stub returning an empty report unconditionally, so `safety_flagged` cannot take a non-false value, yet "zero safety flags" was reported as evidence of safety seven times across pilots 1 to 3 and the model-tier study. The real deterministic pre-LLM denylist floor on untrusted brief material is `validator/slots.py`, whose only caller is the theme-binding path, so it runs for skeleton fills only and a skeleton-free story has no pre-LLM denylist gate. Separately, the gate is provenance-agnostic but not format-agnostic: PL-19, PL-23, CG-3, and RL-13 each parse the `<<FILL words=N>>` grammar, and `enforce_grammar` defaults off so generated stories never run CG-1 through CG-4 at all. | Stop reporting `safety_flagged` as a measured outcome until SAFE-14 is implemented, and mark it explicitly as a stub in any results table; document that the gate's strictness is caller-parameterized so cross-path comparisons state their `scale` and `enforce_grammar` settings. | open | `UW-C111` |
| AL-176 | 2026-08-09 | Adversarial review, strict-bar measurement across the catalog | validator | The `--strict` authoring bar passes 2 of 61 catalog skeletons (0 of 11 at 10-13), because it gates newly drafted skeletons while the shipped catalog is grandfathered. Two consequences were being missed: a proposal pre-registered "strict bar: pass by construction" for the baseline, which is false 97% of the time, and the pilots' own control skeleton `the-clocktower-cipher` is the catalog's single non-production-eligible `(10-13, None, None)` row, so it is never served to a child and, declaring no length or style, is held to band budgets rather than the ADR-011 cell budgets every production 10-13 skeleton faces. Every pilot baseline number was measured on it. | Decide explicitly whether the strict bar is the intended production bar (promoting it as a blocking rule would retire 97% of the catalog, a product decision); and re-baseline future diversity work on a production-eligible skeleton in a real cell. | open | `UW-C112` |
| AL-177 | 2026-08-09 | Length-demand catalog sizing (`scripts/analyze_sibling_exposure.py`, proposal section 2b.3a) | scale | Story-length demand is not flat within a band and the catalog is: every populated cell holds 3 to 4 skeletons while demand concentrates on medium. Because `skeleton_matches_cell` treats only a NULL length as a wildcard and 0 of 58 production-eligible skeletons declare one, cells are hard partitions, so surplus in the short and long cells is stranded capital that can never serve a medium request. Peaking demand leaves the band TOTAL essentially unchanged (one child's band-lifetime consumption however it splits) but raises the SHORTFALL by 23 to 32% and moves the binding constraint to the medium cell in every band, pulling the first likely repeat forward about one request (P(repeat by request 4) at 8-11 rises from 0.386 flat to 0.647 strongly-medium-weighted). | Author against the demand curve rather than filling cells evenly: put the first 6 to 12 skeletons of each band's budget entirely into that band's medium cell, which buys 0.8 to 2.2 more requests before a repeat than an even split; short and long earn marginal skeletons only past roughly request 17 to 20. Measure the real length distribution to replace the assumed regimes. | open | `UW-C113` |
| AL-178 | 2026-08-09 | Length-demand analysis, intake trace (proposal section 2b.3a) | product | Kid auto-approve hardcodes `length=Length.SHORT` band-agnostically (`api/story_requests.py:508`), which steers exactly the requests where nobody is choosing into the thinnest-demand tier, and for the teen bands it is an outright production bug: 13-16 and 16+ have no short cell by ADR-011 design and hold no short skeletons, so `candidates_for_cell` returns empty and the authoring plan 422s every time for a teen-band child with auto-approve enabled. Separately, ADR-011's band-by-length rule is enforced nowhere in the request path (the approve body validates band against style only, and both length selectors render all three lengths for every band), so a guardian can approve a long 3-5 book or a short 13-16 book and hit the same empty-cell failure later. | Make the auto-approve length band-aware (reuse the `_length_of` teen fallback rather than a constant) and default it to the demand-weighted tier rather than short; add band-by-length validation to the approve and authored-create bodies and gate the frontend length options by band, so an unservable cell is rejected at intake rather than 422ing at planning time. | open | `UW-C114` |
| AL-179 | 2026-08-09 | Per-request mutation pilot on `the-midnight-museum` (alternatives proposal section 11.5.1) | scale | Bounded mutation of a single parent is not a catalog multiplier, and the reason generalizes: every accepted mutant preserved 100% of the parent's `<<FILL>>` beat directives (95 of 95 nodes and beats across all three mutants), so mutation moves the graph while recognition is driven by the scene content and decision semantics that live in the beats. Shape-preserving operators scored structural distance exactly 0.0000 (M1 on all five swap pairs tried, including cross-act), the shape-changing M4 reconvergence variant reached 0.0038, and a maximum-length chain using only the parent's own material reached 0.0064, all below the committed `TAU_CELL` of 0.05, while hand-authored same-cell siblings sit at a median of 0.390. A rater landed the same-book verdict at reading position 3 (score 2.0) with the structural difference invisible until position 8. Applicability was not the constraint: M4 had 2,201 eligible sites. | Withdraw per-request mutation as a diversity lever and fund catalog depth instead; if a structural multiplier is still wanted, test cross-skeleton recombination (M3 graft) separately, since the only mutant to clear the floor did so by importing 32 nodes from a different skeleton, which scales with pairs of existing skeletons rather than replacing the need for them. | open | `UW-C115` |
| AL-180 | 2026-08-09 | Per-request mutation pilot, deterministic bench (alternatives proposal section 11.5.1) | validator | `structural_distance` and the committed `TAU_CELL` floor were vindicated on this evidence rather than refuted: the floor rejected exactly the mutants the reader rejected, and the metric's ordering matched the reader's ordering. The metric's known order-blindness and self-declared topology weighting did not bite because no bounded mutant came within an order of magnitude of the threshold. This is not a general exoneration, since the failure mode it cannot see (identical scene function at identical position) is precisely what a mutant preserves. | Keep `TAU_CELL` as the anti-clone gate for catalog-time work, and continue to treat `structural_distance` as unsuitable for judging perceived diversity between artifacts that already share authored content; revisit only if a candidate approaches the threshold. | open | delivered on the story-diversity work, currently OPEN on PR #661; the earlier SHAs live only on a scratch branch that will never merge, so this flips to `applied` on the merge commit |
| AL-181 | 2026-08-09 | Per-request mutation pilot, sibling-fill measurement (alternatives proposal section 11.5.1) | process | A sibling-convergence measurement is invalid when one author writes both arms: the second book's first draft was a sentence-level world swap of the first at 302 shared grams per 1000, and two deliberate de-convergence passes only brought it to 70.4, still an order of magnitude above the 4.0 budget and roughly three times the worst previously measured arm. The number therefore measures the author's de-convergence effort rather than the condition under test. Separately, the recognition rating could not be run in an isolated agent in this environment, so it is author-scored rather than blind. | Require an independence protocol for any sibling-fill arm: one isolated author per arm with no sight of the other arms, and a rater that has not seen the generation plan. Where isolation is impossible, report the measurement as author-scored and treat it as an upper bound on distinctness. | open | `UW-C116` |
| AL-182 | 2026-08-10 | Obligation-variance experiment, contract variant review (`docs/planning/evidence/obligation-variance/`) | process | An admissibility bar that enumerates fields silently exempts every field it omits. The bar for a contract variant required divergence in 2 of `establishes`, `choice_semantics`, `affect`, and `function`; two variants passed it at 26/26 nodes on all three pairs while their `beat_hint` strings were the shipped contract's sentences with single nouns swapped, byte-identical at `n_end_library` across all three variants, at or above 0.85 similarity at 10 of 26 nodes against v1, and above 0.60 at 22 of 26. `beat_hint` is the most direct instruction a fill agent reads, so the intervention the experiment exists to test was largely undelivered; because a null result was the expected outcome, the run would have been uninterpretable, since "obligation variance does not work" could not have been separated from "the obligations were never varied." | Gate variant admissibility with `check_variant_divergence.py --check`, which adds a `beat_hint` similarity ceiling (0.60 difflib) against every other variant including the shipped one. More generally, when a bar enumerates fields, state which fields it does NOT cover, so an exemption is a decision rather than an oversight. | open | `UW-C120` |
| AL-183 | 2026-08-10 | Obligation-variance experiment, blind recognition rating (spec section 12) | product | Obligation variance is refuted as the diversity lever, and the refutation identifies the actual fingerprint: recognition landed at position 2 against a pre-registered margin of past position 5, with distinctness 2.0 against the control's 2.5, while all six quality guards passed. Both contracts varied `choice_semantics` completely at the landing nodes, yet the blind rater recorded the same fork in the same order at position 2 and the same four-room fan at position 5, because a choice's DESTINATION is graph structure rather than contract content: choice 1 still leads to the note-decoding scene, and both authors must still write a stair, a study, a catwalk, and a basement. This unifies the prior nulls rather than adding one, since devices change the nouns in the rooms, prose the wording, model tier the quality, and obligations why the rooms matter, while none of them changes the rooms. | Stop searching for a format fix inside a single graph; treat single-use per child as inherent to a shared armature. Fund catalog depth per cell and evaluate cross-skeleton recombination, the only mechanism that ever cleared the anti-clone floor. Record in the drafting guide that per-node obligations cannot vary what a reader perceives at a fork. | open | `UW-C121` |
| AL-184 | 2026-08-10 | Obligation-variance experiment, residual menu-frame collision (spec section 12.5) | validator | Unslotted choice labels are a recognition channel no check covers. 13 of the clocktower skeleton's 35 choice labels carry no slot token (`Climb the spiral stair.`, `Spin the hands at random and hope.`), so they are byte-identical across every binding; NC-8 warns about unslotted ENDING titles and is silent on choice labels, even though choice-menu semantics was already named as the residual leak in `AL-166`. Observed live: two isolated authors on different contracts and different settings both rewrote `Climb the spiral stair.` to `Head up the spiral stair.`, because the fixed destination noun left only the verb free. | Extend the NC-8 unslotted warning from ending titles to choice labels, and add a per-binding label-divergence check so a label whose destination noun is frozen is flagged at contract time rather than discovered by a sibling-gram round. | open | `UW-C122` |
| AL-185 | 2026-08-10 | Obligation-variance experiment, device collision the bench missed (spec section 12.5) | validator | Mechanic Divergence is category-scoped and blind to cross-category device collisions. MD scored the two arms' bibles 0.978 against tau 0.34 with no within-category kind overlap, while arm C's `cipher_hint_carriers/pattern_in_mechanism` ("long, long, short") and arm D's `cipher_forms/rhythm_code` ("read each pull as long or short") are the same device; the blind rater caught it unaided ("the two books' secret codes are the same code"). A metric that compares multisets within a category cannot see a device that moved category. | Add a cross-category pass to `check_bible_diversity`: compare every category of one bible against every category of the other for semantic collision, or at minimum flag shared distinctive tokens across category boundaries, so MD cannot certify 0.978 for two books sharing a cipher. | open | `UW-C123` |
| AL-186 | 2026-08-10 | Obligation-variance experiment, sibling-gram guard misfire (spec section 12.2) | process | A calibrated budget carries a protocol stage with it, and comparing across stages manufactures a false failure. The 4.0 shared-grams-per-1000 budget is calibrated on POST-revision output because the control pair went through a gate-revise-regate round (`AL-165`); gating un-revised treatment output against it reported a guard failure at 6.4 when the matched-stage comparison was control 9.0 pre-revision against treatment 6.4 pre-revision, so the treatment pair actually started LESS convergent than the control. Separately, `check_sibling_fills --check` gates on grams only, so the spec's margin of 0 menu frames was going unenforced entirely. | State the protocol stage alongside every calibrated threshold, and have comparison batteries report both stages for both arms rather than gating a single number. Gate menu frames explicitly rather than relying on reading the report. | open | `UW-C124` |
| AL-187 | 2026-08-10 | Obligation-variance experiment, arm C isolation disclosure and arm D discretion (spec section 12.6) | process | Isolation between parallel authoring agents leaks through the shared filesystem, and identical briefs still produce asymmetric effort. Arm C ran `ls` on the shared work directory and saw the other arm's filenames before writing (a tool-call audit confirmed it read no content, and the brief had already disclosed a second author, so the exposure was immaterial); more consequentially, arm D volunteered a sentence-splitting pass that cut its reading-level warnings from 19 to 2 while arm C left its 20 standing, so the two books differ in sentence rhythm for reasons unrelated to the treatment. | Give each parallel authoring agent its own output directory so a directory listing cannot reach a sibling, and specify revision passes explicitly in the brief (either required of both arms or forbidden to both) rather than leaving them to agent discretion. Audit agent tool calls rather than accepting self-reported isolation. | open | `UW-C125` |
| AL-188 | 2026-08-10 | Decision-variance experiment, blind relabelling (`docs/planning/decision-variance-experiment-spec-2026-08-10.md` section 8.2) | process | A metric computed from self-declared labels cannot be measured by the party declaring the labels. An author asked to minimise decision overlap, scored on a signature block it also authored, moved tradeoff reuse from 0.929 to 0.179 and passed every gate; an independent annotator applying one written principle to the same two artifacts scored the identical pair at 1.000. Inspection showed the drop came from relabelling rather than rewriting: `n_clockface`'s four choices read "answer the test on its own terms" against "set the levers on the board's own terms", the same four decisions with different nouns, while `c_face_random` was reclassified PHYSICAL_RISK to MORAL and `c_face_panel` INFORMATION to CRAFT. The author was not cheating, it was optimising what it was scored on. | Annotation must be independent of authoring for any signature-based diversity metric. Applies directly to the externally proposed decision-program compiler, which would both emit DecisionSignatures and be scored on them; that design needs a separate scorer or a derived-from-artifact labelling step. | open | `UW-C126` |
| AL-189 | 2026-08-10 | Decision-variance experiment, instrument construction (spec section 8.2) | validator | A free-text action field makes an exact-repetition bar vacuous, because the surface changing while the decision does not IS the defect being measured. Two contracts scored 0 of 35 identical action strings while a blind annotator judged 34 of 35 the same decision: `set_the_dial_deliberately` against `set_the_levers_deliberately` is one act and two strings. Separately, six of eighteen nodes offered a single option; a one-option node is a page turn rather than a decision, and including them pulled the aggregate family rate to 0.486, inside its 0.6 ceiling, while `n_vault` and `n_setjam` sat at 1.00 and the three principal forks at 0.67 to 0.75. | Compute the hard bar on a normalized act (action family, target role, tradeoff) rather than on a written string, exclude single-option nodes from any decision metric, and gate the worst fork alongside the mean so an aggregate cannot pass on the strength of nodes that ask nothing of the reader. | open | `UW-C127` |
| AL-190 | 2026-08-10 | Decision-variance experiment, two authoring attempts on a fixed graph (spec section 8.3) | product | Decisions appear to be pinned by the graph together with fact-graph closure, not merely by authorial habit. Two attempts to author genuinely different decisions over one unchanged 26-node graph in one unchanged world, the second explicitly instructed to move tradeoff and consequence rather than the verb and iterated against a metric, produced 28 of 28 options that a blind annotator called the same decision. The single fork that did move, `n_open`, is the one with no incoming constraint. The mechanism is that each fork's children must deliver what their own descendants require and merge closure propagates those requirements backward, so the kind of thing a branch accomplishes is fixed even when every prop and sentence changes. | Treat the verifiability/freedom tension as evidenced rather than hypothetical: a shape-only skeleton is decision-free only if its entry and exit facts are also free, and free facts cannot be merge-verified before prose. A decision-program compiler must therefore generate the fact graph and the decisions together rather than assigning decisions onto a fixed fact skeleton. Retest on a production-eligible graph before treating the magnitude as settled. | open | `UW-C128` |
| AL-191 | 2026-08-10 | Decision-variance experiment, run termination (spec sections 8, 8.5) | process | Terminating an experiment at the point the intervention cannot be constructed is cheaper and more informative than running it to a rating. This run stopped before any fill because blind annotation showed the treatment was not delivered, which saved two fills and a rating and produced a sharper finding than a null rating would have: not that varying decisions fails to help, but that varying them on a fixed graph is much harder than assumed. A null rating would have been uninterpretable in exactly the way AL-182 describes. | Add a delivery check to every intervention spec: an independent verification, before any measurement is spent, that the intervention actually differs from the control on the dimension it claims to vary. Make it a standing precondition rather than a per-experiment afterthought. | open | `UW-C129` |
| AL-192 | 2026-08-10 | Metric build, retention exemption review (`supabase/migrations/20260810000000_*`, `publishing/state_machine.py`) | tooling | A docstring's claim about the codebase rots, and reasoning from it silently produced a wrong production predicate. `publishing/state_machine.py` said the `draft --auto_reject--> needs_revision` hop "has no slice-1 caller"; slice 2 landed and `moderation/pipeline.py` drives it on a classifier hard BLOCK. An agent building a data-retention exemption read that docstring, concluded `needs_revision` implies a human send-back, and wrote a predicate that would have preserved every machine-rejected story's raw LLM output indefinitely: widening ADR-007's retention window with no human decision to justify it, and filling the calibration corpus it existed to build with rows carrying no reviewer judgment. The correct key is the `SENT_BACK` pipeline event, which only the human path writes. | Fixed the docstring and keyed the exemption on the event. More generally, a docstring asserting that something has no caller is a claim that must carry a `#VERIFY` and be re-checked by grep before anything is built on it; prefer keying policy on an event that only the intended actor emits rather than on a status any actor can reach. | open | `UW-C130` |
| AL-193 | 2026-08-10 | Metric build, inter-annotator agreement measurement (`docs/planning/decision-signature-labelling-principle.md`) | process | Independent annotation of decision signatures is reliable, which corrects the diagnosis implied by AL-188. Two annotators given only a contract and a written convention, with no knowledge of what was being compared, reached Fleiss' kappa 0.796 on action_family, 0.816 on target_role, and 0.758 on both tradeoff and consequence over 35 choices, all clearing the 0.60 floor. The earlier 0.179-versus-1.000 swing was therefore never annotator unreliability; it was one annotator having authored the artifact and being scored on the result. As a free by-product, both annotators independently raised the same three vocabulary gaps (no family separating searching a mechanism from reading a document, no public-versus-private tradeoff axis, and `consequence: LOCATION` doing double duty for arrival and progress), which is stronger evidence for a v2 vocabulary than either alone. | Invest in independence and a versioned written convention rather than in a richer vocabulary or a stronger annotator model, and treat the externally proposed signature architecture as viable on that condition. Take the three converged vocabulary gaps as the starting point for v2, and re-measure kappa on any version bump since a rule change invalidates agreement measured under the old one. | open | `UW-C131` |
| AL-194 | 2026-08-10 | Metric build, branch-obligation checker (`scripts/check_branch_obligations.py`) | tooling | A deterministic property of the plan reproduced two results that had each cost a full measurement round. Comparing, per branch, the facts a destination presupposes that its fork does not already guarantee, the checker scores same-shape rate 1.000 on the pair a blind annotator judged 28 of 28 identical decisions, and 1.000 again on the earlier pair a human rater put at recognition position 2. It also surfaces the mechanism, seven one-for-one fact renames such as `past_the_seal` to `past_the_barred_door`. It needs no model, no annotation and no prose, so it runs in milliseconds before a fill is committed. Its limits are real: it was validated on two known-bad pairs with no known-good example, so it is a screen rather than an arbiter and its threshold is uncalibrated. | Run the deterministic screen before spending any measurement on an artifact, and look for a computable-from-the-plan proxy before commissioning a fill or a rating in future experiments. Calibrate the threshold once a genuinely divergent contract exists, and keep independent annotation as the arbiter. | open | `UW-C132` |
| AL-195 | 2026-08-10 | Decision-variance experiment, v5 arm fill shell (`docs/planning/decision-variance-experiment-spec-2026-08-10.md` section 9) | tooling | A fill shell that pre-binds choice labels hands the author the previous book's vocabulary, and the author stays inside it while rewriting every word. Because 13 of 35 skeleton choice labels carry no slot token (`AL-184`), binding produced clocktower-dial wording in a salvage story, and the author changed 35 of 35 labels yet still wrote "Force the hands into place instead" and "Spin the hands and hope for the best" against the shell's "Force the hands by guesswork" and "Spin the hands at random and hope". Every deterministic guard passed while sibling grams hit 28.8 per 1000, the highest ever measured, with 4 shared menu frames. Rebuilding the shell with every label as a FILL directive and refilling took the same pair to 8.9 and 0. | A shell handed to a fill agent must express every author-controlled string as a FILL directive and carry no skeleton prose at all. The act a choice performs belongs in the contract's `choice_semantics`, which states it without dictating the nouns. Gate this: a shell builder should refuse to emit a label that is not a FILL directive. | open | `UW-C133` |
| AL-196 | 2026-08-10 | Decision-variance experiment, v5 arm rating (spec section 9) | tooling | Device collision between two books in one world dominates a reader's verdict and can invalidate a whole measurement round. The treatment binding shared 14 of 24 props with its control, and two blind raters reading in opposite orders both named exactly those props as decisive, one adding that stripping two of the affected forks would have made the pairs indistinguishable. Only 6 of the 14 sat at the same node; the other 8 were the same props relocated, which a same-slot diff scores as fresh and which a rater described unprompted as variety fork by fork but pure rearrangement at the book level. Sharing a world is the series contract and must never be penalised; sharing the props inside it reads as the same book. | Built `scripts/check_device_collision.py`, which compares every prop against every prop rather than slot against slot, so relocation is caught, and flags rare shared vocabulary so a device reworded into another category is caught too (it fires on the `AL-185` rhythm pair that `check_bible_diversity` certified at 0.978). Run it as a standing pre-fill gate on any sibling binding. | applied | `scripts/check_device_collision.py`, calibrated at 0.583 on the known-bad pair and 0.000 on the known-good pair |
| AL-197 | 2026-08-10 | Decision-variance experiment, v5 contract (spec section 9) | validator | The branch-obligation screen has a measured false negative, which corrects the framing in `AL-194`. A third contract over the same graph, written to break the obligation structure, scored same-shape rate 0.000 with zero one-for-one renames, meaning the fact graph was rebuilt rather than repainted. Blind independent annotation of that contract against the original still returned 28 of 28 options asking the same decision, with action-family, tradeoff, consequence and ordered-sequence rates all 1.000, worst fork `n_backpanel`. A high same-shape rate is therefore strong evidence the decisions repeat; a low one is no evidence at all that they differ. This also refutes the fact-graph-closure explanation in `AL-190`: closure was broken and the decisions did not move. | Treat the screen as one-way, for cheap rejection only, and never as a delivery certificate; independent annotation stays the only delivery check for a decision-variance intervention. Docstring corrected in place. The surviving explanation to test next is that the decision space at a fork is genuinely small (learn something, spend physical effort, or ask a person), which no rearrangement over a fixed graph can move. | applied | `scripts/check_branch_obligations.py` |
| AL-198 | 2026-08-10 | Decision-variance experiment, rating instrument (spec section 9) | process | The five-question instrument is mostly dead weight on a shared skeleton, and two blind raters found the same missing question without conferring. Q1 (same kinds of actions) and Q4 (repeated sequence) both saturated at 5 for both pairs, because with one node skeleton across all three books those answers are fixed before a word is read; Q2 came out a wash or mildly against the eventual verdict, leaving the whole judgement resting on Q3. Both raters independently proposed the same sixth item, whether solving the first book gives you the answer to the second, and both said it would have separated the pairs immediately. One reported having to smuggle it into Q3. | Add a solution-transfer question to the instrument and treat it as the primary item, since it is the only one that discriminated. Retire or demote Q1 and Q4 whenever the books under comparison share a skeleton, and record why, so a saturated item is never read as a null result. | open | `UW-C134` |
| AL-199 | 2026-08-10 | Decision-variance experiment, rating instrument (spec section 9) | product | Two blind raters independently reported a per-book defect that the pair framing structurally hides: the `n_inside` four-room fork has no consequence in any of the three books, every room yields the same pattern and every road reconverges at `n_clockface`, and roughly seven of nine forks on this pilot graph either do not branch or reconverge one node later. Because the defect is identical in all three books it scores the same everywhere and cancels out of every pairwise comparison, so a diversity programme measuring only pairs can run indefinitely without seeing it. Both raters then attached a product judgment to that structural fact, calling it illusory choice and a likely threat to the reading experience. The owner rejected that judgment: reconverging exploration paths are a convention of the form. The lesson is therefore about raters over-reaching from an accurate structural observation into a product call that is not theirs, which is the same class of error as the earlier misreading of the central defect. | Add a per-book illusory-choice check before any pair comparison: flag every fork whose branches reconverge with no difference in what they deliver, computable from the contract as branches whose obligations are equal and whose subgraphs rejoin within N nodes. Report it per book, never per pair. | rejected | owner ruling, `decision-variance-experiment-spec-2026-08-10.md` section 9.9: loop-back exploration is a convention of the form, not a flaw, on the tabletop analogy that sweeping every room is the play. The structural observation stands; the defect framing does not, and no gate will be built. |
| AL-200 | 2026-08-10 | Decision-variance experiment, second attempt (`docs/planning/decision-variance-experiment-spec-2026-08-10.md` section 9.5) | validator | The decision-signature instrument ranks two book pairs in the opposite order from readers, on every axis. One blind annotator labelling all three plans scored the control pair 24 of 28 same decisions (family 0.929, tradeoff 0.893, sequence 0.909) against the treatment pair's 28 of 28 and 1.000, while two blind raters in counterbalanced order both judged the treatment pair the LESS decision-repetitive of the two. Replicated: two further independent annotators put the control at 24 and 23 of 28. Unreliability does not explain it, since kappa between annotators on the same artifacts is 0.961 and 0.962 on `action_family` and 1.000 on `consequence`. `AL-193` established reliability; this establishes that reliability was not sufficient. | Stop treating any v1 signature field as decision-carrying: report only, route nothing, until a vocabulary is validated against reader judgement rather than against another annotator. The externally proposed decision-program compiler must not be built to emit and optimise v1 signatures, since on this evidence it would optimise away from what readers prefer. | applied | `docs/planning/decision-signature-labelling-principle.md` validity section |
| AL-201 | 2026-08-10 | Decision-variance experiment, signature diff (spec section 9.6) | validator | The inversion has an exact and fixable cause, and it runs in both directions at once. Deaf where it counts: at the three forks both raters independently named decisive (`n_clockface`, `n_vault`, `n_setjam`), all three plans carry identical signatures choice for choice, because "compute a value and dial it" and "match a shape against a full-size drawing and seat the part" are one signature and two kinds of thinking. Over-sensitive where it should not be: the control pair's entire measured advantage comes from four entry forks where a change of world turned "get past the building" into "read what the building remembers", moving PHYSICAL_RISK/BARRIER/ACCESS to INFORMATION/LOCATION/KNOWLEDGE with no change of act. The annotators were faithful; the scenery had already entered the plan's own `choice_semantics`. | Add a reasoning-kind dimension (compute, match, recall, infer, negotiate, exert) to the v2 vocabulary, ahead of the three gaps in `AL-193`. Extend the labelling principle's Rule 2 upstream to whoever writes `choice_semantics`, since a rule binding only the annotator cannot help when the plan describes one act in two vocabularies. | open | `UW-C136` |
| AL-202 | 2026-08-10 | Decision-variance experiment, rating (spec sections 9.3, 9.4) | product | Varying the act a choice asks for does reduce perceived decision repetition, thinly, and the reader-invented question is what detects it. Two blind raters in counterbalanced order both chose the control pair as more decision-repetitive, agreeing exactly on Q2 (5,5 against 4,4), Q3 (2,2 against 3,3) and Q6 (4,4 against 3,3), and both named the same three decisive forks. The result holds against a stacked deck: the treatment shares the base book's entire world while the control changes world wholesale, and one rater noted the verdict would flip if shared world counted, which the owner's definition says it must not. Q6, solution transfer, was proposed independently by both raters of the discarded round and on first use discriminated cleanly while Q1 and Q4 saturated at 5 for both pairs exactly as they predicted. Both raters called the effect thin: three re-skins of one graph, separation resting on three forks of eleven. | Treat solution transfer as the primary construct for the diversity programme rather than action-family overlap, and retest on a production-eligible graph before treating the magnitude as real. Separate the two changes the treatment bundled, different decision content and four rooms yielding distinct components, since one rater credited the latter explicitly and they are confounded here. | open | `UW-C137` |
| AL-203 | 2026-08-10 | Diversity test register, D-3 (`docs/planning/diversity-test-register.md`) | validator | The signature instrument was annotating the wrong artifact, which is a layer error rather than a vocabulary error. Adding `reasoning_kind` and `stake` to the annotation of the contracts fixed nothing: `reasoning_kind` inverted the reader ordering exactly as `action_family` had (control 0.929 against treatment 1.000) and `stake` tied at 1.000, with zero of six fields ordering the pairs as readers did. The reason is verifiable without any annotator: the contracts describe the decisive fork as "answer the test on its own terms, set it deliberately" and "fit the piece the way the diagram shows, deliberately", which Rule 2 of the labelling principle correctly calls one decision. The distinction both raters named, adding two numbers and carrying against holding a part against its outline, lives in the binding (`clock_arithmetic`, `rhythm_code`, `pictogram_code`), not in the contract. This also partly retracts `AL-201`: the vocabulary was not deaf at the climax, the contract was silent there and the vocabulary reported that silence faithfully. The scenery-leakage half of `AL-201` stands. | Annotate contract plus binding rather than contract alone, and re-specify any architecture that plans decision variety at the contract layer. Three of the proposed architectures (reviewer 2's decision-program compiler, reviewer 1's decision-first abstract routing, and the in-house decision-axis scheduling) schedule decisions in a representation that provably omits the property readers respond to, which is a defect in those designs and not only in our instrument. | open | `UW-C138` |
| AL-204 | 2026-08-10 | Diversity test register, D-3b and D-3c (`docs/planning/diversity-test-register.md`) | validator | Annotating the binding alongside the contract recovers the property readers respond to, and the recovery is real but fragile. With each plan's bound devices attached, `reasoning_kind` becomes the only field of six that orders the two book pairs as readers did, and the pre-registered primary (that field over the six forks the book's code runs through) passed under both annotators, as did the prediction that the entry forks would invert. But the magnitude collapsed on replication, 1.000 against 0.000 for one annotator and 0.333 against 0.167 for the other, and over the full option set the second annotator ties. The cause is one underspecified vocabulary boundary: kappa on `reasoning_kind` is 1.000 on the arithmetic plan and 0.919 on the pictogram plan but 0.719 on the rhythm plan, because decoding a symbolic notation sits between COMPUTE and MATCH and the definitions do not say which. That single call decides whether the control's two books share a reasoning kind. | Write the COMPUTE/MATCH boundary explicitly for symbolic decoding, and treat the rewrite as a reliability fix only. Re-scoring these same three plans under a sharpened rule cannot re-confirm the hypothesis, because the sharpening determines the decisive item by construction and a motivated author would choose the direction that rescues the result. Validity has to be retested on artifacts the vocabulary has never seen. | open | `UW-C139` |
| AL-205 | 2026-08-10 | Diversity test register, commit `2b854d5` | process | Chaining a file edit and a git commit in one shell invocation lets a failed edit commit anyway, and the commit message then describes content that is not in the diff. A Python heredoc doing the edit raised `AssertionError` on an anchor that no longer matched; because the commit was a separate command in the same invocation rather than chained to the edit's exit status, it ran and pushed three label files under a message claiming to record a result and pre-register the follow-up analysis, neither of which was present. Caught on the next read of the file, corrected by a follow-up commit rather than a history rewrite, since the bad commit was already pushed. | Never chain an edit and its commit in one invocation. Verify the staged diff against the intended message before committing, and make anchor-based edits fail loudly enough to stop the sequence. This matters more than ordinary commit hygiene here because these commits are the evidence record for experiments whose results nobody can reproduce from memory. | open | `UW-C140` |
| AL-206 | 2026-08-10 | D-2 fills, arm P (`docs/planning/diversity-test-register.md`) | process | The presence of an agent's output file is not evidence the agent has finished, and acting on one mid-run silently corrupts the work. Arm P's `filled_P.json` existed on disk and parsed cleanly, so it was read, verified, and character-renamed; the author then kept working and rewrote the same path four minutes later, making the renamed copy a derivative of a draft that no longer existed (10,608 words against the final 10,504). The author's own report flagged the orphan file it had not created, which is how it was caught. It was also reported upward as "all three fills are in" while one was still writing. | Wait for the completion notification before reading, verifying or transforming any agent artifact, and never infer completion from a file existing. Where a derived copy is made, record the source's modification time alongside it so staleness is detectable rather than invisible. | applied | rename redone against the final files; `renamed_P.json` rebuilt and verified structurally identical to the shell |
| AL-207 | 2026-08-10 | D-2 fills, cross-arm convergence (register, D-2 section) | product | Same-model idiom convergence reaches proper nouns and titles, not just phrasing, and it survives strict isolation. Three authors in separate directories, given one device-agnostic contract and three deliberately non-colliding bindings, all named the companion `Teo` (38, 61 and 47 uses), two named the protagonist `Nora`, and all three titled the book identically. Three ending titles collided across arms, one of them (`Goodnight Through the Door`) in all three at the same node. Grepping every input file confirmed `Teo` and `Nora` appear in none of them, so the name convergence has no leak to explain it. The title convergence does: the shell still carried the skeleton's `id`, `sk_midnight_frequency`, which every author read and named its book from. | Neutralise every identifying field in a fill shell, not just the prose fields: `id`, `title`, metadata and slugs all leak. Treat cross-arm name and title collision as a measurable convergence signal in its own right and check it before rating, since it is invisible to the shared-gram measure that is supposed to catch convergence. | open | `UW-C141` |
| AL-208 | 2026-08-10 | D-2 guard battery (`docs/planning/diversity-test-register.md`) | product | **Sharing one narrative contract across books makes the contract's own wording the fingerprint.** Three isolated authors writing from one contract and three non-colliding bindings produced 653 and 717 shared 4-grams per pair, 59.2 and 63.8 per 1000 words against a budget of 4.0, and 51 and 41 identical choice-menu openings out of 131. The pilot, whose books scored 1.8 to 2.7 per 1000 and zero shared menus, differed in exactly one respect: its arms read *different* contracts, whose `choice_semantics` were 0 of 35 identical, where D-2's three arms read 131 of 131 identical. The first diagnosis, that the contract was lexically over-prescriptive, was tested and refuted: labels reuse a distinctive word from their own semantics at 46.6 percent in D-2 against 54.3 percent in the pilot, so per-book reuse is *higher* in the non-converging case. It is sharing, not prescriptiveness. | Any architecture that reuses one plan across many books must treat the plan's own prose as a convergence source and either neutralise `choice_semantics` wording, generate it per book, or require authors to diverge from it explicitly. This bears directly on every proposed architecture built on a reusable plan object. For D-2 specifically the arm design was wrong: it mirrored the pilot's arm structure while varying only bindings, where the pilot varied contracts. | open | `UW-C142` |
| AL-209 | 2026-08-10 | D-2 guard battery, reading level | validator | Reading level degrades badly at production scale and the gate cannot stop it, because every finding is advisory. All three 101-node books landed at whole-book Flesch-Kincaid 8.14 to 8.41 against a 5.5 target with 1.5 tolerance, with only 16 to 20 of 101 nodes inside the band and 81 to 85 RL-13 warnings each, yet all three returned `blocked=False`. The pilot's 26-node books ranged 4.99 to 7.45 on the same measure. Model tier is a candidate but not established: one pilot book written on the stronger tier also sat out of band at 7.45, so length and node count are confounded with tier. | Make aggregate reading level a blocking condition rather than 85 advisory warnings nobody reads: a book with four fifths of its nodes outside the band is not age-appropriate whatever the per-node rule says. Separately, test tier against scale directly before attributing this to the cheaper model. | applied | `scripts/check_reading_level.py` |
| AL-210 | 2026-08-10 | D-4, `scripts/check_solution_transfer.py` | validator | **A rarity threshold calibrated on a large corpus silently inverts when applied to a subset.** The device-collision check treats a word used by at most two props as rare, calibrated over 24-prop bindings. Reused inside a 6-prop and then a 2-prop solution chain, the corpus shrinks to four props and very nearly every content word qualifies, so the signal fires on everything. It reported three plainly different waypoint marks, a scratched star, a painted spiral and an inked triangle, as the same device, on the shared words `logbook's`, `mark` and `margins`, which are the contract's own framing for that slot rather than any book's device. | Judge rarity over the whole artifact a threshold was calibrated on, never over the subset being scored, and state the corpus explicitly in any measure that inherits a calibrated threshold. Audit every other measure that reuses a calibrated constant on a filtered input. | open | `UW-C144` |
| AL-211 | 2026-08-10 | D-4, operation tiers against the 101-node bindings | validator | **A word-list classifier for "what kind of thinking does this puzzle ask" ranks on the artifacts it was written from and collapses on any other.** It reproduced three known reader rankings on the pilot, then classified 2 of 6 chain props on an unseen contract. Two failure modes, neither fixable by adding words: no negation, so "a page of small hand-drawn icons instead of numbers" scores as arithmetic; and polysemy, so "a short tail" on a drawn symbol scores as rhythm. The one cross-arm match it did report was an artifact of the slot, since every waypoint mark in that world is a drawn symbol. This is the second measure in this programme good enough to rank and not good enough to gate, after the fill-fidelity triage. | Stop building lexical proxies for semantic properties in this programme; the expected outcome is now a ranking aid, not a gate. Where a property must be checkable, declare it as a field in the narrative contract rather than inferring it from the prose: the plan schema needs an explicit operation field. | open | `UW-C145` |
| AL-212 | 2026-08-10 | D-6 rig construction (`docs/planning/evidence/d6-contract-sharing/build.py`) | product | **Choice semantics cannot be neutralised mechanically from the fact graph, because the fact graph does not contain the decision.** Deriving each branch's semantics from the facts its destination presupposes needs no author and so cannot smuggle in a voice, which is why it was tried first. It also destroys the fork: at the pilot's central four-way decision, answering the code, forcing the dial, going round the back and guessing at random all own exactly the same obligation and flatten to one identical sentence. This is `AL-197` reached from the opposite direction, and it means the D-2 repair that looked cheapest and most objective is unavailable. | Any "flatten the plan's wording" repair must be authored under a stated rule and treated as itself under test, not derived. More generally, stop proposing fact-graph derivations for properties that D-3 and `AL-197` have already located above the fact layer. | open | `UW-C146` |
| AL-213 | 2026-08-10 | Q-1 catalog count (`docs/planning/diversity-test-register.md`) | process | **The catalog's depth was being counted in the wrong unit, and nobody had counted either.** 61 skeletons over 17 band-by-length cells, 13 of them holding four or fewer, so the exhaustion premise holds. But 2 of 61 skeletons carry a narrative contract, which is the artifact every measure in this programme and every proposed architecture actually needs, at roughly 1.7KB per node of hand authoring. Whether that cost is per skeleton or per book is decided by `AL-208`, so a research result decides which capital purchase this is. | Track narrative-contract coverage as a first-class catalog metric alongside skeleton count, and do not scope any depth purchase until the contract-sharing question is settled. | open | `UW-C147` |
| AL-214 | 2026-08-10 | D-6 (`docs/planning/diversity-test-register.md`) | product | **Contract sharing is confirmed as a cause of cross-book convergence, and neither cheap repair fixes it.** Holding graph, bindings, model and isolation constant and changing only whether two arms read one contract or two moves shared 4-grams from 2.9 to 16.9 per 1000 against a budget of 4.0. Flattening the shared wording reaches 11.4 and instructing authors to diverge from it reaches 12.9, both still around three times budget; at one pair per condition their ordering flips depending on whether labels are counted, so neither is better, both are insufficient. | Treat `AL-208` as established and stop proposing wording-level repairs as sufficient. The untested third repair, generating the decisional stratum per book, is now the only candidate left and is predicted to fall short alone. | open | `UW-C148` |
| AL-215 | 2026-08-10 | D-6 gram attribution (`docs/planning/diversity-test-register.md`) | product | **The convergence leak has at least four channels and `choice_semantics` is not the main one.** Tracing shared grams to the contract field they draw on: `choice_semantics` under half, `beat_hint` similar, **`premise` as much or more**, and about a quarter tracing to no contract field at all, which is same-model idiom (`AL-207`) that no plan-level intervention reaches. Categories overlap, so this is indicative rather than a partition. Two consequences: a reusable plan that shares the dramatic question is already sharing the largest traceable channel, and there is an idiom floor bounding what a shared-language guard can ever certify. | Move `premise` into the per-book stratum alongside `choice_semantics` and `beat_hint`, and set the shared-gram budget for reusable-plan architectures with the idiom floor measured rather than assumed. Amend the architecture re-specification, which put the premise on the shared side. | open | `UW-C149` |
| AL-216 | 2026-08-10 | D-6 confound check | process | **A cross-experiment comparison inherited a second difference that nearly invalidated it, and the check that saved it was cheap.** The pilot's shells shipped draft choice labels, 13 of 35 identical across its arms, where D-6's authors wrote every label from scratch, so the baseline and the treatment differed in two ways rather than one. Splitting the measure by prose part settled it in one run: labels contribute zero shared 4-grams in every condition including the pilot, so the whole signal is in bodies, which were authored from scratch throughout. | Before comparing a new run against a historical baseline, diff the two authoring *tasks* and not only the artifacts, and where a confound is found try to partition the measure rather than caveating the result. | open | `UW-C150` |
| AL-217 | 2026-08-10 | D-6 addendum, generator idiom floor | product | **The shared-gram budget of 4.0 was never validated against what the generator can actually achieve, and it turns out to be just above it.** Measured on book pairs sharing nothing but the model and the age band (different graph, different contract, different world), the floor is **3.3 shared 4-grams per 1000**, range 1.9 to 5.0. Two consequences neither of which was known: the budget is reachable, so no architecture fails it on a technicality; and the pilot's one-contract-per-book design scores 2.9, indistinguishable from books sharing nothing, so **not converging is already a solved problem** and the only open question is whether reuse can be bought back. D-6's repairs sit at 3.5 times the floor, so they failed with a factor of three of headroom unclaimed rather than against a hard limit. The floor also corroborates the D-6 gram attribution independently: a quarter of 16.9 is 4.2, against a directly measured 3.3. | Record 3.3 as the measured floor beside the 4.0 budget wherever the budget is cited, and re-measure it whenever the generation model changes, since it is a property of the model rather than of any architecture. | open | `UW-C151` |
| AL-218 | 2026-08-10 | M-2 deterministic pre-test (`docs/planning/diversity-test-register.md`) | product | **The catalog's large graphs are linear-with-decorations, not worlds, and it shows in their shape rather than in any reading.** Removing the hub from each of the 21 graphs at 200-plus nodes and taking the components of the remainder: 18 have one spoke holding nearly every node (the 677-node graph puts 656 of 677 in a single mass) with the rest being 1-to-4-node early exits, so there is no second region from which to cut a disjoint tour. Only `the-skyrail-heist` (10-13, 246 nodes, spokes of 83/82/77), `the-year-of-four-banners` and `the-tricameral-city` are genuinely world-shaped. A first attempt using max-flow node-disjoint paths returned 1 for all 21 and looked like a total refutation; the single cut node is the hub at depth 1 to 5 in every case, which is what a hub-and-spoke world should have, so strict node-disjointness was the wrong requirement. | Scope M-2 to the three named graphs rather than to "the largest", and add spoke-balance to the catalog metrics so world-shaped graphs are identifiable without re-deriving it. When a graph-theoretic test returns a uniform extreme across every input, check what the cut object *is* before reporting the result. | open | `UW-C152` |
| AL-219 | 2026-08-10 | Q-3 skeleton-free generation (`docs/planning/diversity-test-register.md`) | product | **An unaided model builds structurally sound story graphs, and fails only the rules nobody gave it.** Six authors with no skeleton, no example story and no validator in the loop produced 6 of 6 graphs with zero dangling targets, unreachable nodes, sinks or trapped cycles, at 27 to 35 nodes. All six were then blocked by the project gate, and every blocking finding violates a constraint absent from the brief: branch-depth budget (4 of 6), first-decision floor (2 of 6), ending-object schema (2 of 6). The brief stated the structural rules and the model met all of them. | State the band budgets, the opening floor and the ending schema in any generation brief, then re-run to test whether the gap closes; the current result cannot distinguish model incapacity from an incomplete brief, and the brief is the cheaper thing to fix first. | open | `UW-C153` |
| AL-220 | 2026-08-10 | Q-3 convergence, read against D-6 | product | **Removing the shared plan entirely does not vary the premise, because the model converges there unaided.** Six stories sharing no plan sit at the idiom floor on average (3.5 per 1000 against a floor of 3.3), which is the cleanest evidence that D-2 and D-6's convergence came from the shared contract rather than from the model. But 4 of 15 pairs breach the 4.0 budget, and the worst pairs are premise twins: two coastal lost-signal mysteries, two woodland camp discoveries, and all six are 'children find a thing left by an older person and follow clues to it'. D-6 identified `premise` as the largest traceable leak channel and concluded a reusable plan must vary it per book; Q-3 shows sharing less does not achieve that. | Vary the premise with something that actively repels, not by withholding a shared plan and not by instruction. This is the concrete requirement R1-3's repulsion term should be pointed at, alongside the bound solution chain. | open | `UW-C154` |
| AL-221 | 2026-08-10 | M-4 contract authoring, first attempt (`docs/planning/diversity-test-register.md`) | process | **Instructing an author to write independently produces the surface markers of independence and not the substance.** A contract briefed with wording-independence as a hard, itemised experimental constraint came back structurally excellent (26 nodes, 52 facts, zero fact-closure violations under an independent reimplementation) with a different world, a different slug and **zero fact-name overlap**, and at **126.7 shared 4-grams per 1000 with its reference**: `beat_hint` 260.3, `world_recipe`+`safety_envelope` 127.5, `choice_semantics` 96.2. For scale, D-6's worst fill condition was 16.9 and the generator floor is 3.3. This is D-6's `diverge` condition one layer up and far worse: at fill time the same instruction cut traceable grams 45 percent, at contract-authoring time it achieved essentially nothing. The checkable markers were all satisfied while the property was not. | Enforce independence by what the author is not shown, not by instruction: supply the format as a written schema and withhold the reference artifact. Note the consequence for R1-3, whose promotion rested on generating a fresh contract per book: fresh-per-book is not independent-per-book. | open | `UW-C155` |
| AL-222 | 2026-08-10 | M-4 contract authoring, controlled comparison (`docs/planning/diversity-test-register.md`) | product | **Independence comes from what an author is not shown, not from what it is told.** Same task, same shell, same requirement, one variable: with the reference contract visible plus an itemised instruction to diverge from it, a fresh contract shares **126.7** 4-grams per 1000 with that reference; with the reference withheld and the format supplied as a written schema instead, **1.0**. A 127-fold reduction. Both artifacts are structurally sound on the same independent checks. Instructing divergence is close to useless at the plan-authoring layer and is the intervention every proposal reaches for first. **But the premise converged anyway**: the withheld-reference author independently chose a clock tower against the reference's clocktower, so withholding closes the wording channel completely and the premise channel not at all (`AL-220` a third time). | Generate each book's decisional stratum from the structural stratum alone, never from a sibling book, and treat that as the implementable form of `AL-208`'s third repair. Name it explicitly in the architecture re-specification and in R1-3. Handle the premise with a separate repulsion mechanism, since withholding provably does not touch it. | open | `UW-C156` |
| AL-223 | 2026-08-10 | M-4 confound check (`docs/planning/diversity-test-register.md`) | product | **Cross-book convergence does not scale with plan similarity; the threshold is sentence identity.** Checking a new contract's similarity to the base before using it as an arm turned up that the pilot's two contracts are **118.4** shared 4-grams per 1000, almost exactly as similar as an attempt discarded as unusable at 126.7, and **their fills sat at the floor, 2.9**. They share vocabulary heavily while sharing **0 of 35 `choice_semantics` strings**. Two authors handed the same sentence converge on it; two handed different sentences sharing vocabulary do not, even at 118 per 1000. This refutes the proportional reading of `AL-208`, which was mine, and makes the repair far cheaper: a per-book decisional stratum needs only to be separately written, not independent of its siblings. Limit: the pilot's contracts also differ in premise, so sentence-difference and premise-difference are bundled in the one available comparison. | Amend the `AL-208` and `AL-222` guidance to require separate generation rather than independence, and run the disambiguating test: two contracts sharing a premise but not a sentence, filled and measured. That is the cheapest open question in this line. | open | `UW-C157` |
| AL-224 | 2026-08-10 | M-4 guard battery, `scripts/check_fill_integrity.py` | validator | **Every shell in this programme now defers the book title, and the fill-integrity checker fails on that by default.** Since `AL-207`, where a shell `id` leaked one title into three arms, shells carry `<<FILL book_title>>`, so a filled title necessarily differs from its shell. `check_fill_integrity.py` counts the top-level `title` as non-fillable unless `--allow-title-rewrite` is passed, and reports `FAIL structure: filled story differs from skeleton outside node bodies and choice labels (ids, choices, targets, endings, variables, or metadata)`, which reads like a serious structural defect and is not one. Verified across D-2 arm P, three D-6 fills and the M-4 arm: all fail without the flag, all pass with it, and an independent graph-level comparison confirms the title is the only difference every time. The flag exists for exactly this case per `AL-161`; the trap is that its default became wrong once title deferral became standard. | Teach the checker to treat any field the shell wrote as a `<<FILL ...>>` directive as fillable, which removes the flag and cannot drift as more fields are deferred. Until then name the flag in the guard-battery runbook. | applied | `scripts/check_fill_integrity.py` |
| AL-225 | 2026-08-10 | M-4 rating round 1 (`docs/planning/diversity-test-register.md`) | process | **A treatment arm's `label_style` encoded the treatment, making the book identifiable from its labels alone.** The stake-economics contract offered `name the cost before the reward` among its `label_styles`; the author picked it and **all 35 labels began with the word "Spend"**, against comparison books using neutral styles (`terse imperatives`, `first-person plural resolves`). The first blind rater named it unprompted as "the single most distinctive authorial signature among the three texts" and cited it in the forced comparison, so its verdict cannot separate priced-failure-in-the-fiction from a label template. This is `AL-195` in a new place: contamination entering through a field nobody was watching, here one I introduced by letting the treatment contract author its own label styles. | Hold `label_style` constant across arms, or draw it from a shared neutral pool, and never let a treatment contract supply style options that name the treatment. Add a first-word-concentration check to the guard battery: no more than about a sixth of labels sharing an opening word. | applied | `scripts/check_label_template.py` |
| AL-226 | 2026-08-10 | M-4 rating round 1, blinding procedure | process | **Blinding renamed the files and left the provenance inside them.** The two books from the older shells carry `"id": "sk_clocktower_cipher"` and the new arm carries `"id": "d6"`, and the second blind rater found it unaided and called it "the single most concrete signal in the whole set", explicitly noting it traces two of the three books to one shared origin object. This is `AL-207` a second time (a shell `id` leaking, there into titles, here into a rating) and it biases the comparison in the direction of the observed result. | Blinding must normalise every provenance-bearing field, not just the filename: `id`, `metadata`, `version`, and anything else that is not prose the reader would see. Write it as a reusable blinding step rather than doing it by hand per round, since it has now failed twice. | applied | `scripts/blind_books.py` |
| AL-227 | 2026-08-10 | M-4 rating round 1, rater finding on `filled_D` | product | **A choice label promises one thing and its destination depicts another, in an artifact used as a control in three rating rounds.** The option reads "Call the risk not worth it", which in the other two books resolves as declining the crossing; in this book the text has the character attempt it and slip ("Sam's foot went out from under him on the second plank... Idris's hand closed hard on his sleeve"). A child choosing the cautious option gets a near-fall anyway. Found by a blind rater, not by any guard, and it has survived every battery this book has been through. | Add a label-to-destination consistency check to the review surface, at minimum for the decline/retreat family where the mismatch is most legible. Note that this is a model-judgement check, not a lexical one, per the `Q-5` and `AL-211` pattern. | applied | `scripts/build_prose_review_worklist.py` |
| AL-228 | 2026-08-10 | M-4 rating round 1, rater finding on `filled_costly` | validator | **Fact-graph closure can hold in the contract and be violated by the prose, and nothing checks that direction.** The contract passed a closure walk under two independent implementations, yet the merge node's prose reads "Priya reads the first lever's knot, matches it to the pattern from the loft... Dez does the second, checking it against the ledger's margin notes", naming clues from two of the four rooms when a reader visits exactly one. `check_fill_fidelity.py` asks whether a node *delivered* its obligations; nothing asks whether a node *assumed* more than its `entry_state` guarantees. This is the mirror image of `Q-5`'s `logic_earned` finding and is invisible to every guard in the battery. | Add an over-assumption check at merge nodes: a model pass asking whether the prose references any fact the node's `entry_state` does not guarantee. Merge nodes are few (three in this graph) so the cost is bounded, and they are exactly where the error is structurally likely. | applied | `scripts/build_prose_review_worklist.py` |
| AL-229 | 2026-08-10 | M-4 rating round 2, rater finding traced to the contracts | validator | **A choice promises an object, and the fact representing it survives only on the branch that declines it.** At the crossing fork, `c_pendulum_oil` reads "cross with care and claim the maker's set-aside reward" and its destination carries only the puzzle facts, dropping `reward_glimpsed` entirely; `c_pendulum_giveup` ("decline it") is the branch whose destination *does* carry it. The claiming branch has no fact representing possession, so no author can write the payoff, and none did. A blind rater found the resulting hole in all three books unprompted: "the label promises an object; the text simply drops it", quoting all three. **The pattern reproduces in `contract_v2`, `contract_v3` and `contract_costly`, three independently authored contracts, one of which was written without sight of the others**, so it is an error the graph shape invites rather than one author's slip. It is invisible to fact-graph closure, which only forbids assuming too much and treats dropping a fact as always safe, and invisible to `check_branch_obligations.py`, which computes what a branch owes rather than what it silently drops. | Add an asymmetric-drop check at forks: a fact established at a fork and carried by one branch but not another is a candidate defect, and one whose dropping branch's `choice_semantics` name that fact's subject is a confirmed one. Cheap, deterministic, and it catches a class three guard batteries missed. | applied | `scripts/check_promise_discharge.py` |
| AL-230 | 2026-08-10 | M-4 rounds 1 and 2 (`docs/planning/diversity-test-register.md`) | product | **A rating effect that two blind raters agreed on disappeared entirely once two contaminations were removed.** Round 1: both raters, opposite orders, ranked the free-failure control pair more decision-repetitive, Q6 gap one point. The arm carried a label template (35 of 35 labels opening "Spend") and a provenance leak (`id` shared by two of three books), both found and cited by the raters themselves. Round 2, same books with a neutral label style and tool-based blinding: **raters split, both call it close, Q6 ties at 5,5**, and M-4's own falsifier fires. The caveated version of round 1 would have been reported as a positive with a footnote. Note a residual handicap that runs the other way: M-4's "same goal" requirement forces the treatment arm to share the base's premise engine while the control does not, so the null may understate the effect. | Treat agreement between blind raters as necessary and not sufficient; a contaminated round can produce unanimity. Keep `AL-191`'s rebuild-rather-than-caveat rule and cite this as the case that justifies it. Where a design requires holding a variable constant, check whether that constant is itself a similarity channel before reading a null. | open | `UW-C164` |
| AL-231 | 2026-08-10 | Instrument audit across three rating rounds (`docs/planning/diversity-test-register.md`) | process | **One item of the six-question instrument has never varied, and the only item that ever carried a result has now saturated too.** Across the three rounds with recorded per-question scores, twelve cells each: Q4 (sequence repeats) is 5 in **12 of 12** and separated the pairs in **0 of 3** rounds; Q3 never leaves a two-point band at the bottom; and Q6 (solution transfer), which carried every earlier result, tied at 5,5 in the one uncontaminated round. Two raters recommended retiring Q4 unprompted after the D-5 round and the recommendation was recorded and never acted on. The consequence is concrete: D-1, which proposes three more arms rated on this graph with this instrument, would spend three contracts and three fills to produce a null that is an instrument artifact rather than a finding. | Drop Q4 from the scored instrument and keep it as a description of the condition. Do not schedule another rating round on this graph until either the graph varies its central puzzle structurally between arms, which is why Q6 saturates, or a discriminating measure exists that is not at ceiling. Audit instrument ceilings from accumulated cells before commissioning a round, which costs nothing and would have saved this one. | open | `UW-C165` |
| AL-232 | 2026-08-11 | D-7 and D-7b (`docs/planning/diversity-test-register.md`) | product | **A plan can be shared across books without them converging, but only if the shared half carries no prose at all.** D-7 shared a stratum of topology, obligations, fact names and 32 one-line fact *glosses*, generating everything else per book: 12.9 shared 4-grams per 1000 against a budget of 4.0, exactly where D-6's `diverge` landed, with 62 percent of shared grams tracing to the glosses. D-7b removed the glosses and changed nothing else, verified byte-identical on every other key: **3.2**, under budget and at the 3.3 generator floor. **422 words of prose in the shared artifact was the difference between 12.9 and 3.2.** The stratification proposed in the architecture re-specification is therefore sound and its contents were wrong: I classified prose glosses as structure. | Define the shareable stratum by exclusion rather than enumeration: it may contain identifiers, relations and enumerated categories, and no free text of any kind. Enumerating prose fields has now missed some twice (`constraints` and `mechanisms` in `AL-215`'s follow-up, then `facts`). Amend the architecture re-specification's section 2 table accordingly. | open | `UW-C166` |
| AL-233 | 2026-08-11 | D-7b fact readings | product | **Bare fact names bind two independent authors to the same obligation without a gloss.** The worry was that stripping definitions would make the shared plan too vague to be a plan. Measured: two authors given only names, node `function` and their own binding wrote 0 of 32 identical reading sentences and 0 of 35 identical choice semantics, chose different engines, and agreed in meaning on every fact sampled (`logic_earned` as "one real, usable piece of the tide arithmetic needed to set the dial" against "one working piece of the peal-cipher's logic, enough to actually attempt a setting"). A name plus a node function plus a binding is a sufficient specification. | Drop fact glosses from the shared stratum as standard, and require each book's plan to record its own `fact_readings`, which makes interpretation drift measurable per book instead of assumed away. | open | `UW-C167` |
| AL-234 | 2026-08-11 | Second external review, question on device categories, checked against the D-7b artifact | process | **We published a rule describing an artifact we had not measured, and the artifact contradicts it.** D-7b was reported as sharing a plan with "no free text at all". Measured directly: the shared kernel still carries **473 words** of free text (`world_recipe` binding notes, per-node `invention` notes, eight `title_contract.must_not` clauses, the affect ceiling), which is *more* than the 422 words the experiment deleted (895 to 473). The measured result is unaffected and still holds at the corrected body-only scope, 13.6 to 2.3, but the stated mechanism cannot be right: "no free text at all" is not what the passing arm implements. The reviewer reached the same objection from the document alone, by reasoning that device categories carry no prose yet still steer the puzzle; the artifact was worse than their inference. Note this also weakens `AL-232`, whose figures are the superseded mixed-scope pair and whose "no free text of any kind" rule this corrects. | State the rule as: free text attached to the **fact vocabulary nodes reference** drove convergence; free text **instructing the binding process** did not, at this volume. Whether the operative variable is what the text describes or how often it is re-read is open and settled by a third arm that deletes the 473 and keeps the 422. Standing process change: before publishing any claim of the form "the shared artifact contains no X", assert it programmatically against the artifact rather than against the build script's intent. | open | `UW-C168` |
| AL-235 | 2026-08-11 | Second external review, question 1 on rater provenance | process | **The brief calls model raters "human".** Section 14 reads "a deterministic decision-overlap score on top of blind human annotation". Every rater, annotator and "blind reader of the finished books" in Parts I and II was an LLM agent instance; no human and no child has read any book in this programme for rating purposes, and the reported Fleiss kappas (0.96, 1.00) are inter-model agreement. Part I's methods appendix says "a rater agent" correctly, so the document contradicts itself, and every other use of "rater", "annotator" and "reader" is unqualified and therefore ambiguous. An external reviewer had to ask which it was before they could weight any result, which is the cost. | Correct section 14, and add one provenance statement covering every rated result in both parts, stating that rater verdicts are hypotheses about reader response rather than reader evidence. Adopt as a standing rule: name the rater's nature at every site where a verdict is reported, never once in an appendix. | open | `UW-C169` |
| AL-236 | 2026-08-11 | Fourth external review, A1.1 attack on 16l, run rather than argued | product | **The 16l pair passes every lexical gate and sits at the top of our observed range on shared cognitive operation.** A reviewer objected that 16l measures verbatim overlap while the defect is decision repetition, and that the pair had never been measured for the second. Run on the existing artifacts, so the cost was zero generations: solution transfer over the shared chain gives **answer 0.167 (passes the tier-1 gate), operation 0.500, solution 0.467**. **Correction, same day: my first reading of this was wrong and the known-bad battery caught it.** The operation tier is not monotone with badness, because the tiers are exclusive and the strongest wins: a prop matching by *answer* never counts as an operation match, so a literally identical binding scores operation 0.000. Ranking must use the composite. On the composite `solution_transfer`, scored against a battery built for this purpose, the ordering is: identical binding 1.000, same-chain-different-dressing 1.000, the known-contaminated pair 1.000, relocated chain 0.700, **the 16l pair 0.467**, most distinct real pair 0.225. So the 16l pair is **mid-range, not the worst we hold**: four of its six chain props transfer at some tier, which is materially short of clean and well short of catastrophic. Separately, on aligned choice positions in the two filled books: **zero identical labels and 11 of 35 sharing their first content word**, several of them the same act with a swapped noun ("Turn Back Together" against "Turn back now"; "Stay and Read" against "Stay and read the logs"). Caveat that must travel with this: the operation tier is advisory and was **not** the tier that reproduced rater orderings in D-4, only tier 1 was, and n is 6 chain props on one contract. | Stop reporting 16l as a distinctness result. It is a convergence result, and the first cheap test of the property 1.3 actually defines comes back unfavourable. Add a decisional sibling to the exclusion rule: sharing a structural kernel must also schedule the reasoning kind per fork, not merely leave it to the binding. Validate the operation tier against a constructed known-bad battery before it is allowed to gate anything, and never quote 0.500 as a gate result. | open | `UW-C170` |
| AL-237 | 2026-08-11 | Fourth external review, catalog-median erratum | docs | **Two different medians are used interchangeably without saying which is which.** The brief cites 151 in 7 and 149 in 16i and 16o, and a reviewer read them as an inconsistency. Measured: the catalog median across all 61 skeletons is **151**; 149 is the median of the 10-13 band, which is the pilot's band. Both numbers are right and the document never says they are different quantities. | Label every median with its population at the point of use. Cheap, and it removes an apparent error a reviewer spent attention on. | open | `UW-C171` |
| AL-238 | 2026-08-11 | Q-3b, the constrained rerun of skeleton-free generation | product | **Stating a constraint works for anything the author can verify by tracing its own artifact, and does nothing for anything requiring a statistic it cannot compute.** Six mutually isolated authors, no skeleton, one pass, no repair round and no self-review prompt: **6 of 6 structurally clean**, and the entire band-budget failure class that sank the first run (opening-decision floor, arc floor, ending schema) went to **zero of six** simply by being written down. Two independent checkers agree. In the same prompt, an explicit Flesch-Kincaid 4-6 target produced **5 of 6 books over the 7.0 band edge**, with no book placing more than 48 percent of its nodes inside the band. The model can trace reachability and path length; it cannot count syllables. | Split the constraint set by self-verifiability when designing any generation prompt. Structural and budget constraints: state them and expect compliance. Reading level: a per-node repair loop against the deterministic checker is mandatory, not an optimisation, because the prompt provably does not reach it. This also prices every drafting pipeline in the candidate architectures, since all three propose model-drafted artifacts checked by program. | open | `UW-C172` |
| AL-239 | 2026-08-11 | Q-3b premise inspection | product | **Six authors sharing nothing produced five coastal lighthouse-or-lantern stories with fog or a storm and an elder keeper.** One outlier. This is worse than the first run reported, because the convergence is on a specific setting archetype rather than a plot engine, and it happened with no shared plan, no sibling visibility and no common prompt beyond an age band. Convergence is **not lexical**: median 1.56 shared four-grams per 1000 bodies-only, 11 of 15 pairs at or below the 3.3 floor, so wording independence is free when nothing is shared. But **4 of 15 pairs breach the 4.0 budget and every breach is a same-archetype pair**, which is the content-driven breach the first run predicted and this one reproduces. | Treat premise scheduling as a mandatory shared service in every candidate architecture rather than a property any one of them provides. Removing the plan buys wording independence and nothing else. The scheduler must allocate from a curated enumerated space, since the failure is in the model's own distribution and no amount of withholding touches it. | open | `UW-C173` |
| AL-240 | 2026-08-11 | Q-3c, premise mode across model tiers | product | **Two independent instances of the same model, sharing no context and reading no file, invented the same place name and produced titles differing by one preposition: "The Lantern Under Marrow Hill" against "The Lantern Beneath Marrow Hill".** Four of five words identical. The probe held task and prompt constant and varied the model tier, to test whether `AL-239`'s lighthouse archetype was one model's mode. It is not: pooled across three tiers, **8 of 10 independent generations put a light-or-signal beacon at the centre**, most coastal or fog-bound, several with an elder keeper. The two outliers were a kite race and a forest threatened by a shopping mall. Limit that must travel with this: all three tiers are one model family, so this is silent on whether the mode is training-distribution-level across vendors, and that remains the open question a genuine cross-family run would answer. | Stop treating independent generation as a diversity mechanism at all. It delivers wording independence and nothing above it, and the collision here is on an invented proper noun, which is as far below the plan layer as a collision can get. Premise allocation from a curated enumerated space is now a precondition for every candidate architecture rather than one architecture's feature. Fund a genuine cross-vendor replication before quoting any idiom floor or diversity margin as general. | open | `UW-C174` |
| AL-241 | 2026-08-11 | Q-3d, constraint-stated generation at 100+ nodes | product | **Structure survives the scale jump and one-pass generation does not.** Two isolated authors at 101 and 103 nodes: **2 of 2 structurally clean, zero failures, every stated budget met**, which answers the question both external architecture rankings turned on. But **neither managed it in one pass**: one built a scripted build-validate-patch loop, the other's first draft produced a longest path of 43 against a ceiling of 24 and needed trimming and repadding. Cost ran 210k and 337k tokens at 60 and 269 tool calls, against roughly 85k and 3 calls for a 30-node graph, so generation cost is dominated by the repair loop rather than by node count. | Treat iteration-against-a-checker as the architecture's requirement rather than an implementation detail, and price it that way. Structural validity at production scale is available without any reusable narrative artifact, which removes the strongest remaining argument for reuse-based designs; what it does not remove is the premise problem or the cost. | open | `UW-C175` |
| AL-242 | 2026-08-11 | Q-3d reading level, the two arms diverging | validator | **Same prompt, same explicit reading-level target, opposite outcomes, decided by whether the author chose to instrument it.** One author added sentence-splitting for readability to its repair loop and reached **FK 5.12 with 84 percent of nodes in band**, the first book in this programme to pass reading level at production scale, against a prior 101-node attempt that reached 8.1 to 8.4. The other instrumented depth and word count but not reading level and reached **8.35 with 11 percent in band**. This is `AL-238`'s self-verifiability split reproduced at scale and sharpened: stating the constraint is not merely insufficient, it is a coin flip resolved by an authoring choice nobody specified. | Put the reading-level repair loop in the harness, not the prompt, and make it non-optional. An author that is free to choose which constraints to instrument will sometimes choose wrong, and the failure is invisible to every per-node advisory check. This is now the single best-evidenced build item in the programme. | open | `UW-C176` |

---

## Related documents

- [Handoff document](./handoff-authoring-lessons-and-story-quality-2026-07-27.md), branch state, the rebase work the merged diversity and personalization PRs require, environment traps, and the open-work triage
- [Story quality lessons from the Wyrmreach build](./story-quality-lessons-2026-07.md), the narrative behind AL-001..AL-023
- [Adversarial review record](./reviews/ceiling-scale-review-2026-07-25.md), the raw evidence and measurements behind AL-026..AL-040
- [Wyrmreach series build report](./wyrmreach-series-report.md), the run that produced them
- [Reader path and engagement design](./reader-path-engagement-design.md), the design AL-019/AL-020 point at
- [Capability register](./capability-register.md), A11
- [Validation rule catalog](./validator-rules.md), the target of AL-003, AL-006, AL-013, AL-015, AL-017, AL-021
- `docs/template_feedback.md`, the parallel log for issues belonging to the cookiecutter template
  rather than to authoring. Unlinked on purpose: unlike this log, that one is gitignored and
  local-only, so it is absent from a fresh clone and from the published docs site (see the Template
  Feedback Requirement in `CLAUDE.md`)
