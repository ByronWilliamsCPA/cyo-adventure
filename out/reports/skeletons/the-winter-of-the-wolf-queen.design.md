# Design report: the-winter-of-the-wolf-queen

- **Cell**: 10-13 / long / prose (brief #20, Wave 5)
- **Topology / tier**: open_map / 2 (second stateful open_map; largest tier-2 yet at 250 nodes)
- **Designer model**: fable (design interrupted once by a session limit, resumed cleanly; zero repair cycles)
- **Reviewer model**: opus (independent; reproduced the full 28,512-config walk and every disclosure)
- **Disposition**: APPROVED for the fill stage (two optional author-guidance notes applied by the supervisor)

## Scripted validation (final)

```text
stats: nodes=250 endings=40 fill_nodes=250 cell=(10-13, long, prose) topology=open_map tier=2
ok: skeleton passes gate and brief checks
```

Layer-2 walk: 28,512 reachable configurations, uncapped, zero blocking
findings; reviewer-verified: all 40 endings reachable, zero choiceless
states, unconditional-companion audit clean at all 15 gated nodes, the
3-success best cluster exact at frost==2 and nowhere else.

## State machine

`frost` 0-2 (season/timing: once-per-leg increments; Fellgate CLOSES at 2
while the Thornmere tarn OPENS at 1, a deliberate closing+opening pair the
reviewer judged legible: "the deepening freeze shuts the high pass but
hardens the black-ice tarn"); `provisions` 0-3 (capability: seven gated
dark/long lines, each beside an always-open base route; three restock
sites); `laden` bool (inventory: chest/passenger, cleared in-spoke at the
deliveries with hub/muster backstops; cross-edges verified leak-free per the
flooded-quarter cargo lesson).

## Review verdict (7 categories): all PASS, OVERALL APPROVE

- The 19-node floor path is the full muster/ridge/reveal finale spine and
  lands the recognition with zero legs walked; the success cluster stays
  behind the full winter (frost==2), making PL-20 structurally safe.
- Dignified failures exemplary ("a right turn-back is counted in the guild
  book as a save"); squall-waiting written as winning.
- Wolf at distance in every beat, verified node by node; the closest beat is
  adult-led, warm-mythic, no proximity.
- One cosmetic fork (gh_maps' two identical returns) and two laden-agnostic
  ending notes were the only findings, all optional.

## Supervisor guidance applied (beats notes, gate re-validated)

1. `gh_maps`: the two hub-return choices must fill as distinct textures.
2. `e_sc_hoist` / `e_sc_morning`: keep laden-agnostic; attribute any chest
   arrival to Scarpsett's carrier team, never the reader.

## Milestone

This completes the 10-13 band's Wave 5 slate (6 new skeletons: frequency,
comet, envoy, flood, skyrail, wolf-queen), and the band now spans open_map
(x2, one stateful), branch_and_bottleneck (tier 2), and sorting_hat (x2)
against its three pre-run b&b skeletons.
