# Regenerate Playwright visual-regression baselines on the CI runner.
#
# Why this exists: the `*-chromium-linux.png` snapshots in
# frontend/e2e/visual.spec.ts-snapshots/ are pixel-exact to the Chromium build
# and font stack of the CI runner. An intentional UI change invalidates them,
# and they can only be regenerated correctly in that same environment. This
# workflow runs `playwright test --update-snapshots` on the runner and commits
# the refreshed PNGs back to the target branch.
#
# The commit is created server-side via the GraphQL createCommitOnBranch
# mutation (not `git push`): this repo's branch protection requires every commit
# to carry a verified signature, and GitHub signs commits it constructs through
# its API, so the result shows "Verified" with no key management in CI. This
# mirrors the proven mechanism in release.yml.
#
# After it runs, re-run CI on the branch (the visual step will now match) and
# review the committed baseline diff in the PR before merging: an update accepts
# whatever the app renders, so a human must confirm the new appearance is
# correct, not a regression.
#
# Bump: 2026-09-01 regenerate after the review-screen remediation (PR #797).
# Every admin baseline moved, not only the review page: AdminShell gained an
# "Outstanding decisions" nav entry, which renders on all of them. The review
# and queue baselines moved on their own account (ranked findings lead the
# detail page; the queue row now names its top finding).
# Re-run trigger: touch this file (a comment bump is enough) and push to a
# feature branch to regenerate after a UI change or a merge from main.
#
# Baseline bump 2026-07-25: kid-surface UX polish (picker busy state, library
# hero heading and branded loading, reader chrome clearance) changed the kid
# picker, library, and reader visuals.
#
# Baseline bump 2026-07-25 (2): the library shelf gained the "Ask for a new
# story" end-cap tile and the fresh-shelf "Pick a book!" heading.
#
# Baseline bump 2026-07-28: GuardianShell gained a persistent
# .guardian-shell__footer carrying the privacy-page link, so it renders on
# every guardian surface. That legitimately shifts the guardian books,
# assign-children dialog, guardian profiles, and profile-form dialog
# baselines. Pushing this branch runs the job and commits the refreshed PNGs.
#
# Baseline bump 2026-07-29: exactly one baseline, admin-provider-allowlist-page,
# moves by exactly 1 pixel. The prettier sweep pulled <Link> up onto the same
# line as the text preceding it in ProviderAllowlistPage's intro paragraph,
# which let prettier drop the explicit {' '} escape that had been holding the
# space open across the old line break. The rendered STRING is unchanged (a
# space on the same line as the tag survives; one adjacent to a newline is
# collapsed, which is exactly why the escape existed). What changed is the DOM:
# React previously emitted the text and the " " as two adjacent children and now
# emits one merged text node, and browsers shape and kern per text run, so a
# glyph pair spanning the old node boundary lands on a different subpixel.
# Cosmetically and semantically inert; the baseline is simply pixel-exact.
#
# Baseline bump 2026-08-01 (2): the three kid baselines (picker, library,
# reader) were being captured with `/v1/me/progress` and
# `/v1/profiles/story-status` UNROUTED, so both adapters failed closed against a
# backend that is not running and the weekly ring, badge-case button,
# endings-gallery button and "new story!" pill were absent from every baseline.
# visual.spec.ts now mocks both endpoints with the ON settings a 6-8 reader
# actually resolves to, so those three baselines legitimately gain the
# gamification chrome this PR added. Review the diff as an addition of chrome
# that was previously invisible, not as a layout regression.
#
# Re-run bump 2026-08-02: the a815080 run regenerated kid-picker and library
# baselines but its createCommitOnBranch raced a concurrent push (b517604,
# coverage-matrix registration) and was rejected on expectedHeadOid; bump to
# regenerate and commit them on the current head.
#
# Baseline bump 2026-08-05: KidNav gained a persistent "Ask a grown-up" link in
# the kid Library header (PR #618), so exactly one baseline legitimately moves.
# The mocked tier failed with 1769 pixels (ratio 0.01) different on
# library-page.png and no other baseline changed, which is the signature of a
# single control added to a header rather than a layout shift. Review the diff
# for the new link appearing in the library header and nothing else moving.
#
# Baseline bump 2026-08-09: the reader gained a persistent BookmarksButton
# (Phase 4b bookmarks feature, Reader.tsx), so reader-page.png legitimately
# moves. The mocked tier failed with 1056 pixels (ratio 0.01) different on
# reader-page.png and no other baseline changed, consistent with a single new
# control in the reader chrome rather than a layout shift.
#
# Baseline bump 2026-08-10: the landing page gained a persistent footer carrying
# the Privacy and Support links (PR #668), so landing-page.png legitimately
# moves and it should be the ONLY one that does. The mocked tier failed on
# visual.spec.ts:39 with 1 failed / 254 passed, which is the signature of a
# single element added to one page rather than a shared-layout change. Review
# the diff for two underlined links appearing under the landing hero and
# nothing else moving; the footer sits inside the existing .landing flex column
# so it should not shift the elements above it.
#
# Baseline bump 2026-08-15: ConsolePage's reviewer line changed from "Stories
# are checked by your family's safety reviewer" to "our safety reviewer"
# (UW-J28: the possessive named an authority inside the reader's family, while
# approval is admin-only and cross-family per api/approval.py). That line sits
# above the fold on the guardian console, so guardian-console-page.png
# legitimately moves and it should be the ONLY baseline that does.
#
# Two specific things to check in the diff, because this bump has a wider
# blast radius than its one-line cause suggests. First, the replacement is
# SHORTER than the original, so the paragraph may reflow from two lines to one
# and lift everything below it; that is expected, and anything moving on a
# page other than the guardian console is not. Second, the same PR changed the
# landing page's how-it-works step 4 and its safety trust card, which do NOT
# appear here on purpose: landing-page.png is a viewport screenshot, not
# fullPage, and both edits sit below the fold. If landing-page.png moves in
# this run, that is a real finding rather than an expected update, and it
# means the funnel's above-the-fold content shifted for some other reason.
name: Update Visual Snapshots

on:
  # workflow_dispatch only works once this file is on the default branch (a
  # GitHub limitation: the dispatch trigger is read from the default branch).
  # Until then, the push trigger below runs it from a feature branch: touching
  # this file runs the job once. The baseline commit it makes touches only PNG
  # paths, so it does not re-trigger this workflow.
  workflow_dispatch:
    inputs:
      branch:
        description: Branch to regenerate visual baselines on (defaults to the selected branch)
        required: false
        type: string
  push:
    branches-ignore:
      - main
      - master
      - develop
    paths:
      - .github/workflows/update-visual-snapshots.yml

# contents:write lets the createCommitOnBranch mutation land the refreshed
# baselines when it falls back to the job's GITHUB_TOKEN (a fine-grained
# RELEASE_TOKEN, when configured, carries its own scope instead).
permissions:
  contents: write

concurrency:
  group: update-visual-snapshots-${{ inputs.branch || github.ref_name }}
  cancel-in-progress: true

jobs:
  update:
    name: Regenerate visual baselines
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.branch || github.ref_name }}
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Install Playwright browsers
        working-directory: ./frontend
        run: npx playwright install --with-deps chromium

      # --update-snapshots rewrites any baseline that differs and reports the
      # run as passing for those; a genuinely broken (non-visual) test still
      # fails. Keep going either way so the refreshed baselines are committed;
      # any remaining functional failure surfaces on the next CI run.
      - name: Regenerate visual snapshots
        id: regen
        working-directory: ./frontend
        continue-on-error: true
        run: npm run test:e2e -- --update-snapshots

      - name: Commit refreshed baselines (verified, via createCommitOnBranch)
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN || github.token }}
          BRANCH: ${{ inputs.branch || github.ref_name }}
          SNAP_DIR: frontend/e2e/visual.spec.ts-snapshots
        run: |
          set -euo pipefail

          # Collect added/modified baseline PNGs (porcelain code then path).
          mapfile -t FILES < <(
            git status --porcelain -- "${SNAP_DIR}" \
              | awk '{ $1=""; sub(/^ /, ""); print }'
          )
          if [ "${#FILES[@]}" -eq 0 ]; then
            echo "No baseline changes; nothing to commit."
            exit 0
          fi
          printf 'Updating %s baseline(s):\n' "${#FILES[@]}"
          printf '  %s\n' "${FILES[@]}"

          # Build fileChanges.additions in a file so the (large, base64) blobs
          # never land on a command line: --rawfile reads content from disk and
          # the assembled array is passed to the final jq via --slurpfile.
          ADDITIONS="$(mktemp)"
          B64="$(mktemp)"
          echo '[]' > "${ADDITIONS}"
          for f in "${FILES[@]}"; do
            base64 -w0 "${f}" > "${B64}"
            jq -c --arg p "${f}" --rawfile c "${B64}" \
              '. + [{path: $p, contents: $c}]' "${ADDITIONS}" > "${ADDITIONS}.next"
            mv "${ADDITIONS}.next" "${ADDITIONS}"
          done

          BASE_SHA="$(git rev-parse HEAD)"
          # shellcheck disable=SC2016  # literal GraphQL text, expansion not wanted
          QUERY='mutation($input: CreateCommitOnBranchInput!) {
            createCommitOnBranch(input: $input) { commit { oid } } }'

          BODY="$(mktemp)"
          jq -n \
            --arg q "${QUERY}" \
            --arg repo "${GITHUB_REPOSITORY}" \
            --arg branch "${BRANCH}" \
            --arg oid "${BASE_SHA}" \
            --arg headline "test(frontend): regenerate visual regression baselines" \
            --slurpfile adds "${ADDITIONS}" \
            '{
              query: $q,
              variables: {
                input: {
                  branch: {repositoryNameWithOwner: $repo, branchName: $branch},
                  message: {headline: $headline},
                  expectedHeadOid: $oid,
                  fileChanges: {additions: $adds[0]}
                }
              }
            }' > "${BODY}"

          RESPONSE="$(gh api graphql --input "${BODY}")"
          OID="$(echo "${RESPONSE}" | jq -r '.data.createCommitOnBranch.commit.oid // empty')"
          if [ -z "${OID}" ]; then
            echo "::error::createCommitOnBranch did not return a commit oid."
            echo "${RESPONSE}"
            exit 1
          fi
          echo "Committed refreshed baselines as ${OID} on ${BRANCH}."

      - name: Report regeneration test outcome
        if: steps.regen.outcome == 'failure'
        run: |
          echo "::notice::The e2e run reported failures even with" \
            "--update-snapshots. Visual baselines were still refreshed and" \
            "committed; any remaining failure is functional (non-visual) and" \
            "will surface on the next CI run for the branch."
